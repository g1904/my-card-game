# Open questions — 跨 session 待答清单（索引）

> 本文件是**客户端**（Godot 项目）待答清单的**索引**：问题条目本身按主题拆在 `open-questions/` 下的分片里。
> 后端侧的待答清单在 `backend-design-documents/open-questions.md`（`backend-design` 分支）。
>
> 每次 session 结束时，未答的 Open questions 汇总到对应分片，供下次拾起；一旦答定，就从分片中移除、
> 归档进对应主题文档的 `## 待决问题` / `## 决策`，并在 `answer-logs/log-<draftSuffix>.md` 记一笔。
>
> 本清单**只跟踪仍待答的问题**（不留已解决区），是导航 / 拾取清单，**权威归属在各主题文档**。
>
> 最近更新：2026-08-28c — 一次定点修复：手牌上限定案 7 · 三份根级横切同批改齐 · 四份文档的陈旧 ADR 候选行 · `RelicData` 遗留清零 · `balance.md` 补三问判据（详见 `open-questions/update-log.md` · `answer-logs/`）
> （逐次变更摘要见 `open-questions/update-log.md`；已答定问题的逐条移出记录见 `answer-logs/`）

## 分片导航

| 分片 | 内容 |
|------|------|
| `open-questions/update-log.md` | 每次运行的更新摘要（答结 / 推翻 / 新增落点），倒序，**只留最近 10 条**。不含问题条目本身。 |
| `open-questions/update-log-archive.md` | 更早的更新摘要，原样归档、按时间正序。只读，不写新条目。 |
| `open-questions/01-combat.md` | **① 战斗机制**（焦点之首）：能力剥夺与统计计数的残留（片区主体已于 08-10c 答结）、结构与配置、内容与数值（多数留待内容扩充后的统计校准）、**信息面**（意图移除后图鉴是唯一事前通道）、呈现。 |
| `open-questions/02-event-options.md` | **② eventOptions 生成流程**：生成 / 加权与配比、物化字段、优先级、寿元打穿、Explore 揭示池、Travel 出场、location 与图鉴连边。 |
| `open-questions/03-adventure-event-types.md` | **③ 逐类型 AdventureEvent 机制**（五类各开一场专门 session）。 |
| `open-questions/04-hidden-attributes-plot.md` | **④ 隐藏属性 / 剧本机制**：档位阈值、跨档叙事、`lifeSpanCost` 分档、隐藏属性推拉映射、DnD 选分支的触发点与 UI、剧本内容的分发粒度。 |
| `open-questions/05-service-contracts.md` | **⑤ 服务契约 / 工程侧残留**：`#if DEBUG` 判据与 `Control` 自动翻译的实测、需求流水线形态、`architecture.md` 的三条结构残留、层级词的过早性。 |
| `open-questions/06-meta-progression.md` | **⑥ 元进程的失败侧与中长期规划感**：死亡 / 轮回结束屏尚无设计、轮回内的进度感是否需要补充、失去法则三支的频次预算、角色模板池的形态。 |
| `open-questions/07-codex-monetization.md` | **⑦ 图鉴族与商业化**：`CharacterPower`、图鉴族、premium bundle。 |
| `open-questions/cross-boundary.md` | **跨边界承接**：对侧（后端）已定案、本库尚未落笔的条目。**不是待答问题**——答案已有，等的只是落笔；形态、关闭条件与维护者分工见分片抬头。 |
| `open-questions/deferred-content.md` | **已搁置：内容充实**（07-30 起暂不推进）＋ **美术与音频（`art/`，08-04 加入）** ＋ 随内容搁置的 UX 呈现细节 ＋ 尚未设计的占位主题。 |

## 当前焦点：各系统机制细节

> **焦点判据（07-30 定）：** **规则、字段语义、流程与算法 = 机制细节 = 焦点**（分片 ①–⑦）；
> **具体条目目录与数值 = 内容充实 = 搁置**（`open-questions/deferred-content.md`）。
> 与既定开发路线「框架 → 内容 → 平衡与体验 → 社交及其他」的第 ① 阶段一致。
> Source: `handoffs/2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown.md`。
>
> 焦点顺序即分片编号顺序；**① 战斗机制**优先级最高。

## derive 就绪度

> **本小节由 `/assess-derive-readiness` 独占写入**（`/analyze-new-ideas` 与 `/summarize-open-questions` 均不得改动）。就绪度是全库横切判断，须基于一次性全量扫描才有意义；逐次 handoff 顺带的评估会迅速过时且互相矛盾。

**最近全量评估：2026-08-28（由 `/assess-derive-readiness` 产出）。** 扫描范围：`vision/`（3）· `systems/**`（54）· `art/**`（10，不含 2 份 `_TEMPLATE`）· `ux/`（5）+ 根级横切 3 份（`terminology.md` / `program-overview.md` / `system-overview.md`）= **主题文档 75 份**，另加 `decisions/`（123 = 121 份 Accepted + `ADR-0001` 占位 + 台账）与 `content/` / `requirements/` 的台账与模板。本次重估的触发是 08-26 ~ 08-28 的连续落笔：三批 `/batch-analyze-new-ideas`（08-26b/c/d · 08-27 · 08-28）与 08-28b 的一次 `/write-adr all` —— 后者固化 **23 份新 ADR**（`ADR-0100` ~ `ADR-0122`），`decisions/` 由 99 份增至 122 份、Accepted 由 98 增至 121。

> **计数口径变更：本次逐份文件计数**（上次多处把 `_index.md` 与 `common-properties.md` 并作一行），故与上次的绝对数字不可直接相减；下文的升降一律按**文档**而非按行陈述。

**全局结论：ready 6 份 · partial 37 份 · blocked 32 份（主题文档 75 份口径，不含 121 份 ADR 本体与台账 / 模板）—— 08-26 记录的三处承重卡点全部倒下，玩法侧首次出现「效果原语层可以 derive」。**

- **✅ 效果原语 / 异能语法定案（`ADR-0115`）—— 上次全库最大的一处欠账倒下。** 它曾卡住卡牌 / 神通 / 法宝 / 法则 / 古宝五个内容类型的共同底座。现落为 `EffectData` 抽象基类 + 一原语一 `[GlobalClass]` 子类树（`Dictionary<Type, IEffectHandler>` 分派、不引判别枚举）+ 首批八原语参数表 + 并列定义体 `StaticModifierData` + `AbilityData` 两格 XOR + `TriggerConditionData` / `TimingIds` 十时点 + `EffectCondition` 三谓词 AND + `CardData` 字段清单收口 + 19 条加载期校验 + **存档面零新增字段**。直接把 `deck/` 两份由 blocked 升为 partial，并解锁 `power/` / `player-power/` / `item/` 的字段面。
- **✅ capability flag 与 modifier pipeline 体系定案（`ADR-0116`）。** 上次点名「承重、含聚合面宿主服务、账号级至今无专属服务」——现宿主已定为 `profile-service.CapabilityManager`，注册面**两层共用**，叠加 = 集合并且幂等，**冲突在结构上关死**（全部 flag 恒为增益向 + 封闭三词动词表 `{Reveal, Show, Unlock}` + 禁否定式命名），modifier 合并算法四行完整且**结果与顺序无关 ⇒ 不设优先级**。连带把 `player-power/` 两份由 blocked 升为 partial。
- **✅ `combat` 子流的三句互相矛盾已裁决（`ADR-0112`）。** 上次列为「第 12、20 两步的硬前置、成本最低可立刻做」——已裁为**两侧共用单一 `combat` 子流、其上不派生任何层**，且 `combat-service.md` 给出「两侧牌序互不打乱由结构提供、与子流数量无关」的论证（组装时各洗一次即定、抽牌零随机消耗）⇒ **上次所称的『放弃玩家额外抽牌不打乱敌人牌序』这一代价并不存在**。确定性验收标准自此可写。
- **✅ 敌人 AI 与内容资产字段定案（`ADR-0113` / `ADR-0120`）。** 定制策略 = 独立可复用资源 `EnemyAiProfileData`（`AiWeight[]` 稀疏覆写 · 直接类型引用 · 可空即回落兜底），兜底 = 1-ply 加权效用评分 + 确定性 argmax + `AiTerm` 十项 + 零随机零记忆；资产引用 = `Artwork : Texture2D` 顶层共有字段 + `Lines : EnemyLine[]`，判定不开音效字段。
- **✅ 道具使用效果面与批次层提交定案（`ADR-0121` / `ADR-0122`）。** 使用效果按世界分两格（`CombatUseEffects` / `OutOfCombatUseOutcome`），`Abilities` 整格移除，新增 `MaxUsesPerCombat`；**战斗外使用不是决策点、是即时提交**（`Debounced` + `SavePointReason.InventoryChanged`），痕迹落新字段 `CharacterProfile.pastItemUse`。上次记的「战斗外使用的两处空缺」整条关闭。

**仍在的两处大卡点（上次三处，减一）：**

- **🔴 数值标杆的取值**（形状已锚定、只欠取值）。`systems/balance.md` 的 12 条待决全部只欠取值，共同前置仍是**两种货币的获取渠道与掉落权重**（产出侧空白则无从反推消耗侧，两条产出曲线必须一同反推）。本次它的**就绪切片显著扩大**：新增敌人 AI 兜底权重向量的字段形态与取值域 · 道具折价系数 `itemPowerRatio` 按 `Charges` 分层 · 战斗内法则强度上沿与 `IgnoresProtection` ≈5% 两条准入 · 道统残卷三张分档表 · `EncounterTighten` 十个界常量的结构 · 重试上限两行由 `HasPremiumBundle` 选行。
- **🔴 战斗内容本体整体为空**（卡牌条目 / 起始卡组 / 敌人条目目录 / 遭遇编排）。机制骨架与数据形态两侧现已闭合，缺的是**条目**——这是内容阶段的工作，不是设计缺口。

**本次新识别的四处问题 —— 已于 2026-08-28 全部处理完毕（用户指派的一次定点修复，不改任何判定）：**

- **✅ 🔴 手牌上限的硬矛盾已收口，定案 = 7。** 用户拍板：`7` 为现行结论。`terminology.md` 的「手牌上限」词条已由 **9 改为 7** 并补上推导回链（起手 4 + 每回合抽 2 ⇒ 第 2 回合即撞上限）。下游两处引用（`balance.md` 的「不占手牌位 ×1.10」溢价、`EncounterTighten` 的 `MinHandLimit >= MinInitialDraw`）本就按 7 写，无需改动。
- **✅ 🟠 三份根级横切已同批改齐。** `terminology.md`：手牌上限 7 · `ProfileChangeSpec` 补 `ItemElements` / `ItemUseElements` 两列 · capability flag / modifier pipeline 两词条改写为两层共用形态（`ADR-0116`）· 新增 10 条词条（`AbilityData` / `EffectData` / `StaticModifierData` / `TimingId` / `EffectCondition` / `DeckOperation` / `InsertPosition` / `EnemyAiProfileData` / `AiTerm` / `ChapterRetryLimitsData` / `ItemUseEntry`）· `Source:` 补至 08-28。`program-overview.md`：回合数改写为 `EncounterSpec.TurnLimit`（`Standard` 档 10）· 重试上限改写为两档表选行 · `EnemyManager` 删「意图生成」改 1-ply argmax · `CapabilityManager` 改两层聚合 · 新增批次层储物袋即时提交（`ADR-0122`）与收口前只读投影 `Project(spec)`（`ADR-0108`）两处落点。`system-overview.md`：Combat 文件夹补 `BattlefieldManager.cs` / `StackManager.cs`（并把孤例 `CombatantDeck.cs` 改为与另两处一致的 `DeckModule.cs`）· `combat.csv` 注释删 `intent` · autoload 树补 combat-service 的五个 manager。
- **✅ 🟠 四份文档的陈旧「ADR 候选」行已改为指向已 Accepted 的 ADR。** `systems/architecture.md` 10 条 → `ADR-0007` / `0008` / `0009` / `0010` / `0011` / `0012` / `0013` / `0014` / `0108`；`systems/viewmodel.md` → `ADR-0010`；`systems/scoring.md` → `ADR-0018` / `0086` / `0052` / `0088`（同批把「固定 10 回合」改写为 `TurnLimit` 遭遇参数）；`systems/monetization.md` → `ADR-0023` / `0024` / `0101` / `0117`。
  - **✅ 同型遗留已在同日第二轮一并清零（另 9 份文档 · 13 条）**：`adventure-event/common-properties.md` → `ADR-0021` · `exchange/_index.md` → `0020` · `research/_index.md` → `0022` · `enemies/_index.md` → `0113` · `player-power/common-properties.md` → `0017`（+ 形态 `0116`）· `combat-service.md` 两条 → `0018` / `0019` · `content-service.md` → `0007` · `life-cycle-service.md` → `0036` · `plot-manager.md` 四条 → `0007` / `0014` / `0016` / `0015`（并补记 `0029`）· `profile-service.md` 两条 → `0017` + `0116` / `0009`（并补记 `0108`）· `vision/scope.md` 两处 → `0006`。**全库主题文档现已零处「ADR 候选（待固化）」措辞**（仅 `ADR-0004` / `ADR-0005` 的背景段仍如实叙述它们当年曾是候选，属史实，不改）。
- **✅ 🟠 `RelicData` 遗留已清零。** `player-power/_index.md` 5 处、`player-profile/_index.md` 1 处全部改写为 `PowerData` 并指向 `character-profile/power/_index.md` 的字段面权威；`player-power/_index.md` 那条「relic / joker 字段清单尚未设计」的待决项随之改写为「类型面已闭合、缺的只是内容条目」。**`content/_index.md` 的 `player-power/` 行经复核已不含该阻塞理由**（现为 🟢「两层共用 `PowerData`，字段清单齐备 + 三条加载期校验」），上次评估的这半条记录已过期，无需改动。

**转手空洞：两处已全部闭合。**

- **✅ `systems/balance.md` 已补「不设 `GlobalBalanceData` 兜底大表；平衡资源按三问判据逐份切」一节**（三问表 + 不设兜底大表的理由与代价 + 三处既有应用 + 「消费点早于 `LoadAll()` 不进注册表」准入边界 + 尚未定名旋钮逐份随消费者再切）。同批：`CombatRulesData` / `EnemyLevelingData` 各补一句注册形态回链，「同步 / 内容管线旋钮」表为 overlay 下载重试 / 退避标注「不进注册表、写死为常量、不可线上调」。`ADR-0074` 的决策段补写「不设兜底大表 + 三问判据」，其「后果」里那条自陈欠账随之关闭。**derive 第 19 步的前置已解除。**
- `enemies/` 侧的同型转手空洞（AI 字段形态）在上次即已闭合——`ADR-0113` 已在 `enemies/` 两份中对位落笔。
- **仍在办的一处小欠账**（同一份 handoff 的另一半承接，本次未做）：`systems/game-progression.md` 的图校验表里「`LocationMapData` 存在多份 / 零份 → `PushError`」那一行应改为回链到 `content-service.md` 的通用单例条数校验，且 `LocationMapData` 的类定义应加上 `ISingletonContent`。**不阻塞任何 derive 步骤**（净减一条手写校验的对账动作）。

**跨边界闭合（强制检查项）：三处，均不致 blocked。**

- **合规域端点的落笔欠账比本库上次记录的更宽。** 上次写「仅未落笔合规域端点自身的错误码」；实际后端 `contracts/compliance.md` 有**两处**未落笔——六端点的**报文字段表**（请求 / 应答字段 · `taskId` 形态 · 导出任务状态机取值）**与**端点自身的错误码。按「契约权威在后端库」判据，真正 settled 的只有**拦截路径**（它搭 `signin` 的车，不需要任何合规端点报文）。⇒ `account-service.md` derive 时须排除 `ComplianceManager` 中凡触及合规端点请求 / 应答体的部分（ticket 兑付 · 冷静期状态呈现 · 导出任务轮询）；`ux/error-and-blocking-ux.md` derive 时排除该批 `ERR_compliance_*` 键的枚举（键由 `code` 机械变换，不需预知码表）。
- **「flags 是否落客户端本地缓存」两侧都无承接项。** 后端 `content-manifest.md` 标注「归对侧裁决」，而本库 `content-service.md` 与 `open-questions/cross-boundary.md` 均未见承接 —— **一处可能被两侧同时当成对方责任的空档**。
- **「二进制资产能否经 overlay / blob 通道下发」两侧都无承接项。** 影响本次新落的 `Artwork : Texture2D`（挂 `EnemyData` / `CardData` / `ItemData` / `PowerData`），但**不阻塞字段 derive**——本库已写到「overlay 覆盖 `.tres` 时该字段随之被覆盖，且指向必须落在随包基线内已存在的资产」为止，形态自足。
- `open-questions/cross-boundary.md` 的「待承接」仍是 1 条（`ComplianceManager` 客户端覆盖面的切分），对侧明写「归客户端自己裁决」——**它是本库自己的取向，不是跨边界缺口**。反向：后端 `cross-boundary.md`「待承接」为空。

**一处应优先于第 1 步裁决的预警：** **境界晋升是否改变角色 / 敌人外观。** 它今天挂在 `art/visuals/_index.md` 的搁置清单里，但杀伤落在 `systems/common-properties.md:247` —— 该文档自标 ⚠「若答为随境界改变，`Artwork` 须升为按境界索引的结构，本节形态要重做」。而 `common-properties.md` 是 derive 顺序第 1 位的 ready 文档，**这条不先裁掉，第一批 `.tres` 的 `Artwork` 基数可能推倒重来**。

### 判定表

| 文档 | 判定 | 卡点 / 就绪切片 |
|------|------|------------------|
| `systems/common-properties.md` | **ready** | 待决问题明写「（无）」，零待复核。整面 = 稳定 `Id` 与展示字段三层切分 · 物化模型 · `LocalizedText`（封闭二值 locale · `Get()` 纯读 · 不落存档）· `ContentEnabled` + `AllEnabled()` / `AllIncludingDisabled()` 双名与编译闸 · `RarityTier` 五档 · **`Artwork : Texture2D` 可空 + 占位回落** · `SourceCode` + `Source` 九值 + 合法子集表 · `ExclusiveSource` · 两级 seeded RNG（轮回级四子流 + `State` / `DrawCount` 持久化；账号级 SplitMix64，8 组测试向量在 `backend-design-documents/contracts/profile-sync.md` §6a，验收可逐位对表）· API 契约总则三形态 / 三失败语义 · `deviceId` 落点。**唯一预警**：`Artwork` 的基数形态自标 ⚠ 依赖「境界晋升是否改变外观」，须在本步之前裁掉 |
| `systems/architecture.md` | **ready** | `## 待决问题` 仅一条自陈「台账事项 · 不阻塞 derive · 不产出新 FR」的 `architecture.md ↔ services/*` 对账，零待复核。整面 = 三层切分 · 七服务边界与 autoload 注册顺序 · EventBus 总则 · API 契约总则 · 边界服务「接口 + Http/Offline 双实现」骨架 · `ElementSpec` / `ModifierKey` / 钳制表 · `ProfileChangeSpec` 逐列 · 「一个新的施加语义该落在哪一列」三级判据 · 八处闭环缺口全闭合。**须与 `systems/viewmodel.md` 同批**；`## 决策(-> ADR)` 的 10 条陈旧候选行**已于 08-28 改为指向已 Accepted 的 ADR** |
| `systems/viewmodel.md` | **ready** | `## 待决问题` 明写「当前无未决项」。整面 = 展示层第三层的结构契约（呈现期对象、不落存档 / 不进云端负载 · 单向依赖且服务 API 面永不返回 ViewModel · 组装源三件套 · 三条重组装触发面 · 只读消费与缓存归属 · `res://art/_placeholder.png` 占位回落单点 · 永不渲染清单）。**须与 `systems/architecture.md` 同批**；陈旧候选行同批已改（→ `ADR-0010`） |
| `systems/services/sync-service.md` | **ready** | `## 待决问题` 仅剩 `pushId` 后端记忆窗口（对侧参数，且报文字段名与序列化形态已由 `backend-design-documents/contracts/envelope.md` 定死），零待复核。整面 = 存档点 ↔ push 解耦 + 5 秒防抖 + 立即 flush 清单 + `PushPolicy` + 本地缓存原子写 + schema 版本化与迁移 / 拒绝 + `CharacterProfile` 级 diff + `revision` CAS / `pushId` 幂等 + `Source` 上行走成员名 + 后端主动写入后的 pull 时序 + 断线降级与退避阶梯 + 两层 Profile 完整字段面 + 回声校验整条 + 图鉴各键序列化形态。**跨边界两向皆闭合**（对侧 `contracts/profile-sync.md` 是后端库首个 ready，`vectors/splitmix64.json` 提供唯一可执行验收检查点） |
| `systems/player-profile/codex/common-properties.md` | **ready** | 无 `## 待决问题` 小节、无待复核项；`ADR-0027` / `ADR-0096` 均 Accepted，存档面整份闭合。整面 = `CodexEntry` 单格 `Id`（`ADR-0070`）+ `CodexKind` 七值 / `CodexUnlock(Kind, Id)` + `CodexElements` 零 `Op` 恒不经 pipeline + 四行失败语义 + 七本的解锁触发（全部搭既有提交，零新增提交点）+ 连锁解锁「一次敌人收录展开为 1 + N 条」+ `eventEnd` 入账射程 + `CodexFlavor` 可选 `LocalizedText` + **本次新增「五本能力 / 道具 / 功法类不套用敌人五项规格」的词条深度表**。**呈现面不在本文件**（在 `codex/_index.md`，blocked），derive 时不取任何图鉴屏 |
| `ux/onboarding.md` | **ready** | `## 待决问题` 明写「当前无未决项」。整面 = 强制登录无游客 + 首版只呈现已实现渠道（手机 / 微信）+ 两步握手 UI（验证码框 + 倒计时始终可见的重发键 + 过期重取，无 hover 提示）+ 绑定 / 解绑不落登录屏 + 首玩篇章门禁 + 解锁为「有可挑战角色」的动态状态。**与 `ux/screen-flow.md` 的登录屏切片重叠，须同批** |
| `systems/services/profile-service.md` | **partial** | **本次切片显著扩大：上次点名的承重卡点「capability flag 枚举与叠加 / 冲突规则（含聚合面宿主服务）」整条倒下**（`ADR-0116`）—— `CapabilityManager` 归本服务，聚合范围 = 两层持有列表 × 同三条与门 · 触发源全走一条 `Recompute()` + EventBus `CapabilitiesChanged` · 叠加 = 集合并幂等 · 冲突结构上关死 · 落地 `HashSet<CapabilityFlag>` 普通序数 enum 不加 `[Flags]` · 新增一个 flag 恰好三步不 bump schema · modifier 合并完整（`ModifierEntry(Key, Op, Value)` · `Scale` 为万分比整数 · 同层求和 → 只乘一次 → 只取整一次 · 两条钳制 · **顺序无关 ⇒ 不设优先级** · 一个 `ModifierKey` 只一个施加点）+ 两条启动期 `#if DEBUG` 断言。其余就绪切片 = `ProfileChangeSpec` **逐列**与逐列失败语义（**列数不写死**——本文件明写「列表数不进承重表述」，08-28 已随 `ADR-0122` 增 `ItemElements` / `ItemUseElements` 两列）+ `CostKey` 16 成员 / `StatKey` 2 项 + `ResourceElements` 配表 + `TryApply` 整批原子性与钳制 + `ActiveCombat` / `RngElements` / `TraceElements` / `CodexElements` / `ItemUseElements` 各通道 + 战斗外使用「一次组装一次事务」门面。其余卡于（全属元进程 / 成就侧）：`status` × 拥有 / 失去的存档编码 · `AchievementManager` 采集面（EventBus 被动 vs 主动上报）· 成就两档奖励内容 · `Achievement` 条目 schema 与各账号级条目的解锁 / 获取 / 失去触发 · `PlayerPower` 平衡边界 |
| `systems/player-profile/player-power/common-properties.md` | **partial** | **本次由 blocked 升级。** 就绪切片 = capability flag + modifier pipeline 两条通道的**声明侧整面**（`ADR-0116`）：扁平 `enum CapabilityFlag` 不分区 · 命名 = 动词三词表 + 禁否定式（union 不变式的可机械检查护栏）· 消费侧「一 flag ↔ 一处消费点」+ `CapabilitiesChanged` 空负载广播 · `ModifierOp {Add, Scale}` 万分比整数 · `ApplyModifier(key, baseValue)` 单点入口 + 两条钳制 · 作用面 opt-in 白名单与四类豁免。**须与 `systems/services/profile-service.md` 同批 derive**（合并算法的权威在服务侧，声明侧字段与命名纪律在本文件；分批必出两份互不相认的叠加规则）。卡于：`status` × 拥有 / 失去的存档编码 |
| `systems/character-profile/deck/common-properties.md` | **partial** | **本次由 blocked 升级——本轮解锁面最大的一处。** 就绪切片 = **效果原语与定义体整面**（`ADR-0115`）：`EffectData` 抽象基类 + 一原语一 `[GlobalClass]` 子类树 + 首批八原语参数表 + 并列定义体 `StaticModifierData`（`ModifierTarget` 五项冻结 · 万分比整数 · 同层求和 → 只乘一次 → 只取整一次）+ `AbilityData` 两格 XOR + `TriggerConditionData` / `TimingIds` 十时点 + `SubjectKind` 相容校验 + `EffectCondition` 三谓词 AND + 关键字哨兵 `int.MinValue` 与恰好一层展开 + **`CardData` 字段清单收口**（`ManaCost` 独立格 · 取消触发器格 · `OnPlay` · `Artwork`）+ **19 条加载期校验** + target / scope 分离与 `EntryFilter` + **存档面零新增字段、空迁移**。**它是卡牌 / 神通 / 法宝 / 法则 / 古宝五类内容的共同底座**，纯数据资源型切片、过三条闸（真实 `.tres` + 注册表按 `Id` 可查 + 负向验收：`TimingId` 不在封闭表 / `Draw.Count < 1` / XOR 违规 ⇒ 启动期 `PushError`）。卡于：starter deck 具体内容 · 抽 / 弃 / 洗数值 |
| `systems/character-profile/deck/_index.md` | **partial** | **本次由 blocked 升级**（「效果原语 / 异能语法未定案」整条倒下）。就绪切片 = 功法 = 卡组构筑单位（`ADR-0054`）+ `CultivationTechniqueData` 轻量 header 形态 + 成员卡反建索引不进散牌产出侧 + 五个 `DeckChangeOp` 与 `Pool == Enemy` 施加侧闸 + `CardType` 五分与无衍生物封闭卡集（`ADR-0041`）+ 次类型体系落地形态（`CardSubtypeData` + id 规范 + 三条加载校验 + 准入判据）+ 卡池三值必填（`ADR-0091`）+ 抽牌堆不重洗与疲劳（`ADR-0052`）+ 手牌上限 **7** 的纯上界语义与四条推论 + 抽牌堆插入位（`ADR-0119`）+ stack / LIFO 不借交互。卡于：starter deck 内容 · 功法规模参数（`MaxTier` / 每层卡数 / 替换幅度）· 关键字与次类型首批清单为空 · 卡组规模与道念量纲基准 |
| `systems/character-profile/power/_index.md` | **partial** | **本次由 blocked 升级**（两条承重卡点由 `ADR-0116` 答定：战斗外复用边界 = 两层共用注册面）。就绪切片 = `PowerData` 字段面（含 `GrantedFlags` / `Modifiers` 两条战斗外通道）+ 两层共用注册面与三条推论 + 三格至少一格非空校验 + 「纯战斗外条目不得挂触发式」校验 + **不得含 `LifeSpan` 产出**（两 `Scope` 皆然）+ 入场三条与门 + 禁用截断五行表（四类通用）+ `IsProtected` 统一置位与 `IgnoresProtection` 唯一后门（`ADR-0106`）+ 战斗内运行态走战场条目 `counters` 零结构增量 + 起手绑定神通（`ADR-0055`）。卡于：持有条目 `status` × 拥有 / 失去的存档编码 · 事件侧获取 / 失去触发 · 与卡牌 / 法宝的边界判据 · 数量与强度尺度 |
| `systems/player-profile/player-power/_index.md` | **partial** | **本次由 blocked 升级**（`RelicData` 一条已作废——该类型名已不存在，字段清单 = `PowerData`；capability flag 体系由 `ADR-0116` 答定）。就绪切片 = 失去的三形态（置换 / 本轮回禁用 / 战斗内 `IgnoresProtection`）+ 同池判据 `(CarrierKind, Scope)` 全同 + 同 `Rarity` + 排除已持有 + 自愿失去阶梯（`ADR-0048`）+ 残卷 Finale 绑定（`ADR-0049`）+ 授予池权重结构与初值 + 「一次轮回至多结算一次」的轮回级布尔位。卡于：战后奖励池按优势档三张权重表 · 获取触发（残卷 / 礼包之外）· 平衡边界（是否影响 cycle seed / 计分公平）· 「失去法则」三支频次预算重配平 · `status` × 拥有 / 失去的存档编码。**5 处 `RelicData` 遗留表述已于 08-28 改写为 `PowerData`** |
| `systems/character-profile/item/_index.md` | **partial** | **切片本次再度扩大**（`ADR-0121` / `ADR-0122`）：使用效果面按世界分两格（`CombatUseEffects` / `OutOfCombatUseOutcome`，必填性由 `UsableScene` 驱动）· `Abilities` 整格移除 · 新增 `MaxUsesPerCombat` 把本场配额从 `Charges` 摘开 · 战斗外三列白名单 + 十列恒空断言 · 不设目标面 · 一次组装一次事务的执行链路 · **战斗外使用不是决策点、是即时提交**（`Debounced` + `SavePointReason.InventoryChanged`，不触发 `RefreshAfterEvent`、照跑终态判定、不消耗 RNG 子流）· 痕迹落 `pastItemUse` · **I-1 ~ I-13 加载期校验** · 储物袋跨两层视图与随售通道、两档回收率的加载期硬校验。**上次记的「战斗外使用两处空缺」整条关闭。** 卡于：**回寿法宝的总量护栏在内容编排面的口径**（承重——取消容量上限把第三道软闸一并拆掉，出现频率 / 库存深度 / 定价三者口径全空）· 道具种类目录 · 「什么该做成卡 / 道具 / 神通」的判据 |
| `systems/services/content-service.md` | **partial** | 就绪切片 = 三层覆盖来源 + 启动期 manifest 比对 + blob 内容寻址 + ES256 detached 签名与 `keyId` 轮换 + 文件级事务 + 增量下载 + overlay 合并与「以 overlay 为准」+ 合并后全量校验 + `ContentEnabled` 不预埋占位 Id + flags 第三层（含单调闸两处观测点，对侧「服务端保证」B 组三条已成文，整条不再排除）+ `contentVersion` 记两个 + 断网降级到随包基线 + 语言覆盖率审计与 `fallback = "zh"` + overlay 剧本例外的 `newIds` 双闸 + 可执行化阶梯 + 单例内容注册与加载期条数校验（`ADR-0030`）+ 统一操作接口。卡于：**唯一待决 = disabled 条目被存档引用时的 UX**（归 `ux/`）。**转手空洞已闭合**：本文件转手给 `systems/balance.md` 的「平衡资源三问判据与逐份落点」已于 08-28 在后者落笔 |
| `systems/services/account-service.md` | **partial** | 就绪切片 = 会话生命周期 + 四个账号方法 + refresh 失败拆两条路径（网络失败 → 缓冲通道 / `auth.session_revoked` → 硬阻塞重登 + 暂停退避，判据为「收到了明确应答」而非「失败了」）+ `AccountInfo` 五字段 + `deviceId` 落点（`ADR-0071`）+ refresh token 明文 `user://cache/refresh-token.json` 带版本 · `AuthManager` 私有 · 不与 `device-id.json` 合并（`ADR-0080`）+ 启动期静默续期使 `Account.Init` 上提到登录屏之前 + 软信号 `reauthRecommended` 的反应形态 + 被挤下线后「先 pull 后 flush」+ 合规拦截路径（搭 `signin` 的车，不需要任何合规端点报文）。卡于：**合规域两处落笔欠账**（对侧 `compliance.md` 的六端点报文字段表**与**端点错误码均未落笔 ⇒ 须排除 `ComplianceManager` 中触及合规端点请求 / 应答体的部分：ticket 兑付 · 冷静期状态呈现 · 导出任务轮询）· `ComplianceManager` 覆盖面切分（本库自己的取向）· 平台密钥库的后置评估 · 多设备并发登录的云端裁决（归后端） |
| `systems/services/future-event-service.md` | **partial** | 就绪切片 = eventOptions 十步管线整条（`ADR-0026`：类型修正乘性 · 多 arc 权重相乘 / 白名单取并 · 三层框定叠加顺序 · `BatchSizeWeights` 掷批次规模且收缩保底走 Travel · `SelectionWeightGrade` 三档 · 满级 Finale 闸门式旁路）+ 候选短缺三道闸（`ADR-0073`）+ `EventOutcomeSpec` 字段面（`ADR-0078`）+ 置换 / 禁用候选前移到物化时掷定 + `Priority = 1` 抬升判据与三条与门子判据（`ADR-0047`）+ Exchange 库存物化与 Explore 取池过滤 + 「本服务纯本地、全部方法形态 A」的 API 面。卡于：`BaseTypeWeights` / `BatchSizeWeights` 每格取值与 Combat 内 `combatTier` 三档配比（统计校准）· 「失去法则」三支频次预算重配平 · **全库唯一残留的 1 项 `[采纳推荐 — 待复核]`**（能力族走 `TryPickGrantableMany` / 其余三族直用 `DrawPool<T>`，属取池门面的落笔位置，按收窄判据不构成降级） |
| `systems/services/plot-manager.md` | **partial** | 就绪切片 = `PlotArcData` + `PlotNodeData` 数据形态（`ADR-0015`，正文内嵌 · key points 每 arc 一条含 `Queued` 态 · 排队 arc 落存档）+ `PlotModulation` 六字段与加载期悬空校验 + 多条 `Active` arc 的合并算法（权重相乘 / 白名单取并）+ `EncounterTighten` 五格增量的方向约束与 `Finale` 整档豁免（`ADR-0077`）+ 档位表 / 阈值 / 回滞 / 跨档叙事的结构面（`ADR-0016`）+ **战斗层零耦合这条边界**（战斗层不读写隐藏属性，全部交互在事件层：生成期调制 · 结算期 outcome 求值 · `eventEnd` 推拉，`ADR-0081`）+ 剧本内容侧 event / EventData 形态 + 剧本树不分包（`ADR-0029`）。卡于：DnD 式选分支的触发点与 UI（含玩家可见 / 不可见分支边界）· 隐藏属性的逐条推拉映射与是否有第四项 · `HiddenStatGrade` 三个映射值与 `EncounterTighten` 六个界常量（只约束标定、不约束结构） |
| `systems/services/life-cycle-service.md` | **partial** | **切片本次扩大**：① `combat` 子流矛盾消解（`ADR-0112`）⇒ 四子流常量清单（map / combat / shop / reward）已无对立面，**确定性验收标准可以写了**；② 08-28 新增「批次层的储物袋操作不是事件内决策点，是独立的即时提交」整段（一次 `TryApply` + 一次本地原子写；三条连带纪律：不触发 `RefreshAfterEvent` · 照跑终态判定 · 不计软阻塞闸门）。就绪切片 = 轮回骨架（两层持有模型 + `ongoing` / `defeated` / `completed` 分类法 + 寿元递减预算与归 0 终态（`ADR-0031`）+ `DefeatReason` 四值与 Finale 失败的显式旁路（`ADR-0025`）+ 「账号级残卷累加先于角色终结提交」顺序不变式 + `AdvanceEventAsync` 固定结算流程与取消语义 + 非战斗四类的事件内决策点 R1/R2/X1/X2/X3 + 判负短路那一路的单次提交 + `SeedManager` 四子流 / `AttachRngState(spec)` 三步机制与「凡消耗子流随机必须同批更新 `State` / `DrawCount`」不变式 + 残卷掷骰走账号级 SplitMix64 与 `FinaleWinOrdinal` 幂等键 + 存档点永不回退（`ADR-0032`）+ 决策点存档（`ADR-0036`）+ 批次层即时提交）。卡于：`experiencePoint` 阈值曲线与产出分布（承重，**与 `lifeSpanCost` 预算互相约束、必须一同反推**）· 隐藏属性的增减触发与是否有第四项 · 元进程各字段的解锁 / 获取 / 失去触发 |
| `systems/services/combat-service.md` | **partial** | **本次排除面由三项减为一项半（本分片最大的判定变化）。** 倒下的两项：② AI 定制策略形态已定（`ADR-0113`：定制层只提供权重向量、不提供代码，与兜底跑同一条 argmax 循环 / 同一套 term / 同一个候选集；兜底 = 1-ply 加权效用评分 + 确定性 argmax + `score(EndTurn) ≡ 0` + 决策粒度逐张 + 零随机零记忆 + `ActiveCombat` 一格不加）；③ `combat` 子流矛盾消解（`ADR-0112`）。就绪切片 = 战斗机制骨架整条（栈与 LIFO 结算 + 战报（`ADR-0086`）· `ActionResult` / `CombatFeedEntry` / `Declared`+`Actual` / `CauseEntryId`（`ADR-0087`）· 疲劳为一等栈条目 + `TurnLimit`（`ADR-0088`）· 三步回合与借 stack 不借交互（`ADR-0039`）· 抽牌堆不重洗、抽空每张扣 1 道念（`ADR-0052`）· 敌人意图整条移除（`ADR-0059`）· AI 两层结构与对称可见输入面（`CombatSnapshot` 双视角化 `ViewerSide`，`ADR-0092`）· **效果流水线四阶段与 `Evaluate` 纯 / `Apply` 唯一写点 + 「原语内随机只能在 `Apply` 内取」** · `ActiveCombat` 存档块与索引序承重 · 决策点 D0–D7 与每点提交形态 · `RewardPickState` 与逐项领取 / 跳过 / 不可反悔 / 候选恒 3 项预先算定（`ADR-0082`）· 战斗前确认页带按钮但不构成决策点（`ADR-0094`）· counters 键空间（`ADR-0075`）· 先后手 `EncounterSpec.FirstSide` · 挂起态恢复与取消）。卡于：**战斗内容本体整体为空**（卡牌 / 起始卡组 / 敌人条目目录 / 遭遇编排——字段面已收口，缺的是条目）· Finale 奖励加厚幅度与逐项领取后的厚度重估（统计校准） |
| `systems/adventure-event/common-properties.md` | **partial** | **排除面本次缩小一条**：`OutcomeRule.DeckOperation` 走池抽的取池链已答定（`ADR-0118`：仅 `AddLooseCard` 开放、其余四 `Op` 的 `TargetId` 必填、复用 `RngStream.Reward`、物化时掷定落存档、新增 `CardTypeFilter` 一格、短缺走清单式 `PushWarning` + 物化期降级）。就绪切片 = `EventOption` 物化字段面 + `OutcomeRule` 与 `HiddenStatGrants` + 「产出即定稿、不得回查模板重算」（`ADR-0012`）+ 事件收口的事务语义（`ADR-0020`）+ `PastEventEntry` 与 `AppliedChange`（`ADR-0021`）+ 寿元回复只走 outcome 侧（`ADR-0066`）+ `lifeSpanCost` 形态定值三变异位（`ADR-0109`）+ 九条加载期校验。**它是后续全部事件类型 FR 的共同类型地基。** 卡于：`lifeSpanCost` 定价表逐格与五类配比取值（统计校准） |
| `systems/adventure-event/travel/_index.md` · `travel/common-properties.md` | **partial** | 就绪切片 = `LocationData` + 单份 `LocationMapData` 载体与恒启用（`ADR-0042`）· 80/20 全局常量不可调制 · 目的地取自邻接集合、`DestinationLocationId` 物化时掷定且不得事后算 · 闸门形态（`ADR-0043`：最高 `Priority = 1`、无 `IsMandatory` 字段）· 结算写入判据为 `DestinationLocationId != ""` 而非 `EventType == Travel` · 痕迹侧记出发地 · 两条 `StatusAssignment` 由 life-cycle-service 组装 · 不计入 `eventCountLimit` 与剧本免疫（`ADR-0111`）· 专有字段与结算写入面（含 Explore 遮罩 Travel 的边界情形）。**两份同批。** 卡于：Travel 行定价（结构已定为 > 0 且为常规基准的 1/3 ~ 1/2）· **location 无法被 flags 秒关时的运营替代通道**（location 恒启用、不参与三层过滤，是 `AllEnabled()` 运营通道在本作唯一的空洞；明确不预设形态） |
| `systems/adventure-event/explore/_index.md` · `explore/common-properties.md` | **partial** | `common-properties.md` 的 `## 待决问题` 明写「无」。就绪切片 = 揭示机制整套（`ADR-0064`）—— 真身分布 = 条目池组成 × 既有加权抽取、三处数据类一律不加字段 · 取池期「真身须同样 enabled」过滤 · 揭示 = `eventStart` 内一次 `with` 派生且 **resolver 按真身选取** · 遮罩卡与其余选项完全同构 · 全屏覆盖层转场、不进屏幕栈、不给任何部分线索 · `IsRevealed` 只在物化侧 · `RevealedEventId` 一格与四条加载期校验。**两份同批。** 卡于：真身占比 `5:3:2` 与定价表 Explore 行取值（内容编排口径）· 转场时长 ≈1.2s 与一次短音效的真机实测 |
| `systems/adventure-event/research/_index.md` · `research/common-properties.md` | **partial** | 就绪切片 = 构筑面板 = 复数决策槽（`ADR-0022`）· 六类操作闭合 · 产出面 = 卡组 + `manaLimit` + `lifeTotal` · 候选物化时掷定走 `RngStream.Reward` · `AllowDecline` 默认 `true` · 不另收资源代价 · 产出禁令扩为「不给任何货币」· 闭关与开局构筑两个取池点各叠 `Pool != Enemy`（`ADR-0091`）· 三道短缺闸 · `ResearchSlotSpec` / `ResearchSlot` / `ResearchCandidate` 形态 · `ProfileChangeSpec.DeckElements` 承载。**两份同批。** 卡于：`Recuperate` 回复量 / 走火入魔风险档权重 / 开局构筑 `lifeSpanCost = 0` 覆盖登记三个数值格 · **功法层数上限 `MaxTier`**（决定 `ResearchCandidate.Amount` 的取值域，权威在 `deck/_index.md` 且仍未定）· 构筑面板竖屏呈现与风险档的非 hover-only 说明通道未设计（归 `ux/`） |
| `systems/adventure-event/exchange/_index.md` · `exchange/common-properties.md` | **partial** | 就绪切片 = 仍走 `GenericEventResolver`（不开第三个 resolver）· 交易逐笔即时提交 · 库存物化时经 `RngStream.Shop` 掷定并落存档 · 五个商品族映射到既有仓储、不新建抽取池 · 「商品族 × 稀有度」定价表的**结构**（格值 = 支付币种 + 基准价，`ExchangeOffer` 增 `Currency` 作物化快照）与两条折扣通道 · `ModifierKey.ShopPrice` 在物化侧单点施加 · 刷新是店级动作、恒以灵石计价 · **售出两条通道**（`ADR-0098`）与「可售出 ⟺ `Kind == CharacterItem`」的代码级常量判据 · 价签必须标明币种 · 三道短缺闸 · 加载期 / 运行期校验与日志约定。**两份同批。** 卡于：四组数值格全欠（定价表逐格 + 每格填哪种币 + 刷新基价与递增量 + 两档回收率绝对值 + 槽位上界，绝对数字被两种货币的获取渠道阻塞）· **Exchange 是否支持以物易物**（口头意图提到，本库零承载——要支持需同时动支付侧形状 / 库存规则字段 / `CanAfford` 语义三处，**待用户拍板**） |
| `systems/character-profile/_index.md` | **partial** | **字段表本次由 24 增至 25**（新增 `pastItemUse : IReadOnlyList<ItemUseEntry>`，与 `pastEvent` 平级的第二条只追加序列 + 寿元曲线读取算法 + 与两个新 spec 列合并为一次 bump、空迁移）。就绪切片 = `CharacterProfile` 完整存档 schema（逐格标注类型 / 写入通道 / 权威 + `Status` 具名子类 12 格 + `currentMana` 不入 `Status` 的判据 + 集合型 build 状态与 `Status` 平级 + `activeEvent` / `eventOption` 两块 + 7 条读档校验）+ 角色 = 有身份模板（`ADR-0055`）。卡于：**角色模板池的内容侧取值面**（池规模 / 是否账号级解锁 / 能否重抽或指定——改写元进程压力模型，承重）· 隐藏属性是否有第四项 |
| `systems/player-profile/_index.md` | **partial** | **字段表本次由 15 增至 16**（`playerItem` 写入通道扩为 `AbilityElements` + `ItemElements`）。就绪切片 = 16 字段表整张（写入通道列无空洞）+ 规则层 / 统计层分层判据（`ADR-0050`）+ 三样不进 Profile 的排除项 + 三个具名子类（`PlayerStatistics` / `PlayerPowerFragment` / `PlayerEntitlement`）+ 受 `ADR-0028` 回声校验约束的顶层键（两侧齐备）+ 残卷 Finale 绑定（`ADR-0049`）。卡于：`Achievement` 条目 schema 与进度模型 · 各账号级条目的解锁 / 获取 / 失去触发 · `PlayerPower` 平衡边界。**1 处 `RelicData` 遗留表述已于 08-28 改写为 `PowerData`** |
| `systems/character-profile/currency.md` | **partial** | 双币字段面与获取 / 花销通道形态整份闭合（`ADR-0089`）—— 灵石 `spiritStone` 承接原单一货币全部角色 · 仙玉 `immortalJade` 为高阶货币且同为轮回级 · `jade` 标识符整体退役 · 获取走既有 `OutcomeSpec.Elements` 不加字段 · 花销唯一出口是 Exchange · 完全不可兑换 · 随售即产出仙玉且免费取得者是净产出。卡于：**两种货币的获取渠道取值与掉落权重整体未设计**（承重——产出侧空白时无从反推消耗侧，**它卡住商店定价表的全部绝对数字**，且两条产出曲线必须一同反推） |
| `systems/character-profile/mana.md` | **partial** | `## 待决问题` 明写「无」，`ADR-0035` 已 Accepted。就绪切片 = `manaLimit` / `currentMana` 字段面 + 两条来源（每章事件推拉 +1~+2 · 进筑基 / 进金丹各 +1）+ 每回合刷满的三步回合节拍 + 出牌费用即 mana。卡于：三个待校准初值（`RealmBreakthroughManaBonus` · **卡牌费用曲线是否随境界上移**（承重，是本文件结论唯一的翻盘前提）· `EnemyManaLimit`），权威均登记在 `balance.md` |
| `systems/player-profile/game-setting.md` | **partial** | 就绪切片 = 切分判据（`ADR-0072`：「取值是否取决于这台机器」+ 换机自检反问 + 拿不准归设备本地 + 一项只落一侧）+ 账号级四项（三条音量轨 `int [0,100]` + `FastCombatAnimation`）+ 设备本地一项（`locale`）+ `SettingChanges` 通道与 `SettingFields` 配表（`int?` / `bool?` 失败语义）+ `PushPolicy` + 启动期 locale 归一 + 首批不收候选与解除条件。唯一残留：三条音量轨默认值 100 / 80 / 100 是**待实测初值**——不阻塞结构，derive 时按初值写入并标注可调 |
| `systems/player-profile/codex/enemy-codex.md` | **partial（近 ready）** | **上次的排除面「立绘 / 台词 / 音效资产引用尚无字段」已由 `ADR-0120` 闭合**（`Artwork : Texture2D` 顶层共有字段 · `Lines : EnemyLine[]` 形态落定 · 判定不开音效字段）。无 `## 待决问题` 小节。就绪切片 = 五项词条规格 + 慷慨度（关键卡 2–3 张、不给样本卡组完整列表）与退让阶梯 + 词条②指向真实功法条目 + 「关键卡牌必在样本卡组内」是加载期机械检查（基准 = 展开产物 ∪ 散牌）+ 总长 150–280 字 + 图鉴静态性三句。仅余排除：`[Practice, Standard]` 条目的词条须同时说得通「切磋」与「厮杀」的**编写口径**（属内容阶段） |
| `systems/player-profile/codex/technique-codex.md` | **partial（近 ready）** | `## 待决问题` 明写「暂无」。就绪切片 = 词条构成三项全部取自条目自身已有字段 + **词条不列该功法的卡牌清单**（承重：否则可沿「敌人词条② → 功法图鉴」反查出敌人完整样本卡组，实质绕过敌人本的慷慨度裁决）+ 两条解锁路径（零新增提交点）+ 幂等收录 / 升阶不产生新条目 / 弃置不删条目 + `Pool == Enemy` 照常收录且标注由呈现层现算 + 完成度分母取 `AllIncludingDisabled()` + 存档面纯加法空迁移（后端零配合）。卡于：呈现面整体不在本文件（在 `codex/_index.md`，blocked）· `MaxTier` 未定值阻塞条目编写（不阻塞词条结构） |
| `systems/enemies/_index.md` · `enemies/common-properties.md` | **partial** | **两处排除面本次倒下**：定制 AI 策略的表达形态已定（`ADR-0113`：独立可复用资源 `EnemyAiProfileData` + `AiWeight[]` 稀疏覆写 · 直接类型引用 · 可空即兜底 · 不挂 `Rarity`），资产引用已定（`ADR-0120`）。就绪切片 = `EnemyData` 数据形态与全部加载期校验 + `TechniqueRef` 构筑面 + 游离散牌 + `KeyCardIds` 2–3 + `EncounterScopes` / `ChapterScope`（`ADR-0110`）/ `PoolScope` 与六条取池校验 + 赋级带 `±2` 无例外（`ADR-0044`）+ 展开产物不得写回条目 + item / power 持有列表（含 `IgnoresProtection` 两条硬准入）+ 编排护栏 + **AI 兜底算法 1-ply 加权效用评分 + 确定性 argmax + `AiTerm` 十项 + 零随机零记忆 + 三条结构性上界**（`Value` 钳制越界 `PushError`）+ AI 策略字段六条校验。**两份同批。** 卡于：**`LineSlot` 成员清单为空**（枚举写不出，`Lines` 只能恒为空数组，待一次战斗 UX 专场）· 道念产出缩放曲线取值 · 敌人条目目录整体为空 |
| `systems/game-progression.md` | **partial** | 就绪切片 = 篇章 / 境界推进与检查点重试模型（`ADR-0004`）· 存档点即结束点、途中死亡从章首重试 · 修行等级体系（realm + level，`ADR-0034`）· `eventCountLimit` 的地域配额语义与剧本免疫（`ADR-0111`）· 事件循环与 Finale 胜负即篇章闸门（`ADR-0025` / `ADR-0065`）· location 与 Travel 路由的 80/20 全局常量 · 经验点进程（`ADR-0038`）。卡于：中长期**进度感**那一半（图鉴只给方位感，是否需篇章进度条 / 前瞻提示未定）· 选择区排布与滑动手感（批次规模 1–5，两端差 5 倍，竖屏如何同屏容下 + 寿元告警静态标注如何嵌入）· `BaseTypeWeights` 五类配比取值 · **blind / ante 缩放曲线整体尚未陈述** |
| `systems/balance.md` | **partial** | **就绪切片本次显著扩大**（08-27 ~ 28 新增五组）。可作为第一批平衡资源 `.tres` 落地的已定格 = `baseMomentum` 表 · 全局等级序 1–22（`ADR-0034`）· 赋级带 `±2` 与 `EnemyLevelingData`（`ADR-0044`）· `lifeTotal` 境界基线 10/25/40（`ADR-0045`）· 起手 4 / 每回合抽 2 / **手牌上限 7** · `manaLimit` 每次大境界 +1（`ADR-0035`）· 越级追分形状锚点 · `MaxConcurrentSideArcs = 2` · `TravelFullFanoutChance = 0.80` · `BatchSizeWeights` / `SelectionWeightGrades` · `GrantPoolWeights` 40/27/18/10/5 · 取池余量三格 · **重试上限两行由 `HasPremiumBundle` 选行（`ADR-0117`）** · **敌人 AI 兜底权重向量与取值域（`ADR-0113`；十个数字为占位值但字段形态 / term 清单 / 校验规则可落笔）** · **道具折价系数 `itemPowerRatio` 按 `Charges` 分层** · **战斗内法则强度上沿与 `IgnoresProtection` ≈5% 两条准入（`ADR-0106`）** · **道统残卷三张分档表** · **`EncounterTighten` 十个界常量的结构与两条硬性约束** · 平衡资源经 `Content.Single<T>()` 取（`ADR-0074`）。其余 12 条待决**全部只欠取值**，共同前置是两种货币的获取渠道。**上次点名的结构性空缺已于 08-28 补齐**：新增「不设 `GlobalBalanceData` 兜底大表；平衡资源按三问判据逐份切」一节（含准入边界与三处既有应用），两份单例资源各补注册形态回链 ⇒ **derive 第 19 步的前置已解除** |
| `systems/scoring.md` | **partial** | 规则面完全闭合且与战斗内容解耦。就绪切片 = **计分规则与转换公式本身**（一组纯函数 + 一份平衡资源，验收靠给定输入验算输出，不必等一场可运行的战斗）—— 道念 = 胜负判据 · 产削与下限 0 的每次结算截断（溢出不结转，LIFO 因此影响结果）· 疲劳为第二条削减通道（`ADR-0052`）· 负侧 1:1 扣 `lifeTotal` · 胜侧两条支路 + 单价表 · `WinMargin` 在 `Finale` 退场（`ADR-0018`）· `combatTier` 三档遭遇参数 · 战斗层不读写隐藏属性（`ADR-0081`）。卡于：三档 `BaseReward` / `RewardPoolId` 取值与卡牌量纲基准（统计校准）· 逐项领取后的奖励厚度重估 |
| `systems/monetization.md` | **partial** | **卡点本次减一**：「重试上限的存档表达三选一」已由 `ADR-0117`（上限两档表住平衡资源、`HasPremiumBundle` 选行）+ `ADR-0101`（计数落 `CharacterProfile.chapterRetry`）答结。就绪切片 = `PlayerEntitlement` 两字段与不变式 `0 ≤ Redeemed ≤ Grant`（`ADR-0023`）+ 空池三道闸 + 购买入口三条前置与灰态 + 兑现事务（`AccountRng.For(PremiumBundle, ordinal)` 一次派生连抽 3 条 → 一次 `TryApply` → `Immediate` push，`ordinal = Redeemed + 1` 逐一追平）+ 五项付费面排除 + 三渠道纳入 MVP（`ADR-0024`）+ 唤起内购失败一律回主菜单、零 Profile 变更零痕迹。卡于：**平台内购 SDK 的选型与封装层未定 ⇒ 购买段无法落地**（牵动 Godot 导出配置与各平台构建，归一次客户端工程蓝图）· `K` 与 `GrantPoolMargin` 数值 · 两个通用池当前条目为零 · 纯外观付费点做成什么 |
| `ux/error-and-blocking-ux.md` | **partial** | 就绪切片 = 翻译键基建整条（文档自命名的 `FR-ux-translation-foundation` 五件事：`res://text/` 分区 CSV · `project.godot` 注册 + 默认 locale `zh` + `fallback = "zh"` · `ErrorText` / `TranslationAudit` · 键命名三条 + 禁令 · 三条审计与定死的调用顺序）+ `ERR_*` 由 `code` 机械变换、无第二张对照表（`ADR-0053`）+ 内容侧不烘焙可翻译文本（`ADR-0084`）+ 两层本地化（`ADR-0056`）+ `reasonKey` 三参 `ErrorText.For` 与静默回落 + 灰态判据 + `BlockingNoticeScreen` 一屏三变体 + 三档版本提示只呈现最高一档 + 诊断编号出口。卡于：逐条中文措辞（内容充实，文档明写不阻塞结构）· `auto_translate_mode` 与 `#if DEBUG` 判据的实测（两种结果下键形态 / 分区表 / 三条审计完全相同，宜合并到 `.csproj` 生成后的一次实测）· **跨边界一小块：`ERR_compliance_*` 键的枚举须排除**（对侧合规域端点错误码未落笔） |
| `ux/screen-flow.md` | **partial** | 就绪切片 = 登录屏 + 主菜单五入口导航骨架与 Store 三条呈现纪律 + 建议更新横幅去重 + 储物袋全屏面板（纵向滚动网格 + 筛选 chip · 无容量上限 · 同屏呈现灵石与仙玉 · 详情卡片「使用」/「售出」两键 · 就地二段确认 + 同币回收价预览 · 售罄 offer 保留占位而售出条目直接移出的两条相反判据各有理由）+ 玩家档案屏「持有的古宝」只读区 + 绑定 / 解绑两处二次确认 + Settings 屏 + 角色状态条只常驻灵石 + 手势判据（`ADR-0085`）+ 各入口读取的两层 Profile 字段逐格可指。卡于：元婴证书（通关证书）形态 · 成就两档奖励内容 · 寿元告警是否伴音效 / 震动 · Explore 揭示转场时长与音效实测 · `PlayerPower` 获取 / 失去触发与平衡边界 · 图鉴族在主菜单如何组织 |
| `ux/combat-ux.md` | blocked | **上次评估整份漏列**（自述扫描 `ux/`（5）却只列 4 份）。文档实质内容不少（呈现事件流三消费者 · 战报收起 / 展开同一份数据两视图 · 条目粒度 = 结算事件 · 结构化存储渲染期才套翻译键 · 不落存档 · 固定预留 ≈ 屏高 6% · 结算节拍 0.35–0.5s / 敌人回合总时长 ≤ 4s · 持有物两层面板 · 埋伏只给计数 · 敌人等级用境界名），但**两条 `承重` 待决恰好落在唯一候选切片内部**：① 战报条目的文案体系（收起态单行信息量 · 截断量与 fizzle 如何在单行内表达 · 与飘字的分工边界）② 竖屏分区整体是否过载（根子是把 MTG 完整分区模型搬进手机竖屏，**栈是否必须常驻值得重估**；已排期专场，其结论会改写战报的落位与固定预留）。加之另 7 条形态未定。**唯一可能独立成立的非视觉切片（呈现事件流契约）其自然宿主是 `combat-service.md` 的 `ActionResult` / `CombatFeedEntry` 切片，在此 derive 必与之撞车** |
| `systems/adventure-event/_index.md` | blocked | 五类之间的配比与 Combat 内 `combatTier` 三档配比**只欠取值**（运算形态已随 `ADR-0026` 落定）；行为面已被 `future-event-service` 与 `adventure-event/common-properties.md` 的切片覆盖 ⇒ **不另产 FR，避免撞车** |
| `systems/adventure-event/combat/_index.md` · `combat/common-properties.md` | blocked | 六条待决**全部指向战斗内容本体**（量纲基准 · 三档奖励厚薄 · 逐条目 `HiddenStatGrade` 编排 · 叙事一致性口径）；`common-properties.md` 自身唯一待决（敌人 schema 其余字段）已随 `ADR-0120` 基本闭合，但其承载面（单数 `Enemy` · `EncounterSpec` 参数 · 回合结构 · 资源引用）**全部由 `combat-service.md` 与 `enemies/` 承载** ⇒ 无可独立成立的切片 |
| `systems/character-profile/life-total.md` | blocked | 规则面已定（境界基线 10 / 25 / 40、归 0 = 角色终结、单一数值不分层，`ADR-0045`），但**不构成可独立成立的 FR 面**——字段住在 `CharacterProfile` schema 内、消费在战斗与事件两侧，应随那两处一并落地；回复幅度与来源分布归统计校准 |
| `systems/character-profile/item/common-properties.md` · `power/common-properties.md` | blocked | 两份的自有面均只有 `SourceCode` 一条（权威在 `systems/common-properties.md`）+ 三层分工回链，字段清单明写「不复述、权威在 `_index.md`」⇒ **无独立 FR 面**；唯一自有待决格 = `status` × 「拥有 / 失去」两正交维度的 schema 编码（与能力四类同源）。**⚠ `item/common-properties.md` 抬头仍写「占位结构，细节待定」，与 `item/_index.md` 的完整字段面不符** |
| `systems/player-profile/account-info.md` | blocked | 字段面五项已定，仅剩合规字段归属一条待后端分级 ⇒ 表仍可能增行；**且单独不构成可独立成立的 FR 面**，应随 `account-service` / 登录切片一并落地 |
| `systems/player-profile/achievement/_index.md` · `achievement/common-properties.md` | blocked | 成就条目 schema 与进度模型未设计（分组结构、加权进度的权重来源、触发条件表达、隐藏成就揭示时机）· 触发采集面未定（EventBus 被动 vs 主动上报）· 两档具体奖励条目清单未定。「能给什么」与「怎么给」均已答结（法则 / 古宝、指定条目 + `ExclusiveSource == AchievementReward` 不进任何抽取池，故成就奖励恒不落空）；缺的是目录本身，而它又依赖法则 / 古宝条目 |
| `systems/player-profile/codex/_index.md` | blocked | 图鉴族在主菜单的入口与浏览形态未定 · `LocationCodex` 除连边外的词条深度未定、且其呈现形态与其余各本不同（一张逐步显影的图 vs 列表 / 网格）· 边缘顶点显示程度与显影半径 ⇒ **呈现面整体无可 derive 的切片**；存档 / 写入面已下沉到同目录 `common-properties.md`（ready），词条规格已下沉到两本专档（partial）。战斗内一律不可查已由 `ADR-0094` 答定 |
| `systems/player-profile/player-item/_index.md` · `player-item/common-properties.md` | blocked | **排除面本次缩小**（战斗外效果形态已由 `ADR-0121` 答定，权威在 `character-profile/item/_index.md`），但其类型面归 `ItemData`（两层共用）、存档面归 `player-profile/_index.md` ⇒ **无独立 FR 面**；自身的古宝种类目录 / 次数补充机制 / 可购价格与库存权重三项仍未设计，`common-properties.md` 共有字段全为占位 |
| `vision/pillars.md` · `vision/references.md` | blocked | 北极星 / 参考登记，**非 derive 对象**（其余文档的挂靠前置）。`pillars.md` 第 9 条支柱「信息只能靠遭遇获得」已由 `ADR-0093` 覆盖并写明射程边界；`references.md` 唯一悬空项：「月圆之夜 · 规避」仍为待定 |
| `vision/scope.md` | blocked | 非 derive 对象 + **两项仍未定且无处承载**：① 美术资源 AI 生成的商用授权 / 参考素材版权立场（`art/_index.md` 把落点指向本文件，本文件零承载）② **初装包体预算**（`deferred-content.md` 同样指向本文件，零承载；它向下卡住整个 `soundtracks/` 分区的时长 / 码率）。十天来零推进 |
| `terminology.md` | blocked | 根级词表，**非 derive 对象**（行为面由 `systems/**` 承载）。**上次点名的四项已于 08-28 全部改齐**：手牌上限 9 → **7** · `ProfileChangeSpec` 补两列 · capability flag / modifier 改两层共用形态 · `ADR-0115` / `0113` / `0117` / `0118` / `0119` / `0122` 的词条补齐 · `Source:` 补至 08-28 |
| `program-overview.md` · `system-overview.md` | blocked | 根级运行时调用链与 Godot 工程落地形态，**非 derive 对象**。**上次点名的失真已于 08-28 全部改齐**：回合数改写为 `EncounterSpec.TurnLimit`（`Standard` 档 10）· `EnemyManager` 删「意图生成」改 1-ply argmax · 重试上限改两档表选行（`ADR-0117`）· `CapabilityManager` 改两层聚合（`ADR-0116`）· 补 `ADR-0108` 只读投影与 `ADR-0122` 批次层提交两处落点；`system-overview.md` 补 `BattlefieldManager.cs` / `StackManager.cs`、`CombatantDeck.cs` → `DeckModule.cs`、`combat.csv` 注释删 `intent` |
| `art/_index.md` · `art/visuals/_index.md` · `art/soundtracks/_index.md` | blocked | 生产流水线说明与资产类目表（`ADR-0040`）；本库不承载生成产物（归 `game-feature-branch/`）⇒ **结构性地无客户端行为面可 derive**。已实部分不少（`visuals/` 八行类目表 · `Artwork` 挂点与三行例外 · 功法无独立视觉资产（`ADR-0120` 已填实）· full art（`ADR-0083`）· 不烧入可翻译文字（`ADR-0084`））。未决：AI 生成资产的商用授权（落点 `vision/scope.md`，零承载）· 参考素材二进制是否入库 · guide 粒度 · 生成资产落 `game-feature-branch/` 的目录划分 · **二进制能否经 overlay / blob 下发（跨边界两侧均无承接）** · UI 元件是否走 AI 生成 · **境界晋升是否改变外观（倒灌进 `common-properties.md` 的 `Artwork` 形态，见上）** · 音频生成工具未定案（自陈「本分区第一前置」）· BGM 时长码率的包体预算 |
| `art/visuals/art-direction.md` · `art/soundtracks/audio-direction.md` | blocked | 两份的 `## 一句话方向` 本身即 `> _（待写）_` 占位 ⇒ **判据①即失败**。另各 4~5 处占位（色彩 / 光照材质 / 构图三项 / 尺寸格式 / 禁用清单；配器音色 / 调式节奏 / 动态混音 / 格式预算 / 禁用清单）。`ADR-0100`（画风取向）Accepted，但明写「色板、光照材质、构图等逐项约束仍在推敲，落点是本文件」并把「grimdark 低饱和 × 手机高对比」的张力交回 ⇒ ADR 不构成对占位的覆盖 |
| `art/visuals/animations/_index.md` | blocked | 明写占位：范围四类 / 技术载体 / 制作方式 / 与战斗节奏与篇章时长预算的关系**全部「待咨询专业人士后确定」**，尚无设计意图。`ADR-0040` 只固定了「动画不走 AI 流水线」这一条排除性结论。**一条孤悬约束**：「动画可跳过 / 可加速」是交互要求，只存在于本文件，`ux/combat-ux.md` 无承载 |
| `art/visuals/references/_index.md` · `art/soundtracks/references/_index.md` | blocked | `visuals/` 三条登记（三国杀 / 弈仙牌 / Warhammer 40k）已细化到可转 prompt 的粒度但「素材位置」三行全为待定；`soundtracks/` 登记表为空——**一条参考都未登记**，判据①的实质内容不存在 |
| `art/visuals/guides/_index.md` · `art/soundtracks/guides/_index.md` | blocked | 台账均为空，**零份 guide** ⇒ 无 derive 对象；约定本身已成文，但上游两份 direction 文档均 blocked 且「guide 粒度」未决 ⇒ 连第一份 guide 都写不了 |
| `systems/_index.md` · `systems/services/_index.md` · `ux/_index.md` · `content/_index.md` | blocked | 导航索引 / 层级词表 / 登记表，**非 derive 对象**（各自的待决项已下沉到对应主题文档）。`services/_index.md` 承载的七服务边界 / 五级层次词 / 拆分轴 / 两条唯一入口 / API 契约八条总则，权威在 `architecture.md` 与各服务文档 ⇒ 单独 derive 必与之重复。（`content/_index.md` 持内容类型的 **scaffold** 就绪度，与本小节的 derive 就绪度是两套闸，勿混。08-28 复核：其 `player-power/` 行已是 🟢「两层共用 `PowerData`，字段清单齐备 + 三条加载期校验」，上次记录的 `RelicData` 阻塞理由已不在文内） |
| `decisions/ADR-0002` ~ `ADR-0122`（121 份 Accepted） | blocked | 已采纳的决策记录，**非 derive 对象**（作为其余文档的就绪前置，本身不产 FR）。本次由 98 份增至 121 份：2026-08-28 的一次 `/write-adr all` 逐份核对 29 份未被任何 ADR 引为来源的 handoff，固化 **23 份新 ADR**（`ADR-0100` ~ `ADR-0122`），覆盖 sync 族 · 命名通则 · 战斗 RNG 单流 · 敌人 AI 权重向量 · 效果原语语法 · capability flag / modifier · 道具使用效果面与批次层提交 |
| `decisions/ADR-0001-example.md` · `decisions/_index.md` | blocked | 示例占位（`## Decision` 明写「待定」，status 仍为 Proposed —— 全库唯一非 Accepted 的 ADR）与台账，非 derive 对象 |
| `art/*/guides/_TEMPLATE.md` · `content/_TEMPLATE-*.md` · `requirements/_TEMPLATE*.md` | blocked | 模板骨架，非 derive 对象 |

### 建议的 derive 顺序（仅限 ready / partial 项；被依赖者在前）

**落笔前的三件零决策修正 —— ✅ 已于 2026-08-28 全部完成**：① `terminology.md` 手牌上限 9 → 7（用户拍板取 7）；② 四份文档的 `## 决策(-> ADR)` 陈旧候选行已改为指向已 Accepted 的 ADR；③ 三份根级横切已随 `ADR-0100` ~ `0122` 同批改齐。
**落笔前的必须裁决 —— 由两件减为一件**：① **仍在办：境界晋升是否改变外观**（卡第 1 步的 `Artwork` 基数）；② ✅ `balance.md` 的「不设 `GlobalBalanceData` + 三问判据」小节已补齐，**第 19 步的前置已解除**。

1. `/derive-requirements systems/common-properties.md` —— **唯一整份就绪且被几乎所有内容与服务代码依赖的地基**。共有字段类型与两级 RNG 先落，后面每一份 FR 都能直接引用。账号级 RNG 的验收有现成的 8 组测试向量（后端 `contracts/profile-sync.md` §6a），是全库当前最硬的验收标准。文档自己写明排期——`LocalizedText` 与 `DrawPool<T>` 同批、**在写下第一批 `.tres` 之前**。**前置：先裁「境界晋升是否改变外观」。**
2. `/derive-requirements ux/error-and-blocking-ux.md` —— 翻译键基建（文档自己已点名 `FR-ux-translation-foundation`）。**无上游依赖**，是「每屏从第一行起就用键」这条起手纪律的前置，越早越省返工。**排除逐条中文措辞与 `ERR_compliance_*` 枚举。**
3. `/derive-requirements systems/architecture.md` + `systems/viewmodel.md` —— 服务骨架：七个 autoload 的注册顺序、EventBus、边界服务的接口 + Offline stub 双实现、`ProfileChangeSpec` 逐列。**两份同批**：三层切分的定义在前者、展开在后者，分两次必出两份互相打架的 FR。两份均 ready、无排除面。
4. `/derive-requirements systems/services/profile-service.md` + `systems/player-profile/player-power/common-properties.md` —— 写入通道这一层地基（逐列 spec + `TryApply` 原子性与钳制 + 逐列失败语义 + `CapabilityManager` 整面 + modifier 合并算法）。**两份必须同批**：合并算法的权威在服务侧、声明侧字段与命名纪律在 `player-power/common-properties.md`，分批必出两份互不相认的叠加规则。**列清单以 `profile-service.md` 现状为准（本文件明写列数不写死）。排除成就族与 `status` × 拥有 / 失去的存档编码。**
5. `/derive-requirements systems/character-profile/_index.md` + `systems/player-profile/_index.md` + `systems/player-profile/codex/common-properties.md` + `systems/player-profile/game-setting.md` + `systems/character-profile/currency.md` + `systems/character-profile/mana.md` —— 两层 Profile 的存档 schema 地基。**六份同批**：互相引用、共用同一个 `schemaVersion` 与同一条迁移路径，分批必出互相打架的 FR。**只取存档 / 写入面，不取任何图鉴屏或设置屏呈现，不取货币取值。**
6. `/derive-requirements systems/services/content-service.md` —— 内容加载与校验链路（依赖 1、3）。被一切内容读取方依赖。**仅排除 disabled 条目被存档引用时的 UX。**
7. `/derive-requirements systems/services/sync-service.md` —— 本地缓存原子写 + push 调度与退避 + CAS / 幂等 + 两层 diff + 回声校验完整字段面（依赖 1、3、4、5）。**ready，无排除面。**
8. `/derive-requirements ux/screen-flow.md` + `ux/onboarding.md` —— 登录屏 + 主菜单导航骨架 + 储物袋面板 + 玩家档案屏（依赖 2、3、5）。**两份同批**（登录屏切片重叠）。**排除元婴证书、成就奖励、图鉴入口与全部待实测手感项。**
9. `/derive-requirements systems/services/account-service.md` —— 会话生命周期 + 四个账号方法 + refresh 两条路径 + `deviceId` 与 refresh token 落点 + 启动期静默续期 + 合规拦截路径（依赖 2、3、7、8）。**排除 `ComplianceManager` 中触及合规端点请求 / 应答体的部分**（ticket 兑付 · 冷静期状态呈现 · 导出任务轮询）。
10. `/derive-requirements systems/character-profile/deck/common-properties.md` —— **效果原语层地基（本次新解锁，玩法侧最大的一块）**：`EffectData` 子类树 + 八原语 + `StaticModifierData` + `TriggerConditionData` / `TimingIds` + `EffectCondition` + `CardData` 收口 + 19 条加载期校验。**它是五个内容类型的共同底座，必须先于 11–13。** 纯数据资源型切片、过三条闸。**排除 starter deck 与抽 / 弃 / 洗数值。**
11. `/derive-requirements systems/character-profile/deck/_index.md` —— `CultivationTechniqueData` + 成员卡反建索引 + `CardSubtypeData` + `CardPool` 三值 + 五个 `DeckChangeOp` 与 `Pool == Enemy` 施加侧闸 + 手牌上限纯上界。**排除全部规模参数与首批清单。**
12. `/derive-requirements systems/character-profile/power/_index.md` —— `PowerData` 字段面 + 三格非空校验 + 战斗外触发式校验 + 不得产寿元 + 禁用截断五行表（依赖 4、10）。**排除持有条目 `status` 编码与获取 / 失去触发。**
13. `/derive-requirements systems/character-profile/item/_index.md` —— `ItemData` 两格使用效果面 + `MaxUsesPerCombat` + I-1 ~ I-13 校验 + 战斗外一次事务链路 + 随售通道（依赖 4、5、7、10、12）。**排除回寿护栏口径与道具目录。**
14. `/derive-requirements systems/adventure-event/common-properties.md` —— `EventOption` 数据形态 + 事件收口事务语义 + `AddLooseCard` 取池链（依赖 1、5、10）。**它是后续全部事件类型 FR 的共同类型地基**，必须先于 18。**不取定价表与配比取值。**
15. `/derive-requirements systems/services/future-event-service.md` + `systems/services/plot-manager.md` —— eventOptions 十步管线 + 三道短缺闸 + `Priority` 抬升判据，以及它的调制输入。**两份必须同批**：管线的第二、三层框定读的就是 plot-manager 的输出。**排除全部权重取值与隐藏属性的逐条推拉映射。**
16. `/derive-requirements systems/services/life-cycle-service.md` —— 轮回骨架、角色状态分类法、`DefeatReason` 四值与 Finale 旁路、存档点与决策点清单、`SeedManager` 四子流、批次层即时提交（依赖 4、14、15）。**`combat` 子流的裁决已由 `ADR-0112` 完成，本步不再有前置。排除 `experiencePoint` 曲线与隐藏属性触发。**
17. `/derive-requirements systems/game-progression.md` —— 篇章 / 境界推进与 `eventCountLimit` 配额（依赖 15、16）。**排除选择区呈现、进度感与 blind / ante。**
18. 四个非战斗事件子类型，按依赖薄厚依次：`travel/` → `explore/` → `research/` → `exchange/`（各自两份同批；依赖 14、15、16）。**四份的机制面已闭合，全部排除数值取值**；`research/` 另排除构筑面板竖屏呈现与 `MaxTier` 相关取值域；`exchange/` 另须排除「是否支持以物易物」（待用户拍板）。
19. `/derive-requirements systems/balance.md` —— **第一批平衡资源 `.tres`**：已定值 / 已定形的那些表 + `Content.Single<T>()` 读取面 + 加载期条数校验（依赖 1、6）。**只取已定格，全部未定格显式留空并标注。**（「不设 `GlobalBalanceData` + 三问判据」那一小节已于 08-28 补齐，本步不再有前置。）
20. `/derive-requirements systems/enemies/_index.md` + `enemies/common-properties.md` —— `EnemyData` 数据形态与十余条加载期校验 + `EnemyAiProfileData` 与兜底 argmax（依赖 10、11、19）。**两份同批。** 纯数据资源型切片、过三条闸。**排除 `LineSlot` / `Lines` 整块与全部数值缩放。**
21. `/derive-requirements systems/player-profile/codex/enemy-codex.md` + `codex/technique-codex.md` —— 两本的词条规格与解锁触发（依赖 5、20）。**两份同批**（连锁解锁横跨两本）。**只取词条数据源与解锁写入，不取任何图鉴屏呈现。**
22. `/derive-requirements systems/scoring.md` —— 道念计分规则与两条转换支路（依赖 19）。**一组纯函数 + 一份平衡资源**，验收靠给定输入验算输出。**排除三档 `BaseReward` / `RewardPoolId` 取值。**
23. `/derive-requirements systems/services/combat-service.md` —— 战斗机制骨架 + 效果流水线四阶段 + 决策点 D0–D7 + AI 两层结构（依赖 10、16、20、22）。**只排除战斗内容本体与 Finale 奖励加厚取值**（上次记的另两项排除已作废）。验证靠在编辑器里跑一场用占位内容拼出的遭遇观察栈与回合流转。
24. `/derive-requirements systems/player-profile/player-power/_index.md` —— 失去三形态 + 残卷 + 授予池结构（依赖 4、5、12）。**排除战后奖励池三张权重表与获取触发。**
25. `/derive-requirements systems/monetization.md` —— 兑现段 + 三道闸 + 入口前置条件 + 兑现水位幂等（依赖 1 的账号级 RNG、7 的 push 调度）。**排在最后**：依赖的两个通用内容池当前为空；**只取兑现与拦截，购买段等平台 SDK 选型。**

> **仍然不要**对元进程失败侧 / 成就 / 古宝目录 / 图鉴呈现侧 / `ux/combat-ux.md` / art 任何文档 derive。玩法侧的欠账由上次的**三处收敛为两处**（效果原语已定案、`combat` 子流已裁决）：**① 数值标杆的取值**（只欠取值不欠结构，前置是两种货币的获取渠道）；**② 战斗内容本体**（条目目录整体为空，属内容阶段而非设计缺口）。

### 最短解锁路径（blocked 项，按解锁面从大到小）

1. **一次「战斗 / 事件 UX 专场」** —— 解锁面最宽的一次会：① `ux/combat-ux.md` 的两条承重（竖屏分区是否过载 / 栈是否必须常驻 · 战报条目的文案体系）② `enemies/` 的 `LineSlot` 成员（台词呈现落点，解锁 `Lines` 与三条相关校验）③ `research/` 的构筑面板竖屏呈现与风险档非 hover-only 通道 ④ `codex/_index.md` 的图鉴族入口与浏览形态。**一场会同时把 `ux/combat-ux.md` 送进 partial 并收窄三份 partial 的排除面。** → `/analyze-new-ideas`
2. **一次裁决：`status`（启用 / 禁用）× 「拥有 / 失去」两个正交维度如何编码进 schema** —— 能力四类同源，一次答齐即解锁 `item/common-properties.md` · `power/common-properties.md` · `player-power/` 持有条目面，并同时收窄 `profile-service.md` / `power/_index.md` / `player-power/_index.md` 三份 partial 的排除面。**成本最低、解锁面第二宽。** → `/analyze-new-ideas`
3. **两种货币的获取渠道取值与掉落权重** —— 它是 `balance.md` 全部 12 条待决取值的共同前置，也是 `currency.md` / `exchange/` 唯一的实质卡点。两条产出曲线必须一同反推。→ `/provide-solution-draft` 后 `/analyze-new-ideas`
4. **一次 handoff 定 `Achievement` 条目 schema + 进度模型 + 采集面**（EventBus 被动 vs 主动上报）—— 解锁 `achievement/` 两份，并收窄 `profile-service.md` / `player-profile/_index.md` 的排除面；奖励目录另需法则 / 古宝条目先存在。→ `/analyze-new-ideas`
5. **一次 handoff 定古宝种类目录 + 次数补充机制 + 可购价格 / 库存权重** —— 解锁 `player-item/` 两份（类型面已由 `ItemData` 覆盖，无需再设计字段）。→ `/analyze-new-ideas`
6. **`vision/scope.md` 的两条零承载项**（AI 生成资产的商用授权与版权立场 · 初装包体预算）—— 它们被 `art/_index.md` 与 `deferred-content.md` 指名落点为本文件而本文件零承载；答定后再开一次美术方向会与一次音频方向会填实两份 direction 的占位，`art/` 分区才可能开始产 guide。**art 台账类文档即便全部答完仍结构性地不产 FR**，不必期待它们进 derive 队列。→ `/analyze-new-ideas`
7. **对侧欠账：`backend-design-documents/contracts/compliance.md` 的六端点报文字段表与端点错误码** —— 关闭它才能写出 `account-service` 合规呈现面与 `ERR_compliance_*` 的完整验收断言。它是后端库唯一向本库传导的欠账。→ `/analyze-new-ideas backend`
8. `art/visuals/animations/_index.md` 明写「待咨询专业人士」，属**外部输入**，不在本库可自解锁范围。

## 下一阶段

- **ADR 状态：** `decisions/` 现有 **121 份 Accepted**（`ADR-0002` ~ `ADR-0122`，`ADR-0001` 为 Proposed 示例占位）。
  2026-08-28 的一次 `/write-adr all` 全量扫描（逐份核对 29 份未被任何 ADR 引为来源的 handoff）把散落其中、已落笔主题文档却无档案的定案固化为 **23 份新 ADR**（`ADR-0100` ~ `ADR-0122`），覆盖 sync 族（`revision` CAS · flush 非阻塞 · 闸门口径）· 命名通则 · 战斗 RNG 单流 · 敌人 AI 权重向量 · 效果原语语法 · capability flag / modifier · 道具使用效果面与批次层提交等。
  台账与逐条影响文档见 `decisions/_index.md`。**当前待固化的 ADR 候选：0 条。**
  - ✅ 上一条候选「**不设 `GlobalBalanceData` 兜底大表，平衡资源按三问判据（消费者 / 覆写纪律 / 跨字段不变式）逐份切**」（`handoffs/2026-08-22-singleton-balance-resource-registry.md`）已于 2026-08-28 收口：`systems/balance.md` 补上了该判据本身那一小节（此前只有逐处应用），**并直接改写既有的 `ADR-0074`**（它本就是「凡可调数值一律住平衡资源」那条决策的档案，同一件事不另开编号——见本库「改决定直接改 ADR」的治理原则）；`ADR-0074` 后果里自陈的那条欠账随之关闭。**无需再跑 `/write-adr`。**
  （注：ADR 现可自由编辑，改决定直接改 ADR，不再新开取代 ADR。）
- **流水线闭环（07-30）：** design → code 链路补上 `/breakdown-requirements`（一份 FR → 一个文件夹的可执行子需求），完整形态见 `README.md` 与 `requirements/_index.md`。
- **架构闭环缺口：** 8 处**全部闭合**（移出记录见 `answer-logs/log-0725c.md` 与 `log-0726b.md`）；状态表见 `systems/architecture.md` 的「闭环缺口」小节。残留细节已下沉为各焦点分片的普通待决问题。
