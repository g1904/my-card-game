# Open questions — 跨 session 待答清单（索引）

> 本文件是**客户端**（Godot 项目）待答清单的**索引**：问题条目本身按主题拆在 `open-questions/` 下的分片里。
> 后端侧的待答清单在 `backend-design-documents/open-questions.md`（`backend-design` 分支）。
>
> 每次 session 结束时，未答的 Open questions 汇总到对应分片，供下次拾起；一旦答定，就从分片中移除、
> 归档进对应主题文档的 `## 待决问题` / `## 决策`，并在 `answer-logs/log-<draftSuffix>.md` 记一笔。
>
> 本清单**只跟踪仍待答的问题**（不留已解决区），是导航 / 拾取清单，**权威归属在各主题文档**。
>
> 最近更新：2026-08-30 — 全量整理：移出 1 条 · 合并 2 处 · 清 3 处失真（详见 `open-questions/update-log.md` · `answer-logs/log-0830.md`）
> （逐次变更摘要见 `open-questions/update-log.md`；已答定问题的逐条移出记录见 `answer-logs/`）

## 分片导航

| 分片 | 内容 |
|------|------|
| `open-questions/update-log.md` | 每次运行的更新摘要（答结 / 推翻 / 新增落点），倒序，**只留最近 10 条**。不含问题条目本身。 |
| `open-questions/update-log-archive.md` | 更早的更新摘要，原样归档、按时间正序。只读，不写新条目。 |
| `open-questions/01-combat.md` | **① 战斗机制**（焦点之首）：能力剥夺与统计计数的残留（片区主体已于 08-10c 答结）、结构与配置、内容与数值（多数留待内容扩充后的统计校准）、**信息面**（意图移除后图鉴是唯一事前通道）、呈现。 |
| `open-questions/02-event-options.md` | **② eventOptions 生成流程**：生成 / 加权与配比、物化字段、优先级、寿元打穿、Explore 揭示池、Travel 出场、location 与图鉴连边。 |
| `open-questions/03-adventure-event-types.md` | **③ 逐类型 AdventureEvent 机制**（五类各开一场专门 session）。 |
| `open-questions/04-hidden-attributes-plot.md` | **④ 隐藏属性 / 剧本机制**：档位阈值、跨档叙事、`lifeSpanCost` 分档、隐藏属性推拉映射、DnD 选分支的触发点与 UI、剧本内容的分发粒度。 |
| `open-questions/05-service-contracts.md` | **⑤ 服务契约 / 工程侧残留**：`#if DEBUG` 判据与 `Control` 自动翻译的实测、需求流水线形态、`architecture.md` 的三条结构残留、层级词的过早性。 |
| `open-questions/06-meta-progression.md` | **⑥ 元进程的失败侧与中长期规划感**：死亡 / 轮回结束屏尚无设计、轮回内的进度感是否需要补充、失去法则三支的频次预算、角色模板池的形态。 |
| `open-questions/07-codex-monetization.md` | **⑦ 图鉴族与商业化**：`CharacterPower`、图鉴族、premium bundle。 |
| `open-questions/cross-boundary.md` | **跨边界承接**：对侧（后端）已定案、本库尚未落笔的条目。**不是待答问题**——答案已有，等的只是落笔；形态、关闭条件与维护者分工见分片抬头。 |
| `open-questions/deferred-content.md` | **已搁置：内容充实**（07-30 起暂不推进）＋ **美术与音频（`art/`，08-04 加入）** ＋ 随内容搁置的 UX 呈现细节 ＋ 尚未设计的占位主题。 |

## 当前焦点：各系统机制细节

> **焦点判据（07-30 定）：** **规则、字段语义、流程与算法 = 机制细节 = 焦点**（分片 ①–⑦）；
> **具体条目目录与数值 = 内容充实 = 搁置**（`open-questions/deferred-content.md`）。
> 与既定开发路线「框架 → 内容 → 平衡与体验 → 社交及其他」的第 ① 阶段一致。
> Source: `handoffs/2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown.md`。
>
> 焦点顺序即分片编号顺序；**① 战斗机制**优先级最高。

## derive 就绪度

> **本小节由 `/assess-derive-readiness` 独占写入**（`/analyze-new-ideas` 与 `/summarize-open-questions` 均不得改动）。就绪度是全库横切判断，须基于一次性全量扫描才有意义；逐次 handoff 顺带的评估会迅速过时且互相矛盾。

**最近全量评估：2026-08-30（由 `/assess-derive-readiness` 产出）。** 扫描范围：`vision/`（3）· `systems/**`（54）· `art/**`（10，不含 2 份 `_TEMPLATE`）· `ux/`（5）+ 根级横切 3 份（`terminology.md` / `program-overview.md` / `system-overview.md`）= **主题文档 75 份**，另加 `decisions/`（132 份文件 = 131 份 Accepted + `ADR-0001` Proposed 占位，台账 132 行零不一致）与 `content/` / `requirements/` 的台账与模板。旁证：`requirements/` **零 FR**（无「已覆盖」项）· `inbox/` 顶层为空 · 08-30 的 8 份 handoff **全部 `distilled`**。本次重估的触发是 08-30 的四次连续落笔：两批 `/batch-analyze-new-ideas`（08-30 六份 decided 草稿 · 08-30b 两份 solution-draft）· 一次 `/write-adr game`（固化 `ADR-0123` ~ `ADR-0132`）· 一次 `/summarize-open-questions game` 全量整理。五个分片并行只读评估，逐份给证据路径。

**全局结论：ready 6 份 · partial 38 份 · blocked 31 份（主题文档 75 份口径，不含 131 份 Accepted ADR 本体与台账 / 模板）—— 08-28 记录的两处「落笔前必须裁决」全部倒下，derive 第 1 步自此零前置，服务骨架主链（第 3 → 4 → 6 → 7 → 9 步）完全通畅。**

- **✅ 全库最高优先级预警消除：「境界晋升是否改变外观」已定案（`ADR-0124`）。** 上次它挂在 `art/visuals/_index.md` 的搁置清单里，杀伤却落在 derive 顺序**第 1 位**的 `systems/common-properties.md` —— 该文档自标 ⚠「若答为随境界改变，`Artwork` 须升为按境界索引的结构，本节形态要重做」。现已收口为**基数恒为一条内容一格、境界维度不进本字段**（`common-properties.md:247`，附「只有一个落点的字段不进 `common-properties.md`」的判据卡），境界覆写整条外移为稀疏 `CharacterData.RealmArtworks` + 两级回落 + 三条加载期校验。两侧成对落笔：`art/visuals/_index.md` 的该条已移出待决区，`art-direction.md:13` 的「境界越高画面越沉」同批收窄为**按条目自身的叙事定位取沉**。⇒ **`systems/common-properties.md` 由「ready（带 ⚠ 前置）」升为无保留 ready，第一批 `.tres` 的 `Artwork` 基数不再有推倒重来的风险。**
- **✅ 寿元合并（`ADR-0127`）—— 全库波及面最大的一次结构收缩，且传播干净。** `lifeTotal` 整体并入 `lifeSpan`，寿元退出隐藏属性体系（Band 门控整体退役、明文常驻恒精确）。连带收缩：`DefeatReason` 4→**3** · `CostKey` 16→**15** · `OutcomeDirection` 5→**4** · `Status` 子表 12→**9** 行 · 隐藏属性 12→**9** 档 · Research 六类操作→**五类**（`Recuperate` 整条退役）。**逐分片检索确认：`systems/` · `ux/` · `vision/` · `art/` · `content/` · 根级三份中 `lifeTotal` / `life-total.md` / `Recuperate` 残留均为零**，旧计数（`DefeatReason` 四值 · `CostKey` 16 · `Source` 九值）亦零命中；仅 `answer-logs/` 与 `update-log-archive.md` 保留史实记录（正确形态）。**跨边界零义务**：全部字段变动落在 `characterProfile` 不透明段，后端库全库检索 `lifeTotal` 零命中。
- **✅ Exchange 以物易物定案（`ADR-0126`）—— 上次唯一一条「待用户拍板」的卡点倒下。** 支付侧二选一（货币 element 或点名的轮回级法宝）· 定值不经取池链、不掷 `Shop`、不参与刷新与三道闸 · 白送漏洞由门面级只读 `Holds(...)` 前置拒绝堵死、**`CanAfford` 一字不改** · 新增 `Source.ExchangeBarter = 10`（只开 `Op == Remove` × `(Item, Character)`）· 不持有则灰显且支付要求保持可见、不按持有面过滤呈现。`exchange/` 两份的剩余卡点自此**纯数值**，与其余四类事件同型。
- **✅ 灵根 `Affinity` 与角色模板池定案（`ADR-0123` + `ADR-0055` 改写）—— `character-profile/_index.md` 的承重卡点倒下。** 五行灵根挂 `CharacterData`，功法侧 `RequiredAffinities` / `MaxCharacterAffinityCount` 构成**硬性修习准入**（契合度三态 / 层数折减 / 取池加权全部否决），准入不进 `DrawPool<T>`、由调用方在 `PickMany` 前筛掉（零新原语零签名变更），四处玩家侧取池点各叠一层、敌人侧一律不叠、卡牌侧一格不加。角色池**全池指定 · 规模 5 · 不做账号级解锁**，且池规模不是数值旋钮而是 `content/character/` 里 `ContentEnabled == true` 的条数。**`CharacterData` 字段表首次成文**（七格 + 十一条加载期校验）。
- **✅ 两处跨边界空档成对闭合（`ADR-0125` / `ADR-0130`）。** 上次点名「flags 是否落客户端本地缓存」与「二进制资产能否经 overlay / blob 下发」**两侧都无承接项、可能被两侧同时当成对方责任**。08-30 已跨库成对落笔且互相回链、无一处复述：本库落 `content-service.md` 的 `flags.json` 落盘纪律（`schemaVersion` · 写入时点唯一 = 过单调闸并被应用之后 · 三条失效语义 · **不设 TTL** · 登出不删）与 overlay 只承载 `.tres` 的两道处置、`common-properties.md` 的资产引用格 overlay 收口、`art/visuals/_index.md` 的可撤销性与代价清单；对侧落 `no-cache` 层次澄清、**后端零义务四行否定表**、blob 通道的能力中立声明。**两侧无遗留欠账。**
- **✅ `experiencePoint` 的阈值曲线与产出分布答结** —— 上次列为 `life-cycle-service.md` 的承重卡点。阈值 `threshold(L) = 4 + floor((L−1)/2)`（L=12 特例取 10；ch1 合计 79 / ch2 32·38·44 / ch3 38·46·54）· 给予量走 `ExperienceGrade` 枚举 × 平衡表映射（ch1 4 / ch2 12 / ch3 16，与阈值同比放大，内容侧不落裸数字）· **带经验产出点约占事件总数 75%** · 失败 = 同档 × `FailureRatio`（整数百分比默认 50、向下取整下限 1，取整数而非 float 是为 `AppliedChange` 可重放）· 加载期断言「单次给予量 ≤ 该境界最小阈值」。权威在 `systems/balance.md` 与 `systems/game-progression.md`。**连带：中长期规划感的时间那一半已由经验条常驻承接，仅剩「还有几步到 Finale」的空间那一角。**

**仍在的两处大卡点（与上次同，一字未动）：**

- **🔴 数值标杆的取值**（形状已锚定、只欠取值）。`systems/balance.md` 现有 **14 条**待决全部只欠取值，共同前置仍是**两种货币的获取渠道与掉落权重**（产出侧空白则无从反推消耗侧，两条产出曲线必须一同反推）。本次就绪切片再度扩大：经验阈值全表 + `ExperienceGrade` 倍率映射 · **`lossPerMomentum` 篇章 × 系数表（只有篇章一维、三档 `combatTier` 共用）** · 「商品族 × 稀有度」定价表格值 `(CostKey Currency, int BasePrice)` · `SelectionWeightGrades` · `RealmBreakthroughManaBonus = 1`。08-30 新增两条待决同属此类：`lossPerMomentum` 的 ch2/ch3 系数（形状锚已定：一次带内最坏落差的失败恒落在本章可用预算的 8%–12%，须与经验阈值曲线一同反推）· 回寿量三档的绝对点数。
- **🔴 战斗内容本体整体为空**（卡牌条目 / 起始卡组 / 敌人条目目录 / 遭遇编排）。机制骨架与数据形态两侧现已闭合，缺的是**条目**——这是内容阶段的工作，不是设计缺口。

**本次新识别的一处结构面倒退（唯一一处，必须写进 derive 指令）：**

- **🔴 `MoveCardEffect` 缺一格 `Side : SideConstraint` 方位声明。** `deck/common-properties.md:167` 给它的 `[Export]` 只有 `From : CardZone` / `To` / `Insert` / `Count` / `Selection`，而同表另五个原语逐个都有 `Side`；`ADR-0119` 却断言 `From` 可取**对手**抽牌堆 ⇒「削减对手抽牌堆」在字段面尚未闭合。三种收法（补 `Side` / 复用 `EffectScope` / 恒作用己方）各有代价，登记在 `open-questions/01-combat.md`，需一次独立推演。**它不改判定档，但改变 `deck/common-properties.md` 就绪切片的边界：derive 第 10 步须显式排除 `MoveCardEffect` 一行**（其余七原语 + 定义体 + 20 条加载期校验可全量落），否则会把一个错的字段面冻进第一批 `.tres`；**第 23 步 `combat-service.md` 同须排除 `MoveCard` 的方位面**。裁定后补一次纯加法即可。

**本次新识别的台账 / 投影缺口（不阻塞 derive，但会误导落笔；权威在主题文档，编辑归用户 / `/analyze-new-ideas`）：**

- **🟠 `sync-service.md` 的 schema bump 清单已漏三批，而后端把 bump 权威指回它。** 该表自陈「bump 清单只有上表一份，别处提到『增列 ⇒ bump』指的都是这同一次」，但表内**零处**出现 `pastItemUse` / `ItemElements` / `ItemUseElements`（`ADR-0122`）· `StatusChanges` 列（`ADR-0128`）· `Status` 删 `lifeTotal` / `LifeSpanBand` / `ChapterLifeSpanBudget` 三格（`ADR-0127`）· 栈条目 `itemId`（`ADR-0132`）。同时 `character-profile/_index.md:160` 自己写着「随 `immortalJade` 落定 bump schema 版本」——**主题文档侧已存在第二处 bump 断言，与「清单只有一份」互斥**。对侧 `backend-design-documents/contracts/profile-sync.md:187` 明写「bump 清单的权威在客户端 `sync-service.md`，本库只声明须与之同批」⇒ **两侧都以为对方在记。** 它不改 `sync-service.md` 的 ready 判定（`## 待决问题` 确实只剩 `pushId` 后端记忆窗口），但**是 derive 第 7 步落笔前必须先补的一格**。
- **🟠 `ADR-0123` ~ `ADR-0132` 十份 ADR 在全部主题文档中零反向引用。** 事实内容都已写进主题文档（逐条核实过），缺的是 `## 决策(-> ADR)` 小节里的那一行回链——`life-span.md` 的「`lifeSpan` 是角色唯一的资源命线」是本轮最承重的一条决定，却没有任何 ADR 链接，而 `ADR-0127` 恰恰就是它。成因可追：08-30c 的 `/write-adr` 明写「只写 `decisions/` 与候选清单」。同型缺口另有三处：`future-event-service.md` 的 `## 决策(-> ADR)` **整节为空** · `content-service.md` 未收录 `ADR-0125` / `ADR-0130` · `life-cycle-service.md` 未收录 `ADR-0127` / `ADR-0128` · `balance.md` 的该节亦为空。**不构成 derive 阻塞**（判据第 3 条要的是「约束它的 ADR 均 Accepted」，成立），但让「哪条结论受哪份 ADR 约束」在主题文档侧不可检索。
- **🟠 `exchange/_index.md:165` 仍标「ADR 候选，待 `/write-adr` 立档」而 `ADR-0126` 已 Accepted。** 全库其余文档的同型措辞扫描结果为零，这是唯一一处漏网。
- **🟠 `ux/error-and-blocking-ux.md:179` 的 `COMBAT_` 分区行仍写 `intent`。** 意图机制已由 `ADR-0059` 整条移除，且 `system-overview.md:119` 的**同一张分区表**已于 08-28 改为「无 `intent` —— 意图机制已整条移除」⇒ 两份文档对同一张表给出互相矛盾的覆盖描述。**这条会直接导致 derive 第 2 步建出一批为已删机制预留的键位。** 同型残留另一处：`systems/_index.md:45` 的 `combat-service` 行仍写「敌人 AI 与意图（三档揭示）」，与三处上游承重定案「不设揭示档位」正面冲突——索引是找文档的第一跳，这处失真会误导 derive 起点。
- **🟠 barter 灰显的 `EVENT_` 说明键在权威文档零承接。** `screen-flow.md:171` 已落笔「不持有支付物 → 灰显、走 `EVENT_` 普通分区」，但灰态判据的**权威**在 `error-and-blocking-ux.md` 的灰态判据表与其键清单，**两处均无 barter 行**——而按该表自陈的判据（「必然无结果的操作」）它明确属于表内情形，且与「买不起则灰显」一行逐字同构。⇒ `ADR-0126` 的呈现侧落笔只做了一半，derive 第 2 步时该键无处可查。
- **🟡 `game-progression.md:194` 的待决区与同一文件的 `:56` 自相矛盾**：`:194` 仍写「仍待定的是进度感那一半」，而 `:56` 与 `screen-flow.md:92` 均已明写经验条常驻是该条的一个答复（时间那一半），08-30d 亦据此收窄了 `06-meta-progression.md`。
- **🟡 三处陈旧计数 / 措辞**：`item/common-properties.md:3` 抬头仍写「占位结构，细节待定」，与 `item/_index.md` 已完整成文的字段面 + I-1~I-13 校验直接矛盾（**08-28 已点名、第二次报告**）· `power/_index.md:60` 写「见 `../_index.md` 的 23 字段表」而该表现为 **25 行** · `common-properties.md:169` 的 `ContentEnabled` 段仍写「overlay 只改这个既有布尔字段」，未提 `ADR-0130` 引入的 flags 第三层覆盖来源（属加法式覆盖、结构无变化，不降级 ready，但措辞现在读起来是排他的）。
- **🟡 「动画可跳过」与「演出只能加速不能跳过」两侧措辞相抵**：`art/visuals/animations/_index.md:20` 要求「须可跳过 / 可加速」，`ux/combat-ux.md:22` 明写「只能加速不能跳过」。上次记为「一条孤悬约束、`combat-ux.md` 无承载」，**实际是相抵而非缺载**。两处都不阻塞 derive（art 侧不产 FR），但 animations 分区开工时会以一条已被否决的前提为起点。
- **🟡 `terminology.md:89` 的「大限将至」措辞待复核。** 08-30 定案是「寿元三条 Band 文案条目删除、『大限将至』不另找载体」，词条现文「递减到 0 →「大限将至」→ 角色 defeated」读作终态叙事名尚可，但与被删的三条 Band **预警文案**同名，极易被后来者读成残留并据此重建一条文案通道。

**跨边界闭合（强制检查项）：由三处减为一处。**

- **仍未闭合的唯一一条：`backend-design-documents/contracts/compliance.md` 的六端点报文字段表**与**端点自身的错误码**（后端库自陈两处均未落笔）。按「契约权威在后端库」判据 ⇒ `account-service.md` derive 时须排除 `ComplianceManager` 中凡触及合规端点请求 / 应答体的部分（ticket 兑付 · 冷静期状态呈现 · 导出任务轮询）。**本次可把排除面精确到半条**：`compliance.*` 四条**拦截**码的 `code` × `reasonKey` 取值表已在对侧 `compliance.md:63` 成文 ⇒ `ux/error-and-blocking-ux.md` derive 时**只需排除「合规域端点自身」那一批 `ERR_*` 键的验收断言**，拦截路径那一批可以写出完整验收。这比上次的记录更精确，排除面据此缩小。
- **✅ 两处闭合**：flags 客户端本地缓存 · 二进制资产经 overlay / blob（见上）。
- `open-questions/cross-boundary.md` 的「待承接」仍是 1 条（`ComplianceManager` 客户端覆盖面的切分），对侧明写「归客户端自己裁决」——**它是本库自己的取向，不是跨边界缺口**。反向：后端 `cross-boundary.md`「待承接」为空。
- **逐份核对客户端 08-30 的 8 份 handoff 对后端的义务**：7 份零义务（各自 handoff 明写且经对侧库检索核实）；`exchange-barter-support` 有一条**既有机械义务**（`EventOption` 增一格 ⇒ bump 存档 schema ⇒ 新 `schemaVersion` 须进对侧 `envelope.md` §7e 兼容矩阵），由 §7e 通则唯一承接、矩阵落 `operations/` 而后端栈未定故当前空置 ⇒ **不改契约、不改判定**，但对侧零留痕（08-25 同类情形是记了 handoff 的）。
- `ADR-0131`（`Upgrade` 非闸门点不硬阻塞）的两侧对位**完全闭合**，无跨边界残留。

### 判定表

| 文档 | 判定 | 卡点 / 就绪切片 |
|------|------|------------------|
| `systems/common-properties.md` | **ready** | **本次由「ready（带 ⚠ 前置）」升为无保留 ready** —— `Artwork` 基数的唯一预警已由 `ADR-0124` 消除（基数恒为一条内容一格、境界维度不进本字段；覆写外移 `CharacterData.RealmArtworks`），全库检索「境界晋升是否改变外观」零命中。`## 待决问题` 仍明写「（无）」。整面 = 稳定 `Id` 与展示字段三层切分 · 物化模型 · `LocalizedText`（封闭二值 locale · `Get()` 纯读 · 不落存档）· `ContentEnabled` + `AllEnabled()` / `AllIncludingDisabled()` 双名与 `[Obsolete(error:true)]` 编译闸 · `RarityTier` 五档 · `Artwork : Texture2D` 可空 + 占位回落 + 七类挂载面 · `SourceCode` + **`Source` 由九值扩为十值**（新增 `ExchangeBarter = 10`）+ 合法子集表 + 「三个成员只出现在 `Op == Remove` 上」与扩三格的校验 · `ExclusiveSource` · 两级 seeded RNG（轮回级四子流 + `State` / `DrawCount` 持久化；账号级 SplitMix64，8 组测试向量在 `backend-design-documents/contracts/profile-sync.md` §6a，验收可逐位对表）· API 契约总则三形态 / 三失败语义 · `deviceId` 落点。**全库地基、derive 顺序第 1 位，现已零前置。落笔时须按 `Source` 十值（非九值）写枚举与合法子集表** |
| `systems/architecture.md` | **ready** | 维持。`## 待决问题` 仅一条自陈「台账事项 · 不阻塞 derive · 不产出新 FR」的 `architecture.md ↔ services/*` 对账，零待复核。**本次切片扩大**：`ProfileChangeSpec` 增 `StatusChanges : IReadOnlyList<StatusAssignment>`（绝对置值，`ADR-0128`）· 共享核心类型新增 `enum HiddenStatDirection { Raise, Lower }` 与三格 `HiddenStatGrant(Stat, Grade, Direction)`（`ADR-0129`）· `ADR-0130` / `ADR-0131` 均以本文件为落点且 Accepted。其余同前：三层切分 · 七服务边界与 autoload 注册顺序 · EventBus 总则 · 八条 API 契约总则 · 边界服务「接口 + Http/Offline 双实现」骨架 · `ElementSpec` / `ModifierKey` / 钳制表 · `ProfileChangeSpec` 逐列 · 三级判据 · 八处闭环缺口全闭合。**须与 `systems/viewmodel.md` 同批** |
| `systems/viewmodel.md` | **ready** | 维持。`## 待决问题` 明写「当前无未决项」。**本次同批落笔 `ADR-0124` 的两级回落**：`CharacterData.RealmArtworks` 按 `realm` 匹配 → 共有字段 `Artwork` 基础图 → `res://art/_placeholder.png`，无 `CharacterProfile` 的场合跳过第 ① 级；占位入口仍唯一。其余 = 展示层第三层的结构契约（呈现期对象、不落存档 / 不进云端负载 · 单向依赖且服务 API 面永不返回 ViewModel · 组装源三件套 · 三条重组装触发面 · 只读消费与缓存归属 · 永不渲染清单）。**须与 `systems/architecture.md` 同批** |
| `systems/services/sync-service.md` | **ready**（**derive 前须补一格**） | 维持 ready，`## 待决问题` 仍只剩 `pushId` 后端记忆窗口（对侧参数，且报文字段名与序列化形态已由 `backend-design-documents/contracts/envelope.md` 定死），零待复核。整面 = 存档点 ↔ push 解耦 + 5 秒防抖 + 立即 flush 清单 + `PushPolicy` + 本地缓存原子写 + schema 版本化与迁移 / 拒绝 + `CharacterProfile` 级 diff + `revision` CAS / `pushId` 幂等 + `Source` 上行走成员名 + 后端主动写入后的 pull 时序 + 断线降级与退避阶梯 + 两层 Profile 完整字段面 + 回声校验整条 + 图鉴各键序列化形态。跨边界两向皆闭合。**⚠ derive 前置（本次新识别）：本文件的 schema bump 清单自称「只有一份」却已漏三批**（`pastItemUse` / `ItemElements` / `ItemUseElements` · `StatusChanges` · `Status` 删三格 · 栈条目 `itemId`），而对侧 `profile-sync.md:187` 把 bump 权威指回它 ⇒ 落笔前须先补齐该表，否则迁移器会漏格 |
| `systems/player-profile/codex/common-properties.md` | **ready** | 维持，零变化。无 `## 待决问题` 小节、无待复核项；`ADR-0027` / `ADR-0070` / `ADR-0096` 均 Accepted，存档面整份闭合。整面 = `CodexEntry` 单格 `Id` + `CodexKind` 七值 / `CodexUnlock(Kind, Id)` + `CodexElements` 零 `Op` 恒不经 pipeline + 四行失败语义 + 七本的解锁触发（全部搭既有提交，零新增提交点）+ 连锁解锁「一次敌人收录展开为 1 + N 条」+ `eventEnd` 入账射程 + `CodexFlavor` 可选 `LocalizedText` + 「五本能力 / 道具 / 功法类不套用敌人五项规格」的词条深度表。**呈现面不在本文件**（在 `codex/_index.md`，blocked），derive 时不取任何图鉴屏 |
| `ux/onboarding.md` | **ready** | 维持。`## 待决问题` 明写「当前无未决项」。**本次新增一条 08-30 落笔**：角色选择是首玩者唯一一次起手选择 · 首玩局标注推荐项（内容侧一格标记）· **明确不做「首局跳过选择」的特判**（两条起手路径必各自漂移）。其余 = 强制登录无游客 + 首版只呈现已实现渠道（手机 / 微信）+ 两步握手 UI（验证码框 + 倒计时始终可见的重发键 + 过期重取，无 hover 提示）+ 绑定 / 解绑不落登录屏 + 首玩篇章门禁 + 解锁 = 「有可挑战角色」的动态状态。**与 `ux/screen-flow.md` 同批**（本次重叠面由一处增至两处：登录屏 + 角色选择屏的门禁语义 ↔ 屏形态）。**derive 时排除角色选择屏本体** |
| `systems/character-profile/_index.md` | **partial**（切片本次扩大最多） | **上次的唯一承重卡点「角色模板池的内容侧取值面」整条倒下**（`ADR-0123` / `ADR-0055`）：**全池指定 · 池规模 5 · 不做账号级解锁**，且池规模不是数值旋钮而是 `content/character/` 里 `ContentEnabled == true` 的条数，增减角色是纯加法。**`CharacterData` 字段表首次成文**（七格 + 十一条加载期校验），含 `Affinities : Affinity[]`（唯一规则后果 = 硬性修习准入、不落存档、不进上行负载）与 `RealmArtworks` 稀疏境界覆写 + 两级回落 + R-1/R-2/R-3 三条校验 + 资产量级 20/10/5 张。**寿元合并完整落地**：`Status` 子表 **9 行**（`LifeSpanBand` / `ChapterLifeSpanBudget` / `lifeTotal` 三格已删）· `defeatReason` 为**三值**封闭枚举且不设 `None` 哨兵 · `StatusChanges` 写入通道已落位。字段表 **25 行**（含 `pastItemUse` 与寿元曲线读取算法）+ 7 条读档校验 + 角色 = 有身份模板。卡于（三条全部非结构性）：**`CharacterData` 第 8 行仍是 ⟨待定⟩「两门绑定功法的初始层数」**（阻塞 `content/character/` 条目编写，**不阻塞 schema 与加载期校验的 derive**）· 全池指定下角色强度是否塌缩为单一最优（待实测）· 隐藏属性是否有第三项 |
| `systems/character-profile/life-span.md` | **partial** | **本次由 blocked 升级**（原 `life-total.md` 退役、本文件 08-30 新建，`ADR-0127`）。升级理由是文档地位变了：原文件被判 blocked 的理由是「不构成可独立成立的 FR 面」；合并后寿元是角色**唯一**的资源命线，自持一整套可验收的规则面。就绪切片 = 单值形态与三条不变式（**无上限字段 / 无上限截断 / 无「上限推拉」**，`ADR-0045` 连文件名一并改挂寿元）· 两个扣减来源（事件 `lifeSpanCost` · 战斗 / 修炼失败按道念差 × `lossPerMomentum`）与各自施加点 · 归 0 → `DefeatReason.LifeSpanExhausted` · 回复口**恒为三通道**（回寿事件 outcome / 补天丹类法宝 / 商店购入）· **战斗内不读写寿元、只在收口时刻扣减**这条资源纪律（由 `ADR-0081` 的隐藏属性侧管辖收窄后改挂本文件）· 明文常驻恒精确、退出隐藏属性体系 · 负侧换算形状与 ch1 `lossPerMomentum = 1`。卡于：`lossPerMomentum` 的 **ch2 / ch3 系数取值**（前置是尚未定的道念量纲基准）· 回复三档的绝对点数须在新量纲下重估。**derive 纪律：字段仍住在 `CharacterProfile.Status`、消费点在 combat-service（收口扣减）与 life-cycle-service（终态判定）两侧 ⇒ 与那两处同批，不单列 FR** |
| `systems/services/profile-service.md` | **partial** | 维持，**切片本次因 `ADR-0127` / `ADR-0128` / `ADR-0126` 再度扩大**：`CostKey` **由 16 收缩为 15 行**且「本表已覆盖 `CostKey` 全部成员、与两层 Profile 字段表写入通道双向满射」的启动期断言随之重述 · 新增 `StatusChanges` 列及其「恒不经 modifier pipeline」理由链 · 产出侧 `Key == LifeSpan && BaseValue < 0` 的整批拒绝校验 · 投影钳制明写「不钳制则重算方会看到 `lifeSpan < 0`」· 新增门面级只读 `Holds(...)`（`ADR-0126` 的承重落点，barter 白拿漏洞的唯一堵法）。其余就绪切片 = `ProfileChangeSpec` **逐列**与逐列失败语义（**列数不写死**）+ `CostKey` 15 成员 / `StatKey` 2 项 + `ResourceElements` 配表 + `TryApply` 整批原子性与钳制 + `CapabilityManager` 整面（聚合范围 = 两层持有列表 × 同三条与门 · 一条 `Recompute()` + EventBus `CapabilitiesChanged` · 叠加 = 集合并幂等 · 冲突结构上关死 · `HashSet<CapabilityFlag>` 不加 `[Flags]` · 新增一个 flag 恰好三步不 bump schema）+ modifier 合并完整（同层求和 → 只乘一次 → 只取整一次 · 两条钳制 · **顺序无关 ⇒ 不设优先级**）+ 各通道 + 战斗外使用「一次组装一次事务」门面。`## 待决问题` **五条一字未变**（全属元进程 / 成就侧）：`status` × 拥有 / 失去的存档编码 · `AchievementManager` 采集面 · 成就两档奖励内容 · `Achievement` 条目 schema 与各账号级条目触发 · `PlayerPower` 平衡边界。**须与 `player-power/common-properties.md` 同批** |
| `systems/player-profile/player-power/common-properties.md` | **partial** | 维持，零变化。就绪切片 = capability flag + modifier pipeline 两条通道的**声明侧整面**（`ADR-0116`）：扁平 `enum CapabilityFlag` 不分区 · 命名 = 动词三词表 `{Reveal, Show, Unlock}` + 禁否定式（union 不变式的可机械检查护栏）· 消费侧「一 flag ↔ 一处消费点」+ `CapabilitiesChanged` 空负载广播 · `ModifierOp {Add, Scale}` 万分比整数 · `ApplyModifier(key, baseValue)` 单点入口 + 两条钳制 · 作用面 opt-in 白名单与四类豁免。**须与 `systems/services/profile-service.md` 同批 derive**（合并算法权威在服务侧，声明侧字段与命名纪律在本文件；分批必出两份互不相认的叠加规则）。卡于：`status` × 拥有 / 失去的存档编码 |
| `systems/character-profile/deck/common-properties.md` | **partial（就绪切片须排除一个原语）** | 判定档维持，**但本次新增一处结构卡点，且它落在原语表内部**：🔴 **`MoveCardEffect` 缺一格 `Side : SideConstraint`**（同表另五个原语逐个都有；`ADR-0119` 却断言 `From` 可取对手抽牌堆）⇒ **derive 时须显式排除 `MoveCardEffect` 一行**，否则会把一个错的字段面冻进 `.tres`。其余整面本次又前进一步：`ADR-0132` 的落点已兑现——加载期校验表由 19 条增至 **20 条**（第 20 条 = 「`TimingId == card.played` 且 `TriggerFilter.CardTypes` 仅含 `Item` ⇒ `PushWarning`」的清单式软检查）。就绪切片 = `EffectData` 抽象基类 + 一原语一 `[GlobalClass]` 子类树 + 其余七原语参数表 + 并列定义体 `StaticModifierData`（`ModifierTarget` 五项冻结 · 万分比整数 · 同层求和 → 只乘一次 → 只取整一次）+ `AbilityData` 两格 XOR + `TriggerConditionData` / `TimingIds` 十时点 + `SubjectKind` 相容校验 + `EffectCondition` 三谓词 AND + `SideConstraint` 一律相对施放者解析 + 关键字哨兵与恰好一层展开 + **`CardData` 字段清单收口** + target / scope 分离与 `EntryFilter` + **存档面零新增字段、空迁移**。**它是卡牌 / 神通 / 法宝 / 法则 / 古宝五类内容的共同底座**，纯数据资源型切片、过三条闸。卡于：starter deck 具体内容 · 抽 / 弃 / 洗数值 · **`MoveCardEffect.Side`（新增）** |
| `systems/character-profile/deck/_index.md` | **partial** | 维持，**切片本次扩大一整块：灵根修习准入（`ADR-0123`）**。新增就绪面 = `CultivationTechniqueData` header 增两格 `RequiredAffinities` / `MaxCharacterAffinityCount` + 可选 `CodexFlavor` · 判定式 `technique.RequiredAffinities ⊆ character.Affinities` · 四处玩家侧取池点各叠一层准入（弃置候选不叠、升阶候选叠准入不叠 `Pool`）· **准入不进 `DrawPool<T>`、由调用方在 `PickMany` 前筛掉**（依据「这道过滤需不需要读 `Profile`」的既定分界判据，零新原语零签名变更）· 敌人侧一律不做准入 · **卡牌侧一格不加**且理由是结构性的 · 空数组 = 通用功法与「漏填不可区分」的代价明写 + 收紧路径已预留。其余同前（功法 = 卡组构筑单位 `ADR-0054` + 轻量 header + 成员卡反建索引 + 五个 `DeckChangeOp` 与 `Pool == Enemy` 施加侧闸 + `CardType` 五分与封闭卡集 `ADR-0041` + 次类型体系 + 卡池三值必填 `ADR-0091` + 不重洗与疲劳 `ADR-0052` + 手牌上限 **7** 的纯上界语义 + 抽牌堆插入位 `ADR-0119` + stack / LIFO 不借交互）。卡于（**由 5 条增至 7 条**）：starter deck 内容 · 功法规模参数（`MaxTier` / 每层卡数 / 替换幅度）· 关键字与次类型首批清单为空 · 卡组规模与道念量纲基准 · **多灵根角色的强度对齐换算（08-30 新增，首批全单灵根故不发生，引入第一个多灵根角色前必须先答）** · **通用功法占比口径（08-30 新增）** |
| `systems/character-profile/power/_index.md` | **partial** | 维持，零变化。就绪切片 = `PowerData` 字段面（含 `GrantedFlags` / `Modifiers` 两条战斗外通道）+ 两层共用注册面与三条推论 + 三格至少一格非空校验 + 「纯战斗外条目不得挂触发式」校验 + **不得含 `LifeSpan` 产出**（两 `Scope` 皆然）+ 入场三条与门 + 禁用截断五行表（四类通用）+ `IsProtected` 统一置位与 `IgnoresProtection` 唯一后门（`ADR-0106`）+ 战斗内运行态走战场条目 `counters` 零结构增量 + 起手绑定神通（`ADR-0055`）。卡于：持有条目 `status` × 拥有 / 失去的存档编码 · 事件侧获取 / 失去触发 · 与卡牌 / 法宝的边界判据 · 数量与强度尺度。**失真：`:60` 仍写「见 `../_index.md` 的 23 字段表」而该表现为 25 行** |
| `systems/player-profile/player-power/_index.md` | **partial** | 维持，零变化。就绪切片 = 失去的三形态（置换 / 本轮回禁用 / 战斗内 `IgnoresProtection`）+ 同池判据 `(CarrierKind, Scope)` 全同 + 同 `Rarity` + 排除已持有 + 自愿失去阶梯（`ADR-0048`）+ 残卷 Finale 绑定（`ADR-0049`）+ 授予池权重结构与初值 40/27/18/10/5 + 「一次轮回至多结算一次」的轮回级布尔位 + 类型面已闭合的自陈（缺的只是条目目录，开张动作归 `/scaffold-content-type player-power`）。卡于：战后奖励池按优势档三张权重表 · 获取触发（残卷 / 礼包之外）· 平衡边界（是否影响 cycle seed / 计分公平）· 「失去法则」三支频次预算重配平 · `status` × 拥有 / 失去的存档编码 |
| `systems/character-profile/item/_index.md` | **partial** | 维持，**但唯一那条承重卡点因寿元合并而变重**。就绪切片 = 使用效果面按世界分两格（`CombatUseEffects` / `OutOfCombatUseOutcome`，必填性由 `UsableScene` 驱动）· `Abilities` 整格移除 · `MaxUsesPerCombat` 把本场配额从 `Charges` 摘开 · 战斗外三列白名单 + 十列恒空断言 · 不设目标面 · 一次组装一次事务的执行链路 · **战斗外使用不是决策点、是即时提交**（`Debounced` + `SavePointReason.InventoryChanged`）· 痕迹落 `pastItemUse` · **I-1 ~ I-13 加载期校验** · 储物袋跨两层视图与随售通道、两档回收率的加载期硬校验。卡于：**回寿法宝的总量护栏在内容编排面的口径**（承重且**变重**——文档自陈「它是寿元这条压力线的**唯一剩余数量闸**」：`ADR-0127` 把两条压力线并成一条之后，规则层不设持有上限这个决定的全部重量都压在这条内容编排纪律上，而出现频率 / 库存深度 / 定价三者口径仍全空）· 道具种类目录 · 「什么该做成卡 / 道具 / 神通」的判据 |
| `systems/services/content-service.md` | **partial** | 维持 partial，**但上次记录的跨边界空档本次闭合**：本文件现有完整的 `user://cache/flags.json` 段（schema 四格 · 走共享 `AtomicJsonFile` 原子写 · 写入时点唯一 = 过单调闸并被应用之后 · 盘上 `flagsVersion` 只进日志不回填 · 解析失败丢弃 + `PushWarning` · **不设时间 TTL** 及其理由 · 报文侧对位不复述，逐条回链后端保证），`ADR-0130` Accepted。就绪切片其余 = 三层覆盖来源 + 启动期 manifest 比对 + blob 内容寻址 + ES256 detached 签名与 `keyId` 轮换 + 文件级事务 + 增量下载 + overlay 合并与「以 overlay 为准」+ 合并后全量校验 + `ContentEnabled` 不预埋占位 Id + flags 第三层（含单调闸两处观测点）+ `contentVersion` 记两个 + 断网降级到随包基线 + 语言覆盖率审计与 `fallback = "zh"` + overlay 剧本例外的 `newIds` 双闸 + 可执行化阶梯 + 单例内容注册与加载期条数校验（`ADR-0030`）+ **overlay 只承载 `.tres` 的两道处置**（`ADR-0125`）+ 统一操作接口。卡于：**唯一待决 = disabled 条目被存档引用时的 UX**（归 `ux/`）。**失真：`## 决策(-> ADR)` 未收录 `ADR-0125` / `ADR-0130`** |
| `systems/services/account-service.md` | **partial** | 维持，卡点逐字不变。就绪切片 = 会话生命周期 + 四个账号方法 + refresh 失败拆两条路径（网络失败 → 缓冲通道 / `auth.session_revoked` → 硬阻塞重登 + 暂停退避，判据为「收到了明确应答」而非「失败了」）+ `AccountInfo` 五字段 + `deviceId` 落点（`ADR-0071`）+ refresh token 明文 `user://cache/refresh-token.json` 带版本 · `AuthManager` 私有 · 不与 `device-id.json` 合并（`ADR-0080`）+ 启动期静默续期使 `Account.Init` 上提到登录屏之前 + 软信号 `reauthRecommended` 的反应形态 + 被挤下线后「先 pull 后 flush」+ 合规拦截路径（搭 `signin` 的车，不需要任何合规端点报文）。卡于：**合规域两处落笔欠账**（对侧 `compliance.md` 的六端点报文字段表**与**端点错误码今日复核**均仍未落笔** ⇒ 须排除 `ComplianceManager` 中触及合规端点请求 / 应答体的部分）· `ComplianceManager` 覆盖面切分（本库自己的取向）· 平台密钥库的后置评估 · 多设备并发登录的云端裁决（归后端） |
| `systems/services/future-event-service.md` | **partial** | 维持，**切片扩大**：`HiddenStatGrant.Direction` 的符号展开点已落笔（`sign = g.Direction == Raise ? +1 : -1`，「取负只在此处发生一次」，两侧各展开一份相同 element、不加顶层第三格 `Always`，`ADR-0129`）· Exchange 取池链已叠入**灵根修习准入**（功法族另叠一道过滤，`ADR-0123`）。就绪切片其余 = eventOptions 十步管线整条（`ADR-0026`）+ 候选短缺三道闸（`ADR-0073`）+ `EventOutcomeSpec` 字段面（`ADR-0078`）+ 置换 / 禁用候选前移到物化时掷定 + `Priority = 1` 抬升判据与三条与门子判据（`ADR-0047`）+ Exchange 库存物化与 Explore 取池过滤 + 「本服务纯本地、全部方法形态 A」的 API 面。`## 待决问题` **由三项减为一项**：`BaseTypeWeights` 每格取值 + Combat 内 `combatTier` 三档配比（统计校准）。**全库唯一残留的 1 项 `[采纳推荐 — 待复核]` 仍在**（能力族走 `TryPickGrantableMany` / 其余三族直用 `DrawPool<T>`），按收窄判据不构成降级。**失真：`## 决策(-> ADR)` 整节为空**（`ADR-0026` / `0047` / `0073` / `0078` / `0123` / `0129` 全部以本文件为落点却零回链）。**须与 `plot-manager.md` 同批** |
| `systems/services/plot-manager.md` | **partial** | 维持，卡点结构不变但**射程收窄一处**：待决第 2 条已明写「**寿元不在本清单内，它已不是隐藏属性**」⇒ 隐藏属性由 12 档收为 9 档、Band 门控对寿元整体退役。`EncounterTighten` 仍是**五格**（与 08-30b「疲劳维持不进覆写组、不开第六格」一致，无矛盾）。就绪切片 = `PlotArcData` + `PlotNodeData` 数据形态（`ADR-0015`）+ `PlotModulation` 六字段与加载期悬空校验 + 多条 `Active` arc 的合并算法（权重相乘 / 白名单取并）+ `EncounterTighten` 五格增量的方向约束与 `Finale` 整档豁免（`ADR-0077`）+ 档位表 / 阈值 / 回滞 / 跨档叙事的结构面（`ADR-0016`，减档禁令射程已收窄为「不得为文案密度而减档」）+ **战斗层零耦合这条边界**（`ADR-0081`，管辖收窄为道心 / 煞气）+ 剧本内容侧 event / EventData 形态 + 剧本树不分包（`ADR-0029`）。卡于：DnD 式选分支的触发点与 UI（含玩家可见 / 不可见分支边界）· 隐藏属性的逐条推拉映射与是否有第三项以外的项 · `HiddenStatGrade` 三个映射值与 `EncounterTighten` 界常量（只约束标定、不约束结构） |
| `systems/services/life-cycle-service.md` | **partial** | 维持，**切片本次因 `ADR-0127` 再度扩大**：**`DefeatReason` 由四值收为三值**（主动弃置 / 寿元耗尽 / 渡劫失败），并显式写明「里面没有『输掉一场普通战斗』这一项，输一场 `Practice` / `Standard` 扣的是寿元，对应项是 `LifeSpanExhausted`」+ 结转口径「占本章预算百分比是平衡表书写口径、不是运行期字段」。就绪切片 = 轮回骨架（两层持有模型 + `ongoing` / `defeated` / `completed` 分类法 + 寿元递减预算与归 0 终态（`ADR-0031`）+ **`DefeatReason` 三值**与 Finale 失败的显式旁路（`ADR-0025`）+ 「账号级残卷累加先于角色终结提交」顺序不变式 + `AdvanceEventAsync` 固定结算流程与取消语义 + 非战斗四类的事件内决策点 + 判负短路那一路的单次提交 + `SeedManager` 四子流 / `AttachRngState(spec)` 三步机制与「凡消耗子流随机必须同批更新 `State` / `DrawCount`」不变式 + 残卷掷骰走账号级 SplitMix64 与 `FinaleWinOrdinal` 幂等键 + 存档点永不回退（`ADR-0032`）+ 决策点存档（`ADR-0036`）+ 批次层即时提交）。卡于：元进程持久化范围（`Achievement` schema + `PlayerPower` / `PlayerItem` 触发 + `PlayerPower` 平衡边界）· 隐藏属性细节。**排除面本次收窄**：`experiencePoint` 已只需排除验收侧的 N 值，曲线 / 分档 / 分布 / 失败折算均已定（权威在 `balance.md` / `game-progression.md`）——**但本文件的待决区措辞尚未跟上，仍把整条记为未决，其排除面被高估了**。**失真：`## 决策(-> ADR)` 未收录 `ADR-0127` / `ADR-0128`** |
| `systems/services/combat-service.md` | **partial** | 维持，**切片本次因 `ADR-0132` 扩大**：`StackEntryKind` 五值 `{ PlayedCard, TriggeredAbility, ActivatedAbility, UsedItem, Fatigue }` + 栈条目新增 `itemId` 一格 · `itemId ↔ kind == UsedItem` 双向不变式与「三个来源格对道具全部恒空」的完整论证 · **读档校验 ②/⑥ 分档**且 `CombatItemSave.ItemId` 被 ⑥ 丢弃时该栈条目照常结算 · `CombatFeedKind` **六类**含 `ItemUse` · `UseItem` 补齐 mana 扣费 / `InsufficientMana` / 入栈填法 · 明写「使用道具时」不开为触发时点。就绪切片其余 = 战斗机制骨架整条（栈与 LIFO 结算 + 战报 `ADR-0086` · `ActionResult` / `CombatFeedEntry` / `Declared`+`Actual` / `CauseEntryId`（`ADR-0087`）· 疲劳为一等栈条目 + `TurnLimit`（`ADR-0088`）· 三步回合与借 stack 不借交互（`ADR-0039`）· 抽牌堆不重洗、抽空每张扣 1 道念（`ADR-0052`）· 敌人意图整条移除（`ADR-0059`）· AI 两层结构与对称可见输入面（`CombatSnapshot` 双视角化 `ViewerSide`，`ADR-0092`）· **效果流水线五阶段**与 `Evaluate` 纯 / `Apply` 唯一写点 · `ActiveCombat` 存档块与索引序承重 · 决策点 D0–D7 与每点提交形态 · `RewardPickState` 与逐项领取（`ADR-0082`）· 战斗前确认页（`ADR-0094`）· counters 键空间（`ADR-0075`）· `EncounterSpec.FirstSide` · 挂起态恢复与取消 · 单一 `combat` 子流（`ADR-0112`））。卡于（**由两项增为两项半**）：**战斗内容本体整体为空**（字段面已收口，缺的是条目）· Finale 奖励加厚幅度与逐项领取后的厚度重估（统计校准）· **`MoveCardEffect` 的方位面（新增，等 `deck/common-properties.md` 补 `Side : SideConstraint`）** |
| `systems/adventure-event/common-properties.md` | **partial** | 维持。**`ADR-0127` 的收缩已完整落地**：校验 2 的 `ResourceKey` 白名单已是四值 `{ LifeSpan, ManaLimit, SpiritStone, ImmortalJade }`（`LifeTotal` 已删）· 寿元回复三通道共用一条 `ChangeElement(CostKey.LifeSpan, +n)` 施加路径 · 校验 6「Travel 条目不得回寿」与「`HiddenStatGrants` 不需要禁寿校验」的理由已改挂新依据。就绪切片 = `EventOption` 物化字段面 + `OutcomeRule` 与 `HiddenStatGrants` + 「产出即定稿、不得回查模板重算」（`ADR-0012`）+ 事件收口的事务语义（`ADR-0020`）+ `PastEventEntry` 与 `AppliedChange`（`ADR-0021`）+ 寿元回复只走 outcome 侧（`ADR-0066`）+ `lifeSpanCost` 形态定值三变异位（`ADR-0109`）+ `AddLooseCard` 取池链（`ADR-0118`）+ 九条加载期校验。**它是后续全部事件类型 FR 的共同类型地基。** 卡于：`lifeSpanCost` 定价表逐格与五类配比取值（统计校准）· 回寿量三档的绝对点数 · 回寿法宝总量的内容编排面口径 |
| `systems/adventure-event/travel/_index.md` · `travel/common-properties.md` | **partial** | 维持，两份同批、卡点无变化。就绪切片 = `LocationData` + 单份 `LocationMapData` 载体与恒启用（`ADR-0042`）· 80/20 全局常量不可调制 · 目的地取自邻接集合、`DestinationLocationId` 物化时掷定且不得事后算 · 闸门形态（`ADR-0043`：最高 `Priority = 1`、无 `IsMandatory` 字段）· 结算写入判据为 `DestinationLocationId != ""` 而非 `EventType == Travel` · 痕迹侧记出发地 · 两条 `StatusAssignment` 由 life-cycle-service 组装 · 不计入 `eventCountLimit` 与剧本免疫（`ADR-0111`）· 专有字段与结算写入面（含 Explore 遮罩 Travel 的边界情形）。卡于：Travel 行定价（结构已定为 > 0 且为常规基准的 1/3 ~ 1/2）· **location 无法被 flags 秒关时的运营替代通道**（location 恒启用、不参与三层过滤，是 `AllEnabled()` 运营通道在本作唯一的空洞；明确不预设形态） |
| `systems/adventure-event/explore/_index.md` · `explore/common-properties.md` | **partial** | 维持。`common-properties.md` 的 `## 待决问题` 明写「无。专有字段清单闭合」。就绪切片 = 揭示机制整套（`ADR-0064`）—— 真身分布 = 条目池组成 × 既有加权抽取、三处数据类一律不加字段 · 取池期「真身须同样 enabled」过滤 + 真身为 Exchange 时其库存池前置须过 · 揭示 = `eventStart` 内一次 `with` 派生且 **resolver 按真身选取** · 遮罩卡与其余选项完全同构 · 全屏覆盖层转场、不进屏幕栈、不给任何部分线索 · `IsRevealed` 只在物化侧 · 派生实例落 `activeEvent` 随后续第一个决策点写盘、不新增揭示专属存档点 · `RevealedEventId` 一格与四条加载期校验。**两份同批。** 卡于：真身占比 `5:3:2` 与定价表 Explore 行取值（内容编排口径；**注：Explore 行的独立定值现同时承担 `pillars.md` 第 9 条的定价指纹护栏，护栏由呈现门控升为定价结构**）· 转场时长 ≈1.2s 与一次短音效的真机实测 |
| `systems/adventure-event/research/_index.md` · `research/common-properties.md` | **partial**（卡点由 3 个数值格减为 2 个） | **本次由「三个数值格」订正为两个**：第三格「`Recuperate` 的 `lifeTotal` 回复量」随 `ADR-0127` **整条退役**（`Recuperate` 操作删除、Research 收敛为纯构筑事件、回复只留三通道），两份文件中 `Recuperate` / `lifeTotal` 零残留。就绪切片 = 构筑面板 = 复数决策槽（`ADR-0022`）· **五类**操作闭合 · 产出面 = 卡组 + `manaLimit` · `manaLimit` 下降挂玩家自选风险档 · 候选物化时掷定走 `RngStream.Reward` · `AllowDecline` 默认 `true` · 不另收资源代价 · 产出禁令扩为「不给任何货币」· 闭关与开局构筑两个取池点各叠 `Pool != Enemy`（`ADR-0091`）· 三道短缺闸 · `ResearchSlotSpec` / `ResearchSlot` / `ResearchCandidate` 形态 · 载体 `CostKey.ManaLimit` · `ProfileChangeSpec.DeckElements` 承载。**两份同批。** 卡于：走火入魔风险档权重 + 开局构筑 `lifeSpanCost = 0` 覆盖登记**两个**数值格 · **功法层数上限 `MaxTier`**（决定 `ResearchCandidate.Amount` 的取值域，权威在 `deck/_index.md` 且仍未定）· 构筑面板竖屏呈现与风险档的非 hover-only 说明通道未设计（归 `ux/`） |
| `systems/adventure-event/exchange/_index.md` · `exchange/common-properties.md` | **partial**（**承重卡点倒下一条**） | **本次由「partial（含一条待用户拍板的承重卡点）」升为「partial（纯数值卡点）」**：`ADR-0126` 已落地以物易物，上次记的「**Exchange 是否支持以物易物 —— 本库零承载，待用户拍板**」**整条倒下**且已从两份文件的待决区消失。新增就绪切片（逐条成文）：支付侧二选一、两种形态并存 · 一笔 barter 的 spec = `AbilityChangeElement(Remove, Item, Character, PayItemId, Source.ExchangeBarter)` + 产出侧既有 element，`PairKey` 留空，原子性由 `TryApply` 全有或全无保证，**一枚货币都不动 ⇒ 不触碰两币不可兑换纪律** · **`CanAfford` 一字不改（承重）**，改走门面级只读 `Holds(...)` 前置拒绝，`false` → `ApplyResult.Fail` · 灰显判据从此有两条来源 · 不持有则灰显、支付要求保持可见、**不按持有面过滤呈现**、文案走 `EVENT_` 分区不占 `ERR_` · `ExchangeBarterRule` 定值规则、可空 = 无 barter 格 · barter 的 `SoldOut` 与「产出目标已持有」两条失败语义。其余就绪切片 = 仍走 `GenericEventResolver` · 交易逐笔即时提交 · 库存物化时经 `RngStream.Shop` 掷定并落存档 · 五个商品族映射到既有仓储 · 定价表**结构**与两条折扣通道 · `ModifierKey.ShopPrice` 单点施加 · 刷新是店级动作恒以灵石计价 · **售出两条通道**（`ADR-0098`）· 价签必须标明币种 · 三道短缺闸 · 加载期 / 运行期校验。**两份同批。** 卡于：**四组数值格全欠**（定价表逐格 + 每格填哪种币 + 刷新基价与递增量 + 两档回收率绝对值 + 槽位上界，绝对数字被两种货币的获取渠道阻塞）。**失真：`_index.md:165` 仍标「ADR 候选，待 `/write-adr` 立档」而 `ADR-0126` 已 Accepted** |
| `systems/player-profile/_index.md` | **partial** | 维持，零变化（08-30 的三条 ADR 对本表零影响——寿元合并全落在 `CharacterProfile` 侧）。就绪切片 = 16 字段表整张（写入通道列无空洞，`playerItem` 为双通道 `AbilityElements` + `ItemElements`）+ 规则层 / 统计层分层判据（`ADR-0050`）+ 三样不进 Profile 的排除项 + 三个具名子类（`PlayerStatistics` / `PlayerPowerFragment` / `PlayerEntitlement`）+ 受 `ADR-0028` 回声校验约束的顶层键（两侧齐备）+ 残卷 Finale 绑定（`ADR-0049`）。卡于：`Achievement` 条目 schema 与进度模型 · 各账号级条目的解锁 / 获取 / 失去触发 · `PlayerPower` 平衡边界 |
| `systems/character-profile/currency.md` | **partial** | 维持，零变化。双币字段面与获取 / 花销通道形态整份闭合（`ADR-0089`）—— 灵石 `spiritStone` 承接原单一货币全部角色 · 仙玉 `immortalJade` 为高阶货币且同为轮回级 · `jade` 标识符整体退役 · 获取走既有 `OutcomeSpec.Elements` 不加字段 · 花销唯一出口是 Exchange · 完全不可兑换 · 随售即产出仙玉且免费取得者是净产出。卡于：**两种货币的获取渠道取值与掉落权重整体未设计**（承重——产出侧空白时无从反推消耗侧，**它卡住商店定价表的全部绝对数字**，且两条产出曲线必须一同反推）。**derive 纪律：这两条不得以占位数字充数**——占位会向下游传播成假的就绪 |
| `systems/character-profile/mana.md` | **partial** | 维持，零变化。`## 待决问题` 明写「无」（数值待定项已全部转记到 `balance.md`），`ADR-0035` Accepted。就绪切片 = `manaLimit` / `currentMana` 字段面 + 两条来源（每章事件推拉 +1~+2 · 进筑基 / 进金丹各 +1）+ 每回合刷满的三步回合节拍 + 出牌费用即 mana + `currentMana` 不入 `Status`（落 `activeCombat`）。卡于：三个待校准初值（`RealmBreakthroughManaBonus` · **卡牌费用曲线是否随境界上移**（承重，是本文件结论唯一的翻盘前提）· `EnemyManaLimit`），权威均登记在 `balance.md` |
| `systems/player-profile/game-setting.md` | **partial** | 维持，零变化。就绪切片 = 切分判据（`ADR-0072`：「取值是否取决于这台机器」+ 换机自检反问 + 拿不准归设备本地 + 一项只落一侧）+ 账号级四项（三条音量轨 `int [0,100]` + `FastCombatAnimation`）+ 设备本地一项（`locale`）+ `SettingChanges` 通道与 `SettingFields` 配表（`int?` / `bool?` 失败语义）+ `PushPolicy` + 启动期 locale 归一 + 首批不收候选与解除条件。唯一残留：三条音量轨默认值 100 / 80 / 100 是**待实测初值**——不阻塞结构，derive 时按初值写入并标注可调 |
| `systems/player-profile/codex/enemy-codex.md` | **partial（近 ready）** | 维持，零变化。无 `## 待决问题` 小节。就绪切片 = 五项词条规格 + 慷慨度（关键卡 2–3 张、不给样本卡组完整列表）与退让阶梯 + 词条②指向真实功法条目 + 「关键卡牌必在样本卡组内」是加载期机械检查（基准 = 展开产物 ∪ 散牌）+ 总长 150–280 字 + 图鉴静态性三句 + 资产字段已由 `ADR-0120` 闭合。仅余排除：`[Practice, Standard]` 条目的词条须同时说得通「切磋」与「厮杀」的**编写口径**（属内容阶段） |
| `systems/player-profile/codex/technique-codex.md` | **partial（近 ready）** | 维持，**切片本次小幅扩大**（`ADR-0123` 落点之一）：词条构成新增**「属性（灵根要求）」一行** —— `RequiredAffinities` 空数组渲染为「无属性要求」、`MaxCharacterAffinityCount == 1` 时另加「单灵根专属」标注，且明写**它由呈现层从条目字段现场算出、零新增存档字段**，并**不表达「你能不能修」**（那取决于当前角色灵根，而图鉴是跨轮回知识面）。`## 待决问题` 明写「暂无」。其余 = 词条构成三项全部取自条目自身已有字段 + **词条不列该功法的卡牌清单**（承重反查护栏）+ 两条解锁路径（零新增提交点）+ 幂等收录 / 升阶不产生新条目 / 弃置不删条目 + `Pool == Enemy` 照常收录 + 完成度分母取 `AllIncludingDisabled()` + 存档面纯加法空迁移（后端零配合）。卡于：呈现面整体不在本文件（在 `codex/_index.md`，blocked）· `MaxTier` 未定值阻塞条目编写（不阻塞词条结构） |
| `systems/enemies/_index.md` · `enemies/common-properties.md` | **partial** | 维持，两份同批，**就绪切片本次小幅扩大**：`ADR-0127` 把「条目之间层数散布」的护栏整条迁到 `enemies/_index.md` 并明标「**承重 · 本文件是它的权威**」——敌人功法层数按篇章给基准档、逐条目偏离 ≤ ±1 档，核对落 `/audit-content` 汇总（只报告不阻断），依据已由「`lifeTotal` 境界基线推导」改挂难度曲线可控性；赋级带 `±2` 无例外（`ADR-0044`）取值不变。其余 = `EnemyData` 数据形态与全部加载期校验 + `TechniqueRef` 构筑面 + 游离散牌 + `KeyCardIds` 2–3 + `EncounterScopes` / `ChapterScope`（`ADR-0110`）/ `PoolScope` 与六条取池校验 + 展开产物不得写回条目 + item / power 持有列表（含 `IgnoresProtection` 两条硬准入）+ 编排护栏 + **AI 兜底 1-ply 加权效用评分 + 确定性 argmax + `AiTerm` 十项 + 零随机零记忆 + 三条结构性上界**（`Value` 钳制越界 `PushError`）+ AI 策略字段六条校验（`ADR-0113` / `ADR-0120`；AI 侧 `KeyCardAffinity` 已按 `ADR-0123` 改名 `KeyCardBias`，四处改齐、旧名零残留）。卡于：**`LineSlot` 成员清单为空**（枚举写不出，`Lines` 只能恒为空数组，待一次战斗 UX 专场）· 道念产出缩放曲线取值 · 敌人条目目录整体为空 |
| `systems/game-progression.md` | **partial**（就绪切片本次显著扩大） | **承重卡点减一：`experiencePoint` 阈值曲线与产出分布已答结**并在本文件成文（阈值公式与三章合计 · `ExperienceGrade` 四档枚举 + 平衡表映射、内容侧不落裸数字、阈值与给予量同比放大 · **带经验产出点约占事件总数 75%** 及其两侧论证 · 失败产出 = `ExperienceGrade` + `FailureRatio`（整数百分比、向下取整下限 1、为 `AppliedChange` 可重放而取整数）· 经验条常驻的补偿设计与「eventOption 卡片不标注经验档位」· 已知风险与缓解）。其余就绪切片 = 篇章 / 境界推进与检查点重试模型（`ADR-0004`）· 存档点即结束点、途中死亡从章首重试 · 修行等级体系（`ADR-0034`）· `eventCountLimit` 的地域配额语义与剧本免疫（`ADR-0111`）· 事件循环与 Finale 胜负即篇章闸门（`ADR-0025` / `ADR-0065`）· location 与 Travel 路由的 80/20 全局常量 · 经验点进程（`ADR-0038`）。卡于：**中长期规划感只剩空间那一角**（「还有几步到 Finale」，时间那一半已由经验条常驻承接）· 选择区排布与滑动手感（批次规模 1–5，两端差 5 倍，竖屏如何同屏容下 + 寿元告警静态标注如何嵌入）· `BaseTypeWeights` 五类配比取值 · **blind / ante 缩放曲线整体尚未陈述**（本文件唯一「连形态都没有」的一条）· 失败正反馈螺旋的容错量验收 N 与「典型失败」口径。**失真：`:194` 的待决区与同文件 `:56` 自相矛盾（见上）** |
| `systems/balance.md` | **partial**（就绪切片本次再度扩大） | 维持。**上次列的「`lifeTotal` 境界基线 10/25/40（`ADR-0045`）」须从就绪切片划掉** —— `ADR-0127` 删除了境界基线公式，全文件零 `lifeTotal` / `ADR-0045` 残留。**新进就绪切片**：`experiencePoint` 阈值曲线全表 + `ExperienceGrade` 倍率映射 + 「单次给予 ≤ 该境界最小阈值」的加载期断言 + `HiddenStatGrade` 与 `ExperienceGrade` 的「形态同构非同表」分工 · **`lossPerMomentum` 篇章 × 系数表（只有篇章一维、三档 `combatTier` 共用；`Practice` 的轻量化由 `TurnLimit` 8 / `WinMargin` 0 / `ExperienceGrade` 偏置三个既有旋钮承担）** · 「商品族 × 稀有度」定价表格值 `(CostKey Currency, int BasePrice)` 与「支付币种由格决定、内容侧零新增书写位」 · `SelectionWeightGrades` 档位映射 · `RealmBreakthroughManaBonus = 1` 与「mana 不设境界基线表」。仍在的已定格 = `baseMomentum` 表 · 全局等级序 1–22 · 赋级带 `±2` 与 `EnemyLevelingData` · 起手 4 / 每回合抽 2 / **手牌上限 7** · 越级追分形状锚点 · `MaxConcurrentSideArcs = 2` · `TravelFullFanoutChance = 0.80` · `BatchSizeWeights` · `GrantPoolWeights` 40/27/18/10/5 · 取池余量三格 · 重试上限两行由 `HasPremiumBundle` 选行（`ADR-0117`）· 敌人 AI 兜底权重向量与取值域（`ADR-0113`，十个数字为占位值但字段形态 / term 清单 / 校验规则可落笔）· `itemPowerRatio` 按 `Charges` 分层 · 战斗内法则强度上沿与 `IgnoresProtection` ≈5% 两条准入 · 道统残卷三张分档表 · `EncounterTighten` 十个界常量的结构与两条硬性约束 · 平衡资源经 `Content.Single<T>()` 取（`ADR-0074`）· 「不设 `GlobalBalanceData` 兜底大表、按三问判据逐份切」一节（**derive 第 19 步前置保持解除**）。卡于：**14 条待决全部只欠取值**，共同前置是两种货币的获取渠道。**失真：`## 决策(-> ADR)` 一节为空**（正文引用至少七份 ADR） |
| `systems/scoring.md` | **partial** | 维持。**上次列的「负侧 1:1 扣 `lifeTotal`」已过时，须改为：负侧 = 道念差 × `lossPerMomentum`（ch1 = 1），扣的是唯一那条命 `lifeSpan`。** 规则面仍完全闭合且与战斗内容解耦。就绪切片 = **计分规则与转换公式本身**（一组纯函数 + 一份平衡资源，验收靠给定输入验算输出，不必等一场可运行的战斗）—— 道念 = 胜负判据、战斗不以任何资源池归零为终止条件 · **战斗过程中寿元不被读写**（承重资源纪律，战斗内可用道具与法则不得产出 `LifeSpan`）· 产削与下限 0 的每次结算截断（溢出不结转，LIFO 因此影响结果）· 疲劳为第二条削减通道（`ADR-0052` / `ADR-0088`）· 胜侧两条支路 + 单价表 · 终止条件 = `EncounterSpec.TurnLimit` 而非常量 · `WinMargin` 在 `Finale` 退场（`ADR-0018`）· `combatTier` 三档遭遇参数 · 战斗层不读写隐藏属性（`ADR-0081`）。卡于：三档 `BaseReward` / `RewardPoolId` 取值 · 卡牌产 / 削道念的量纲基准（切入点是 starter deck 设计）。**注：上次单列的「逐项领取后的奖励厚度重估」已于 08-30d 并入 `01-combat.md` 的 `RarityTier` 条作从属项，不再是独立卡点** |
| `systems/monetization.md` | **partial** | 维持，卡点与就绪切片与上次逐条相同（08-30 的四条变更对本文件零影响）。就绪切片 = `PlayerEntitlement` 两字段与不变式 `0 ≤ Redeemed ≤ Grant`（`ADR-0023`）+ **`BundleGrantOrdinal` 在 `ResourceElements` 表中无行、也不是 `CostKey` 成员**及其两条否决论据（承重）+ 空池三道闸 + 购买入口三条前置与灰态 + 兑现事务（`AccountRng.For(PremiumBundle, ordinal)` 一次派生连抽 3 条 → 一次 `TryApply` → `Immediate` push，`ordinal = Redeemed + 1` 逐一追平）+ 五项付费面排除 + 三渠道纳入 MVP（`ADR-0024`）+ 重试上限两档表由 `HasPremiumBundle` 选行（`ADR-0117`）与计数落 `chapterRetry`（`ADR-0101`）+ 唤起内购失败一律回主菜单、零 Profile 变更零痕迹。卡于：**平台内购 SDK 的选型与封装层未定 ⇒ 购买段无法落地**（跨边界，归后端支付渠道选型 + 一次客户端工程蓝图）· `K` 与 `GrantPoolMargin` 数值 · 两个通用池当前条目为零 · 纯外观付费点做成什么 · 合规交互归后端。**处置 = 缩小切片而非整份 blocked**：兑现段只读 `BundleGrantOrdinal`（后端写、经整份 profile pull 下行、不经 `TryApply`），不依赖任何未定的支付协议形状 ⇒ **可独立 derive；唤起内购整段排除在首批 FR 之外** |
| `ux/error-and-blocking-ux.md` | **partial** | 维持，判定与排除面与上次一致。就绪切片 = 翻译键基建整条（自命名的 `FR-ux-translation-foundation` 五件事：`res://text/` 分区 CSV · `project.godot` 注册 + 默认 locale `zh` + `fallback = "zh"` · `ErrorText` / `TranslationAudit` · 键命名三条 + 禁令 · 三条审计与定死的调用顺序）+ `ERR_*` 由 `code` 机械变换、无第二张对照表（`ADR-0053`）+ `reasonKey` 二级键同为机械变换与前缀式反向审计 + 内容侧不烘焙可翻译文本（`ADR-0084`）+ 两层本地化（`ADR-0056`）+ `ErrorText.For` 三参与静默回落 + 灰态判据表 + `BlockingNoticeScreen` 一屏三变体 + 三档版本提示只呈现最高一档（与 `ADR-0131` 一致，⚠「三个变体 ≠ 三处硬阻塞」原样成立，跨边界侧逐字对位）+ 诊断编号出口。卡于：逐条中文措辞（内容充实，文档明写不阻塞结构）· `auto_translate_mode` 与 `#if DEBUG` 判据的实测 · **跨边界一小块（本次可精确到半条）：只须排除「合规域端点自身」那一批 `ERR_compliance_*` 键的验收断言；四条拦截码的 `code` × `reasonKey` 取值表已在对侧成文，拦截路径那一批可以写出完整验收**。**落笔前两处零决策修正**：① 删掉 `:179` 的 `COMBAT_` 行里的 `intent`；② 把 barter 灰显补进灰态判据表与键清单 |
| `ux/screen-flow.md` | **partial**（就绪切片本次四处扩大） | 维持。**新落笔四处**：① **角色选择屏**整段（主菜单子步骤、不新增入口、横滑选择区沿用 eventOptions 手感、每张卡 = `Artwork` + 灵根一行 + 神通名 + 两门绑定功法名、不展示数值、不写灵根品级、点选 + 独立确认键、`MENU_` 分区）；② **功法候选卡的灵根属性标记**（闸关 / 开局构筑 / 商店 / 战后奖励四处共用同一套卡片 + 单灵根专属角标 + 不需要灰显态 + 卡牌卡面不加属性标记）；③ **`ADR-0127` 的寿元显性化整条对位**（明文常驻恒精确 · < 10% 转红字为纯视觉强调 · `selectCost` 与回寿数字对称恒精确 · 道具描述正文定性 + UI 追加一行精确值 · **明写「不做『大限将至』一类预警文案」且终态 `DefeatReason` 呈现照常保留**）；④ **`ADR-0126` 以物易物的呈现侧**（barter 格与普通 offer 同网格同滚动、价格位改为支付物图标 + 名称、不持有则灰显且支付要求保持可见、走 `EVENT_` 普通分区、不按持有面过滤呈现、灰显驱动源 `Holds(...)` 与 `CanAfford` 各管一半）。其余 = 登录屏 + 主菜单五入口导航骨架与 Store 三条呈现纪律 + 建议更新横幅去重 + 储物袋全屏面板 + 玩家档案屏「持有的古宝」只读区 + 绑定 / 解绑两处二次确认 + Settings 屏 + 角色状态条 + 手势判据（`ADR-0085`）+ 各入口读取的两层 Profile 字段逐格可指。**中长期规划感的时间那一半已由经验条常驻承接并在本文件明写。** 卡于：元婴证书（通关证书）形态 · 寿元红字是否伴音效 / 震动 · Explore 揭示转场时长与音效实测 · 成就两档奖励内容 · `PlayerPower` 获取 / 失去触发与平衡边界 |
| `ux/combat-ux.md` | **blocked** | 维持。文档实质内容不少（呈现事件流三消费者 · 战报收起 / 展开同一份数据两视图 · 条目粒度 = 结算事件 · 结构化存储渲染期才套翻译键 · 不落存档 · 固定预留 ≈ 屏高 6% · 结算节拍 0.35–0.5s / 敌人回合总时长 ≤ 4s · 持有物两层面板 · 埋伏只给计数 · 敌人等级用境界名），但**两条 `承重` 待决恰好落在唯一候选切片内部**：① 战报条目的文案体系（收起态单行信息量 · 截断量与 fizzle 如何在单行 / 永不换行 / 超长省略下表达 · 与飘字的分工边界）② 竖屏分区整体是否过载（根子是把 MTG 完整分区模型搬进手机竖屏，**栈是否必须常驻值得重估**；已排期专场，其结论会改写战报的落位与固定预留）。加之另 7 条形态未定。**唯一可能独立成立的非视觉切片（呈现事件流契约）其自然宿主是 `combat-service.md` 的 `ActionResult` / `CombatFeedEntry` 切片，在此 derive 必与之撞车**。08-30d 报告的「意图区与待决区双重记账」复核仍未修 |
| `systems/adventure-event/_index.md` | **blocked** | 维持：两条待决**只欠取值**（五类配比 `BaseTypeWeights` · Combat 内 `combatTier` 三档配比），运算形态已随 `ADR-0026` 落定；行为面已被 `future-event-service` 与 `adventure-event/common-properties.md` 的切片覆盖 ⇒ **不另产 FR，避免撞车** |
| `systems/adventure-event/combat/_index.md` · `combat/common-properties.md` | **blocked** | 维持：五条待决全部指向战斗内容本体（量纲基准 · 敌人平衡 · 三档奖励厚薄 · 叙事一致性口径 · 逐条目 `HiddenStatGrade` 编排）；`common-properties.md` 自身唯一待决（敌人 schema 其余字段）已随 `ADR-0120` 基本闭合，但其承载面全部由 `combat-service.md` 与 `enemies/` 承载 ⇒ 无可独立成立的切片。**`ADR-0127` 已完整落地**（寿元战斗中不被读写、只在收口按道念差 × `lossPerMomentum` 扣减；`lossPerMomentum` 三档统一只按篇章分） |
| `systems/character-profile/item/common-properties.md` · `power/common-properties.md` | **blocked** | 维持。两份的自有面均只有 `SourceCode` 一条（权威在 `systems/common-properties.md`）+ 三层分工回链，字段清单明写「不复述、权威在 `_index.md`」⇒ **无独立 FR 面**；唯一自有待决格 = `status` × 「拥有 / 失去」两正交维度的 schema 编码（与能力四类同源）。`power/common-properties.md` 无 `## 待决问题` 小节，其自有面 = 形状对标 PlayerPower + 两个正交维度的声明 + 生命周期轮回级（与 PlayerPower 的唯一本质分界）+ 写入经 `ProfileManager`。**⚠ `item/common-properties.md:3` 抬头仍写「占位结构，细节待定」，与 `item/_index.md` 的完整字段面不符——08-28 已点名，第二次报告** |
| `systems/player-profile/account-info.md` | **blocked** | 维持。字段面五项已定，仅剩合规字段归属一条待后端分级 ⇒ 表仍可能增行；**且单独不构成可独立成立的 FR 面**，应随 `account-service` / 登录切片一并落地 |
| `systems/player-profile/achievement/_index.md` · `achievement/common-properties.md` | **blocked** | 维持。成就条目 schema 与进度模型未设计（分组结构、加权进度的权重来源、触发条件表达、隐藏成就揭示时机）· 触发采集面未定（EventBus 被动 vs 主动上报）· 两档具体奖励条目清单未定。「能给什么」与「怎么给」均已答结（法则 / 古宝、指定条目 + `ExclusiveSource == AchievementReward` 不进任何抽取池，故成就奖励恒不落空）；缺的是目录本身，而它又依赖法则 / 古宝条目 |
| `systems/player-profile/codex/_index.md` | **blocked** | 维持。图鉴族在主菜单的入口与浏览形态、竖屏排布未定 · `LocationCodex` 除连边外的词条深度未定、且其呈现形态与其余各本不同（一张逐步显影的图 vs 列表 / 网格）· 边缘顶点显示程度与显影半径 ⇒ **呈现面整体无可 derive 的切片**；存档 / 写入面已下沉到同目录 `common-properties.md`（ready），词条规格已下沉到两本专档（partial）。战斗内一律不可查已由 `ADR-0094` 答定 |
| `systems/player-profile/player-item/_index.md` · `player-item/common-properties.md` | **blocked** | 维持。类型面归 `ItemData`（两层共用）、存档面归 `player-profile/_index.md` ⇒ **无独立 FR 面**；自身的古宝种类目录 / 次数补充机制 / 可购价格与库存权重三项仍未设计，`common-properties.md` 自陈共有字段「均为占位，无实质设计」 |
| `vision/pillars.md` · `vision/references.md` | **blocked** | 北极星 / 参考登记，**非 derive 对象**（其余文档的挂靠前置）。**本次核实 `ADR-0127` 未松动第 9 条支柱**：`pillars.md` 的「信息靠遭遇获得」+ 射程边界逐字未改，Explore 定价指纹泄漏改由「Explore 行的独立定值」恒常堵死 —— 护栏由呈现门控升为定价结构，第 9 条不承担该负担。`references.md` 唯一悬空项：「月圆之夜 · 规避」仍为待定；其指向 `systems/character-profile/life-span.md` 的路径**存在**（更名已同步到位） |
| `vision/scope.md` | **blocked** | 非 derive 对象 + **两项仍未定且无处承载，十二天来零推进**：① 美术资源 AI 生成的商用授权 / 参考素材版权立场（`art/_index.md` 把落点指向本文件，本文件零承载）② **初装包体预算**（`deferred-content.md` 与 `soundtracks/_index.md` 同样指向本文件，零承载；它向下卡住整个 `soundtracks/` 分区的时长 / 码率） |
| `terminology.md` | **blocked** | 根级词表，**非 derive 对象**。**本次已随 08-30 四份 handoff 同批改齐**：「寿元」词条改写为合并后的单一命线（两个扣减来源 · 战斗过程中不被读写 · 明文常驻恒精确 · 单值无上限字段）· **全文零处 `lifeTotal` / 「耐久」残留** · 新增「以物易物」词条（`ExchangeBarterRule` / `BarterOffer` / `Source.ExchangeBarter`）· 新增「道具使用痕迹 `ItemUseEntry`」· `Source:` 已补至 08-30。**🟡 `:89` 的「大限将至」措辞待复核**（与被删的三条 Band 预警文案同名，易被读成残留） |
| `program-overview.md` · `system-overview.md` | **blocked** | 根级运行时调用链与 Godot 工程落地形态，**非 derive 对象**。08-28 的修复保持有效：`system-overview.md` 的 `combat.csv` 行明写「**无 intent** —— 意图机制已整条移除」；`program-overview.md` 的战斗段已按合并后寿元改写（`寿元 ≤ 0 ─▶ DefeatCharacter()`，零 `lifeTotal` 残留），`Source:` 已补 `2026-08-30-life-lifespan-merge`。`system-overview.md` 的 `Source:` 仍止于 08-13，但正文与 08-30 变更无冲突面 |
| `art/_index.md` · `art/visuals/_index.md` · `art/soundtracks/_index.md` | **blocked** | 生产流水线说明与资产类目表（`ADR-0040`）；本库不承载生成产物（归 `game-feature-branch/`）⇒ **结构性地无客户端行为面可 derive**。**但上次记录的两处重大卡点本次已闭合**：① **「境界晋升是否改变外观」整条移出**——类目表已改写为「一条内容一张、不随境界分版，唯一例外是角色形象的稀疏境界覆写落 `CharacterData.RealmArtworks`、不改共有字段 `Artwork` 的基数」（`ADR-0124`）；② **「二进制能否经 overlay / blob 下发」跨边界空档成对闭合**——已明写「资产变更随发版，不经热更通道」并给出可撤销性与代价清单（`ADR-0125`）。未决仅余：AI 生成资产商用授权（落点 `vision/scope.md`，零承载）· 参考素材二进制是否入库 · guide 粒度 · 生成资产落 `game-feature-branch/` 的目录划分 · UI 元件是否走 AI 生成 · 音频生成工具未定案（自陈「本分区第一前置」）· BGM 时长码率的包体预算 |
| `art/visuals/art-direction.md` · `art/soundtracks/audio-direction.md` | **blocked** | 两份的 `## 一句话方向` 本身即 `> _（待写）_` 占位 ⇒ **判据①即失败**。**排除面本次缩小两处**：`art-direction.md` 的「基调」已随 `ADR-0124` 同改（「境界越高画面越沉」收窄为按条目自身叙事定位取沉、不按运行时赋级取沉，并明写「故没有任何类目需要为境界出多套资产」），「构图」一节已有实质内容（竖版 / full art 不预留文字区 / 缩略可辨跨卡可区分），仅余一行待写。仍占位：色彩 · 光照与材质 · 尺寸与格式 · 禁用清单（视觉侧）；配器与音色 · 调式与节奏 · 动态与混音 · 格式与预算 · 禁用清单（音频侧）。`ADR-0100` Accepted 但明写逐项约束落点是本文件 ⇒ ADR 不构成对占位的覆盖 |
| `art/visuals/animations/_index.md` | **blocked** | 明写占位：范围四类 / 技术载体 / 制作方式 / 与战斗节奏与篇章时长预算的关系**全部「待咨询专业人士后确定」**，尚无设计意图。`ADR-0040` 只固定「动画不走 AI 流水线」这一条排除性结论。**一条孤悬约束升级为相抵**：本文件要求「须可跳过 / 可加速」，而 `ux/combat-ux.md` 明写「只能加速不能跳过」——上次记为「无承载」，实际是**两侧措辞相抵** |
| `art/visuals/references/_index.md` · `art/soundtracks/references/_index.md` | **blocked** | `visuals/` 三条登记（三国杀 / 弈仙牌 / Warhammer 40k）已细化到可转 prompt 的粒度但「素材位置」前两行仍为待定；`soundtracks/` 登记表为空——**一条参考都未登记**，判据①的实质内容不存在 |
| `art/visuals/guides/_index.md` · `art/soundtracks/guides/_index.md` | **blocked** | 台账均为空，**零份 guide** ⇒ 无 derive 对象；约定本身已成文，但上游两份 direction 文档均 blocked 且「guide 粒度」未决 ⇒ 连第一份 guide 都写不了 |
| `systems/_index.md` · `systems/services/_index.md` · `ux/_index.md` · `content/_index.md` | **blocked** | 导航索引 / 层级词表 / 登记表，**非 derive 对象**（各自的待决项已下沉到对应主题文档）。`services/_index.md` 的内容面本身**已就绪**（五级层级词 · 拆分轴 · 七服务 × manager 清单与各服务文档逐一对得上 · 两条唯一入口 + 一个唯一物化点），但它承载的权威在 `architecture.md` 与各服务文档 ⇒ **单独 derive 必与之重复，随第 3 步同批兑现、不另产 FR**；其唯一残留（第四 / 第五级层级词是否过早）自标「暂不落实例、保持空是健康的」，不阻塞。`content/_index.md` 持内容类型的 **scaffold** 就绪度，与本小节的 derive 就绪度是两套闸，勿混（18 行登记表本次复核无失真，`character/` 行已含 `Affinities` 与 `RealmArtworks` 两格）。**⚠ `systems/_index.md:45` 的 `combat-service` 行仍写「敌人 AI 与意图（三档揭示）」，与三处上游承重定案正面冲突——索引是找文档的第一跳，这处失真会误导 derive 起点** |
| `decisions/ADR-0002` ~ `ADR-0132`（131 份 Accepted） | **blocked** | 已采纳的决策记录，**非 derive 对象**（作为其余文档的就绪前置，本身不产 FR）。**计数经机械核对一致**：`decisions/` 下 132 份 `ADR-*.md`，台账登记 132 行、零缺登记、零悬空行；扣除 `ADR-0001`（Proposed 示例）即 **131 份 Accepted**。本次由 121 增至 131：2026-08-30 一次 `/write-adr game` 固化 `ADR-0123` ~ `ADR-0132`（灵根修习准入 · `Artwork` 单格与 `RealmArtworks` · 二进制不经 overlay · Exchange 以物易物 · 寿元合并 · `StatusChanges` 置值列 · `HiddenStatDirection` · flags 第三层覆盖来源 · `Upgrade` 非闸门不硬阻塞 · `StackEntryKind.UsedItem`）。**⚠ 这十份在全部主题文档中零反向引用**（见上方台账缺口） |
| `decisions/ADR-0001-example.md` · `decisions/_index.md` | **blocked** | 示例占位（`## Decision` 明写「待定」，status 仍为 Proposed —— 全库唯一非 Accepted 的 ADR）与台账，非 derive 对象。无失真 |
| `art/*/guides/_TEMPLATE.md` · `content/_TEMPLATE-*.md` · `requirements/_TEMPLATE*.md` | **blocked** | 模板骨架（6 份），非 derive 对象 |

### 建议的 derive 顺序（仅限 ready / partial 项；被依赖者在前）

**落笔前的必须裁决 —— ✅ 两条全部倒下，队列首次零前置：** ① 境界晋升是否改变外观 → `ADR-0124`；② `balance.md` 的三问判据小节 → 08-28 已补齐。
**落笔前的零决策修正（不需要任何裁决，但会影响首两步的产出正确性）：** ① `sync-service.md` 的 schema bump 清单补齐三批（第 7 步前置）；② `error-and-blocking-ux.md:179` 删 `intent` + barter 灰显补进灰态判据表与键清单（第 2 步前置）；③ `systems/_index.md:45` 删「意图（三档揭示）」。

1. `/derive-requirements systems/common-properties.md` —— **唯一整份就绪且被几乎所有内容与服务代码依赖的地基，现已零前置。** 共有字段类型与两级 RNG 先落，后面每一份 FR 都能直接引用。账号级 RNG 的验收有现成的 8 组测试向量（后端 `contracts/profile-sync.md` §6a），是全库当前最硬的验收标准。文档自己写明排期——`LocalizedText` 与 `DrawPool<T>` 同批、**在写下第一批 `.tres` 之前**。**落笔时按 `Source` 十值写枚举与合法子集表。**
2. `/derive-requirements ux/error-and-blocking-ux.md` —— 翻译键基建（文档自己已点名 `FR-ux-translation-foundation`）。**无上游依赖**，是「每屏从第一行起就用键」这条起手纪律的前置，越早越省返工。**排除逐条中文措辞与「合规域端点自身」那一批 `ERR_compliance_*` 的验收断言（拦截路径那一批不排除）。**
3. `/derive-requirements systems/architecture.md` + `systems/viewmodel.md` —— 服务骨架：七个 autoload 的注册顺序、EventBus、边界服务的接口 + Offline stub 双实现、`ProfileChangeSpec` 逐列（含 `StatusChanges`）、`HiddenStatGrant` 三格。**两份同批**：三层切分的定义在前者、展开在后者，分两次必出两份互相打架的 FR。两份均 ready、无排除面。`services/_index.md` 的层级词与七服务 × manager 清单随本步一并兑现，不另产 FR。
4. `/derive-requirements systems/services/profile-service.md` + `systems/player-profile/player-power/common-properties.md` —— 写入通道这一层地基（逐列 spec + `TryApply` 原子性与钳制 + 逐列失败语义 + `CapabilityManager` 整面 + modifier 合并算法 + **门面级 `Holds(...)`**）。**两份必须同批**。**`CostKey` 写 15 行**（不写死列数）。**排除成就族与 `status` × 拥有 / 失去的存档编码。**
5. `/derive-requirements systems/character-profile/_index.md` + `systems/player-profile/_index.md` + `systems/player-profile/codex/common-properties.md` + `systems/player-profile/game-setting.md` + `systems/character-profile/currency.md` + `systems/character-profile/mana.md` —— 两层 Profile 的存档 schema 地基。**六份同批**：互相引用、共用同一个 `schemaVersion` 与同一条迁移路径。`CharacterData` 字段表（七格 + 十一条加载期校验 + `Affinities` + `RealmArtworks`）随本步一并落。**只取存档 / 写入面，不取任何图鉴屏或设置屏呈现，不取货币取值**（两条产出曲线**不得**以占位数字充数）；**`content/character/` 的条目编写另需先答「绑定功法初始层数」，不在本步射程内。**
6. `/derive-requirements systems/services/content-service.md` —— 内容加载与校验链路（依赖 1、3）。被一切内容读取方依赖。**flags 本地缓存整段纳入本步**（上次之外的净增量）。**仅排除 disabled 条目被存档引用时的 UX。**
7. `/derive-requirements systems/services/sync-service.md` —— 本地缓存原子写 + push 调度与退避 + CAS / 幂等 + 两层 diff + 回声校验完整字段面（依赖 1、3、4、5）。**ready、无排除面，但落笔前须先补齐本文件的 schema bump 清单**（漏了三批，且对侧把 bump 权威指回它）。
8. `/derive-requirements ux/screen-flow.md` + `ux/onboarding.md` —— 登录屏 + 角色选择屏 + 主菜单导航骨架 + 储物袋面板 + 玩家档案屏（依赖 2、3、5）。**两份同批**（本次重叠面由一处增至两处：登录屏 + 角色选择屏的门禁语义 ↔ 屏形态）。**排除元婴证书、成就奖励、图鉴入口、全部待实测手感项，以及角色模板池的规模与解锁形态**（屏的结构与之无关，但不得在 FR 里写死池规模）。
9. `/derive-requirements systems/services/account-service.md` —— 会话生命周期 + 四个账号方法 + refresh 两条路径 + `deviceId` 与 refresh token 落点 + 启动期静默续期 + 合规拦截路径（依赖 2、3、7、8）。**排除 `ComplianceManager` 中触及合规端点请求 / 应答体的部分。**
10. `/derive-requirements systems/character-profile/deck/common-properties.md` —— **效果原语层地基**：`EffectData` 子类树 + **其余七原语** + `StaticModifierData` + `TriggerConditionData` / `TimingIds` + `EffectCondition` + `CardData` 收口 + **20 条加载期校验**。**它是五个内容类型的共同底座，必须先于 11–13。** 纯数据资源型切片、过三条闸。**排除 starter deck 与抽 / 弃 / 洗数值，并显式排除 `MoveCardEffect` 一行**（等 `Side : SideConstraint` 裁定后补一次纯加法）。
11. `/derive-requirements systems/character-profile/deck/_index.md` —— `CultivationTechniqueData`（含 `RequiredAffinities` / `MaxCharacterAffinityCount`）+ 灵根准入判定式与四处取池点 + 成员卡反建索引 + `CardSubtypeData` + `CardPool` 三值 + 五个 `DeckChangeOp` + 手牌上限纯上界（依赖 5 的 `CharacterData.Affinities`、10）。**不得先于 5**（准入判定式跨两份文档）。**排除全部规模参数与首批清单。**
12. `/derive-requirements systems/character-profile/power/_index.md` —— `PowerData` 字段面 + 三格非空校验 + 战斗外触发式校验 + 不得产寿元 + 禁用截断五行表（依赖 4、10）。**排除持有条目 `status` 编码与获取 / 失去触发。**
13. `/derive-requirements systems/character-profile/item/_index.md` —— `ItemData` 两格使用效果面 + `MaxUsesPerCombat` + I-1 ~ I-13 校验 + 战斗外一次事务链路 + 随售通道（依赖 4、5、7、10、12）。**排除回寿护栏口径与道具目录。**
14. `/derive-requirements systems/adventure-event/common-properties.md` —— `EventOption` 数据形态 + 事件收口事务语义 + `AddLooseCard` 取池链（依赖 1、5、10）。**它是后续全部事件类型 FR 的共同类型地基**，必须先于 18。**不取定价表与配比取值。**
15. `/derive-requirements systems/services/future-event-service.md` + `systems/services/plot-manager.md` —— eventOptions 十步管线 + 三道短缺闸 + `Priority` 抬升判据 + `HiddenStatGrant.Direction` 符号展开点，以及它的调制输入。**两份必须同批**。**排除全部权重取值与隐藏属性的逐条推拉映射。**
16. `/derive-requirements systems/services/life-cycle-service.md` + `systems/character-profile/life-span.md` —— 轮回骨架、角色状态分类法、**`DefeatReason` 三值**与 Finale 旁路、存档点与决策点清单、`SeedManager` 四子流、批次层即时提交，以及寿元的三条不变式与两个扣减来源（依赖 4、14、15）。**两份同批**（`life-span.md` 不单列 FR，消费点在本服务与 combat-service 两侧）。**排除面收窄：`experiencePoint` 只需排除验收侧的 N 值，曲线与分布已定。**
17. `/derive-requirements systems/game-progression.md` —— 篇章 / 境界推进与 `eventCountLimit` 配额 + **经验阈值曲线 / `ExperienceGrade` 映射 / `FailureRatio` 折算 / 75% 覆盖率**（依赖 15、16）。**排除选择区呈现、blind / ante 与 `BaseTypeWeights` 取值。**
18. 四个非战斗事件子类型，按依赖薄厚依次：`travel/` → `explore/` → `research/` → `exchange/`（各自两份同批；依赖 14、15、16）。**四份的机制面已闭合，全部排除数值取值**；`research/` 另排除构筑面板竖屏呈现与 `MaxTier` 相关取值域；**`exchange/` 的 barter 全套结构可整体进切片**（上次的「待用户拍板」排除项已作废），但需 4 的 `Holds(...)` 先成立。
19. `/derive-requirements systems/balance.md` —— **第一批平衡资源 `.tres`**：已定值 / 已定形的那些表（含经验阈值全表 · `ExperienceGrade` 映射 · `lossPerMomentum` 篇章表（ch1 = 1，ch2/ch3 留空）· 定价表格值二元组 · `SelectionWeightGrades`）+ `Content.Single<T>()` 读取面 + 加载期条数校验（依赖 1、6）。**只取已定格，全部未定格显式留空并标注。**
20. `/derive-requirements systems/enemies/_index.md` + `enemies/common-properties.md` —— `EnemyData` 数据形态与全部加载期校验 + `EnemyAiProfileData` 与兜底 argmax + 两条护栏（`±2` / `±1 档`）（依赖 10、11、19）。**两份同批。** 纯数据资源型切片、过三条闸。**`Lines` 恒为空数组落笔**（`LineSlot` 待战斗 UX 专场），不因此推迟整份；**排除全部数值缩放。**
21. `/derive-requirements systems/player-profile/codex/enemy-codex.md` + `codex/technique-codex.md` —— 两本的词条规格与解锁触发（依赖 5、11、20）。**两份同批**（连锁解锁横跨两本）。**只取词条数据源与解锁写入，不取任何图鉴屏呈现。**
22. `/derive-requirements systems/scoring.md` —— 道念计分规则与两条转换支路（**负侧 = 道念差 × `lossPerMomentum`**）（依赖 19）。**一组纯函数 + 一份平衡资源**，验收靠给定输入验算输出。**排除三档 `BaseReward` / `RewardPoolId` 取值。**
23. `/derive-requirements systems/services/combat-service.md` —— 战斗机制骨架 + **效果流水线五阶段** + 决策点 D0–D7 + AI 两层结构 + `StackEntryKind` 五值与栈条目 `itemId`（依赖 10、16、20、22）。**排除战斗内容本体、Finale 奖励加厚取值与 `MoveCardEffect` 的方位面。** 验证靠在编辑器里跑一场用占位内容拼出的遭遇观察栈与回合流转。
24. `/derive-requirements systems/player-profile/player-power/_index.md` —— 失去三形态 + 残卷 + 授予池结构（依赖 4、5、12）。**排除战后奖励池三张权重表与获取触发。**
25. `/derive-requirements systems/monetization.md` —— **仅兑现段** + 三道闸 + 入口前置条件 + 兑现水位幂等（依赖 1 的账号级 RNG、7 的 push 调度）。**排在最后**：依赖的两个通用内容池当前为空；**唤起内购整段等平台 SDK 选型。**

> **仍然不要**对元进程失败侧 / 成就 / 古宝目录 / 图鉴呈现侧 / `ux/combat-ux.md` / art 任何文档 derive。玩法侧的欠账仍是两处：**① 数值标杆的取值**（只欠取值不欠结构，前置是两种货币的获取渠道）；**② 战斗内容本体**（条目目录整体为空，属内容阶段而非设计缺口）。本次新增第三处、但性质不同且成本极低：**③ `MoveCardEffect.Side` 一格**（结构面，一次独立推演即可关闭）。

### 最短解锁路径（blocked 项，按解锁面从大到小）

1. **一次「战斗 / 事件 UX 专场」** —— 解锁面最宽的一次会：① `ux/combat-ux.md` 的两条承重（竖屏分区是否过载 / 栈是否必须常驻 · 战报条目的文案体系）② `enemies/` 的 `LineSlot` 成员（解锁 `Lines` 与三条相关校验）③ `research/` 的构筑面板竖屏呈现与风险档非 hover-only 通道 ④ `codex/_index.md` 的图鉴族入口与浏览形态。**一场会同时把 `ux/combat-ux.md` 送进 partial 并收窄三份 partial 的排除面。** → `/analyze-new-ideas`
2. **一次裁决：`status`（启用 / 禁用）× 「拥有 / 失去」两个正交维度如何编码进 schema** —— 能力四类同源，一次答齐即解锁 `item/common-properties.md` · `power/common-properties.md` · `player-power/` 持有条目面，并同时收窄 `profile-service.md` / `power/_index.md` / `player-power/_index.md` 三份 partial 的排除面。**成本最低、解锁面第二宽。** → `/analyze-new-ideas`
3. **两种货币的获取渠道取值与掉落权重** —— 它是 `balance.md` 全部 14 条待决取值的共同前置，也是 `currency.md` / `exchange/` 唯一的实质卡点。两条产出曲线必须一同反推。→ `/provide-solution-draft` 后 `/analyze-new-ideas`
4. **`MoveCardEffect` 的方位声明（`Side : SideConstraint` 三种收法择一）** —— **成本极低、且它是唯一一处挡在已就绪切片内部的结构缺口**：关闭后 derive 第 10 步无排除面、第 23 步减一项。→ `/provide-solution-draft` 后 `/analyze-new-ideas`
5. **一次 handoff 定 `Achievement` 条目 schema + 进度模型 + 采集面**（EventBus 被动 vs 主动上报）—— 解锁 `achievement/` 两份，并收窄 `profile-service.md` / `player-profile/_index.md` 的排除面；奖励目录另需法则 / 古宝条目先存在。→ `/analyze-new-ideas`
6. **一次 handoff 定古宝种类目录 + 次数补充机制 + 可购价格 / 库存权重** —— 解锁 `player-item/` 两份（类型面已由 `ItemData` 覆盖，无需再设计字段）。→ `/analyze-new-ideas`
7. **`vision/scope.md` 的两条零承载项**（AI 生成资产的商用授权与版权立场 · 初装包体预算）—— 它们被 `art/_index.md` 与 `deferred-content.md` 指名落点为本文件而本文件零承载；答定后再开一次美术方向会与一次音频方向会填实两份 direction 的占位，`art/` 分区才可能开始产 guide。**art 台账类文档即便全部答完仍结构性地不产 FR**，不必期待它们进 derive 队列。→ `/analyze-new-ideas`
8. **对侧欠账：`backend-design-documents/contracts/compliance.md` 的六端点报文字段表与端点错误码** —— 关闭它才能写出 `account-service` 合规呈现面与「合规域端点自身」那批 `ERR_*` 的完整验收断言。它是后端库唯一向本库传导的欠账。→ `/analyze-new-ideas backend`
9. `art/visuals/animations/_index.md` 明写「待咨询专业人士」，属**外部输入**，不在本库可自解锁范围；其「须可跳过」与 `combat-ux.md`「只能加速不能跳过」的相抵需在该会上一并裁掉。

## 下一阶段

- **ADR 状态：** `decisions/` 现有 **131 份 Accepted**（`ADR-0002` ~ `ADR-0132`，`ADR-0001` 为 Proposed 示例占位）。
  2026-08-28 的一次 `/write-adr all` 全量扫描（逐份核对 29 份未被任何 ADR 引为来源的 handoff）把散落其中、已落笔主题文档却无档案的定案固化为 **23 份新 ADR**（`ADR-0100` ~ `ADR-0122`），覆盖 sync 族（`revision` CAS · flush 非阻塞 · 闸门口径）· 命名通则 · 战斗 RNG 单流 · 敌人 AI 权重向量 · 效果原语语法 · capability flag / modifier · 道具使用效果面与批次层提交等。
  台账与逐条影响文档见 `decisions/_index.md`。
  2026-08-30 的一次 `/write-adr game` 固化 **10 份新 ADR**（`ADR-0123` ~ `ADR-0132`）：上述 4 条登记候选（灵根修习准入 · `Artwork` 单格与 `RealmArtworks` · 二进制不经 overlay · Exchange 以物易物）全部立档；另逐份核对未被任何 ADR 引为来源的 handoff，补固化 6 条散落定案（寿元合并与显性化 · `StatusChanges` 置值列 · `HiddenStatDirection` · flags 第三层覆盖来源 · `Upgrade` 非闸门点不硬阻塞 · `StackEntryKind.UsedItem`）。**当前待固化的 ADR 候选：暂无。**
  - ✅ 更早的一条候选「**不设 `GlobalBalanceData` 兜底大表，平衡资源按三问判据（消费者 / 覆写纪律 / 跨字段不变式）逐份切**」（`handoffs/2026-08-22-singleton-balance-resource-registry.md`）已于 2026-08-28 收口：`systems/balance.md` 补上了该判据本身那一小节（此前只有逐处应用），**并直接改写既有的 `ADR-0074`**（它本就是「凡可调数值一律住平衡资源」那条决策的档案，同一件事不另开编号——见本库「改决定直接改 ADR」的治理原则）；`ADR-0074` 后果里自陈的那条欠账随之关闭。**无需再跑 `/write-adr`。**
  （注：ADR 现可自由编辑，改决定直接改 ADR，不再新开取代 ADR。）
- **流水线闭环（07-30）：** design → code 链路补上 `/breakdown-requirements`（一份 FR → 一个文件夹的可执行子需求），完整形态见 `README.md` 与 `requirements/_index.md`。
- **架构闭环缺口：** 8 处**全部闭合**（移出记录见 `answer-logs/log-0725c.md` 与 `log-0726b.md`）；状态表见 `systems/architecture.md` 的「闭环缺口」小节。残留细节已下沉为各焦点分片的普通待决问题。
