# Open questions — 跨 session 待答清单（索引）

> 本文件是**客户端**（Godot 项目）待答清单的**索引**：问题条目本身按主题拆在 `open-questions/` 下的分片里。
> 后端侧的待答清单在 `backend-design-documents/open-questions.md`（`backend-design` 分支）。
>
> 每次 session 结束时，未答的 Open questions 汇总到对应分片，供下次拾起；一旦答定，就从分片中移除、
> 归档进对应主题文档的 `## 待决问题` / `## 决策`，并在 `answer-logs/log-<draftSuffix>.md` 记一笔。
>
> 本清单**只跟踪仍待答的问题**（不留已解决区），是导航 / 拾取清单，**权威归属在各主题文档**。
>
> 最近更新：2026-09-08 — 战斗屏竖屏形态与候选项列表语言整块收口：移出 12 条 · 新增 2 条（详见 `open-questions/update-log.md` · `answer-logs/`）
> （逐次变更摘要见 `open-questions/update-log.md`；已答定问题的逐条移出记录见 `answer-logs/`）

## 分片导航

| 分片 | 内容 |
|------|------|
| `open-questions/update-log.md` | 每次运行的更新摘要（答结 / 推翻 / 新增落点），倒序，**只留最近 10 条**。不含问题条目本身。 |
| `open-questions/update-log-archive.md` | 更早的更新摘要，原样归档、按时间正序。只读，不写新条目。 |
| `open-questions/01-combat.md` | **① 战斗机制**（焦点之首）：能力剥夺与统计计数的残留（片区主体已于 08-10c 答结）、结构与配置（`EncounterTighten` 六界常量 · `EnemyManaLimit`）、内容与数值（多数留待内容扩充后的统计校准）、呈现（形态已整体定案，只剩实测校准项与美术取值）。 |
| `open-questions/02-event-options.md` | **② eventOptions 生成流程**：生成 / 加权与配比、物化字段、优先级、寿元打穿、Explore 揭示池、Travel 出场、location 与图鉴连边。 |
| `open-questions/03-adventure-event-types.md` | **③ 逐类型 AdventureEvent 的收口后残留**：五类机制面已全部收口（各自开过专场），片内只剩**只欠取值 / 只欠呈现形态**的格——Travel 定价、Exchange 两组数值格、Explore 三个待定取值、走火入魔权重、三档奖励厚薄与 `HiddenStatGrade` 编排。 |
| `open-questions/04-hidden-attributes-plot.md` | **④ 隐藏属性 / 剧本机制**（档位表 · 跨档叙事 · 数据编码 · 寿元增长通道 · **属性清单定稿两项**均已答结）：仅剩逐条目的推拉映射与两条剧情线的具体内容、`HiddenStatGrade` 三个映射值。 |
| `open-questions/05-service-contracts.md` | **⑤ 服务契约 / 工程侧残留**（API 面 · `revision` · 契约边界 · 翻译键 · 共有属性分层 · 纪律可执行化均已答结）：location 无法被 flags 秒关的运营替代通道、**首次生成 `.csproj` 后的三项实测**（`#if DEBUG` 判据 · `Control` 自动翻译 · 平台凭据存储能力）、`schemaVersion` 登记表覆盖对账的两件残留。 |
| `open-questions/06-meta-progression.md` | **⑥ 元进程的失败侧**（中长期规划感三角、四支频次预算与失败螺旋容错量均已于 09-06 答结）：胜侧 `experiencePoint` 单价是否计入供给账、通用功法占比口径、角色强度差与多灵根换算。 |
| `open-questions/07-codex-monetization.md` | **⑦ 图鉴族与商业化**：`CharacterPower`、图鉴族、premium bundle。 |
| `open-questions/cross-boundary.md` | **跨边界承接**：对侧（后端）已定案、本库尚未落笔的条目。**不是待答问题**——答案已有，等的只是落笔；形态、关闭条件与维护者分工见分片抬头。 |
| `open-questions/deferred-content.md` | **已搁置：内容充实**（07-30 起暂不推进）＋ **美术与音频（`art/`，08-04 加入）** ＋ 随内容搁置的 UX 呈现细节 ＋ 尚未设计的占位主题。 |

## 当前焦点：各系统机制细节

> **焦点判据（07-30 定）：** **规则、字段语义、流程与算法 = 机制细节 = 焦点**（分片 ①–⑦）；
> **具体条目目录与数值 = 内容充实 = 搁置**（`open-questions/deferred-content.md`）。
> 与既定开发路线「框架 → 内容 → 平衡与体验 → 社交及其他」的第 ① 阶段一致。
> Source: `handoffs/2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown.md`。
>
> 焦点顺序即分片编号顺序；**① 战斗机制**优先级最高。

## derive 就绪度

> **本小节由 `/assess-derive-readiness` 独占写入**（`/analyze-new-ideas` 与 `/summarize-open-questions` 均不得改动）。就绪度是全库横切判断，须基于一次性全量扫描才有意义；逐次 handoff 顺带的评估会迅速过时且互相矛盾。

**最近全量评估：2026-09-07（由 `/assess-derive-readiness` 产出）。** 扫描范围：`vision/`（3）· `systems/**`（55）· `art/**`（10，不含 2 份 `_TEMPLATE`）· `ux/`（5）+ 根级横切 3 份 = **主题文档 76 份**（与 09-06 口径逐格一致，无增删）。机械对账：`decisions/` **182 份文件 ↔ 台账 182 行**，双向「有文件无登记」0 / 「有登记无文件」0，**181 份 Accepted + `ADR-0001` Proposed 占位**；`requirements/` **仍零 FR**（3 个文件全是模板与台账）；`content/` **仍零条目**，类型登记表 **18 行、开张列 0 个 ✓**；`inbox/` 顶层在办草稿 **0 份**；`handoffs/` **167 份实际 handoff 中 166 份 `distilled`**，唯一 `raw` 是 `2026-07-12-example.md`（示例占位），`triaged` 0 ⇒ 无未消化输入。本次重估的触发是 09-06 的两批 `/batch-analyze-new-ideas`（09-06b 六稿 / 09-06c 七稿）与 2026-09-07 一次 `/write-adr game`（固化 `ADR-0159` ~ `ADR-0182` 共 24 份）。五个分片并行只读评估，逐份给证据行号；一处跨分片事实矛盾以直读裁决（见文末）。

**全局结论：ready 20 份 · partial 31 份 · blocked 25 份（主题文档 76 份口径，不含 181 份 Accepted ADR 本体与台账 / 模板）—— ready 由 12 份增至 20 份、blocked 由 29 份减至 25 份，零降级。09-06 记的两条承重卡点倒下其一（`status` × 拥有 / 失去的存档编码），跨边界唯一欠账（平台内购 SDK 选型）关闭，derive 队列自第 1 步起连续二十余步无结构性排除面。**

- **✅ 全库解锁面最宽的那次裁决已落地：`status`（启用 / 禁用）×「拥有 / 失去」两个正交维度不再是任何文档的卡点。** 09-06 的直读裁定认定「权威侧自相矛盾」不成立——编码本体早已成文，**真实缺口是没有写入通道**，现补为 `ProfileChangeSpec.AbilityStatusChanges` 列（绝对置值、恒不走 modifier pipeline，`architecture.md:355` / `:548`、`profile-service.md:37` / `:116`–`:118`）+ 门面 `SetAbilityStatus(kind, scope, abilityId, enabled)`（`profile-service.md:387`，未持有 → `ApplyResult.Fail` 供 UI 灰显）+ 三维生效与门（`profile-service.md:360`：拥有 ∧ `status == 启用` ∧ 不在 `disabledAbility` 内），一个门面覆盖四类持有条目（`:413`）。⇒ **`item/common-properties.md` 由 blocked 直升 ready**（`:30` 待决明写「无」）· **`power/common-properties.md` 由 blocked 升 partial** · **`player-power/common-properties.md` 由 partial 升 ready**（`:53` 待决清零、`:14`–`:16` 写入通道具名）· `profile-service.md` / `power/_index.md` / `player-power/_index.md` / `player-profile/_index.md` 四份的排除面同时收窄（`profile-service.md:446` 已把该维明确移出待决范围并回链）。**上一版判定表引用的 `profile-service.md:336` / `:420` 两个行号现已失效**，那处「自相矛盾」在当前文本中不存在。
- **✅ 跨边界唯一欠账关闭：平台内购 SDK 的选型与封装层已定案。** 客户端侧 `ADR-0178`~`ADR-0181` 全部 Accepted 并落笔（三渠道一律自维护插件 · iOS StoreKit 2 + 部署下限 15 · `StoreChannelManager` / `IStoreChannel` 运行时探测不走 `#if` / 第四个窄接口 `IPurchaseBackend` / 非商店平台 Store 入口**不渲染**），落点 `sync-service.md:167`–`:205` 与 `monetization.md:105`–`:136`；对侧 `backend-design-documents/contracts/purchase.md` 三端点 · verify 报文与判别式 · 逐渠道 receipt 形态 · 幂等窗口永久保留均已成文，其残留 Open questions 归运维且明写「不回头改契约」。⇒ **`monetization.md` 的「唤起内购整段排除」取消**，购买段与兑现段可一并 derive。**两库 `cross-boundary.md` 的「待承接」双双为空，全线闭合。**
- **✅ 事件族地基整片就绪：`adventure-event/common-properties.md` 由 partial 升 ready**（`:409` 待决改写为「无 + 转指 `balance.md`」），**`adventure-event/_index.md` 由 blocked 升 ready**（`:44` 同批关闭），**`future-event-service.md` 由 partial 升 ready**（`:545` 两条卡点倒下）。共同原因是 `ADR-0172`（`BaseTypeWeightsData` 五格初值与三条校验）与 `ADR-0173`（`Practice : Standard = 1 : 1` 是内容编排口径、管线不掷 tier）落表（`balance.md:455` / `:469`–`:472` / `:211`）。
- **✅ `enemies/_index.md` 由 partial 升 ready**（`:228` 明写「无」）：`ADR-0171` 判定「敌人各等级道念产出缩放曲线」为落笔残留并整条删除（全库检索零命中），`LineSlot` 空枚举的处置已成文（`:136`：无成员期间 `Lines` 对任何条目只能是空数组）⇒ 不构成排除面，不因它推迟整份。
- **✅ `game-progression.md` 待决由 4 条减为 1 条**（只剩选择区排布与滑动手感，归 `ux/`）：`ADR-0163` 把 blind / ante 整条关闭并代之以「难度与数值缩放的分格轴」整节（`:184`–`:204`：两条轴 + 十一条曲线登记 + 「第三条轴须先立 ADR」）· `ADR-0175` / `ADR-0176` 的失败容错 N = 2 已写进验收项（`:56`）。**全库「连形态都未陈述」这一档现已清空。**
- **✅ `account-info.md` 由 partial 升 ready**（`:56` 待决明写「无」；跨边界由对侧 `contracts/compliance.md` 的「不下发」清单正面闭合 ⇒ 字段表不因合规增行）。上一版判 partial 的理由「不构成独立 FR 面」属编批约束、不是三条判据之一，本次按判据归位为 ready、编批约束改在 derive 顺序里表达。
- **✅ 另有多条结构面收口**：`plot-manager.md` 的「是否有第三项隐藏属性」由 `ADR-0168` **永久关闭**（定稿道心 / 煞气两项 + 准入四问 + 不留占位枚举成员）· `life-cycle-service.md` 新进切片三条（`ADR-0162` 主动放弃角色 · `ADR-0165` 轮回出口三层处置与 `chapterStartSnapshot` · `ADR-0166` 痕迹跨篇章只追加，且明写不新增存档点、不 bump schema、后端零配合）· `combat-service.md:591` 的奖励候选池已追平为四族混合（`ADR-0169`）· `account-service.md:348` 由「仅剩多设备并发」收紧为「**本文档当前无待决项**」（对侧 `contracts/auth.md` §4a 答结）· `character-profile/_index.md` 卡点减为一条 · `player-power/_index.md` 的失去能力四支频次预算已配平（`ADR-0161`，持久三支合计 ≈1.0 次 / 轮回、`IgnoresProtection` 移出上层分子并降为 ≈3%）· `exchange/` 两份互相打架的待决区**已收口**（两侧现逐字同构）。

**仍在的承重卡点（一条 —— 由两条减为一条）：**

- **🔴 战斗内容本体整体为空**（卡牌条目 / 起始卡组 / 敌人条目目录 / 遭遇编排）。机制骨架与数据形态两侧均已闭合，缺的是**条目**——属内容阶段，不是设计缺口。连带两项量纲：**卡牌产 / 削道念的基准**（切入点是 starter deck）与**战后奖励池 / 商店库存的 `RarityTier` 分布权重**。二者同时卡住 `scoring.md:93`/`:94` · `combat/_index.md:202`/`:203` · `balance.md:916`/`:917` · `deck/_index.md:275` · `exchange/` 的定价复核 —— **一次专场可同批打掉五处，是全库当前投入产出比最高的一次裁决。**

**次承重（非结构、但反向卡住依赖闭合）：**

- **🟠 回寿法宝的总量护栏在内容编排面三口径全空**（`item/_index.md:152`：出现频率 / 库存深度 / 定价）。它是寿元这条压力线的**唯一剩余数量闸**，并反向卡住 `life-span.md` 的依赖闭合（该文件回复侧验收断言不得先于它落案）。其解锁前置是「古宝种类目录 + 次数补充机制 + 可购价格 / 库存权重」那一次 handoff。

**台账 / 投影缺口（不阻塞 derive，但会误导落笔；权威在主题文档，编辑归用户 / `/analyze-new-ideas`）：**

- **🟠 `## 决策(-> ADR)` 回链缺口 —— 第四次报告，本次点名 14 处以上。** 整节为空：`balance.md:908`（正文实引 11 份 ADR）· `future-event-service.md:539`（八份以本文件为落点的 ADR 零回链）· `currency.md:37` · `item/_index.md:144` · `game-setting.md:112` · `player-power/_index.md:121` · `player-item/` 两份 · `achievement/` 两份 · **`ux/` 四份主题文档全部整节为空**（`combat-ux.md:134` · `screen-flow.md:378` · `error-and-blocking-ux.md:378` · `onboarding.md:18`，而台账侧把 `ADR-0142`/`0143`/`0147`/`0148`/`0150`/`0151`/`0162`/`0170`/`0181` 成批登记落点为这几份）。收录不全：`architecture.md:691`（缺 `ADR-0159`/`0167`/`0168`）· `plot-manager.md:565` · `life-cycle-service.md:379` · `combat-service.md:665` · `game-progression.md:211` · `monetization.md:165`（缺 `ADR-0178`~`0181`）· `adventure-event/common-properties.md:403`（只两条）· `enemies/_index.md`（缺 `ADR-0171`）· `animations/_index.md`（对 `ADR-0170` 零回链）。**结论不变：`decisions/_index.md` 的「落点」列与文档侧该小节仍不是双向满射，两侧只能靠台账单向导航。**
- **🟠 落笔前的零决策修正（本次登记 12 处，均为陈旧副本 / 悬空指针，无一需要裁决）：**
  ① **`balance.md:823`** 写战后奖励池「族维度含卡牌 / 道具 / 功法**三类**」，同文件 `:917` 与 `ADR-0169`、`combat-service.md:591` 均为**四类**（`Practice` 档不产神通）—— **以直读裁决为准：`:823` 是陈旧副本**（详见文末）。
  ② `enemies/common-properties.md:26` 仍写 `IgnoresProtection` ≈5%，权威三处（`balance.md:774` · `player-power/_index.md:102`/`:107`）均为 ≈3%。
  ③ `adventure-event/common-properties.md:85` · ④ `terminology.md:91` · ⑤ `ux/combat-ux.md:23` 三处仍留旧篇章时长 30–40 / 35–45 / 45–55，权威 `balance.md:118`–`:120` 为 45–55 / 50–60 / 60–70（`ADR-0164`）。
  ⑥ `content-service.md:490` 的「ADR 候选，待 `/write-adr` 立档」应改为回链已 Accepted 的 `ADR-0182`。
  ⑦ `account-service.md:241` 的 `Source:` 行漏列 `handoffs/2026-09-02-platform-keystore-upgrade-triggers.md`。
  ⑧ `combat-service.md` 有 3 处（`:14` / `:20` / `:168`）把回合数写成无限定的「定长 10 回合」，权威是 `EncounterSpec.TurnLimit`（8 / 10 / 12，`:11` / `:38` / `:437` 均写对）。
  ⑨ `life-cycle-service.md:23` 的「阈值曲线与产出分布未定，见待决问题」是**悬空指针**（本文件待决三条无一涉及 `experiencePoint`），实际权威在 `balance.md`。
  ⑩ `program-overview.md` §2 职责矩阵漏 `StoreChannelManager`（sync 行）与 `CodexManager` / `GrantPoolManager`（profile 行），与 `services/_index.md:47`–`:55`、`system-overview.md:79` 打架，权威在前者。
  ⑪ `player-item/_index.md:28` 与 `monetization.md:32` 仍沿用已被 `ADR-0161` 废除的「1% 分子」口径。
  ⑫ `power/common-properties.md` 缺 `## 意图` / `## 决策(-> ADR)` / `## 待决问题` 三个标准小节（同族 `item/common-properties.md` 齐全）——内容层面已闭合，补三个小节即可无保留 ready；在此之前 derive worker 无法从该文件自证「待决为空」。
- **🟡 低信号（口径本身无处成文，不判为失真）：** `terminology.md` 仍零命中 `CycleEndScreen` / `ChapterEndScreen` / `FinaleDiff`；`codex/common-properties.md` 的「无待决」是由缺节推得而非明写。

**跨边界闭合（强制检查项）：本次全线闭合，零收窄、零 blocked。**

- `sync-service.md` 报文字段与序列化形态 → 对侧 `contracts/envelope.md`；两层 Profile 字段面 / 回声校验 / 白名单 → `contracts/profile-sync.md` §5 系列；`profile-schema-versions.md` 的 v1 已登入后端 `operations/version-matrix.md`：**三向皆闭合，本库零复述。**
- **购买段（本次新增依赖面，首次核）** → `contracts/purchase.md` §1 三端点 · §3 verify 与判别式 · §3a 逐渠道 receipt 与 `receiptId` 取值 · §3b 下单 · §4 收据状态幂等读 · §7 幂等窗口永久保留 · 四条 `purchase.*` 错误码：**闭合**。对侧残留两问（冷存归档形态 · 对账阈值 N）归运维，明写不回头改契约 ⇒ 不构成客户端排除面。
- `account-service.md` 合规六端点与错误码 → `contracts/compliance.md` §10 / §11：**闭合**；多设备并发裁决 → `contracts/auth.md` §4a：**闭合**，客户端 `:344` 已就地改写为回链。09-06 记的对侧措辞不一致（`envelope.md` §6 四条 `compliance.*` 的「客户端处置」列）**已改为回链客户端库，该保留项取消。**
- `content-service.md` 的 manifest / blob / flags → `contracts/content-manifest.md`：**闭合**。
- **两库 `cross-boundary.md` 的「待承接」双双为空**，无一侧已定案而另一侧零承载的情形。

### 判定表

| 文档 | 判定 | 卡点 / 就绪切片 |
|------|------|------------------|
| `systems/common-properties.md` | **ready** | `## 待决问题` 明写「（无）」（`:351`）。全库地基、derive 第 1 位、零前置。整面 = 稳定 `Id` 与展示字段三层切分 · 物化模型 · `LocalizedText` · `ContentEnabled` + `AllEnabled()` 双名与编译闸 · `RarityTier` 五档 · `Artwork` 可空 + 占位回落 · `SourceCode` + `Source` **十值** + 合法子集表 · 两级 seeded RNG（轮回级四子流 + 账号级 SplitMix64，8 组测试向量在对侧 `contracts/profile-sync.md` §6a）· API 契约三形态 / 三失败语义 · `deviceId` 落点 |
| `systems/architecture.md` | **ready** | `:740` 明写「当前无未决项」，架构体检 8 处缺口全闭合（`:721`）。**新进承重切片：`ProfileChangeSpec.AbilityStatusChanges` 列**（`:355`，按 `(Kind, Scope, AbilityId)` 绝对置值）与它相对 `AbilityElements` 的五面差异及「恒不走 modifier pipeline」的理由（`:548`）。其余 = 三层切分 · 七服务边界与 autoload 注册顺序 · EventBus 总则 · 八条 API 契约总则 · 边界服务双实现骨架 · `OpError` 十值（`:134`）· `ElementSpec` / `ModifierKey` / 钳制表 · `CostKey`/`StatKey` 成员名构成契约只可追加（`:512`）· 三级纪律判据。**须与 `viewmodel.md` 同批** |
| `systems/viewmodel.md` | **ready** | `:80` 明写「当前无未决项」。整面 = 展示层第三层结构契约（呈现期对象 · 不落存档 / 不进云端负载 · 单向依赖且服务 API 面永不返回 ViewModel · 组装源三件套 · 三条重组装触发面 · 永不渲染清单 · `Artwork` 两级回落）。**须与 `architecture.md` 同批** |
| `systems/services/_index.md` | **ready**（**由 blocked 升级**） | `:80` 明写「各服务的残留待决项见其各自文档；API 契约本身已定案」。整面 = 五级层级词 · 拆分轴 · 七服务 × manager 清单（**已含 `StoreChannelManager`** `:50` 与 `CodexManager` / `GrantPoolManager` `:51`）· `profile-schema-versions.md` 的台账例外说明（`:64`）。**它承载的权威在 `architecture.md` 与各服务文档 ⇒ 随第 3 步同批兑现、不另产 FR** |
| `systems/services/account-service.md` | **ready**（**排除面本次再收紧一格**） | `:348` 明写「**本文档当前无待决项**」——09-06 记的多设备并发那条已被对侧 `contracts/auth.md` §4a 答结并就地划掉。整面 = 会话生命周期 + 四个账号方法 + refresh 失败拆两条路径 + `AccountInfo` 五字段 + `deviceId` 与 refresh token 落点 + 启动期静默续期 + 平台密钥库四端能力矩阵与五条升级触发（`:84`，`ADR-0160`）+ `ComplianceManager` 四域切分与「不做任何判定」纪律（`:246`）+ 实名表单两条输入侧纪律（`:238`）+ 合规拦截路径与失败映射逐 `code` 表。**`account-info.md` 随本份同批兑现** |
| `systems/services/sync-service.md` | **ready**（切片显著扩大） | `:404` 仅剩 `pushId` 后端记忆窗口，明标「属后端侧参数、客户端语义已定」。**新进承重切片 = 购买段两条腿**：`StoreChannelManager` + `IStoreChannel`（`:174`）· 第四个窄接口 `IPurchaseBackend`（`:191`）· 运行时探测不走 `#if`（`:186`）· `UnavailableStoreChannel` 兜底与 `DebugStoreChannel` / `OfflinePurchaseBackend` 同 `#if DEBUG` 区（`:205`）· 购后强制 pull 与「购后 pull 失败阻塞主菜单」（`:144`）。原有整面（存档点 ↔ push 解耦 + 5 秒防抖 + 立即 flush 清单 + `user://cache/` 原子写 + schema 版本化与两种清晰拒绝 + `CharacterProfile` 级 diff + `revision` CAS / `pushId` 幂等 + 回声校验 + 断线降级与退避阶梯 + JSON camelCase 单点命名策略）不变。**须与 `profile-schema-versions.md` 同批** |
| `systems/services/future-event-service.md` | **ready**（**本次由 partial 升级**） | `:545` 已改写为「（无。五类配比与 `combatTier` 配比见 `systems/balance.md`；管线与公式零改动。）」——两条卡点由 `ADR-0172` / `ADR-0173` 关闭且**下游依赖已实际落笔**（`balance.md:455`/`:469`/`:211`）。整面 = eventOptions 十步管线 + 候选短缺三道闸 + `EventOutcomeSpec` 字段面 + 置换 / 禁用候选物化时掷定 + `Priority = 1` 抬升判据 + `HiddenStatGrant.Direction` 符号展开点 + Exchange 库存物化与 Explore 取池过滤 + 灵根准入叠入取池链 + `FinaleDiff` 旁路 + 遭遇参数五格代入（`:210`/`:225`）+ `TryGetPlotSegment` 只读转发 + 「本服务纯本地、全部方法形态 A」。**唯一残留：`:296` 的 1 项 `[采纳推荐 — 待复核]`**（按现值写、标待复核，不外溢）。**须与 `plot-manager.md` 同批** |
| `systems/character-profile/currency.md` | **ready** | `:43` 明写「无」。整面 = 双币轮回级定位与取值域两行 `ResourceElements` · 跨篇章结转、篇章边界零货币动作 · 灵石获取口径 = 战斗 · 仙玉唯一主动获取 + 四条否定边界 · 花销唯一出口 = 高阶 Exchange · 两币完全不可兑换、售出同币回收因而不产生事实汇率 · 两个修正列恒 `null`。**derive 时不取任何绝对数字**（权威在 `balance.md`） |
| `systems/character-profile/mana.md` | **ready** | `:62` 明写「无」，两项数值已转记 `balance.md`。整面 = `manaLimit` / `currentMana` 字段面 · 炼气基线 5/5 不结转 · 每回合恢复三步节拍 · 上限两条来源走 `CostKey.ManaLimit` 增量语义 · 不设下界护栏 / 死牌转化 · 下降唯一承载点 = 闭关自选风险档 · `currentMana` 落 `activeCombat`。**derive 时排除「卡牌费用曲线是否随境界上移」的验收断言** |
| `systems/character-profile/item/common-properties.md` | **ready**（**本次由 blocked 直升两档**） | `:30` 待决明写「无」。自有面虽窄但闭合 = `SourceCode` 落持有条目 / 本层四值 + 读档兜底 `Unknown` / 本层无规则消费点（`:10`–`:13`）· **`Status` 与「拥有 / 失去」正交、拥有 = `magicPack` 成员资格、本轮回禁用是第三维、生效 = 三条与门**（`:15`–`:18`）· 关掉的法宝仍占储物袋位、`Charges` 不变（`:19`）· **写入经 `AbilityStatusChanges` / `SetAbilityStatus`**（`:20`）。**须与 `profile-service.md` 同批**（自有 FR 面很窄，作该批附属条目而非独立 FR） |
| `systems/player-profile/account-info.md` | **ready**（**本次由 partial 升级**） | `:56` 待决明写「无」。跨边界正面闭合：`:44` 明写「合规域一格也不落 `AccountInfo`」，对侧 `contracts/compliance.md` 的「不下发」清单可逐句对表 ⇒ 字段表不因合规增行。整面 = 五格字段（`AccountId` / `AccountSeed` hex16 / `CreatedAtUtc` / `Identities` / `Nickname`）+ `BoundIdentity` record + 三条方向纪律 + 首版不做头像 + 不持有账号状态 + 字段增删即 bump 与两路迁移 + `/accountInfo/accountSeed` 透明路径。**随 `account-service` / 登录切片同批，不另产 FR** |
| `systems/player-profile/game-setting.md` | **ready** | 唯一登记项是三条音量轨「待实测初值」（`:118`），取值已存在于 `profile-service.md` 的 `SettingFields` 表 ⇒ 属校准而非缺口。整面 = `ADR-0072` 切分判据 + 两侧对照表 + 账号级四项 + 设备本地 `device-settings.json` 一项 + `SettingChanges` 通道与 `SettingFields` 配表 + `PushPolicy` + 启动期 locale 归一 + 首批不收候选与解除条件 |
| `systems/player-profile/codex/common-properties.md` | **ready** | 无 `## 待决问题` 小节、无待复核项，存档面整份闭合。整面 = `CodexEntry` 单格 `Id` + `CodexKind` 七值 / `CodexUnlock` + `CodexElements` 零 `Op` + 四行失败语义 + 七本解锁触发（全部搭既有提交、零新增提交点）+ 连锁解锁「1 + N」+ `eventEnd` 入账射程 + `CodexFlavor` + 词条深度分野表。**derive 时排除 `LocationCodex` 的词条正文形态**。低信号：三个标准小节缺，「无待决」由缺节推得 |
| `systems/player-profile/codex/enemy-codex.md` | **ready** | 全文无 `## 待决问题` 小节，`## 决策(-> ADR)` 八条全为结论式。整面 = 五项词条写作规格 + 关键卡 2–3 张由 `KeyCardIds` 显式标注、正文绝不写等级 + 「关键卡牌必在样本卡组内」加载期检查 + 词条②指向真实功法条目的悬空校验 + 慷慨度与三级退让阶梯 + 呈现时刻 = 战斗前确认页 + 图鉴静态性三句 + 资产字段。**derive 时排除 `KeyCardIds` 具体取值与退让阶梯的上调动作** |
| `systems/player-profile/codex/technique-codex.md` | **ready** | `:70` 待决明写「暂无」。整面 = 词条构成五行（属性行由呈现层现算、零新增存档字段、且不表达「当前角色能否修」）+ **词条不列该功法的卡牌清单**（承重反查护栏）+ 两条解锁路径皆幂等、升阶不产生新条目、弃置不删条目 + `Pool == Enemy` 照常收录 + 完成度分母取 `AllIncludingDisabled()` + 存档面纯加法空迁移。**derive 时排除功法条目本身与完成度分母绝对值** |
| `systems/player-profile/player-power/common-properties.md` | **ready**（**本次由 partial 升级**） | 两条卡点同时倒下：`:53` 待决明写「无」；`:14`–`:16` 给出存档表达（`bool Status` 默认 `true`、老档缺格补 `true`）+ **写入通道 `AbilityStatusChanges` / `SetAbilityStatus`** + 第三维落 `CharacterProfile.disabledAbility`。整面 = capability flag + modifier pipeline 声明侧（扁平枚举不分区 · 正向三词表 `{Reveal, Show, Unlock}` + 禁否定式 · 一 flag ↔ 一处消费点 · `CapabilitiesChanged` 空负载广播 · 叠加 = 幂等集合并、冲突结构上关死 · 注册面两层共用 · `ModifierOp {Add, Scale}` 万分比整数 · `ApplyModifier` 单点入口 + 两条钳制 · opt-in 白名单与四类豁免）。**须与 `profile-service.md` 同批**（合并算法权威在服务侧，分批必出两份互不相认的叠加规则） |
| `systems/enemies/_index.md` | **ready**（**本次由 partial 升级**） | `:228` 待决明写「无」。`ADR-0171` 判「敌人各等级道念产出缩放曲线」为落笔残留并整条删除（全库零命中）；`LineSlot` 空枚举的处置已成文（`:136`：无成员期间 `Lines` 恒为空数组）⇒ 不构成排除面。整面 = `EnemyData` 全字段面与加载期校验 + `TechniqueRef` 构筑面 + 游离散牌 + `KeyCardIds` + `EncounterScopes` / `ChapterScope` / `PoolScope` 与六条取池校验 + 展开产物不写回条目 + item / power 持有列表两条硬准入 + 编排护栏（赋级带 `±2` · 层数偏离 ≤ ±1 档）+ AI 兜底 1-ply 加权效用与确定性 argmax + `AiTerm` 十项 + 三条结构性上界 + `Lines` / `EnemyLine` 稀疏覆写形态。**`Lines` 恒为空数组落笔**；**须与 `enemies/common-properties.md` 同批** |
| `systems/adventure-event/_index.md` | **ready**（**本次由 blocked 升级**） | `:44` 待决改写为「（无。五类配比与 `combatTier` 配比的权威见 `balance.md` 的 `BaseTypeWeights`。）」。`## 意图` 为真实内容（五类分类法表 + 分类粒度判据 + 开局强制构筑归 Research，`:9`–`:31`）。**但切片全部由 `common-properties.md` 与 `future-event-service.md` 承载 ⇒ 不另产 FR，只作分类法参照**（避免撞车） |
| `systems/adventure-event/common-properties.md` | **ready**（**本次由 partial 升级**） | `:409` 待决改写为「（无 + 转指 `balance.md`）」。整面 = `EventOption` 物化字段面 + `OutcomeRule` 与 `HiddenStatGrants` + 「产出即定稿、不得回查模板重算」+ 事件收口的事务语义 + `PastEventEntry` 与 `AppliedChange` + 寿元回复只走 outcome 侧 + `lifeSpanCost` 形态定值三变异位（`:88`–`:96`）+ Travel 双禁令（不得回寿且不得产两种货币）+ `AddLooseCard` 取池链 + 九条加载期校验。**它是后续全部事件类型 FR 的共同类型地基，derive 顺序应最靠前。** 落笔前须清 `:85` 的旧时长副本（零决策修正） |
| `ux/onboarding.md` | **ready** | `:23` 明写「当前无未决项」。整面 = 强制登录无游客 + 首版只呈现已实现渠道（不遍历 `LoginChannel`）+ 两步握手 UI（验证码框 + 常驻倒计时重发键 + 过期重取，无 hover 提示）+ 绑定 / 解绑不落登录屏 + 首玩篇章门禁 + 角色选择是唯一一次起手选择、首玩局标注推荐项、**明确不做「首局跳过选择」特判** + 解锁 = 「有可挑战角色」的动态状态。**与 `screen-flow.md` 同批** |
| `systems/services/profile-service.md` | **partial** | **09-06 那条最重的卡点已倒**：`:446` 明写 `PlayerPower` / `PlayerItem` 的持有条目形态（含 `Status` × 拥有 / 失去的存档编码）**不在本条待决范围内**、写入通道见 `AbilityStatusChanges`。新进切片 = 该列定义（`:37`）+ 三条校验行（`:116`–`:118`）+ 「在此之前这一格没有任何写入通道」的承重段（`:238`）+ 门面 `SetAbilityStatus`（`:387`）+ 四类覆盖同款判据（`:413`）+ 三维生效与门（`:360`）。其余整面 = `ProfileManager` 唯一写入面 + `TryApply` 全量校验 / 全有或全无 + `Project` 只读投影 + `ProfileChangeSpec` 逐列与逐列失败语义（**列数不写死**）+ `CostKey` 15 成员 + 产出侧 `LifeSpan < 0` 整批拒绝 + 门面级只读 `Holds(...)` + `CapabilityManager` 整面 + modifier 合并（顺序无关 ⇒ 不设优先级）+ CodexManager / GrantPoolManager。卡于（四条，全属元进程 / 成就侧，`:443`–`:449`）：`AchievementManager` 采集面 · 成就两档奖励内容 · `Achievement` 条目 schema · `PlayerPower` 平衡边界。**`ReportProgress(AchievementSignal)` 那一行按 `:397` 的「暂定形态、非定案」原样标注，不得据它写采集面验收**（经直读，09-07 报的「与待决抵触」不成立，两侧互相回链、口径一致） |
| `systems/services/profile-schema-versions.md` | **partial** | 两条待决均自陈为**承接 / 排期**而非设计缺口（`:125`–`:126`）：`PlayerProfile.achievement` 尚未进 v1 清单（前置 = `Achievement` schema，答定后补入仍属 v1、不 bump；`:67` 明写「写一个推测形状比空着更危险」）· golden 快照 `profile-shape-v1.json` 待建、卡在 `game-feature-branch/` 尚无 `.csproj`，`:126` **明写「不阻塞任何 derive」**。就绪切片 = v1 首发形状逐条清单（`:22`，逐条带权威回链）+ 形态纪律六条（`:71`/`:90`：本表不写任何计数）+ `ProfileShapeCheck` 规格整套（`:112`–`:117`）。**须与 `sync-service.md` 同批** |
| `systems/services/content-service.md` | **partial** | 唯一待决 = **disabled 条目被存档引用时的 UX**（`:495`，归 `ux/`）⇒ derive 时该分支须留空、不写 UI，不得按「完全静默照常可用」落笔。就绪切片 = 三层覆盖来源 + 启动期 manifest 比对 + blob 内容寻址 + ES256 detached 签名与 `keyId` 轮换 + 文件级事务 + 增量下载 + overlay 合并与「以 overlay 为准」+ 合并后全量校验 + `ContentEnabled` 不预埋占位 Id + flags 第三层与落盘纪律 + `contentVersion` 记两个 + 断网降级到随包基线 + 语言覆盖率审计 + overlay 剧本例外双闸 + 打包工具两项产出 + 单例内容注册与条数校验 + **基线 `Id` 超集不变式与退役三分**（正文已成文；`:490` 的「ADR 候选」行应改回链 `ADR-0182`） |
| `systems/services/combat-service.md` | **partial** | **09-06 点名的台账卡点已追平**：`:591` 现写四类混合候选族且 `PowerData` 只在 `Standard` / `Finale` 进池、`Practice` 整族排除（`ADR-0169`），并回链「口径权威在 `power/_index.md`」。就绪切片 = 战斗机制骨架整条（栈与 LIFO + 战报 · `ActionResult` / `CombatFeedEntry` 六类与因果树 · 疲劳为一等栈条目 + `TurnLimit` · 三步回合 · 抽牌堆不重洗、抽空每张扣 1 道念 · 敌人意图整条移除 · AI 两层与 `CombatSnapshot` 按 `ViewerSide` 投影 · **效果流水线五阶段**与 `Evaluate` 纯 / `Apply` 唯一写点 · `StackEntryKind` 五值与栈条目 `itemId` 双向不变式 · `ActiveCombat` 存档块 · 决策点 D0–D7 · `RewardPickState` 与逐项领取 · 可选奖励固定 3 项预抽落存档 · 战斗前确认页 · counters 键空间 · 挂起态恢复与取消 · 单一 `combat` 子流）。卡于（两条，`:693`）：**战斗内容本体整体为空** · Finale 奖励加厚幅度待统计校准。**落笔一律取 `EncounterSpec.TurnLimit`，不得把 10 写成常量**（`:14`/`:20`/`:168` 三处措辞失真） |
| `systems/services/plot-manager.md` | **partial**（**结构面本次收口**） | **「是否有第三项隐藏属性」由 `ADR-0168` 永久关闭**：`:80` 定稿道心 / 煞气两项 + `:85`/`:89` 准入四问与举证责任 + 不留占位枚举成员、不预留 band 字段。就绪切片其余 = `PlotArcData` / `PlotNodeData` + `PlotModulation` 六字段与悬空校验 + 多 `Active` arc 合并算子 + `EncounterTighten` 五格增量的方向约束与 `Finale` 整档豁免 + 档位表 / 阈值 / 回滞 / 跨档叙事 + 档号方向承重定义（`:74`）+ 取值域与起始值（`:78`）+ 隐藏属性不进 `selectCost`（`:56`）+ 影响战斗靠拧参数不加判定条件（`:54`）+ 战斗层零耦合 + 剧本树不分包 + 分支落点与 `BranchLabel` 焊死 + 门面投影 1 → 2。卡于（两条，均属内容编排 / 标定，`:578`）：隐藏属性增减触发的逐类编排 · `HiddenStatGrade` 三个映射值。**须与 `future-event-service.md` 同批** |
| `systems/services/life-cycle-service.md` | **partial**（切片显著扩大） | **新进切片三条**：`ADR-0162` 主动放弃角色（只对 `ongoing`、`DefeatCharacter` 扩为 `(reason, characterId)`）· `ADR-0165` 轮回出口三层处置与 `chapterStartSnapshot`（`:285`，含「`chapterRetry` 落第三类否则重试上限静默失效」`:288` · `defeated` 墓碑形态 `:294` · snapshot 只回收 ch1 墓碑 `:296` · 单记录跨三篇章 `:300`–`:304` · **不新增存档点、不 bump schema、后端零配合** `:324`）· `ADR-0166` 痕迹跨篇章只追加。其余整面 = 轮回骨架 + 两层持有模型 + 三分类法 + 寿元递减预算与归 0 终态 + `DefeatReason` 三值与 Finale 显式旁路 + 「账号级残卷累加先于角色终结提交」+ `AdvanceEventAsync` 固定结算流程与取消语义 + 非战斗四类的事件内决策点 + `SeedManager` 四子流与 `AttachRngState` 三步 + 残卷掷骰走账号级 SplitMix64 + 存档点永不回退 + 决策点存档 + 批次层即时提交 + 两处结束摘要组装。卡于：元进程持久化范围（`Achievement` schema · `PlayerPower` 触发与平衡边界）· 隐藏属性增减触发（转指 `plot-manager.md`）。`:23` 的 `experiencePoint` 指针悬空，应改指 `balance.md` |
| `systems/character-profile/_index.md` | **partial**（近 ready，卡点减为一条） | 就绪切片 = `CharacterData` 六格 + **十一条加载期校验**（`:96`–`:108`，含灵根四条）+ `CharacterProfile` 完整字段表（`:114` 起）+ `Status` 具名子类子表（`:145`）+ `Affinities` 唯一规则后果 = 硬性修习准入且不碰其余旋钮（`:94`）+ 首批五角色单灵根是编排口径非字段约束（`:91`）+ 角色池全池指定 / 规模 5 / 不做账号级解锁 + 读档校验 + 绑定功法层数恒 1、`CharacterData` 不加字段（`:28`）。**「隐藏属性是否有第三项」已随 `ADR-0168` 删除。** 卡于唯一一条：全池指定下角色强度是否塌缩为单一最优（`:387`，明写待实测）⇒ 排除该条验收断言，其余整面可写。**须与 `deck/_index.md` 同批且不得倒序** |
| `systems/character-profile/life-span.md` | **partial** | 就绪切片 = 单值形态与三条不变式 + 与 mana 的非对称对照 + 预算 1000 / +1000 / +3000 / +5000 与跨篇章结转（`:20`）+ 扣减恰两来源与「同一次收口事务落到同一个值」+ 内容侧恒不得写负向 `LifeSpan`（`:25`）+ **战斗内不读写寿元**这条资源纪律（`:26`）+ 回复三通道共用一条 `ChangeElement(CostKey.LifeSpan, +n)` + 归 0 → `LifeSpanExhausted` + 明文常驻恒精确、< 10% 转红 + `PastEventEntry.LifeSpanAfter` + `lifeSpanCost = round(t × λ)`（λ 已重算为 15 / 17 / 44，`balance.md:146`）。卡于：`lossPerMomentum` ch2/ch3 仍是候选值 5 / 10（`:54`）· 回复三档绝对点数未定案（`:55`）· **依赖不闭合：回寿总量护栏挂在 `item/_index.md:152` 且三口径全空**。**derive 纪律：不单列 FR，与 combat-service / life-cycle-service 同批**；λ 的两个反推格子文档自陈不得作承重依据 ⇒ 不把 15/17/44 写进验收断言 |
| `systems/character-profile/deck/common-properties.md` | **partial**（排除面清零） | 就绪切片 = `EffectData` 抽象基类 + 一原语一 `[GlobalClass]` 子类树 + **八原语**参数表（`MoveCardEffect` 含 `Side`、`From`/`To` 恒同侧、跨方转移结构上不可表达、日后加 `ToSide` 是纯加法，`:167`）+ **加载期校验 21 条**（第 21 条 `:277`）+ `StaticModifierData`（`ModifierTarget` 五项冻结 · 万分比整数 · 同层求和 → 只乘一次 → 只取整一次）+ `AbilityData` 两格 XOR 与启动代价面（`:374`）+ `TriggerConditionData` / `TimingIds` 十时点 + `SubjectKind` 相容校验 + `EffectCondition` 三谓词 AND + `SideConstraint` + 关键字哨兵与恰好一层展开 + `CardData` 字段收口 + 目标 / 作用域分建模与 `EntryFilter` + 按视角前提机械化 + 存档面零新增字段。**它是卡牌 / 神通 / 法宝 / 法则 / 古宝五类内容的共同底座**，纯数据资源型切片、过三条闸。卡于（两条，全属内容）：starter deck（`:382`）· 抽 / 弃 / 洗数值（`:383`） |
| `systems/character-profile/deck/_index.md` | **partial** | 就绪切片 = 功法 = 卡组构筑单位 + 轻量 header + `CultivationTechniqueData` 两格 + `Pool` 必填 + **灵根修习准入整块与判定式**（`:119`–`:145`，空数组语义与代价明写 `:133`）+ 四处玩家侧取池点各叠一层 / 敌人侧不叠 / 升阶候选叠准入不叠 `Pool`（`:107`）+ 成员卡反建索引 + 五个 `DeckChangeOp` 与 `Pool == Enemy` 施加侧闸 + `CardType` 五分与封闭卡集 + 次类型体系 + 卡池三值必填 + 不重洗与疲劳 + 手牌上限 7 纯上界 + 抽牌堆插入位 + 「一切增益减益都是战场条目」。卡于（六条，全落内容 / 量纲，`:265`–`:275`）：多灵根强度对齐换算（引入第一个多灵根角色前必须先答）· 通用功法占比口径 · `MaxTier` / 每层替换幅度 · 关键字与次类型首批清单为空 · starter deck · **道念产 / 削量纲基准（承重）**。**不得先于 `character-profile/_index.md`** |
| `systems/character-profile/item/_index.md` | **partial** | 就绪切片 = 三层分工表（`:7`，**09-06 记的「占位结构」抬头已闭合**）+ `ItemData` 完整字段形态与两条校验（`:41`）+ 使用效果面按世界分两格、必填性由 `UsableScene` 驱动（`:53`）+ 战斗外三列白名单（`:60`）与符号 / pipeline 纪律（`:70`）+ 战斗外使用 = 即时提交非决策点（`:83`）+ 配额面两格分工与哨兵（`:91`）+ **I-1 ~ I-13 加载期校验**（`:101`–`:113`）+ 储物袋跨两层视图与随售通道、两档回收率的加载期硬校验 + 跨载体三行边界判据表的回链。卡于（三条）：**回寿法宝总量护栏三口径全空（承重，`:152`）** · 道具种类目录（`:150`）· 三条获取通道的分布与数量口径（`:151`）。**不得先于 `power/_index.md`**（边界判据表权威在那边） |
| `systems/character-profile/power/_index.md` | **partial**（卡点由三减为二） | 就绪切片 = `PowerData` 字段面 + **跨载体三行边界判据表与四条推论**（`:49`–`:56`，**本表是卡牌 / 道具 / 神通三者边界的唯一权威**）+ 获取通道四个 `Source` 已闭合（`:46`）+ 失去恰三形态、不开无同意永久剥夺、买得到卖不掉（`:47`–`:49`）+ 强度定性三条与 `baseMomentum` 比例刻度、不设合计总闸（`:57`）+ **内容编排口径表**（`:61`–`:69`：开放 `InitialGrant` / `CombatReward`（收窄 `Standard`·`Finale`）/ `ExchangePurchase`；`EventOutcome` 保留机制零条目；`Travel` / `Explore` 恒不产出；条目数下限 ≥ 5）+ 绑定神通不填 `ExclusiveSource` + 两条加载期校验且明确不做总量前置检查 + 三格至少一格非空 + 「纯战斗外条目不得挂触发式」+ 不得含 `LifeSpan` 产出 + `IsProtected` 与 `IgnoresProtection` 唯一后门 + 战斗内运行态走战场条目零结构增量。**`status` 编码已不在卡点之列。** 卡于（两条，均属内容 / 量纲，`:101`）：获取侧频次与分布 · 强度定量三格 |
| `systems/character-profile/power/common-properties.md` | **partial**（**本次由 blocked 升级**） | 承重卡点已消失：`:23` 完整写出 `bool Status`（默认 true）· 拥有 = `characterPower` 成员资格 · 本轮回禁用第三维 · **写入经 `AbilityStatusChanges` / `SetAbilityStatus`** · record 形态权威在 `player-profile/_index.md`。另有条目为内容资源走 `AllEnabled()`（`:7`）· 形状对标 PlayerPower 且两维正交（`:8`）· 生命周期轮回级 = 与 PlayerPower 的唯一本质分界（`:9`）· 写入不绕过唯一写入面（`:10`）· `SourceCode` 四行（`:11`–`:14`）。**未升 ready 的唯一理由 = 文件仍缺 `## 意图` / `## 决策(-> ADR)` / `## 待决问题` 三个标准小节**（全文 27 行）⇒ 判据①在字面上不成立、判据②无法自证。内容层面已闭合，补三个小节即可无保留 ready |
| `systems/player-profile/_index.md` | **partial**（排除面收窄） | 就绪切片 = 16 字段表整张（写入通道列无空洞）+ 规则层 / 统计层分层判据 + 三样不进 Profile 的排除项 + 三个具名子类 + 受回声校验约束的顶层键 + 残卷 Finale 绑定 + 子系统导航六条 + **四类持有条目 record 形态（`bool Status` 均为活字段，`:57`–`:65`）+ 老档缺格补 `true`（`:70`）+ 写入通道 `AbilityStatusChanges` / `SetAbilityStatus` 并明写「`StatusChanges` 不承载 `Status`」（`:71`）+ 四类一律开放开关（`:72`）** ⇒ 该维不再需要排除。卡于（一条两半，`:206`）：`achievement` 条目 schema 未定（依赖不闭合）· `PlayerPower` 平衡边界。**derive 时 `achievement` 顶层键只保留键位与写入通道**。**不得先于 `profile-service.md`** |
| `systems/player-profile/codex/_index.md` | **partial** | 就绪切片 = 七本清单 + 三层浏览（索引页 → 单本页 → 词条、七本共用一套排布，`:65`）+ 完成度只计已解锁顶点 + 共同形状八条 + 四条族级结论（账号级 / 不与成就奖励挂钩因而后端零配合 / 战斗中不可查 / 连锁解锁，`:77`–`:80`）+ 边缘顶点三式与呈现三态表、半径固定 1 跳。**剩余卡点仍恰一条**：`LocationCodex` 除连边外的词条深度未定，连带**单本页内容区如何画那张图**（缩放 / 平移 / 顶点布局）与词条载体（`:85`）⇒ **六本可写、`LocationCodex` 单本页整块排除**。注：七本表中五本仍 ⟨待播种⟩，不得假定各有专属文档 |
| `systems/player-profile/player-power/_index.md` | **partial**（卡点由四减为三） | **09-06 那条承重卡点已关闭**：`:100` 承重表述改「持久三支合计 ≈1 次 / 完整轮回」、百分比降为括号副本 · `:102` `IgnoresProtection` 移出上层合计并自持战斗分母（≈3%）· `:103`–`:112` 四支目标频次表逐格有数 · `:114` 旋钮全落内容侧、零字段零校验零 `CycleState` 状态位（`ADR-0161`，分母 114 与 `ADR-0164` 咬合）。另 `:14` 的 `status` 写入通道已成文。就绪切片其余 = 失去三形态 + 同池判据 `(CarrierKind, Scope)` 全同 + 同 `Rarity` + 排除已持有 + 自愿失去阶梯 + 残卷 Finale 绑定 + 授予池权重结构与初值 40/27/18/10/5 + 「一次轮回至多结算一次」的轮回级布尔位。卡于（三条）：平衡边界（是否影响 cycle seed / 计分公平，`:127`）· 第三条获取渠道未陈述 + 开关 UI 未细化（`:128`）· 条目目录为空（内容阶段）。注：战后奖励池权重**不是本文件的卡点**（法则的合法 `Source` 不含 `CombatReward`），它是转记 `balance.md` 的跨文档记账 |
| `systems/balance.md` | **partial**（切片持续扩大） | 就绪切片 = `lifeSpanCost` 21 格与 λ = 15 / 17 / 44（`:137`–`:146`）· 事件数 114 = 35/36/43（`:723`）· `D_c` = 79/247/391（`:289`）· `S(c)` = 10/23/39（`:397`）· 25 格定价外积与币种分布 · 两档回收率 40% / 15% · `BaseTypeWeights` 五格初值 0.35/0.13/0.13/0.32/0.07 与 `Practice : Standard = 1 : 1`（`:455`/`:469`/`:211`）· `FinaleDiff` 初值与下界 · `experiencePoint` 阈值全表与 `ExperienceGrade` 映射 · `lossPerMomentum` ch1 = 10 · `baseMomentum` 表 · 全局等级序 1–22 · 赋级带 `±2` · 起手 4 / 每回合抽 2 / 手牌上限 7 · `MaxConcurrentSideArcs` · `TravelFullFanoutChance` · `BatchSizeWeights` · `GrantPoolWeights` 40/27/18/10/5 · 取池余量三格（可先填 0 不阻塞落地）· 重试上限两行由 `HasPremiumBundle` 选行 · 敌人 AI 兜底权重向量 · `itemPowerRatio` 分层 · 道统残卷三张分档表 · `EncounterTighten` 十个界常量的结构 · 平衡资源经 `Content.Single<T>()` 取 · 不设兜底大表的三问判据。卡于：`## 待决问题` **仍 14 条但已换血**——**有取值待复核 8 条** · **形态已定零取值 5 条** · **口径缺口 1 条**（`:921` 胜侧 `rewardPerMomentum[experiencePoint]` 是否计入经验供给账，答定后须同批重算供给 / 需求比值与 N 反推台账）。**「连形态都无」这一档已清空。** 失真两处：`## 决策(-> ADR)` 整节空（`:908`）· `:823` 的三类 / 四类陈旧副本。**须与 `scoring.md` 同批** |
| `systems/game-progression.md` | **partial**（**待决由 4 条减为 1 条**） | 就绪切片 = 篇章 / 境界推进与检查点重试 · 存档点即结束点、途中死亡从章首重试 · 修行等级体系 · `experiencePoint` 阈值曲线 / 三章合计 / `ExperienceGrade` 四档 / 产出点覆盖率 ≈55% / 失败产出 = `ExperienceGrade` + `FailureRatio` · 「Finale 前必须升满、Finale 自身不供经验」的硬约束与 `FinaleDiff` 咬合（`:51`）· **新增整节「难度与数值缩放的分格轴」**（`:184`–`:204`：两条轴 + 十一条曲线登记表 + 「第三条轴须先立 ADR」，`ADR-0163`）· `:59` 的 Finale 前瞻 / 进度条否决理由（`ADR-0177`）· 篇章时长 45–55 / 50–60 / 60–70（`:19`）· 失败容错 N = 2 已入验收项（`:56`）· `eventCountLimit` 配额语义与剧本免疫 · Travel 路由 80/20。卡于唯一一条：选择区排布与滑动手感（`:217`，归 `ux/`） |
| `systems/scoring.md` | **partial**（规则面已全闭合，零变化） | 就绪切片 = **计分规则与全部转换公式**（一组纯函数 + 一份平衡资源，验收靠给定输入验算输出，不必等一场可运行的战斗）—— 道念 = 胜负判据 · 战斗过程中寿元不被读写 · 产削与下限 0 的每次结算截断（LIFO 因此影响结果）· 疲劳为第二条削减通道 · 胜侧两条支路 + 单价表 · 终止条件 = `EncounterSpec.TurnLimit` · `WinMargin` 在 `Finale` 退场 · `combatTier` 三档遭遇参数 · 战斗层不读写隐藏属性 · 负侧 = 道念差 × `lossPerMomentum`（ch1 = 10）。卡于（两条，`:93`/`:94`）：三档 `BaseReward` 其余 element 与 `RewardPoolId` 取值 · 卡牌产 / 削道念的量纲基准。**须与 `balance.md` 同批** |
| `systems/monetization.md` | **partial**（**排除面清零，购买段解锁**） | **上一版的「唤起内购整段排除」整条取消**：三渠道选型表（`:113`）· 三条否决（`:121`）· 前置条件表第 5 行「无可用渠道 → 入口不渲染」（`:105`）· 三个显式步骤（下单 / 验票 / `finish()`，`:110`–`:112`）· 失败处置五情形表 + `#requestId` 客服面（`:122`–`:136`）。就绪切片其余 = `PlayerEntitlement` 两字段与不变式 + `BundleGrantOrdinal` 无 `ResourceElements` 行也不是 `CostKey` 成员及两条否决论据（承重）+ 空池三道闸 + 购买入口三条前置与灰态 + 兑现事务（一次派生连抽 3 条 → 一次 `TryApply` → `Immediate` push，`ordinal = Redeemed + 1`）+ 五项付费面排除 + 三渠道纳入 MVP + 重试上限两档表由 `HasPremiumBundle` 选行 + 购买域失败收于 `OpError.Purchase` 一档。卡于（三条，`:172`–`:174`，其中两条已被决策覆盖）：**`K` 与 `GrantPoolMargin` 取值**（闸①断言阈值因此不可写死，**这是保留 partial 的唯一理由**）· 纯外观付费点做成什么（架构预留、首批不做已定）· 合规交互归后端（已裁决的排除）。跨边界已闭合 |
| `systems/enemies/common-properties.md` | **partial**（卡点收窄至一条） | 就绪切片 = 全部字段校验表（`:20`–`:31`）+ `Lines` 无成员期间恒空数组的处置（`:39`）。卡于唯一一条：`LineSlot` 成员清单（`:90`，待一次战斗 UX 专场）。**失真：`:26` 仍写 `IgnoresProtection` ≈5%，权威为 ≈3%。须与 `enemies/_index.md` 同批** |
| `systems/adventure-event/travel/_index.md` · `travel/common-properties.md` | **partial**（两份同批） | 就绪切片 = `LocationData` + 单份 `LocationMapData` 载体与恒启用 · 80/20 全局常量不可调制 · 目的地取自邻接集合、`DestinationLocationId` 物化时掷定且不得事后算 · 闸门形态（最高 `Priority = 1`、无 `IsMandatory` 字段）· 结算写入判据为 `DestinationLocationId != ""` · 痕迹侧记出发地 · 两条 `StatusAssignment` 由 life-cycle-service 组装 · 不计入 `eventCountLimit` 与剧本免疫 · **货币产出禁令与回寿禁令合为同一行校验**。定价结构约束已定（`> 0` 且为同章 `Exchange` 行的 1/3~1/2，`:60`），**`balance.md:144` 已给初值 6 / 7 / 18**（取代旧的 9/10/25）。卡于：**location 无法被 flags 秒关时的运营替代通道**（`:95`，明写「本次不预设形态」）⇒ **derive 不得为它臆造闸门**。`common-properties.md:30` 自有面为空、全文转指 |
| `systems/adventure-event/explore/_index.md` · `explore/common-properties.md` | **partial**（两份同批） | 就绪切片 = 揭示机制整套（`:110`–`:113`）—— 真身分布 = 条目池组成 × 既有加权抽取、三处数据类一律不加字段 · 取池期「真身须同样 enabled」+ 真身为 Exchange 时库存池前置须过 · 揭示 = `eventStart` 内一次 `with` 派生且 resolver 按真身选取 · 遮罩卡与其余选项完全同构 · 全屏覆盖层转场、不进屏幕栈、不给部分线索 · `IsRevealed` 只在物化侧 · 派生实例落 `activeEvent` 随后续第一个决策点写盘 · `RevealedEventId` 一格与四条加载期校验。`common-properties.md:29` 明写「无。专有字段清单闭合」。定价 Explore 行**已重算为 24 / 27 / 70**（`balance.md:143`，两份文档零硬编码副本 ⇒ 算术闭合维持）。卡于：真身占比 `5:3:2` 与转场 ≈1.2s 两个待实测初值（`:121`，归 `ux/` / 统计校准，不阻塞结构） |
| `systems/adventure-event/research/_index.md` · `research/common-properties.md` | **partial**（两份同批） | 就绪切片 = 构筑面板 = 复数决策槽 · **五类**操作闭合 · 产出面 = 卡组 + `manaLimit` · `manaLimit` 下降挂玩家自选风险档、风险档是唯一内部张力 · 候选物化时掷定走 `RngStream.Reward` · `AllowDecline` 默认 `true` · 不另收资源代价 · 产出禁令扩为「不给任何货币」· 回寿不作为 Research 操作 · 闭关与开局构筑两个取池点各叠 `Pool != Enemy` · 三道短缺闸 · `ResearchSlotSpec` / `ResearchSlot` / `ResearchCandidate` 形态 · 载体 `CostKey.ManaLimit` · `DeckElements` 承载 · 开局构筑 `lifeSpanCost = 0` 覆盖已登记在案。卡于：走火入魔风险档权重（只欠取值，`:93`）· **`MaxTier`**（决定 `ResearchCandidate.Amount` 取值域，权威在 `deck/_index.md` 且仍未定——**本片唯一未闭合的跨文档依赖**，`:94`）· 构筑面板竖屏呈现与风险档的非 hover-only 说明通道（归 `ux/`） |
| `systems/adventure-event/exchange/_index.md` · `exchange/common-properties.md` | **partial**（两份同批 · **打架已收口**） | **09-06 记的「两份待决区互相打架」经直读确认已收口**：`_index.md:170` 与 `common-properties.md:213` 现为同款两项且都写明「25 格定价、每格币种与两档回收率已有初值，见 `balance.md`」。就绪切片 = barter 支付侧二选一整段（`:165`：`Holds(...)` 前置拒绝 · `CanAfford` 一字不改 · `Source.ExchangeBarter` 只开 `Op == Remove` × `(Item, Character)` · 不持有则灰显且支付要求保持可见 · 不按持有面过滤呈现）· 售出两条通道与同币回收 · 交易逐笔即时提交 · 库存物化经 `RngStream.Shop` 掷定并落存档 · 五个商品族映射到既有仓储 · 定价表结构与两条折扣通道 · `ModifierKey.ShopPrice` 单点施加 · 刷新是店级动作恒以灵石计价 · 价签必须标明币种 · 三道短缺闸 · 加载期 / 运行期校验。卡于（两组数值格，**均不承重**）：刷新基价 / 递增量（首批一律填 0 = 关闭刷新）· 单事件槽位总数上界（建议 ≤ 8） |
| `ux/screen-flow.md` | **partial**（就绪切片本次再扩两处） | **新落笔两处**：① **主动弃置入口整段**（`:54`–`:64`，`ADR-0162`：篇章切换面上 `ongoing` 角色行的次级动作 + 就地二段确认 + 三条 `MENU_DISCARD_CONFIRM_DESC*` 键 + 无冷静期无撤销 + 中文用「放弃」）；② **非商店平台 Store 入口不渲染而非置灰**（`:117`，`ADR-0181`）。其余 = 登录屏 + 主菜单五入口导航骨架与 Store 三条呈现纪律 + 角色选择屏整段 + 揭示转场表（`:181`）+ 战斗前确认页（`:199`）+ `CycleEndScreen` 三变体（`:216`）+ `ChapterEndScreen` 三变体含 ch3 证书（`:252`）+ 剧本段进结算面板与分支全宽按钮（`:309`）+ 图鉴三层浏览（`:322`）+ Exchange 屏（`:353`）+ 同步指示两处 + 寿元显性化整条对位 + barter 呈现侧 + 储物袋全屏面板 + 玩家档案屏只读区 + 绑定 / 解绑二次确认 + Settings 屏 + 手势判据。卡于（四条，`:383`–`:386`）：寿元红字是否伴音效 / 震动 · 揭示转场真机校准 · 成就两档发放何种奖励（依赖 `content/` 零开张）· `PlayerPower` 获取 / 失去触发。**与 `onboarding.md` 同批** |
| `ux/error-and-blocking-ux.md` | **partial**（三条待决均不构成排除面） | 就绪切片 = 翻译键基建整条（自命名的 `FR-ux-translation-foundation` 五件事：`res://text/` 分区 CSV · `project.godot` 注册 + 默认 locale `zh` + `fallback = "zh"` · `ErrorText` / `TranslationAudit` · 键命名三条 + 禁令 · 三条审计与定死的调用顺序）+ `ERR_*` 由 `code` 机械变换、无第二张对照表 + `reasonKey` 二级键与前缀式反向审计 + 内容侧不烘焙可翻译文本 + 两层本地化 + `ErrorText.For` 三参与静默回落 + **灰态判据表六行（含 barter 两键走 `EVENT_` 分区，`:204`/`:212`）** + `BlockingNoticeScreen` 一屏三变体与合规七码逐条判过「一条也不进本表」（`:328`）+ 三档版本提示只呈现最高一档 + 诊断编号出口。卡于（三条，`:383`–`:385`，**均属内容充实，文档自陈不阻塞结构**）：`auto_translate_mode` 在 Godot 4.7 的默认行为待实测（两种结果下键形态 / 分区表 / 三条审计逐字相同）· 兜底文案与 `ERR_*` 逐条中文措辞 · `reasonKey` 逐条措辞 |
| `systems/player-profile/achievement/_index.md` · `achievement/common-properties.md` | **blocked** | 三条全开（`_index.md:35`–`:37`）：两档具体奖励条目清单 · 触发采集面（EventBus 被动订阅 vs 各服务主动上报）· 条目 schema 与进度模型（分组结构 · 加权进度的权重来源 · 触发条件表达 · 隐藏成就揭示时机）。`common-properties.md:17` 自陈「具体字段清单尚未设计」，已有的只有四条形状口径。「能给什么」与「怎么给」均已答结（法则 / 古宝、指定条目 + `ExclusiveSource == AchievementReward` 不进任何抽取池），缺的是目录本身，而它又依赖法则 / 古宝条目。**两份 mtime 分别为 08-17 / 08-16 ⇒ 09-06 / 09-07 两批确认零落点**；`ADR-0159`~`0182` 无一以成就族为落点 |
| `systems/player-profile/player-item/_index.md` · `player-item/common-properties.md` | **blocked** | 类型面归 `ItemData`（两层共用）、存档面归 `player-profile/_index.md` ⇒ **无独立 FR 面**；`_index.md:42` 三项仍未设计：古宝种类目录 · 次数如何补充 · 可购价格 / 库存权重。**注：`common-properties.md` 已于 09-07 改写**（`:23` 分列已定案面 = `SourceCode` 三值 + `Charges > 0` 硬约束 + 两格使用效果面 + `MaxUsesPerCombat` + 必填 `Rarity` + `ItemData` 不加 `Price` / `Purchasable`；`:24` 仍未设计三项）⇒ 上一版「09 月三批零落点」这句已失效，但落点未触及三项未设计项之任一，判定不动。**失真：`_index.md:28` 仍沿用已被 `ADR-0161` 废除的「1% 分子」口径** |
| `systems/adventure-event/combat/_index.md` · `combat/common-properties.md` | **blocked**（卡点由 5 减为 4） | `_index.md:202`–`:205` 四条全部指向战斗内容本体：卡牌产 / 削道念量纲 · `RewardPoolId` 取值与 `BaseReward` 其余 element 厚薄 · 叙事一致性写作口径 · 逐条目 `HiddenStatGrade` 编排。上一版第五条「敌人平衡缩放」已随 `ADR-0171` 删除；本轮新落笔一处（`:35`：`combatTier` 配比是内容编排口径、管线不掷 tier）。`common-properties.md` 全文 30 行、唯一待决整条转指 enemies ⇒ 两份的承载面仍全在 `combat-service.md` / `scoring.md` / `enemies/`，**无可独立成立的切片** |
| `ux/combat-ux.md` | **blocked** | 文档实质内容不少（呈现事件流三消费者 · 战报收起 / 展开同一份数据两视图 · 条目粒度 = 结算事件 · 渲染期才套翻译键 · 不落存档 · 固定预留 ≈ 屏高 6% · 结算节拍 0.35–0.5s / 敌人回合总时长 ≤ 4s · 持有物两层面板 · 埋伏只给计数 · 敌人等级用境界名），但**两条 `承重` 待决恰好落在唯一候选切片内部**：① 战报条目的文案体系（`:141`）② **竖屏分区整体是否过载、栈是否必须常驻值得重估**（`:144`，已排期专场，其结论会改写战报落位与固定预留）。加之另 7 条形态未定。**唯一可能独立成立的非视觉切片（呈现事件流契约，`:44`）其自然宿主是 `combat-service.md`，在此 derive 必撞车。** 失真：`:23` 仍留旧篇章时长 |
| 导航索引三份（`services/_index.md` 已单列为 ready）：`systems/_index.md` · `ux/_index.md` · `content/_index.md` | **blocked**（非 derive 对象） | 导航索引 / 层级词表 / 登记表，各自的待决项已下沉到对应主题文档。`systems/_index.md` 内容侧无失真（`:15` 已把 `game-progression` 职责写成「难度与数值缩放的分格轴」，与 `ADR-0163` 同步）；`ux/_index.md` 的四问判据表与下游两份一致但权威在下游 ⇒ 单独 derive 必重复；`content/_index.md` 持内容类型的 **scaffold** 就绪度（**18 行类型登记、开张列 0 个 ✓**，与 `content/` 实际零失真），与本小节的 derive 就绪度是两套闸，勿混 |
| `vision/pillars.md` · `vision/references.md` | **blocked**（非 derive 对象） | 北极星 / 参考登记，其余文档的挂靠前置。两份均无 `## 待决问题` 小节 ⇒ **挂靠侧零未决，判据③ 对全库成立**。`references.md:22` 月圆之夜条的「规避」仍待定，不承载任何客户端行为面 |
| `vision/scope.md` | **blocked**（非 derive 对象） | **两项仍未定且无处承载、已第十七天零推进**：① AI 生成资产的商用授权 / 版权立场（`art/_index.md:70` 指向本文件）② **初装包体预算**（`art/soundtracks/_index.md:37` 同样指向本文件）。机械核实：`grep '包体\|授权\|版权\|商用'` **零命中**，「美术资源策略」段（`:40`–`:44`）全文只讲 TBA 与挂点。已落笔项：iOS 部署下限 15（`:30`）· 篇章时长 45–55 / 50–60 / 60–70（`:72`） |
| `terminology.md` · `program-overview.md` · `system-overview.md` | **blocked**（非 derive 对象） | 根级词表 / 运行时调用链 / Godot 工程落地形态，供 derive 时查阅。三份均零占位、零 `## 待决问题`；词表侧道心 / 煞气、灵石 / 仙玉双币、赋级带 `EnemyLevelRange` 均在位，`system-overview.md:79` 已含 `StoreChannelManager.cs`。**本次新查出两处失真**：`program-overview.md` §2 职责矩阵漏三个 manager（权威在 `services/_index.md`）· `terminology.md:91` 旧目标时长与「分档表待定」 |
| `art/_index.md` · `art/visuals/_index.md` · `art/soundtracks/_index.md` | **blocked** | 生产流水线说明与资产类目表；本库不承载生成产物（归 `game-feature-branch/`）⇒ **结构性地无客户端行为面可 derive**。未决：AI 生成资产商用授权（落点 `vision/scope.md`，零承载）· 参考素材二进制是否入库 · **各方向文档实质内容整体待写（自陈「不是取向问题，是待写」，`art/_index.md:72`）** · guide 粒度 · 生成资产落 `game-feature-branch/` 的目录划分 · UI 元件是否走 AI 生成 · **音频生成工具未定案（自陈「本分区第一前置」）** · BGM 时长码率的包体预算 |
| `art/visuals/art-direction.md` · `art/soundtracks/audio-direction.md` | **blocked** | 两份的 `## 一句话方向` 本身即 `> _（待写）_`（各 `:5`）⇒ **判据① 直接失败**。视觉侧仍占位：色彩 `:19` · 光照与材质 `:25` · 构图余一行 `:34` · 尺寸与格式 `:55` · 禁用清单 `:59`；音频侧：配器与音色 `:16` · 调式与节奏 `:20` · 动态与混音 `:24` · 格式与预算 `:34` · 禁用清单 `:38`。`ADR-0100` Accepted 但明写逐项约束落点是本文件 ⇒ ADR 不构成对占位的覆盖 |
| `art/visuals/animations/_index.md` | **blocked**（**上一版的卡点描述已失真**） | **不再是「全是占位」**：`:12` 已承载 `ADR-0170` 的实质裁决（战斗外演出默认可跳过 / 快进 · 战斗内不设跳过控件 · 附论证）· `:23` 已把待决收窄为「**在既有三重护栏内**每类动画的时长上界与缓动，不是能否跳过」· `:27` 已写下结构预案。**判定仍 blocked**：`:18` 抬头仍写「本节全部条目待咨询专业人士后确定」，范围四类（`:20`）· 技术载体（`:21`）· 制作方式（`:22`）三条全空 ⇒ 判据①的实质设计意图不存在。属**外部输入**，不在本库可自解锁范围 |
| `art/visuals/references/_index.md` · `art/soundtracks/references/_index.md` | **blocked** | `visuals/` 三条登记已细化到可转 prompt 的粒度（`:11`–`:13`），但前两行「素材位置」仍待定，且与 `:19`「素材文件的存放（未定）」形成自指闭环；`soundtracks/` 登记表**整表为空**（`:11`）——一条参考都未登记 ⇒ 判据①的实质内容不存在 |
| `art/visuals/guides/_index.md` · `art/soundtracks/guides/_index.md` | **blocked** | 台账均为空、**零份 guide** ⇒ 无 derive 对象；约定本身已成文，但上游两份 direction 均 blocked 且「guide 粒度」未决 ⇒ 连第一份 guide 都写不了 |
| `decisions/ADR-0002` ~ `ADR-0182`（181 份 Accepted） | **blocked** | 已采纳的决策记录，**非 derive 对象**（作为其余文档的就绪前置，本身不产 FR）。**计数经机械核对一致**：`decisions/` 下 **182 份** `ADR-*.md`，台账 **182 行**，双向比对「有文件无登记」0 条 / 「有登记无文件」0 条，扣除 `ADR-0001`（Proposed 示例）即 **181 份 Accepted**。本次由 157 增至 181：2026-09-07 一次 `/write-adr game`（范围 `all`）固化 `ADR-0159` ~ `ADR-0182` 共 24 份。**这 24 份在主题文档中的反向引用仍稀疏**（见上方台账缺口） |
| `decisions/ADR-0001-example.md` · `decisions/_index.md` · `requirements/_index.md` · `art/*/guides/_TEMPLATE.md` · `content/_TEMPLATE-*.md` · `requirements/_TEMPLATE*.md` | **blocked** | 示例占位（全库唯一非 Accepted 的 ADR）· 台账 · 流水线容器（`requirements/` 下仍**零 FR**，3 个文件全是模板与台账）· 模板骨架（6 份），**均非 derive 对象** |

### 建议的 derive 顺序（仅限 ready / partial 项；被依赖者在前）

**落笔前的必须裁决：✅ 无 —— 队列连续第三次零前置。**
**落笔前的零决策修正：⚠ 12 处**（见上方台账缺口的 🟠 第二条）。**其中会写进 FR 正文的有三处必须先清**：`balance.md:823` 的三类 / 四类 · `enemies/common-properties.md:26` 的 5% / 3% · `combat-service.md` 三处「定长 10 回合」。其余九处属陈旧副本 / 悬空指针，不改也不会写错 FR，但会误导查阅。

1. `/derive-requirements systems/common-properties.md` —— **唯一整份就绪且被几乎所有内容与服务代码依赖的地基，零前置。** 账号级 RNG 的验收有现成的 8 组测试向量（后端 `contracts/profile-sync.md` §6a），是全库当前最硬的验收标准。`LocalizedText` 与 `DrawPool<T>` 同批、**在写下第一批 `.tres` 之前**。**按 `Source` 十值写枚举与合法子集表。**
2. `/derive-requirements ux/error-and-blocking-ux.md` —— 翻译键基建（文档自己已点名 `FR-ux-translation-foundation`）。**无上游依赖**，是「每屏从第一行起就用键」这条起手纪律的前置，越早越省返工。**排除面仅剩逐条中文措辞。**
3. `/derive-requirements systems/architecture.md` + `systems/viewmodel.md` —— 服务骨架：七个 autoload 的注册顺序、EventBus、边界服务的接口 + Offline stub 双实现、`ProfileChangeSpec` 逐列（**含 `AbilityStatusChanges`**）、`OpError` 十值、`HiddenStatGrant` 三格。**两份同批**，均 ready、无排除面。`services/_index.md` 的层级词与七服务 × manager 清单随本步一并兑现、不另产 FR。
4. `/derive-requirements systems/services/profile-service.md` + `systems/player-profile/player-power/common-properties.md` + `systems/character-profile/item/common-properties.md` + `systems/character-profile/power/common-properties.md` —— 写入通道这一层地基（逐列 spec + `TryApply` 原子性与钳制 + 逐列失败语义 + `Project` 只读投影 + `CapabilityManager` 整面 + modifier 合并算法 + 门面级 `Holds(...)` + **`SetAbilityStatus` 一个门面覆盖四类持有条目**）。**四份必须同批**（后两份的自有面很窄，作附属条目而非独立 FR）。**`CostKey` 写 15 行**（不写死列数）。**只排除成就族四条；`status` × 拥有 / 失去不再排除。**
5. `/derive-requirements systems/character-profile/_index.md` + `systems/player-profile/_index.md` + `systems/player-profile/codex/common-properties.md` + `systems/player-profile/game-setting.md` + `systems/character-profile/currency.md` + `systems/character-profile/mana.md` —— 两层 Profile 的存档 schema 地基。**六份同批**：互相引用、共用同一个 `schemaVersion` 与同一条迁移路径。**只取存档 / 写入面，不取任何图鉴屏或设置屏呈现，不取任何货币绝对数字。**
6. `/derive-requirements systems/services/content-service.md` —— 内容加载与校验链路（依赖 1、3）。**仅排除 disabled 条目被存档引用时的 UX**（该分支须留空、不写 UI）。基线 `Id` 超集不变式与打包工具两项产出整段纳入。
7. `/derive-requirements systems/services/sync-service.md` + `systems/services/profile-schema-versions.md` —— 本地缓存原子写 + push 调度与退避 + CAS / 幂等 + 两层 diff + 回声校验，**加上 v1 首发形状清单与 `ProfileShapeCheck` 护栏规格**（依赖 1、3、4、5）。**两份同批。排除 `achievement` 那一行占位与 golden 快照的实际生成**（挂在 `.csproj` 尚不存在上），**FR 文本里不得出现任何「v1 共 N 格」的计数型表述。** 购买段两条腿可留到第 25 步、与 `monetization.md` 同批。
8. `/derive-requirements ux/screen-flow.md` + `ux/onboarding.md` —— 登录屏 + 角色选择屏 + 主菜单导航骨架 + 储物袋面板 + 玩家档案屏 + 轮回 / 篇章结束屏 + 图鉴三层浏览 + 剧本段落进结算面板 + **主动弃置入口** + **非商店平台 Store 入口不渲染**（依赖 2、3、5）。**两份同批。排除成就奖励、`PlayerPower` 触发、`LocationCodex` 单本页、全部待实测手感项。**
9. `/derive-requirements systems/services/account-service.md` —— 会话生命周期 + 四个账号方法 + refresh 两条路径 + `deviceId` 与 refresh token 落点 + 平台密钥库能力矩阵与五条升级触发 + 启动期静默续期 + 合规四域切分与拦截路径 + 失败映射逐 `code` 表（依赖 2、3、7、8）。`account-info.md` 随本步同批兑现、不另产 FR。**排除面为零。**
10. `/derive-requirements systems/character-profile/deck/common-properties.md` —— **效果原语层地基**：`EffectData` 子类树 + **八原语全量** + `StaticModifierData` + `TriggerConditionData` / `TimingIds` + `EffectCondition` + `CardData` 收口 + **21 条加载期校验**。**它是五个内容类型的共同底座，必须先于 11–13。** 纯数据资源型切片、过三条闸。**排除 starter deck 与抽 / 弃 / 洗数值；无原语级排除项。**
11. `/derive-requirements systems/character-profile/deck/_index.md` —— `CultivationTechniqueData` + 灵根准入判定式与四处取池点 + 成员卡反建索引 + `CardSubtypeData` + `CardPool` 三值 + 五个 `DeckChangeOp` + 手牌上限纯上界（依赖 5、10）。**不得先于 5**。**排除全部规模参数与首批清单；首批全单灵根，多灵根路径不实现。**
12. `/derive-requirements systems/character-profile/power/_index.md` —— `PowerData` 字段面 + **跨载体三行边界判据表（三者边界的唯一权威）** + 强度比例刻度 + 内容编排口径表 + 三格非空校验 + 战斗外触发式校验 + 不得产寿元 + 禁用截断五行表（依赖 4、10）。**排除获取侧频次分布与强度定量三格。**
13. `/derive-requirements systems/character-profile/item/_index.md` —— `ItemData` 两格使用效果面 + `MaxUsesPerCombat` + I-1 ~ I-13 校验 + 战斗外一次事务链路 + 随售通道（依赖 4、5、7、10、12）。**排除回寿护栏口径与道具目录**（回寿护栏是次承重卡点，不得以占位数值代替）。
14. `/derive-requirements systems/adventure-event/common-properties.md` —— `EventOption` 数据形态 + 事件收口事务语义 + `lifeSpanCost` 三变异位 + Travel 双禁令 + `AddLooseCard` 取池链（依赖 1、5、10）。**它是后续全部事件类型 FR 的共同类型地基**，必须先于 18。**本份现已整份 ready、无排除面**；`adventure-event/_index.md` 随本步作分类法参照、不另产 FR。
15. `/derive-requirements systems/services/future-event-service.md` + `systems/services/plot-manager.md` —— eventOptions 十步管线 + 三道短缺闸 + `Priority` 抬升判据 + `HiddenStatGrant.Direction` 符号展开点 + `FinaleDiff` 旁路 + `TryGetPlotSegment` 只读转发 + `ChooseBranch` 提交形态与 `BranchLabel` 焊死 + 剧本调制输入 + **档位表 / 阈值 / 回滞 / 跨档叙事 / 两项属性定稿**。**两份必须同批**。**只排除隐藏属性的逐条推拉编排与 `HiddenStatGrade` 三个映射值；`future-event-service.md` 侧唯一保留是 `:296` 那行 `[待复核]`。**
16. `/derive-requirements systems/services/life-cycle-service.md` + `systems/character-profile/life-span.md` —— 轮回骨架、角色状态分类法、`DefeatReason` 三值与 Finale 旁路、存档点与决策点清单、`SeedManager` 四子流、批次层即时提交、两处结束摘要组装、**轮回出口三层处置与 `chapterStartSnapshot`、主动放弃角色、痕迹只追加**，以及寿元的三条不变式与两个扣减来源（依赖 4、14、15）。**两份同批**（`life-span.md` 不单列 FR）。**排除 `lossPerMomentum` ch2/ch3 与回复三档的绝对点数、回寿总量护栏。**
17. `/derive-requirements systems/game-progression.md` —— 篇章 / 境界推进与 `eventCountLimit` 配额 + 经验阈值曲线 / `ExperienceGrade` 映射 / `FailureRatio` 折算 / ≈55% 覆盖率 + 「Finale 前必须升满」硬约束 + **难度与数值缩放的两条分格轴与十一条曲线登记**（依赖 15、16）。**只排除选择区呈现。**
18. 四个非战斗事件子类型，按依赖薄厚依次：`travel/` → `explore/` → `research/` → `exchange/`（各自两份同批；依赖 14、15、16）。**四份的机制面已闭合**；`travel/` 排除 location 运营替代通道（**不得臆造闸门**）· `research/` 排除 `MaxTier` 相关取值域与构筑面板竖屏呈现 · `exchange/` 的 barter 全套结构可整体进切片；已落表的定价与回收率**可作结构输入引用、不得把绝对数字写进验收条件**。
19. `/derive-requirements systems/balance.md` —— **第一批平衡资源 `.tres`**：已定值 / 已定形的那些表（含 21 格 `lifeSpanCost` 与 λ 15/17/44 · 25 格定价外积 · 两条货币产出曲线 · 两档回收率 · `BaseTypeWeights` 五格 · `FinaleDiff` · 经验阈值全表 · `lossPerMomentum` 篇章表 · `GrantPoolWeights` · `SelectionWeightGrades`）+ `Content.Single<T>()` 读取面 + 加载期条数校验（依赖 1、6）。**只取已定格，全部未定格显式留空并标注；取池余量三格可先填 0；引用经验供给账的断言须标「口径待复核」。**
20. `/derive-requirements systems/enemies/_index.md` + `enemies/common-properties.md` —— `EnemyData` 数据形态与全部加载期校验 + `EnemyAiProfileData` 与兜底 argmax + 两条护栏（`±2` / `±1 档`）（依赖 10、11、19）。**两份同批。** 纯数据资源型切片、过三条闸。**`Lines` 恒为空数组落笔**（`LineSlot` 待战斗 UX 专场），不因此推迟整份；**排除全部数值缩放。**
21. `/derive-requirements systems/player-profile/codex/enemy-codex.md` + `codex/technique-codex.md` + `codex/_index.md` —— 两本的词条规格与解锁触发 + 族级三层浏览与完成度口径（依赖 5、8、11、20）。**三份同批**（连锁解锁横跨两本，排布由族总览定死）。**六本可写，`LocationCodex` 的单本页内容区整块排除。**
22. `/derive-requirements systems/scoring.md` —— 道念计分规则与两条转换支路（依赖 19）。**一组纯函数 + 一份平衡资源**，验收靠给定输入验算输出。**排除三档 `BaseReward` 其余 element 与 `RewardPoolId` 取值。须与 19 同批。**
23. `/derive-requirements systems/services/combat-service.md` —— 战斗机制骨架 + 效果流水线五阶段 + 决策点 D0–D7 + AI 两层结构与 `ViewerSide` 投影 + `StackEntryKind` 五值（依赖 10、16、20、22）。**排除战斗内容本体与 Finale 奖励加厚取值。落笔一律取 `EncounterSpec.TurnLimit`，不得把 10 写成常量。** 验证靠在编辑器里跑一场用占位内容拼出的遭遇观察栈与回合流转。
24. `/derive-requirements systems/player-profile/player-power/_index.md` —— 失去三形态 + 残卷 + 授予池结构 + **四支频次预算表**（可整块落笔并标「待实测校准的初值」）（依赖 4、5、12）。**排除战后奖励池三张权重表、第三条获取渠道与开关 UI、平衡边界。**
25. `/derive-requirements systems/monetization.md` + `systems/services/sync-service.md` 的购买段 —— **兑现段 + 购买段一并**（三渠道选型与封装、`StoreChannelManager` / `IStoreChannel` / `IPurchaseBackend`、三个显式步骤、失败处置五情形、入口三条前置与「无可用渠道不渲染」）+ 三道闸 + 兑现水位幂等 + `OpError.Purchase` 处置（依赖 1 的账号级 RNG、7 的 push 调度）。**排在最后**：依赖的两个通用内容池当前为空。**闸①的断言阈值须留空**（`K` 未定），不得以占位数值写死。

> **仍然不要**对成就 / 古宝目录 / `LocationCodex` 单本页 / `ux/combat-ux.md` / art 任何文档 derive。玩法侧的欠账收敛为**一处半**：**① 战斗内容本体**（条目目录整体为空，属内容阶段而非设计缺口，连带卡牌道念量纲与 `RarityTier` 分布权重两项）；**② 回寿法宝的总量护栏**（次承重，反向卡住 `life-span.md` 的依赖闭合）。上一版列的 `status` × 拥有 / 失去编码与平台内购 SDK 选型**均已关闭**。

### 最短解锁路径（blocked 项，按解锁面从大到小）

1. **一次「战斗 / 事件 UX 专场」 —— 本次升为第一位，解锁面最宽。** 一场会同时处理：① `ux/combat-ux.md` 的两条承重（竖屏分区是否过载 / 栈是否必须常驻 · 战报条目的文案体系）② `enemies/common-properties.md` 的 `LineSlot` 成员（解锁 `Lines` 与三条相关校验）③ `research/` 的构筑面板竖屏呈现与风险档非 hover-only 通道 ④ `codex/_index.md` 的 `LocationCodex` 单本页画图形态 ⑤ `content-service.md` 唯一那条 disabled 条目被引用时的 UX。**它把 `ux/combat-ux.md` 送进 partial，并同时收窄四份 partial 的排除面。** → `/analyze-new-ideas`
2. **starter deck 设计过程** —— 同时是四件事的切入点：卡牌产 / 削道念的量纲基准 · 关键字与次类型的首批清单 · `MaxTier` / 每层替换幅度 · `deck/` 两份与 `scoring.md` / `balance.md` / `combat/` 的最后一批取值。**它与「战后奖励池 / 商店库存的 `RarityTier` 分布权重」一起，是玩法侧最宽的取值前置，一次专场可同批打掉五处。** → `/analyze-new-ideas`
3. **一次 handoff 定古宝种类目录 + 次数补充机制 + 可购价格 / 库存权重** —— 解锁 `player-item/` 两份（类型面已由 `ItemData` 覆盖，无需再设计字段），并关闭 `item/_index.md:152` 的回寿法宝总量护栏这条次承重卡点（寿元压力线的唯一剩余数量闸，反向卡住 `life-span.md` 的依赖闭合）。→ `/analyze-new-ideas`
4. **一次 handoff 定 `Achievement` 条目 schema + 进度模型 + 采集面**（EventBus 被动 vs 主动上报）—— 解锁 `achievement/` 两份，并收窄 `profile-service.md` / `player-profile/_index.md` / `life-cycle-service.md` / `profile-schema-versions.md`（`achievement` 占位行）四份的排除面；奖励目录另需法则 / 古宝条目先存在。**注：`ReportProgress(AchievementSignal)` 是主动上报那一支的超前落笔，不得据它反推采集面已定。** → `/analyze-new-ideas`
5. **`vision/scope.md` 的两条零承载项**（AI 生成资产的商用授权与版权立场 · 初装包体预算）—— 它们被 `art/_index.md` 与 `art/soundtracks/_index.md` 指名落点为本文件而本文件零承载；答定后再开一次美术方向会与一次音频方向会填实两份 direction 的占位（**音频侧另有第一前置：音频生成工具定案**），`art/` 分区才可能开始产 guide。**art 台账类文档即便全部答完仍结构性地不产 FR**，不必期待它们进 derive 队列。→ `/analyze-new-ideas`
6. `art/visuals/animations/_index.md` 的范围四类 / 技术载体 / 制作方式明写「待咨询专业人士」，属**外部输入**，不在本库可自解锁范围。

**以直读裁决的冲突（1 处）：** 两个分片对 `systems/balance.md` 战后奖励池的**族维度**报告不一致（一方称三类、一方称四类）。orchestrator 直读裁定：`:823`（授予池条目段的旁注）写「含卡牌 / 道具 / 功法**三类**」，`:917`（待决问题条目）写「含卡牌 / 道具 / 功法 / **神通**四类；`Practice` 档不产神通，故神通列只在 `Standard` / `Finale` 两张表上非空」。后者与 `ADR-0169`、`combat-service.md:591`、`power/_index.md` 的内容编排口径三处一致 ⇒ **以四类为准，`:823` 是陈旧副本**，已登记为落笔前的零决策修正第 ① 条。

## 下一阶段

- **ADR 状态：** `decisions/` 现有 **222 份 Accepted**（`ADR-0002` ~ `ADR-0223`，`ADR-0001` 为 Proposed 示例占位）。
  2026-08-28 的一次 `/write-adr all` 全量扫描（逐份核对 29 份未被任何 ADR 引为来源的 handoff）把散落其中、已落笔主题文档却无档案的定案固化为 **23 份新 ADR**（`ADR-0100` ~ `ADR-0122`），覆盖 sync 族（`revision` CAS · flush 非阻塞 · 闸门口径）· 命名通则 · 战斗 RNG 单流 · 敌人 AI 权重向量 · 效果原语语法 · capability flag / modifier · 道具使用效果面与批次层提交等。
  台账与逐条影响文档见 `decisions/_index.md`。
  2026-08-30 的一次 `/write-adr game` 固化 **10 份新 ADR**（`ADR-0123` ~ `ADR-0132`）：上述 4 条登记候选（灵根修习准入 · `Artwork` 单格与 `RealmArtworks` · 二进制不经 overlay · Exchange 以物易物）全部立档；另逐份核对未被任何 ADR 引为来源的 handoff，补固化 6 条散落定案（寿元合并与显性化 · `StatusChanges` 置值列 · `HiddenStatDirection` · flags 第三层覆盖来源 · `Upgrade` 非闸门点不硬阻塞 · `StackEntryKind.UsedItem`）。
  2026-09-05 的一次 `/write-adr game`（范围 `all`）固化 **26 份新 ADR**（`ADR-0133` ~ `ADR-0158`）：三条登记候选（货币跨篇章结转 · 灵石渠道口径与 Travel 禁令扩三 key · 仙玉不落可售出族）全部立档；另逐份核对 22 份未被任何 ADR 引为来源的 handoff，补固化 23 条散落定案，覆盖 09-02 / 09-03 / 09-05 三批（图鉴三层浏览 · 轮回结束屏与篇章结束屏 · 阵法启动式异能宿主 · 剧本分支落点与 `BranchLabel` 焊死 · 跨载体边界判据 · 神通强度刻度 · 快照按视角过滤 · 合规域切分判据 · `lifeSpanCost` 耗时正比定价 · 剧本层对外面 · schema 登记权威与不写计数 · `FinaleDiff` 旋钮 · 灰态非禁用 · `OpError.Purchase` · 打包工具两项产出 等），并补立两条早期基础定案（两层档案持有骨架 `ADR-0136` · 设计库类模型化组织 `ADR-0137`）。
  2026-09-07 的一次 `/write-adr game`（范围 `all`）固化 **24 份新 ADR**（`ADR-0159` ~ `ADR-0182`）：两条登记候选（不设 ante 式章内难度阶梯与两条分格轴 `ADR-0163` · 随包基线 `Id` 超集不变式 `ADR-0182`）全部立档；另逐份核对 19 份未被任何 ADR 引为来源的 handoff，补固化 22 条散落定案，覆盖 09-06 批（失去能力频次预算 · 主动放弃角色 · 篇章时长上移 ≈60 分钟 · 轮回出口三层处置与痕迹只追加 · `AbilityStatusChanges` 写入通道 · 隐藏属性收口两项 · 战后奖励四族与档位闸 · 演出跳过权分档 · 敌人产出同因 · `BaseTypeWeightsData` 与 `combatTier` 编排口径 · 类型修正护栏 · 失败容错 N = 2 与偏紧入账 · 不给距 Finale 读数 · IAP 四条）与 09-02 批（`MoveCardEffect` 补 `Side` · 平台密钥库五条触发条件）。三份历史 handoff 判定不建档：`2026-08-02c`（意图族已被 `ADR-0059` 整条推翻）· `2026-08-12b`（已由 `ADR-0051` 承载）· `2026-07-12-example`（模板示例）。
  2026-09-08 的一次 `/write-adr game`（范围 `all`）固化 **41 份新 ADR**（`ADR-0183` ~ `ADR-0223`）：候选清单当时为空，全部来自逐份核对 6 份未被任何 ADR 引为来源的 handoff 后扫出的散落定案。按批：09-07 共 18 条（战斗量纲基准 `momentumPerMana = 1` · 费用曲线随境界上移与牌流 4/2/7 · 起始卡组 15 张与敌方规模 · `MaxTier` 上界 5 与每层 +20% · ch3 越阶追分承担面 · 道念修正只用加法层 · 永久物 rate · 神通 25% 闸门 · 外观预留三个加法窗口 / 品类 / 持有与选用切分 / 条目形态与发版节奏 / 呈现落点 · 成就存档两键 / 内容 schema 与信号表 / 加权进度与隐藏分母 / 采集面与两条新列 / 两档奖励族分配）· 09-08 共 23 条（栈浮层化 · 竖屏六区屏高预算 · 敌方区只读 · 手牌重叠扇与卡面占位 · 道念差徽标 · 不做复合追分指示 · 牌堆可见面 · 只读层图标条 · 0 值处置判据 · `AbilityScope` 形状角标 · 卡框色与角标分工 · 详情页与禁用档 · 战报文案体系 · 疲劳三格呈现 · 战后奖励面板形态 · `LineSlot` 五成员与台词硬纪律 · 候选项排布轴判据 · 候选条目构件与标注层 · 事件选项焦点制 · carousel 的 N 自适应 · 共用卡宽基准 · 闭关面板竖屏形态 · 风险档中性标注）。**当前无待固化的 ADR 候选。**
  - ✅ 更早的一条候选「**不设 `GlobalBalanceData` 兜底大表，平衡资源按三问判据（消费者 / 覆写纪律 / 跨字段不变式）逐份切**」（`handoffs/2026-08-22-singleton-balance-resource-registry.md`）已于 2026-08-28 收口：`systems/balance.md` 补上了该判据本身那一小节（此前只有逐处应用），**并直接改写既有的 `ADR-0074`**（它本就是「凡可调数值一律住平衡资源」那条决策的档案，同一件事不另开编号——见本库「改决定直接改 ADR」的治理原则）；`ADR-0074` 后果里自陈的那条欠账随之关闭。**无需再跑 `/write-adr`。**
  （注：ADR 现可自由编辑，改决定直接改 ADR，不再新开取代 ADR。）
- **流水线闭环（07-30）：** design → code 链路补上 `/breakdown-requirements`（一份 FR → 一个文件夹的可执行子需求），完整形态见 `README.md` 与 `requirements/_index.md`。
- **架构闭环缺口：** 8 处**全部闭合**（移出记录见 `answer-logs/log-0725c.md` 与 `log-0726b.md`）；状态表见 `systems/architecture.md` 的「闭环缺口」小节。残留细节已下沉为各焦点分片的普通待决问题。
