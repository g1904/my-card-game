# Open questions — 跨 session 待答清单（索引）

> 本文件是**客户端**（Godot 项目）待答清单的**索引**：问题条目本身按主题拆在 `open-questions/` 下的分片里。
> 后端侧的待答清单在 `backend-design-documents/open-questions.md`（`backend-design` 分支）。
>
> 每次 session 结束时，未答的 Open questions 汇总到对应分片，供下次拾起；一旦答定，就从分片中移除、
> 归档进对应主题文档的 `## 待决问题` / `## 决策`，并在 `answer-logs/log-<draftSuffix>.md` 记一笔。
>
> 本清单**只跟踪仍待答的问题**（不留已解决区），是导航 / 拾取清单，**权威归属在各主题文档**。
>
> 最近更新：2026-09-06 — 内购 SDK 与基线超集两对跨库落笔（详见 `open-questions/update-log.md` · `answer-logs/log-iap-channel-integration.md` · `log-baseline-superset-and-pack-proof.md`）
> （逐次变更摘要见 `open-questions/update-log.md`；已答定问题的逐条移出记录见 `answer-logs/`）

## 分片导航

| 分片 | 内容 |
|------|------|
| `open-questions/update-log.md` | 每次运行的更新摘要（答结 / 推翻 / 新增落点），倒序，**只留最近 10 条**。不含问题条目本身。 |
| `open-questions/update-log-archive.md` | 更早的更新摘要，原样归档、按时间正序。只读，不写新条目。 |
| `open-questions/01-combat.md` | **① 战斗机制**（焦点之首）：能力剥夺与统计计数的残留（片区主体已于 08-10c 答结）、结构与配置（`EncounterTighten` 六界常量 · `EnemyManaLimit`）、内容与数值（多数留待内容扩充后的统计校准；含**敌人产出缩放曲线的一处主题文档间矛盾**）、呈现（竖屏分区专场 · 战报文案体系 · 敌人台词槽位）。 |
| `open-questions/02-event-options.md` | **② eventOptions 生成流程**：生成 / 加权与配比、物化字段、优先级、寿元打穿、Explore 揭示池、Travel 出场、location 与图鉴连边。 |
| `open-questions/03-adventure-event-types.md` | **③ 逐类型 AdventureEvent 的收口后残留**：五类机制面已全部收口（各自开过专场），片内只剩**只欠取值 / 只欠呈现形态**的格——Travel 定价、Exchange 两组数值格、Explore 三个待定取值、走火入魔权重、构筑面板呈现、三档奖励厚薄与 `HiddenStatGrade` 编排。 |
| `open-questions/04-hidden-attributes-plot.md` | **④ 隐藏属性 / 剧本机制**（档位表 · 跨档叙事 · 数据编码 · 寿元增长通道均已答结）：仅剩**是否有第三项隐藏属性**、逐条目的推拉映射与两条剧情线的具体内容、`HiddenStatGrade` 三个映射值。 |
| `open-questions/05-service-contracts.md` | **⑤ 服务契约 / 工程侧残留**（API 面 · `revision` · 契约边界 · 翻译键 · 共有属性分层 · 纪律可执行化均已答结）：location 无法被 flags 秒关的运营替代通道、**首次生成 `.csproj` 后的三项实测**（`#if DEBUG` 判据 · `Control` 自动翻译 · 平台凭据存储能力）、`schemaVersion` 登记表覆盖对账的两件残留。 |
| `open-questions/06-meta-progression.md` | **⑥ 元进程的失败侧与中长期规划感**：「还有几步到 Finale」的轮回内前瞻、主动弃置的发起入口、`completed` 是否清理角色数据、失去能力四支的频次预算、角色强度差与多灵根换算、失败螺旋的容错量验收。 |
| `open-questions/07-codex-monetization.md` | **⑦ 图鉴族与商业化**：`CharacterPower`、图鉴族、premium bundle。 |
| `open-questions/cross-boundary.md` | **跨边界承接**：对侧（后端）已定案、本库尚未落笔的条目。**不是待答问题**——答案已有，等的只是落笔；形态、关闭条件与维护者分工见分片抬头。 |
| `open-questions/deferred-content.md` | **已搁置：内容充实**（07-30 起暂不推进）＋ **美术与音频（`art/`，08-04 加入）** ＋ 随内容搁置的 UX 呈现细节 ＋ 尚未设计的占位主题。 |

## 当前焦点：各系统机制细节

> **焦点判据（07-30 定）：** **规则、字段语义、流程与算法 = 机制细节 = 焦点**（分片 ①–⑦）；
> **具体条目目录与数值 = 内容充实 = 搁置**（`open-questions/deferred-content.md`）。
> 与既定开发路线「框架 → 内容 → 平衡与体验 → 社交及其他」的第 ① 阶段一致。
> Source: `handoffs/2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown.md`。
>
> 焦点顺序即分片编号顺序；**① 战斗机制**优先级最高。

## derive 就绪度

> **本小节由 `/assess-derive-readiness` 独占写入**（`/analyze-new-ideas` 与 `/summarize-open-questions` 均不得改动）。就绪度是全库横切判断，须基于一次性全量扫描才有意义；逐次 handoff 顺带的评估会迅速过时且互相矛盾。

**最近全量评估：2026-09-06（由 `/assess-derive-readiness` 产出）。** 扫描范围：`vision/`（3）· `systems/**`（**55**，含新文件 `services/profile-schema-versions.md`）· `art/**`（10，不含 2 份 `_TEMPLATE`）· `ux/`（5）+ 根级横切 3 份 = **主题文档 76 份**，另加 `decisions/`（158 份文件 = 157 份 Accepted + `ADR-0001` Proposed 占位，台账 158 行，双向比对零缺登记零悬空）与 `content/` / `requirements/` 的台账与模板。旁证：`requirements/` **零 FR**（无「已覆盖」项）· `inbox/` 顶层为空 · 09-02 / 09-03 / 09-05 三批共 **20 份 handoff 全部 `distilled`**、索引双向零缺口。本次重估的触发是 08-30 之后的三批连续落笔（09-02 八份 · 09-03 六份 · 09-05 六份）与一次 `/write-adr game`（固化 `ADR-0133` ~ `ADR-0158` 共 26 份）。五个分片并行只读评估，逐份给证据行号。

**全局结论：ready 12 份 · partial 35 份 · blocked 29 份（主题文档 76 份口径，不含 157 份 Accepted ADR 本体与台账 / 模板）—— ready 由 6 份翻倍至 12 份，08-30 记录的三条承重卡点（两种货币的获取渠道 · 合规六端点契约 · `MoveCardEffect` 方位面）全部倒下，derive 队列前 10 步无一带结构性排除面。**

- **✅ 全库最大的共同前置消除：两种货币的获取渠道与掉落权重已定案**（`ADR-0133` / `ADR-0134` / `ADR-0135`）。08-30 时它同时卡住 `balance.md` 全部 14 条待决取值、`currency.md` 的唯一实质卡点与 `exchange/` 四组数值格。现两条产出曲线与消耗侧是同一次反推的三个输出并全部成文：灵石 ≈ 战利品（`Research` / `Exchange` 不给）· 仙玉每章 1–2 次、单次 1 / 2 / 4 枚且一格不落可售出族 · 两币跨篇章结转 ⇒ 定价表不设篇章维 · `S(c)` 15 / 35 / 60 与 25 格「稀有度基价 × 族系数」外积 · 两档回收率 40% / 15% 及「商店档优于随售档」的加载期硬校验。⇒ **`currency.md` 由 partial 升为 ready**；`exchange/` 的数值格由「四组全欠」减为两组且均不承重（刷新价参数首批填 0 即整条关闭 · 槽位上界）；`travel/` 行 9/10/25 与 `explore/` 行 37/40/101 落表，Explore 的定价指纹护栏由呈现门控升级为**算术闭合的定价结构**。
- **✅ 跨边界唯一那条欠账闭合：对侧 `contracts/compliance.md` 的六端点报文字段表（§10）与端点自身错误码（§11）均已落笔。** 连同本库侧 `ADR-0155` 的 `ComplianceManager` 四域切分判据、平台密钥库四端能力矩阵与五条升级触发条件（`ADR-0138` / `ADR-0139`），**`account-service.md` 由 partial 升为 ready、排除面清零**；`ux/error-and-blocking-ux.md` 上次那半条排除面（`ERR_compliance_*` 端点侧验收断言）**不再需要排除**，端点侧与拦截侧可一并写出完整验收。
- **✅ 上次全库唯一那处结构面倒退关闭：`MoveCardEffect` 已补 `Side`**（`deck/common-properties.md:167`，与同表五个方位原语逐字同构；`Side` 相对 `controllerSide` 解析、`From` 与 `To` 恒同侧、跨方转移结构上不可表达、日后开放是加 `ToSide` 的纯加法），配套加载期校验由 20 条增至 **21 条**（第 21 条：`Selection == Chosen` 且 `Side != Self` → `PushError`）。⇒ **derive 第 10 步不再需要排除任何原语，第 23 步减一项排除。**
- **✅ schema bump 权威外移，`sync-service.md` 的 derive 前置解除。** 逐版登记新立独立文件 `systems/services/profile-schema-versions.md`（`ADR-0145` / `ADR-0158`），`sync-service.md` 只留执行面 + 「别处一律回链、不得就地宣布 bump」。08-30 记的「漏三批」经 09-05 逐条核实**不成立**（三批在表内 #8 / #22 / #27，`Status` 删三格属首发前删除、按形态纪律不进任何版本行）⇒ **该文件由「ready（derive 前须补一格）」升为无保留 ready。**
- **✅ 图鉴族三份升级（`ADR-0147`）。** 图鉴收进主菜单单一入口、索引页 → 单本页 → 词条三层浏览、七本共用同一套排布 ⇒ `codex/_index.md` **由 blocked 升为 partial**（剩余卡点收窄为一条：`LocationCodex` 除连边外的词条深度，连带它的单本页画图形态）· `enemy-codex.md` 与 `technique-codex.md` **由「近 ready」升为 ready**（两份自身零未决，此前的保留项正是族级浏览形态）。
- **✅ 神通侧一次性落齐（`ADR-0152` / `ADR-0153`，本轮切片扩大最多的一份）。** `power/_index.md` 新增跨载体三行边界判据表 + 四条推论（**本表是卡牌 / 道具 / 神通三者边界的唯一权威**）· 强度定性三条与 `baseMomentum` 比例刻度、不设合计总闸 · 内容编排口径表（首批开放 `InitialGrant` / `CombatReward`（收窄 `Standard`·`Finale`）/ `ExchangePurchase`）。
- **✅ 另有四条卡点倒下**：绑定功法初始层数恒 1、`CharacterData` 不加字段（`ADR-0146`，`character-profile/_index.md` 的 ⟨待定⟩ 行消失）· `plot-manager.md` 的「DnD 式选分支触发点与 UI」整条关闭（`ADR-0150` / `ADR-0151` / `ADR-0157`：落事件结算面板内、`BranchLabel` 非空 ⟺ `Condition.Kind == BranchChosen` 焊死、分支选择零 EventBus 广播）· `screen-flow.md` 的「元婴证书形态」关闭（`ADR-0143` 篇章结束屏一屏三变体、ch3 变体即证书）· `lifeSpanCost` 21 格定价与预算 ×10 落表（`ADR-0156`）。`ux/error-and-blocking-ux.md` 的两处零决策修正（删 `intent` · barter 灰显进灰态判据表与键清单）**均已落笔**（`ADR-0142`），`ux/combat-ux.md` 的「意图区与待决区双重记账」**已修**。

**仍在的承重卡点（三条，性质各异）：**

- **🔴 `status`（启用 / 禁用）×「拥有 / 失去」两个正交维度的存档编码 —— 现为全库解锁面最宽的单一裁决。** 它是 `item/common-properties.md` 与 `power/common-properties.md` 两份 blocked 的**共同且唯一**卡点，同时收窄 `profile-service.md` / `power/_index.md` / `player-power/_index.md` / `player-power/common-properties.md` 四份 partial 的排除面。**且它已在权威侧自相矛盾**：`profile-service.md:336` 已给出语义答案（「失去 = 移出列表，不是置 `status = 禁用`」），而同文件 `:420` 仍把该编码整条登记为未定，四份下游文档全部指向 `:420`。一次专场即可关闭。
- **🔴 战斗内容本体整体为空**（卡牌条目 / 起始卡组 / 敌人条目目录 / 遭遇编排）。机制骨架与数据形态两侧已闭合，缺的是**条目**——属内容阶段，不是设计缺口。连带两项量纲：卡牌产 / 削道念的基准（切入点是 starter deck）与战后奖励池 / 商店库存的 `RarityTier` 分布权重，二者是 `balance.md` 剩余待决的新共同前置。
- **🔴 `blind` / `ante` 缩放曲线整体尚未陈述**（`game-progression.md:181`）——**全库唯一「连形态都没有」的一条**，不是取值缺口。

**台账 / 投影缺口（不阻塞 derive，但会误导落笔；权威在主题文档，编辑归用户 / `/analyze-new-ideas`）：**

- **🟠 `ADR-0133` ~ `ADR-0158` 这 26 份在全部主题文档中近乎零反向引用**（机械检索：全库仅 3 处命中、分布于 `architecture.md` 与 `profile-schema-versions.md` 两份）。事实内容都已写进主题文档（逐条核实过），缺的是 `## 决策(-> ADR)` 里的那一行回链。**且这是第三次报告的系统性缺口**——旧 ADR 侧同样成片：`common-properties.md` 缺 11 份 · `content-service.md` 缺 14 份（含 08-30 已点名的 `ADR-0125` / `ADR-0130`，至今未补）· `sync-service.md` 缺 12 份 · `profile-service.md` 缺 10 份。**结论：`decisions/_index.md` 的「落点」列与文档侧的该小节已不是双向满射，两侧只能靠台账单向导航。** 同型的整节空白共 **10 处**：`balance.md:807` · `future-event-service.md:538` · `currency.md:37`（`ADR-0089`/`0133`/`0134`/`0135` 四份落点、全文零个 `ADR-` 字样，本轮最严重一处）· `item/_index.md:144` · `game-setting.md:112` · `player-item/` 两份 · `achievement/` 两份 · `player-power/_index.md:111`。
- **🟠 `exchange/` 同批两份互相打架**：`common-properties.md:213` 仍把「定价表每格 + 刷新价 + 两档回收率 + 槽位上界」四组一并记为待定，而同批的 `_index.md:170` 与 `balance.md` 已给出 25 格初值与 40% / 15%。**同批两份的待决区必须一次对齐**，否则 derive 会按更宽的那份写出假的排除面。
- **🟠 `combat-service.md:589` 的可选奖励候选池与神通获取通道不一致**：前者仍写「三类混合 `CardData` / `ItemData` / `CultivationTechniqueData`」，而 `power/_index.md:73` 已开 `CombatReward` 并收窄到 `Standard` / `Finale`（`Practice` 不产神通）。奖励生成的权威文档与通道权威文档对不上，**derive 第 23 步落笔前须先追平**。
- **🟠 `life-cycle-service.md:320` 的 `experiencePoint` 待决区措辞仍未跟上**（把「阈值曲线 / 给予量 / 分布 / 失败折算」整条记为未决，而四项均已定、权威在 `balance.md` / `game-progression.md`）⇒ 其排除面被高估。**08-30 已点名，第二次报告。**
- **🟠 `systems/_index.md:45` 的 `combat-service` 行仍写「敌人 AI 与意图（三档揭示）」**，与 `ADR-0059` / `ux/combat-ux.md:11` / 08-15d handoff 三处上游定案正面冲突——它是全库唯一残留的「意图」正面登记，且索引是找文档的第一跳。**08-30 已点名，第二次报告。**
- **🟠 `lossPerMomentum` 的 ch2 / ch3 有三种口径**：`life-span.md:53` 写「候选值 5 / 10，定案待反推」· `balance.md:466` 给出 5 / 10 并标「候选值」且已用形状锚逐格校验通过（9% / 10.0% / 11.3%）· `balance.md:819` 又整条重登为「留待统计校准」。09-03 的反推已做完，三处措辞未收口。
- **🟡 `game-progression.md:195` 与同文件 `:57` 自相矛盾**（前者把「进度感那一半」整条记为未决，后者已判给常驻经验条并称其为 ch2/ch3 唯一连续进度来源）。**08-30 已点名，第二次报告。**
- **🟡 `item/_index.md:3` 与 `item/common-properties.md:3` 抬头均仍写「占位结构，细节待定」**，与两份已成文的字段面 / I-1~I-13 校验 / 跨载体判据回链直接矛盾。**08-28 首次点名，第三次报告。**
- **🟡 `exchange/_index.md:165` 仍标「ADR 候选，待 `/write-adr` 立档」而 `ADR-0126` 已 Accepted**（08-30 已点名，第二次报告）。
- **🟡 `account-info.md:51` 的唯一待决问题已在跨边界被答死但未撤**：对侧 `contracts/compliance.md:202` 的「不下发」清单与 `contracts/auth.md:55` 合起来使合规域一格也不落 `AccountInfo`，字段表不再有增行风险。撤掉它即可把本文件的判定推向 ready（它本就无独立 FR 面、须随登录切片同批）。
- **🟡 `ux/combat-ux.md:22`「只能加速不能跳过」与 `art/visuals/animations/_index.md:20`「须可跳过 / 可加速」两侧措辞相抵**，无一方标注让步。两处都不阻塞 derive（art 侧不产 FR），但 animations 分区开工时会以一条已被否决的前提为起点。**08-30 已点名，第二次报告。**
- **🟡 `profile-schema-versions.md` 的结构与全库不同构**：无 `## 意图` / `## 待决问题` / `## 决策(-> ADR)` 三个标准小节，其两处已知空缺（`:59` `achievement` 未进 v1 清单 · `:15` golden 快照 `profile-shape-v1.json` 待建）因此不落在任何 `## 待决问题` 面上，机械汇总会漏掉它们。

**跨边界闭合（强制检查项）：本次首次全线闭合。**

- **✅ 上次唯一未闭合的那条已关闭**：`backend-design-documents/contracts/compliance.md` §10 六端点报文字段表与 §11 端点错误码均已落笔 ⇒ `account-service.md` 与 `error-and-blocking-ux.md` 的合规排除面**整体取消**。
- `sync-service.md` / `content-service.md` / `profile-schema-versions.md` 三向皆闭合：报文字段名与序列化形态权威在对侧 `envelope.md` / `profile-sync.md` / `content-manifest.md`，本库零复述；Exchange 物化字段与 `Source.ExchangeBarter = 10` 折入 v1（**不是一次 bump**），后端 `operations/version-matrix.md` 已于 09-03 登入 `1`。
- **对侧仅剩一处措辞不一致（不构成卡点）**：`contracts/envelope.md` §6 中四条 `compliance.*` 拦截码的「客户端处置」列仍写「阻塞屏 + XX 动作」，而客户端已定四条一律在登录屏就地呈现、一条也不进 `BlockingNoticeKind` 变体表。呈现形态裁决权在客户端库，语义无冲突 ⇒ derive 时以 `account-service.md:315-327` + `ux/error-and-blocking-ux.md` 为准。
- `open-questions/cross-boundary.md` 的「待承接」仍是 1 条（`ComplianceManager` 客户端覆盖面的切分），而它已由 `ADR-0155` 在本库落笔 ⇒ **该条可关闭**。反向：后端 `cross-boundary.md`「待承接」为空。
- **唯一未定的跨边界依赖**：平台内购 SDK 的选型与封装层（归后端支付渠道选型 + 一次客户端工程蓝图）⇒ `monetization.md` 的唤起内购整段排除，兑现段不受影响。

### 判定表

| 文档 | 判定 | 卡点 / 就绪切片 |
|------|------|------------------|
| `systems/common-properties.md` | **ready** | `## 待决问题` 明写「（无）」（`:348`）。全库地基、derive 顺序第 1 位、零前置。整面 = 稳定 `Id` 与展示字段三层切分 · 物化模型 · `LocalizedText`（封闭二值 locale · `Get()` 纯读 · 不落存档）· `ContentEnabled` + `AllEnabled()` / `AllIncludingDisabled()` 双名与 `[Obsolete(error:true)]` 编译闸 · `RarityTier` 五档 · `Artwork : Texture2D` 可空 + 占位回落 + 七类挂载面 · `SourceCode` + `Source` **十值**（`ExchangeBarter = 10`，`:276`）+ 合法子集表（`:299`）+「三个成员只出现在 `Op == Remove` 上」（`:306`）· 两级 seeded RNG（轮回级四子流 + `State` / `DrawCount` 持久化；账号级 SplitMix64，8 组测试向量在 `backend-design-documents/contracts/profile-sync.md` §6a，验收可逐位对表）· API 契约总则三形态 / 三失败语义 · `deviceId` 落点。**落笔时按 `Source` 十值（非九值）写枚举与合法子集表** |
| `systems/architecture.md` | **ready** | `## 待决问题` 明写「当前无未决项」（`:721`），「闭环缺口（架构体检）」8 处全部已闭合（`:702`）。整面 = 三层切分 · 七服务边界与 autoload 注册顺序 · EventBus 总则 · 八条 API 契约总则 · 边界服务「接口 + Http/Offline 双实现」骨架 · 共享核心类型（`OpError` **十值**，`:134`；`OpError.Purchase` 只由 `purchase.*` 域映入、不建逐 `code` 表，`:297`，`ADR-0140`）· `ElementSpec` / `ModifierKey` / 钳制表 · `ProfileChangeSpec` 逐列（含 `StatusChanges`）· `HiddenStatDirection` 与三格 `HiddenStatGrant` · 三级纪律判据。**须与 `systems/viewmodel.md` 同批** |
| `systems/viewmodel.md` | **ready** | `## 待决问题` 明写「当前无未决项」（`:80`）。整面 = 展示层第三层的结构契约（呈现期对象、不落存档 / 不进云端负载 · 单向依赖且服务 API 面永不返回 ViewModel · 组装源三件套 · 三条重组装触发面 · 只读消费与缓存归属 · 永不渲染清单 · `Artwork` 两级回落）。**须与 `systems/architecture.md` 同批** |
| `systems/services/sync-service.md` | **ready** | `## 待决问题` 仅剩 `pushId` 后端记忆窗口（`:358`）——对侧参数，客户端语义已定、报文字段名与序列化形态由 `backend-design-documents/contracts/envelope.md` 定死。**逐版 schema 登记权威已外移到 `profile-schema-versions.md`，本文件只留执行面（`:321`）⇒ 上次那条 derive 前置解除、排除面清零。** 整面 = 存档点 ↔ push 解耦 + 5 秒防抖 + 立即 flush 清单 + `PushPolicy` + `user://cache/` 原子写 + schema 版本化与迁移 / 拒绝（两种清晰拒绝，`:340`）+ `CharacterProfile` 级 diff + `revision` CAS / `pushId` 幂等 + `Source` 上行走成员名 + 后端主动写入后的 pull 时序 + 断线降级与退避阶梯 + 两层 Profile 完整字段面 + 回声校验整条 + JSON camelCase 单点命名策略与「重命名透明路径 = 破坏性变更」（`:332`）。跨边界两向皆闭合 |
| `systems/services/account-service.md` | **ready**（**本次由 partial 升级**） | `## 待决问题` 仅剩多设备并发登录的云端裁决规则（`:344`）——归后端，客户端表现已定（被挤下线 → 硬阻塞重登 → 先 pull 后 flush）。**上次三条卡点全部倒下**：对侧 §10 六端点报文字段表与 §11 端点错误码已落笔 · `ComplianceManager` 四域切分判据已在本库落笔（`ADR-0155`，`:177`）· 平台密钥库后置评估已兑现（四端能力矩阵 + 五条升级触发条件 + 迁移与失败语义，`:84`）。整面另含 = 会话生命周期 + 四个账号方法 + refresh 失败拆两条路径 + `AccountInfo` 五字段 + `deviceId` 落点 + refresh token 明文带版本 + 启动期静默续期 + 「不做任何判定」纪律与倒计时例外（`:194`）+ 须改名 = 启动链内全屏模态、不进 `BlockingNoticeKind` 变体表（`:205`）+ `complianceTicket` 只在内存（`:219`）+ `GET /v1/compliance/status` 单点调用与 fail-open 降级 + 失败映射逐 `code` 表（`:315`）。**排除面清零** |
| `systems/character-profile/currency.md` | **ready**（**本次由 partial 升级**） | `## 待决问题` 明写「无」（`:43`）——上次那条承重卡点「两种货币的获取渠道取值与掉落权重」整条倒下。整面 = 双币轮回级定位与 `[0, ∞)` 取值域两行 `ResourceElements` · **跨篇章结转、篇章边界零货币动作**（`ADR-0133`）· **灵石获取口径 = 战斗，`Research` / `Exchange` 不给**（`ADR-0134`）· 仙玉唯一主动获取 = 稀有 AdventureEvent 产出 + 四条否定边界（不由付费 / 成就 / Finale / 新事件类型）· 花销唯一出口 = 高阶 Exchange · **两币完全不可兑换 + 售出同币回收因而不产生事实汇率**（`ADR-0135`）· 两个修正列恒 `null`。**derive 时不取任何绝对数字**（定价表 / 逐章给予量 / 回收率的权威在 `balance.md`，落笔取现值并标待校准） |
| `systems/character-profile/mana.md` | **ready**（**本次由 partial 升级**） | `## 待决问题` 明写「无」（`:65`），三个数值项已全部转记 `balance.md` 且其中 `RealmBreakthroughManaBonus = 1` 已有取值。整面 = `manaLimit` / `currentMana` 字段面 · 炼气基线 5/5 不结转 · 每回合恢复至上限的三步节拍 · 上限两条来源（事件推拉 ±1 · 每次大境界 +1，走 `CostKey.ManaLimit` 增量语义、无基线表无置值语义）· 不设下界护栏 / 死牌转化 · 下降唯一承载点 = 闭关自选风险档 · `currentMana` 不入 `Status`（落 `activeCombat`）。**derive 时排除「卡牌费用曲线是否随境界上移」的验收断言** |
| `systems/player-profile/game-setting.md` | **ready**（**本次由 partial 升级**） | 唯一登记项是三条音量轨默认值属「待实测初值」（`:118`），取值已存在于 `profile-service.md` 的 `SettingFields` 表 ⇒ 属校准而非缺口。整面 = 切分判据（`ADR-0072`：「取值是否取决于这台机器」+ 换机自检反问 + 拿不准归设备本地 + 一项只落一侧）+ 两侧对照表 + 账号级四项（三条音量轨 `int [0,100]` + `FastCombatAnimation`）+ 设备本地 `user://cache/device-settings.json` 一项（`locale`）+ `SettingChanges` 通道与 `SettingFields` 配表（`int?` / `bool?` 失败语义）+ `PushPolicy` + 启动期 locale 归一 + 首批不收候选与各自解除条件 |
| `systems/player-profile/codex/common-properties.md` | **ready** | 无 `## 待决问题` 小节、无待复核项；存档面整份闭合。整面 = `CodexEntry` 单格 `Id` + `CodexKind` 七值 / `CodexUnlock(Kind, Id)` + `CodexElements` 零 `Op` 恒不经 pipeline + 四行失败语义 + 七本的解锁触发（全部搭既有提交，零新增提交点）+ 连锁解锁「一次敌人收录展开为 1 + N 条」+ `eventEnd` 入账射程 + `CodexFlavor` 可选 `LocalizedText` + 「五本能力 / 道具 / 功法类不套用敌人五项规格」的词条深度表。**derive 时排除 `LocationCodex` 的词条正文形态**（该本另有专属规则，见 `codex/_index.md`） |
| `systems/player-profile/codex/enemy-codex.md` | **ready**（**本次由「近 ready」升级**） | 无 `## 待决问题` 小节，`## 决策(-> ADR)` 八条全部为结论式（`:83`）；上次的唯一保留项（族级浏览形态）已由 `ADR-0147` 关闭。整面 = 五项词条写作规格（长度 150–280 字 / 口径 / 不含阿拉伯数字）+ 关键卡 2–3 张由 `EnemyData.KeyCardIds` 显式标注、正文绝不写等级 + 「关键卡牌必在样本卡组内」是加载期机械检查 + 词条②指向真实功法条目（悬空或不在该敌人功法列表内 → `PushError`）+ 慷慨度与三级退让阶梯 + 战斗中不可查、呈现时刻 = 战斗前确认页 + 图鉴静态性三句 + 资产字段（`ADR-0120`）。**derive 时排除 `KeyCardIds` 的具体条目取值与退让阶梯的上调动作** |
| `systems/player-profile/codex/technique-codex.md` | **ready**（**本次由「近 ready」升级**） | `## 待决问题` 明写「暂无」（`:70`）。整面 = 词条构成五行（功法名 / 描述 / `Rarity` / **属性（灵根要求）** / 可选 `CodexFlavor`；属性行由呈现层从 `RequiredAffinities` 与 `MaxCharacterAffinityCount` 现算、零新增存档字段、且**不表达「当前角色能否修」**）+ **词条不列该功法的卡牌清单**（承重反查护栏）+ 两条解锁路径皆幂等、升阶不产生新条目、弃置不删条目 + `Pool == Enemy` 照常收录 + 完成度分母取 `AllIncludingDisabled()` + 存档面纯加法空迁移（后端零配合）。**derive 时排除功法条目本身**（`content/` 为空）**与完成度分母的绝对值** |
| `ux/onboarding.md` | **ready** | `## 待决问题` 明写「当前无未决项」（`:21`）。整面 = 强制登录无游客 + 首版只呈现已实现渠道（不遍历 `LoginChannel`）+ 两步握手 UI（验证码框 + 倒计时始终可见的重发键 + 过期重取，无 hover 提示）+ 绑定 / 解绑不落登录屏 + 首玩篇章门禁 + 角色选择是首玩者唯一一次起手选择、首玩局标注推荐项、**明确不做「首局跳过选择」的特判** + 解锁 = 「有可挑战角色」的动态状态。**与 `ux/screen-flow.md` 同批**（共写登录屏、主菜单门禁与角色选择屏） |
| `systems/services/profile-service.md` | **partial** | 就绪切片 = `ProfileManager` 唯一写入面 + `TryApply(spec)` 全量校验 / 全有或全无 + `Project(spec)` 只读投影（`ADR-0108`）+ `ProfileChangeSpec` **逐列**与逐列失败语义（**列数不写死**）+ `CostKey` 15 成员 / `StatKey` 2 项 + `ResourceElements` 配表 + 产出侧 `Key == LifeSpan && BaseValue < 0` 的整批拒绝 + 投影钳制 + 门面级只读 `Holds(...)`（`ADR-0126` 的承重落点，barter 白拿漏洞的唯一堵法）+ `CapabilityManager` 整面（聚合范围 · 一条 `Recompute()` + EventBus `CapabilitiesChanged` · 叠加 = 集合并幂等 · 冲突结构上关死 · 新增一个 flag 恰好三步不 bump schema）+ modifier 合并完整（同层求和 → 只乘一次 → 只取整一次 · 两条钳制 · 顺序无关 ⇒ 不设优先级）+ CodexManager / GrantPoolManager 面 + 战斗外「一次组装一次事务」门面。卡于（四条，全属元进程 / 成就侧，`:416`）：`AchievementManager` 触发采集面（EventBus 被动 vs 主动上报）· 成就两档奖励内容 · `Achievement` 条目 schema · `PlayerPower` 平衡边界。**另含一处自相矛盾：`:336` 已给出 `status` × 拥有 / 失去的语义答案，而 `:420` 仍登记为未定**（见上方台账缺口）。**须与 `player-power/common-properties.md` 同批** |
| `systems/services/profile-schema-versions.md` | **partial**（**新文件，首次判定**） | 无三个标准小节，但抬头（`:1`）载有真实定位；已知空缺明写在文内。就绪切片 = ① **v1 首发形状 33 条条目清单**（`:21`，逐条带权威回链）可直接喂迁移器骨架 ② `ProfileShapeCheck` 护栏规格整套（`:92`：递归导出 JSON path + 类型 → 与 golden 逐字比对；打包管线不通过即不产包 + `#if DEBUG` 启动期同一份校验）③ 形态纪律六条（`:61`）。卡于：`PlayerProfile.achievement` 尚未进 v1 清单（`:59`，随 `Achievement` schema 答定后补，仍属 v1、不 bump）· golden 快照 `profile-shape-v1.json` 待建且落地时点挂在「`game-feature-branch/` 尚无 `.csproj`」上（`:15`、`:104`）。**须与 `sync-service.md` 同批** |
| `systems/services/content-service.md` | **partial** | 就绪切片 = 三层覆盖来源 + 启动期 manifest 比对 + blob 内容寻址 + ES256 detached 签名与 `keyId` 轮换 + 文件级事务 + 增量下载 + overlay 合并与「以 overlay 为准」+ 合并后全量校验 + `ContentEnabled` 不预埋占位 Id + flags 第三层与 `user://cache/flags.json` 落盘纪律（schema 四格 · 走 `AtomicJsonFile` · 写入时点唯一 · 不设 TTL · 三条失效语义）+ `contentVersion` 记两个 + 断网降级到随包基线 + 语言覆盖率审计与 `fallback = "zh"` + overlay 剧本例外的 `newIds` 双闸 + **打包工具两项产出（机读产包证明 + 在架版本基线快照，`:55`，`ADR-0141`）** + 单例内容注册与加载期条数校验（`ADR-0030`）+ overlay 只承载 `.tres` 的两道处置。卡于：**唯一待决 = disabled 条目被存档引用时的 UX**（`:452`，归 `ux/`）⇒ derive 时该分支须留空、不写 UI，不得按「完全静默照常可用」落笔（那会锁死一个未决裁决） |
| `systems/character-profile/_index.md` | **partial** | 就绪切片 = **25 行 `CharacterProfile` 字段表**（`:115`，写入通道列无空洞）+ `Status` 子类九格（`:142`）+ 五格新字段与 `TechniqueEntry` 形态（`:168`）+ `CharacterData` 六格与**十一条加载期校验**（含灵根四条，`:110`）+ `Affinities : Affinity[]`（唯一规则后果 = 硬性修习准入、不落存档、不进上行负载）+ `RealmArtworks` 稀疏境界覆写与两级回落 + 角色池全池指定 · 规模 5 · 不做账号级解锁 + 7 条读档校验。**上次的 ⟨待定⟩「两门绑定功法初始层数」已定案为恒 1、`CharacterData` 不加字段**（`ADR-0146`，`:28` 只留「日后逐条编排 = `BoundTechniques[]` + `InitialTier`」的纯加法退路）。卡于（两条、皆非结构）：全池指定下角色强度是否塌缩为单一最优（`:377`，待实测）· 隐藏属性是否有第三项（`:378`，权威在 `plot-manager.md`）。**须与 `deck/_index.md` 同批**（灵根准入判定式跨两份成立） |
| `systems/character-profile/life-span.md` | **partial** | 就绪切片 = 单值形态与三条不变式（无上限字段 / 无上限截断 / 无「上限推拉」）+ 与 mana 的非对称对照表 + **预算表 1000 / +1000 / +3000 / +5000**（`:21`，×10 量纲）+ 扣减恰两来源与「同一次收口事务落到同一个值」+ **战斗内不读写寿元、只在收口时刻扣减**这条资源纪律 + 回复口恒为三通道且共用一条 `ChangeElement(CostKey.LifeSpan, +n)` + 归 0 → `DefeatReason.LifeSpanExhausted` + 明文常驻恒精确、< 10% 转红 + `PastEventEntry.LifeSpanAfter` 曲线 + `lifeSpanCost = round(t(type) × λ(chapter))`（`ADR-0156`）。卡于：`lossPerMomentum` 的 ch2 / ch3 系数仍标「候选值 5 / 10」（`:53`，三处口径未收口）· 回复三档绝对点数未定案（`:54`）· **回寿法宝的总量护栏挂在仍空白的 `item/_index.md:150` 上 ⇒ 依赖不闭合**。**derive 纪律：字段住 `CharacterProfile.Status`、消费点在 combat-service 与 life-cycle-service 两侧 ⇒ 与那两处同批，不单列 FR** |
| `systems/character-profile/deck/common-properties.md` | **partial**（**排除项清零**） | **上次那处 🔴 结构卡点已闭合**：`MoveCardEffect` 现含 `Side`（`:167`），加载期校验表由 20 条增至 **21 条**（第 21 条 `:277`）⇒ **derive 时不再需要排除任何原语。** 就绪切片 = `EffectData` 抽象基类 + 一原语一 `[GlobalClass]` 子类树 + **八原语**参数表 + 并列定义体 `StaticModifierData`（`ModifierTarget` 五项冻结 · 万分比整数 · 同层求和 → 只乘一次 → 只取整一次）+ `AbilityData` 两格 XOR 与启动代价面 + `TriggerConditionData` / `TimingIds` 十时点 + `SubjectKind` 相容校验 + `EffectCondition` 三谓词 AND + `SideConstraint` 一律相对施放者解析 + 关键字哨兵与恰好一层展开 + `CardData` 字段清单收口 + 目标 / 作用域分建模与 `EntryFilter` + `ADR-0154` 的按视角前提机械化（`:361`）+ 存档面零新增字段、空迁移。**它是卡牌 / 神通 / 法宝 / 法则 / 古宝五类内容的共同底座**，纯数据资源型切片、过三条闸。卡于：starter deck 内容空白 · 抽 / 弃 / 洗数值 · 关键字与次类型两份注册表条目为零（机制成立、清单空） |
| `systems/character-profile/deck/_index.md` | **partial** | 就绪切片 = 功法 = 卡组构筑单位（`ADR-0054`）+ 轻量 header + `CultivationTechniqueData` 两格 `RequiredAffinities` / `MaxCharacterAffinityCount` + **灵根修习准入整块**（`:119`，判定式 `technique.RequiredAffinities ⊆ character.Affinities` · 四处玩家侧取池点各叠一层 · 准入不进 `DrawPool<T>`、由调用方在 `PickMany` 前筛掉 · 敌人侧一律不叠 · 卡牌侧一格不加）+ 成员卡反建索引 + 五个 `DeckChangeOp` 与 `Pool == Enemy` 施加侧闸 + `CardType` 五分与封闭卡集（`ADR-0041`）+ 次类型体系 + 卡池三值必填（`ADR-0091`）+ 不重洗与疲劳（`ADR-0052`）+ 手牌上限 7 的纯上界语义 + 抽牌堆插入位（`ADR-0119`）+ 回合内状态三件套 + 「一切增益减益都是战场条目」。卡于（六条，全落在内容 / 量纲）：**starter deck 装哪些牌仍空白**（`:272`）· 道念产削量纲基准（`:274`，承重）· `MaxTier` / 每层替换幅度（`:270`）· 关键字与次类型首批清单为空 · 通用功法占比口径 · **多灵根角色的强度对齐换算**（首批全单灵根故不发生，引入第一个多灵根角色前必须先答）。**不得先于 `character-profile/_index.md`** |
| `systems/character-profile/item/_index.md` | **partial** | 就绪切片 = 三层分工写死 + 使用效果面按世界分两格（`CombatUseEffects` / `OutOfCombatUseOutcome`，必填性由 `UsableScene` 驱动）+ `Abilities` 整格移除 + `MaxUsesPerCombat` 把本场配额从 `Charges` 摘开 + 战斗外三列白名单 + 十列恒空断言 + 不设目标面 + 一次组装一次事务的执行链路 + 战斗外使用不是决策点、是即时提交（`Debounced` + `SavePointReason.InventoryChanged`）+ 痕迹落 `pastItemUse` + **I-1 ~ I-13 加载期校验** + 储物袋跨两层视图与随售通道、两档回收率的加载期硬校验 + **跨载体三行边界判据表的回链**（`ADR-0152`，权威在 `power/_index.md:53`）。卡于（三条）：**回寿法宝的总量护栏在内容编排面的口径全空**（`:150`，承重——它是寿元这条压力线的**唯一剩余数量闸**，出现频率 / 库存深度 / 定价三者口径仍全空，且反向卡住 `life-span.md` 的依赖闭合）· 道具种类目录未设计 · 三条获取通道的分布与数量口径未给 |
| `systems/character-profile/power/_index.md` | **partial**（**本轮切片扩大最多**） | `character-power-mechanics`（09-03）整批落地。就绪切片 = `PowerData` 字段面（含 `GrantedFlags` / `Modifiers` 两条战斗外通道）+ **跨载体三行边界判据表与四条推论**（`:53`，`ADR-0152`，**本表是卡牌 / 道具 / 神通三者边界的唯一权威**）+ **强度定性三条与 `baseMomentum` 比例刻度、不设合计总闸**（`:63`，`ADR-0153`）+ **内容编排口径表**（`:68`：首批开放 `InitialGrant` / `CombatReward`（收窄 `Standard` · `Finale`，`Practice` 不产神通）/ `ExchangePurchase`；`EventOutcome` 保留机制零条目；`Travel` / `Explore` 恒不产出）+ 两层共用注册面与三条推论 + 三格至少一格非空校验 + 「纯战斗外条目不得挂触发式」校验 + 不得含 `LifeSpan` 产出 + 入场两条与门 + 禁用截断五行表 + `IsProtected` 统一置位与 `IgnoresProtection` 唯一后门（`ADR-0106`）+ 战斗内运行态走战场条目 `counters` 零结构增量 + 起手绑定神通（`ADR-0055`）。字段表计数已订正为 25（`:102`，与 `character-profile/_index.md` 实际一致）。卡于：获取 / 失去的频次与分布未定量 · **`status` × 拥有 / 失去的存档编码**（`:102`）· 强度定量三格（需 starter deck 与功法规模先落地） |
| `systems/player-profile/player-power/common-properties.md` | **partial** | 就绪切片 = capability flag + modifier pipeline 两条通道的**声明侧整面**（`:17`，`ADR-0116`）：扁平 `enum CapabilityFlag` 不分区 · 命名 = 动词三词表 `{Reveal, Show, Unlock}` + 禁否定式（union 不变式的可机械检查护栏）· 消费侧「一 flag ↔ 一处消费点」+ `CapabilitiesChanged` 空负载广播 · 叠加 = 幂等集合并、冲突结构上关死 · 注册面两层共用 · `ModifierOp {Add, Scale}` 万分比整数 · `ApplyModifier(key, baseValue)` 单点入口 + 两条钳制 · 作用面 opt-in 白名单与四类豁免。卡于：**`status` × 拥有 / 失去的存档编码**（`:50`）· 两条通道的消费点条目（`content/player-power/` 为空）。**须与 `systems/services/profile-service.md` 同批**（合并算法权威在服务侧、声明侧字段与命名纪律在本文件；分批必出两份互不相认的叠加规则） |
| `systems/player-profile/player-power/_index.md` | **partial** | 就绪切片 = 失去的三形态（置换 / 本轮回禁用 / 战斗内 `IgnoresProtection`）+ 同池判据 `(CarrierKind, Scope)` 全同 + 同 `Rarity` + 排除已持有 + 自愿失去阶梯（`ADR-0048`）+ 残卷 Finale 绑定（`ADR-0049`）+ 授予池权重结构与初值 40/27/18/10/5 + 「一次轮回至多结算一次」的轮回级布尔位 + 类型面已闭合的自陈（缺的只是条目目录，开张动作归 `/scaffold-content-type player-power`）。卡于（四条）：战后奖励池按优势档三张权重表（`:119`）· 第三条获取渠道未陈述、开关 UI 未细化 · 平衡边界（是否影响 cycle seed / 计分公平）· **「失去能力」四支的频次预算需重新配平且本次变重**（`:121`——`IgnoresProtection` 单支已接近全部预算，而 09-03 又把神通侧的置换 / 禁用挤进同一份预算，分子由三支变四支而合计口径未同批上调） |
| `systems/player-profile/_index.md` | **partial** | 就绪切片 = 16 字段表整张（写入通道列无空洞，`playerItem` 为双通道 `AbilityElements` + `ItemElements`）+ 规则层 / 统计层分层判据（`ADR-0050`）+ 三样不进 Profile 的排除项 + 三个具名子类（`PlayerStatistics` / `PlayerPowerFragment` / `PlayerEntitlement`）+ 受 `ADR-0028` 回声校验约束的顶层键 + 残卷 Finale 绑定 + 子系统导航六条。卡于（一条两半，`:203`）：`achievement` 条目 schema 未定（落在仍 blocked 的 `achievement/*`，属依赖不闭合）· `PlayerPower` 平衡边界未定。**derive 时 `achievement` 顶层键只保留键位与写入通道、不产条目 schema** |
| `systems/player-profile/account-info.md` | **partial**（**本次由 blocked 升级**） | 字段面五格已闭合（`AccountId` / `AccountSeed`(hex16) / `CreatedAtUtc` / `Identities` / `Nickname`）+ `BoundIdentity` record + 三条方向纪律（`Identities` 只读投影靠 pull 取回 · `Nickname` 客户端写后端判 · `accountInfo` 是受回声约束的顶层键）+ 首版不做头像 + 不持有账号状态 + 字段增删即 bump 与两路迁移 + `/accountInfo/accountSeed` 透明路径。**上次那条唯一待决（合规字段归属待后端分级、字段表仍可能增行）已在跨边界被答死**：对侧 `contracts/compliance.md:202` 的「不下发」清单与 `contracts/auth.md:55` 合起来使合规域一格也不落 `AccountInfo`。**判 partial 而非 ready 的理由只剩两条**：本文件尚未吸收该结论（`:51` 未撤）· 单独不构成可独立成立的 FR 面 ⇒ **须随 `account-service` / 登录切片同批** |
| `systems/player-profile/codex/_index.md` | **partial**（**本次由 blocked 升级**） | **上次判 blocked 的三条理由中两条已倒下**：图鉴族入口与浏览形态由 `ADR-0147` 关闭（收进主菜单单一入口、索引页 → 单本页 → 词条三层浏览、七本共用同一套排布、排布细节回链 `ux/screen-flow.md`）· 边缘顶点显示程度与显影半径已定（`Known` / `VisibleE` / `Frontier` 三式 + 呈现三态表 + 半径固定 1 跳）。就绪切片另含 = 七本清单 + 完成度只计已解锁顶点 + 共同形状八条（账号级 / 稳定 `Id` 索引 / 存档只记解锁状态 / 经 `CodexElements` 写入 / 接触即记零新增提交点 / 静态知识不给动态情报 / 战斗中一律不可查 / 连锁解锁）+ 不与成就奖励挂钩因而后端零配合。**剩余卡点收窄为一条**：`LocationCodex` 除连边外的词条深度未定，连带它的**单本页内容区如何画那张图**（缩放 / 平移 / 顶点布局）与词条载体待定（`:85`）⇒ derive 时**六本可写、`LocationCodex` 单本页整块排除**。注：七本表中五本仍为 ⟨待播种⟩，derive 时不得假定这五本各有专属文档 |
| `systems/services/future-event-service.md` | **partial** | 就绪切片 = eventOptions 十步管线整条（`ADR-0026`）+ 候选短缺三道闸（`ADR-0073`）+ `EventOutcomeSpec` 字段面（`ADR-0078`）+ 置换 / 禁用候选前移到物化时掷定 + `Priority = 1` 抬升判据与三条与门子判据（`ADR-0047`）+ `HiddenStatGrant.Direction` 符号展开点（`ADR-0129`）+ Exchange 库存物化与 Explore 取池过滤 + 灵根修习准入叠入取池链（`ADR-0123`）+ **`FinaleDiff` 旁路**（`:205`、`:280`，`ADR-0144`：不掷骰、取 `角色等级 + BandFor(chapter).FinaleDiff`，旁路不绕过带）+ **`TryGetPlotSegment` 只读转发**（`:350`，`ADR-0157`，附「不破坏无记忆纯产出侧」的论证）+ 「本服务纯本地、全部方法形态 A」的 API 面。卡于：`BaseTypeWeights` 每格取值 + Combat 内 `combatTier` 三档配比（统计校准，`:544`）。**全库唯一残留的 1 项 `[采纳推荐 — 待复核]` 仍在**（`:295`），按收窄判据不构成降级。**失真：`## 决策(-> ADR)` 整节为空（`:538`），八份以本文件为落点的 ADR 零回链——第三次报告。须与 `plot-manager.md` 同批** |
| `systems/services/plot-manager.md` | **partial**（**承重卡点关闭一条**） | **上次的「DnD 式选分支的触发点与 UI（含玩家可见 / 不可见分支边界）」整条关闭**：触发点 = `eventEnd` 那次 `TryApply` 提交之后、落事件结算面板内的剧本段（`:531`，`ADR-0150`）· 可见边界焊成 `BranchLabel` 非空 ⟺ `Condition.Kind == BranchChosen`（`:405`，`ADR-0151`；异或违规 `PushError`）· `branchId = ToNodeId` 与同节点唯一性校验 · `ChooseBranch` 写入 = 批次层独立即时提交、不新增存档点 · **分支揭示与 key point 推进零 EventBus 广播、走门面只读查询**（`:542`，`ADR-0157`）· `BranchLabel` 文案的隐藏属性泄露软检查。就绪切片其余 = `PlotArcData` + `PlotNodeData`（`ADR-0015`）+ `PlotModulation` 六字段与加载期悬空校验 + 多条 `Active` arc 的合并算法（权重相乘 / 白名单取并）+ `EncounterTighten` **五格**增量的方向约束与 `Finale` 整档豁免（`ADR-0077`）+ 档位表 / 阈值 / 回滞 / 跨档叙事（`ADR-0016`）+ 战斗层零耦合（`ADR-0081`）+ 剧本树不分包（`ADR-0029`）+ 门面投影 1 → 2。卡于（两条，均属隐藏属性侧的内容编排 / 标定）：隐藏属性完整清单与增减触发（Combat 三档已有默认口径，其余四类欠，`:567`）· `HiddenStatGrade` 三个映射值与 `EncounterTighten` 界常量。**失真：`## 决策(-> ADR)` 未收录 `ADR-0150` / `0151` / `0157` / `0077` / `0081` / `0143` / `0148`** |
| `systems/services/life-cycle-service.md` | **partial** | 就绪切片 = 轮回骨架（两层持有模型 + `ongoing` / `defeated` / `completed` 分类法 + 寿元递减预算与归 0 终态（`ADR-0031`）+ `DefeatReason` **三值**与 Finale 失败的显式旁路（`ADR-0025`）+ 「账号级残卷累加先于角色终结提交」顺序不变式 + `AdvanceEventAsync` 固定结算流程与取消语义 + 非战斗四类的事件内决策点 + 判负短路那一路的单次提交 + `SeedManager` 四子流 / `AttachRngState(spec)` 三步机制与「凡消耗子流随机必须同批更新 `State` / `DrawCount`」不变式 + 残卷掷骰走账号级 SplitMix64 与 `FinaleWinOrdinal` 幂等键 + 存档点永不回退（`ADR-0032`）+ 决策点存档（`ADR-0036`）+ 批次层即时提交）。**新进切片**：`DefeatCharacter` 在清理前组装只读结束摘要（`:163`，`ADR-0148`）· **`CompleteChapter` 组装 `ChapterEndSummary`，时点 = 境界寿元增量之后、`TeardownCycle` 与任何角色数据处置之前**（`:166`，`ADR-0143`）· `TotalCyclesCompleted` 只在 ch3 的 `CompleteChapter` 上 +1 · 两种货币跨篇章结转且不构成 ChapterManager 职责（`ADR-0133`）。卡于：元进程持久化范围（`Achievement` schema · `PlayerPower` / `PlayerItem` 触发 · `PlayerPower` 平衡边界）· 隐藏属性细节（转指 `plot-manager.md`）。**失真：`:320` 的 `experiencePoint` 待决区措辞仍未跟上（排除面被高估，第二次报告）；`## 决策(-> ADR)` 未收录 `ADR-0127` / `0128` / `0133` / `0143` / `0148`** |
| `systems/adventure-event/common-properties.md` | **partial** | 待决**由三条减为一条**：`lifeSpanCost` 21 格全部有数（公式 `round(t × λ)`、λ = 23/25/63，载体 `LifeSpanCostTableData` + 三条加载期校验）· 回寿量三档 ch1 折算 50/100/200。就绪切片 = `EventOption` 物化字段面 + `OutcomeRule` 与 `HiddenStatGrants` + 「产出即定稿、不得回查模板重算」（`ADR-0012`）+ 事件收口的事务语义（`ADR-0020`）+ `PastEventEntry` 与 `AppliedChange`（`ADR-0021`）+ 寿元回复只走 outcome 侧（`ADR-0066`）+ `lifeSpanCost` 形态定值三变异位（`ADR-0109`）+ **Travel 禁令扩为「不得回寿且不得产两种货币」**（`:111`，`ADR-0134`）+ `AddLooseCard` 取池链（`ADR-0118`）+ 九条加载期校验。**它是后续全部事件类型 FR 的共同类型地基。** 卡于：五类配比 `BaseTypeWeights` + Combat 内 `combatTier` 三档配比（统计校准，`:409`）。**失真：`## 决策(-> ADR)` 只两行，九份以本文件为落点的 ADR 零回链** |
| `systems/adventure-event/travel/_index.md` · `travel/common-properties.md` | **partial**（两份同批） | **上次两条卡点之一倒下：Travel 行定价已有数**（ch1/ch2/ch3 = 9/10/25；自校验 `Travel ÷ Exchange` = 0.39/0.40/0.40 落 1/3~1/2 区间，另有 `Travel == 0` 与越界比值各一条加载期校验）。就绪切片 = `LocationData` + 单份 `LocationMapData` 载体与恒启用（`ADR-0042`）· 80/20 全局常量不可调制 · 目的地取自邻接集合、`DestinationLocationId` 物化时掷定且不得事后算 · 闸门形态（`ADR-0043`：最高 `Priority = 1`、无 `IsMandatory` 字段）· 结算写入判据为 `DestinationLocationId != ""` 而非 `EventType == Travel` · 痕迹侧记出发地 · 两条 `StatusAssignment` 由 life-cycle-service 组装 · 不计入 `eventCountLimit` 与剧本免疫（`ADR-0111`）· **outcome 侧货币产出禁令与回寿禁令合为同一行校验**。卡于：**location 无法被 flags 秒关时的运营替代通道**（`_index.md:95`，明写「本次不预设形态」；location 恒启用是 `AllEnabled()` 运营通道在本作唯一的空洞）⇒ derive 不得为它臆造闸门。`common-properties.md` 自有面为空、两节均转指 `_index.md` |
| `systems/adventure-event/explore/_index.md` · `explore/common-properties.md` | **partial**（两份同批） | **上次两条数值卡点均已有取值**：定价表 Explore 行 = 37/40/101 · 真身占比 `5:3:2` 初值已用于 `t(Explore) = 1.6` 的标定推导，且「设计期按真身分布期望标定 ≠ 运行期按真身取价」的禁令已成文 ⇒ **定价指纹护栏在算术上闭合**。`common-properties.md` 明写「无。专有字段清单闭合」。就绪切片 = 揭示机制整套（`ADR-0064`）—— 真身分布 = 条目池组成 × 既有加权抽取、三处数据类一律不加字段 · 取池期「真身须同样 enabled」过滤 + 真身为 Exchange 时其库存池前置须过 · 揭示 = `eventStart` 内一次 `with` 派生且 resolver 按真身选取 · 遮罩卡与其余选项完全同构 · 全屏覆盖层转场、不进屏幕栈、不给任何部分线索 · `IsRevealed` 只在物化侧 · 派生实例落 `activeEvent` 随后续第一个决策点写盘、不新增揭示专属存档点 · `RevealedEventId` 一格与四条加载期校验。卡于：转场时长 ≈1.2s 与一次短音效的真机实测（归 `ux/`，不阻塞结构） |
| `systems/adventure-event/research/_index.md` · `research/common-properties.md` | **partial**（两份同批） | **上次三个数值格已减为两个**：开局构筑 `lifeSpanCost = 0` 覆盖已登记在案（`_index.md:77`，对位 `balance.md:150` 明写「本表在内容侧唯一登记在案的覆盖值」）。就绪切片 = 构筑面板 = 复数决策槽（`ADR-0022`）· **五类**操作闭合 · 产出面 = 卡组 + `manaLimit` · `manaLimit` 下降挂玩家自选风险档、风险档是唯一内部张力 · 候选物化时掷定走 `RngStream.Reward` · `AllowDecline` 默认 `true` · 不另收资源代价 · 产出禁令扩为「不给任何货币」· 回寿不作为 Research 操作、Research 收敛为纯构筑事件 · 闭关与开局构筑两个取池点各叠 `Pool != Enemy`（`ADR-0091`）· 三道短缺闸 · `ResearchSlotSpec` / `ResearchSlot` / `ResearchCandidate` 形态 · 载体 `CostKey.ManaLimit` · `ProfileChangeSpec.DeckElements` 承载。卡于：走火入魔风险档权重取值 · **`MaxTier`**（决定 `ResearchCandidate.Amount` 的取值域，权威在 `deck/_index.md:270` 且仍未定——**本片唯一的跨文档依赖不闭合项**）· 构筑面板竖屏呈现与风险档的非 hover-only 说明通道（归 `ux/`） |
| `systems/adventure-event/exchange/_index.md` · `exchange/common-properties.md` | **partial**（**承重前置已被打掉**） | **上次的「四组数值格全欠、绝对数字被两种货币获取渠道阻塞」不再成立**：25 格定价表初值与币种分布落表（两个一维向量外积、来源可解释）· 仙玉一格不落 `CharacterItem` 族（`ADR-0135`）· 跨篇章结转使定价表不设篇章维（`ADR-0133`）· 两档回收率 **40% / 15%** 已是初值 · 商店档优于随售档的加载期硬校验成文。**数值格由四组减为两组且均不承重**：刷新基价 `RerollBaseCost` / 递增量 `RerollCostStep`（首批一律填 0 = 关闭刷新）· 单事件槽位总数上界（建议 ≤ 8，落 `StockRules.SlotCount` 之和的加载期校验）。就绪切片 = barter 支付侧二选一整段（`ADR-0126`：`Holds(...)` 前置拒绝、`CanAfford` 一字不改、`Source.ExchangeBarter` 只开 `Op == Remove` × `(Item, Character)`、不持有则灰显且支付要求保持可见、不按持有面过滤呈现）· 售出两条通道与同币回收（`ADR-0098`）· 交易逐笔即时提交 · 库存物化经 `RngStream.Shop` 掷定并落存档 · 五个商品族映射到既有仓储 · 定价表结构与两条折扣通道 · `ModifierKey.ShopPrice` 单点施加 · 刷新是店级动作恒以灵石计价 · 价签必须标明币种 · 三道短缺闸 · 加载期 / 运行期校验。**⚠ 落笔前须先对齐同批两份互相打架的待决区**（`common-properties.md:213` vs `_index.md:170`） |
| `systems/services/combat-service.md` | **partial** | 就绪切片 = 战斗机制骨架整条（栈与 LIFO 结算 + 战报 `ADR-0086` · `ActionResult` / `CombatFeedEntry` **六类**含 `ItemUse` / `Declared`+`Actual` / `EntryId` 与 `CauseEntryId` 因果树 · 疲劳为一等栈条目 + `TurnLimit` · 三步回合与借 stack 不借交互 · 抽牌堆不重洗、抽空每张扣 1 道念 · 敌人意图整条移除（`ADR-0059`）· AI 两层结构与 `CombatSnapshot` 按 `ViewerSide` 投影、**对侧 `faceDown` 条目整条不入 `Battlefield`**（`ADR-0154`）· **效果流水线五阶段**与 `Evaluate` 纯 / `Apply` 唯一写点 · `StackEntryKind` 五值与栈条目 `itemId` 双向不变式 · `ActiveCombat` 存档块与索引序承重 · 决策点 D0–D7 与每点提交形态 · `RewardPickState` 与逐项领取（`ADR-0082`）· 可选奖励固定 3 项、预抽定落存档、走 `RngStream.Reward` · 战斗前确认页（`ADR-0094`）· counters 键空间 · `EncounterSpec.FirstSide` · 挂起态恢复与取消 · 单一 `combat` 子流）。**`MoveCardEffect` 方位面已闭合、不再是排除项。** 卡于：**战斗内容本体整体为空**（字段面已收口，缺的是条目）· Finale 奖励加厚幅度待统计校准 · **`:589` 的可选奖励候选池与 `power/_index.md:73` 已定的 `CombatReward` 通道不一致（落笔前须先追平，见上方台账缺口）** |
| `systems/scoring.md` | **partial**（**规则面已全闭合**） | 就绪切片 = **计分规则与全部转换公式**（一组纯函数 + 一份平衡资源，验收靠给定输入验算输出，不必等一场可运行的战斗）—— 道念 = 胜负判据、战斗不以任何资源池归零为终止条件 · **战斗过程中寿元不被读写**（承重资源纪律）· 产削与下限 0 的每次结算截断（溢出不结转，LIFO 因此影响结果）· 疲劳为第二条削减通道 · 胜侧两条支路 + 单价表 · 终止条件 = `EncounterSpec.TurnLimit` 而非常量 · `WinMargin` 在 `Finale` 退场（`ADR-0018`）· `combatTier` 三档遭遇参数 · 战斗层不读写隐藏属性（`ADR-0081`）。**负侧换算已完全落定：道念差 × `lossPerMomentum`、ch1 = 10**（`:85`，扣的是唯一那条命 `lifeSpan`；注意这是 08-30 记的「ch1 = 1」的订正值）。卡于：三档 `BaseReward` 其余 element 与 `RewardPoolId` 取值 · 卡牌产 / 削道念的量纲基准（切入点是 starter deck）。**须与 `balance.md` 同批** |
| `systems/balance.md` | **partial**（**就绪切片本次扩大最多**） | **上次的共同前置「两种货币的获取渠道」已被打掉**：两条产出曲线 + 25 格定价表初值 + 币种分布 + 两档回收率现为同一次反推的三个输出并全部成文（`:311`–`:372`）。就绪切片另含 = `lifeSpanCost` 21 格 = `round(t × λ)` 与预算 1000 / +1000 / +3000 / +5000（`ADR-0156`）· **`FinaleDiff` 初值三章恒 +1、下界恒 1**（`ADR-0144`）· `experiencePoint` 阈值全表 + `ExperienceGrade` 倍率映射 + 「单次给予 ≤ 该境界最小阈值」的加载期断言 · `lossPerMomentum` 篇章系数表（ch1 = 10 锁定）· `baseMomentum` 表 · 全局等级序 1–22 · 赋级带 `±2` 与 `EnemyLevelingData` · 起手 4 / 每回合抽 2 / 手牌上限 7 · 越级追分形状锚点 · `MaxConcurrentSideArcs = 2` · `TravelFullFanoutChance = 0.80` · `BatchSizeWeights` · `GrantPoolWeights` 40/27/18/10/5 · 取池余量三格 · 重试上限两行由 `HasPremiumBundle` 选行 · 敌人 AI 兜底权重向量与取值域 · `itemPowerRatio` 按 `Charges` 分层 · 道统残卷三张分档表 · `EncounterTighten` 十个界常量的结构与两条硬性约束 · 平衡资源经 `Content.Single<T>()` 取（`ADR-0074`）· 「不设 `GlobalBalanceData` 兜底大表、按三问判据逐份切」一节。卡于：`## 待决问题` **仍为 14 条**但性质已分层——**有取值待复核 6 条**（Exchange 定价 25 格 · `lossPerMomentum` ch2/ch3 候选 5 / 10 · 回寿量 ch1 折算 · Explore 真身占比 · `EnemyManaLimit` 5 · 重试上限两档）· **只有形态零取值 6 条**（`BaseTypeWeights` · 刷新价与槽位上界 · 战后奖励池 `RarityTier` 权重与三格 margin · 卡牌道念量纲 · `EncounterTighten` 六界常量 · 走火入魔权重）· **连形态都无 1 条**（blind / ante）· 护栏复核 1 条。**新的共同前置收窄为两项：卡牌道念量纲基准 · 战后奖励池 / 商店库存的 `RarityTier` 分布权重。失真：`## 决策(-> ADR)` 一节为空（`:807`），正文实引至少八份 ADR** |
| `systems/game-progression.md` | **partial** | 就绪切片 = 篇章 / 境界推进与检查点重试模型（`ADR-0004`）· 存档点即结束点、途中死亡从章首重试 · 修行等级体系（`ADR-0034`）· `experiencePoint` 阈值曲线 / 三章合计 / `ExperienceGrade` 四档 / 产出点覆盖率约 75% / 失败产出 = `ExperienceGrade` + `FailureRatio`（默认 50、向下取整下限 1，取整数是为 `AppliedChange` 可重放）· **「Finale 前必须升满、Finale 自身不供经验」的硬约束及其与 `FinaleDiff` 的咬合**（`:51`）· `eventCountLimit` 的地域配额语义与剧本免疫（`ADR-0111`）· 事件循环与 Finale 胜负即篇章闸门 · Travel 路由 80/20 · 经验点进程（`ADR-0038`）。卡于：**blind / ante 缩放曲线连形态都未陈述**（`:181`，全库唯一此类）· 选择区排布与滑动手感（批次规模 1–5，两端差 5 倍，竖屏同屏容纳 + 寿元告警静态标注）· `BaseTypeWeights` 取值 · 失败正反馈螺旋的容错量 N 与「典型失败」口径。**失真：`:195` 与 `:57` 自相矛盾（第二次报告）** |
| `systems/enemies/_index.md` · `enemies/common-properties.md` | **partial**（两份同批） | 就绪切片 = `EnemyData` 数据形态与全部加载期校验 + `TechniqueRef` 构筑面 + 游离散牌 + `KeyCardIds` 2–3 + `EncounterScopes` / `ChapterScope`（`ADR-0110`）/ `PoolScope` 与六条取池校验 + 展开产物不得写回条目 + item / power 持有列表（含 `IgnoresProtection` 两条硬准入）+ 编排护栏（赋级带 `±2` 无例外（`ADR-0044`）· 功法层数按篇章给基准档、逐条目偏离 ≤ ±1 档，核对落 `/audit-content` 只报告不阻断）+ **AI 兜底 1-ply 加权效用评分 + 确定性 argmax + `AiTerm` 十项 + 零随机零记忆 + 三条结构性上界**（`Value` 钳制越界 `PushError`）+ AI 策略字段六条校验（`ADR-0113` / `ADR-0120`；`KeyCardBias` 改名已四处改齐）+ `Lines` / `EnemyLine` 的稀疏覆写数组形态与三条校验。卡于：**`LineSlot` 成员清单仍为空**（`_index.md:131` 枚举体内为 ⟨待定⟩ 注释；`common-properties.md:39` 明写「本格在战斗 UX 专场答定成员之前不可填写」）⇒ **`Lines` 恒为空数组落笔**、不因此推迟整份 · 敌人各等级的道念产出缩放曲线 · 敌人条目目录整体为空 |
| `systems/monetization.md` | **partial**（缩切片处置延续） | 就绪切片 = `PlayerEntitlement` 两字段与不变式 `0 ≤ Redeemed ≤ Grant`（`ADR-0023`）+ **`BundleGrantOrdinal` 在 `ResourceElements` 表中无行、也不是 `CostKey` 成员**及其两条否决论据（承重）+ 空池三道闸 + 购买入口三条前置与灰态 + 兑现事务（`AccountRng.For(PremiumBundle, ordinal)` 一次派生连抽 3 条 → 一次 `TryApply` → `Immediate` push，`ordinal = Redeemed + 1` 逐一追平）+ 五项付费面排除 + 三渠道纳入 MVP（`ADR-0024`）+ 重试上限两档表由 `HasPremiumBundle` 选行（`ADR-0117`）与计数落 `chapterRetry`（`ADR-0101`）+ 唤起内购失败一律回主菜单、零 Profile 变更零痕迹 + **购买域失败的处置轴收于 `OpError.Purchase` 一档、只由 `purchase.*` 域映入、不建逐 `code` 表**（`:113`，`ADR-0140`）。卡于：**平台内购 SDK 的选型与封装层未定 ⇒ 购买段无法落地**（`:165`，跨边界，归后端支付渠道选型 + 一次客户端工程蓝图）· `K` 与 `GrantPoolMargin` 数值 · 两个通用池当前条目为零 · 纯外观付费点做成什么 · 合规交互归后端。**处置 = 缩小切片而非整份 blocked**：兑现段只读 `BundleGrantOrdinal`（后端写、经整份 profile pull 下行、不经 `TryApply`），不依赖任何未定的支付协议形状 ⇒ **可独立 derive；唤起内购整段排除在首批 FR 之外** |
| `ux/screen-flow.md` | **partial**（**就绪切片本次五处扩大**） | **新落笔五处**：① **轮回结束屏 `CycleEndScreen` 一屏三变体**（`:205`，`ADR-0148`）；② **篇章结束屏 `ChapterEndScreen` 一屏三变体、ch3 变体即元婴通关证书**（`:241`，`ADR-0143`：只用已定字段 · 不做站外分享与独立回看 ⇒ 零新增存档字段 / 零 schema bump / 后端零配合 · 允许一次 ≈1.2s 入场演出 + 全屏任意触点跳过 · 出口只有「返回主菜单」· `CYCLE_` 分区扩容为轮回收尾族，不新开 `CHAPTER_`）—— **上次的卡点「元婴证书形态」由此关闭**；③ **剧本段落进事件结算面板 + 分支全宽按钮**（`:300`，`ADR-0150` / `ADR-0151`）；④ **图鉴三层浏览：索引页 → 单本页 → 词条**（`:311`，`ADR-0147`）；⑤ 须改名模态与合规拦截的登录屏就地呈现。其余 = 登录屏 + 主菜单五入口导航骨架与 Store 三条呈现纪律 + 角色选择屏整段 + 功法候选卡的灵根属性标记 + 寿元显性化整条对位 + barter 呈现侧 + 建议更新横幅去重 + 储物袋全屏面板 + 玩家档案屏只读区 + 绑定 / 解绑二次确认 + Settings 屏 + 角色状态条 + 手势判据（`ADR-0085`）+ 各入口读取的两层 Profile 字段逐格可指。卡于（四条，`:370`）：成就两档发放何种奖励（依赖 `player-power` / `player-item` 条目，`content/` 零类型开张）· `PlayerPower` 获取 / 失去触发 · 寿元红字是否伴音效 / 震动 · Explore 揭示转场 1.2s 与短音效的真机校准 |
| `ux/error-and-blocking-ux.md` | **partial**（**两处零决策修正已落笔、跨边界排除面取消**） | **上次记的两处零决策修正均已完成**：① `:182` 的 `COMBAT_` 行已删 `intent`；② barter 灰显已进灰态判据表（`:208`，表由 4 行扩为 6 行）并进键清单（`:212`，两键 `<CONTEXT>` 取 `BARTER`、走 `EVENT_` 分区不占 `ERR_`），**`ADR-0142` 的「灰态是视觉降级不是禁用、灰格必须继续接收触控」同批落笔**（`:215`）。**跨边界已闭合 ⇒ 上次那半条排除面取消**：对侧 §5 / §10 / §11 已给出拦截四码与端点三码的 `code` × `reasonKey` 取值表，端点侧与拦截侧可一并写出完整验收。就绪切片 = 翻译键基建整条（自命名的 `FR-ux-translation-foundation` 五件事：`res://text/` 分区 CSV · `project.godot` 注册 + 默认 locale `zh` + `fallback = "zh"` · `ErrorText` / `TranslationAudit` · 键命名三条 + 禁令 · 三条审计与定死的调用顺序）+ `ERR_*` 由 `code` 机械变换、无第二张对照表（`ADR-0053`）+ `reasonKey` 二级键与前缀式反向审计 + 内容侧不烘焙可翻译文本（`ADR-0084`）+ 两层本地化（`ADR-0056`）+ `ErrorText.For` 三参与静默回落 + 灰态判据表 + `BlockingNoticeScreen` 一屏三变体与合规七码逐条判过「一条也不进本表」（`:322`）+ 三档版本提示只呈现最高一档 + 诊断编号出口。卡于：逐条中文措辞（内容充实，文档明写不阻塞结构）· `auto_translate_mode` 在 Godot 4.7 的默认行为待实测（两种结果下键形态 / 分区表 / 三条审计逐字相同 ⇒ 不构成排除面） |
| `systems/character-profile/item/common-properties.md` · `power/common-properties.md` | **blocked** | 两份的自有面均只有 `SourceCode` 一条（本层合法取值 + 读档兜底 `Unknown` + 本层无规则消费点）+ 三层分工回链，字段清单明写「不复述、权威在 `_index.md`」⇒ **无独立 FR 面**。唯一自有待决格 = **`status`（启用 / 禁用）×「拥有 / 失去」两正交维度的 schema 编码**（`item/common-properties.md:23`、`power/common-properties.md:23`），权威 `profile-service.md:420` 仍登记为未定。`power/common-properties.md` 全文 26 行、三个标准小节全缺。**09-02 / 09-03 / 09-05 三批一条落点也没有。⚠ `item/common-properties.md:3` 抬头仍写「占位结构，细节待定」——第三次报告** |
| `systems/player-profile/achievement/_index.md` · `achievement/common-properties.md` | **blocked** | 三条全开：成就条目 schema 与进度模型未设计（分组结构 · 加权进度的权重来源 · 触发条件表达 · 隐藏成就揭示时机）· 触发采集面未定（EventBus 被动订阅 vs 各服务主动上报）· 两档具体奖励条目清单未定。「能给什么」与「怎么给」均已答结（法则 / 古宝、指定条目 + `ExclusiveSource == AchievementReward` 不进任何抽取池，故成就奖励恒不落空）；缺的是目录本身，而它又依赖法则 / 古宝条目。**09 月三批零落点**；`profile-schema-versions.md` 明确把 `achievement` 留作占位、前置 = 本 schema |
| `systems/player-profile/player-item/_index.md` · `player-item/common-properties.md` | **blocked** | 类型面归 `ItemData`（两层共用）、存档面归 `player-profile/_index.md` ⇒ **无独立 FR 面**；自身的古宝种类目录 / 次数补充机制 / 可购价格与库存权重三项仍未设计（`_index.md:42`），`common-properties.md:23` 自陈共有字段「均为占位，无实质设计」。**09 月三批零落点** |
| `systems/adventure-event/_index.md` | **blocked** | 两条待决**只欠取值**（五类配比 `BaseTypeWeights` · Combat 内 `combatTier` 三档配比），运算形态已随 `ADR-0026` 落定；分类法与开局强制构筑归 `ADR-0002`，行为面已被 `future-event-service.md` 与 `adventure-event/common-properties.md` 的切片覆盖 ⇒ **不另产 FR，避免撞车** |
| `systems/adventure-event/combat/_index.md` · `combat/common-properties.md` | **blocked** | 五条待决全部指向战斗内容本体（卡牌产 / 削道念量纲 · 敌人平衡缩放 · `RewardPoolId` 取值与 `BaseReward` 其余 element 厚薄 · 叙事一致性写作口径 · 逐条目 `HiddenStatGrade` 编排）；**本轮两处收窄**：`BaseReward` 的灵石量已有初值（`Practice ≈ 0.5 × S` / `Standard = S` / `Finale ≈ 2.0 × S`，2.5 倍上界走加载期软检查）· 天劫指派等级改为平衡旋钮 `FinaleDiff`、下界恒 1 ⇒ 越阶叙事恒成立（`ADR-0144`）。`common-properties.md` 自身唯一待决随 `ADR-0120` 基本闭合，但两份的承载面全部由 `combat-service.md` / `scoring.md` / `enemies/` 承载 ⇒ **仍无可独立成立的切片** |
| `ux/combat-ux.md` | **blocked** | 文档实质内容不少（呈现事件流三消费者 · 战报收起 / 展开同一份数据两视图 · 条目粒度 = 结算事件 · 结构化存储渲染期才套翻译键 · 不落存档 · 固定预留 ≈ 屏高 6% · 结算节拍 0.35–0.5s / 敌人回合总时长 ≤ 4s · 持有物两层面板 · 埋伏只给计数 · 敌人等级用境界名），但**两条 `承重` 待决恰好落在唯一候选切片内部**：① 战报条目的文案体系（收起态单行信息量 · 截断量与 fizzle 如何在「单行 / 永不换行 / 超长省略」下表达 · 与飘字的分工边界，`:141`）② 竖屏分区整体是否过载、**栈是否必须常驻值得重估**（已排期专场，其结论会改写战报的落位与固定预留，`:144`）。加之另 7 条形态未定。**唯一可能独立成立的非视觉切片（呈现事件流契约）其自然宿主是 `combat-service.md` 的 `ActionResult` / `CombatFeedEntry` 切片，在此 derive 必与之撞车。** 08-30 记的「意图区与待决区双重记账」**已修**（`:11` 明写意图区整族移除，待决区零复登记） |
| `systems/player-profile/account-info.md` 之外的导航索引：`systems/_index.md` · `systems/services/_index.md` · `ux/_index.md` · `content/_index.md` | **blocked** | 导航索引 / 层级词表 / 登记表，**非 derive 对象**（各自的待决项已下沉到对应主题文档）。`services/_index.md` 的内容面本身**已就绪且无失真**（五级层级词 · 拆分轴 · 七服务 × manager 清单 · `profile-schema-versions.md` 这份非服务文档的登记亦在位），但它承载的权威在 `architecture.md` 与各服务文档 ⇒ **单独 derive 必与之重复，随第 3 步同批兑现、不另产 FR**。`content/_index.md` 持内容类型的 **scaffold** 就绪度（19 类型登记、开张列全为 ✗、与 `content/` 实际零失真），与本小节的 derive 就绪度是两套闸，勿混。**⚠ `systems/_index.md:45` 的 `combat-service` 行仍写「敌人 AI 与意图（三档揭示）」——全库唯一残留的「意图」正面登记，第二次报告** |
| `vision/pillars.md` · `vision/references.md` | **blocked** | 北极星 / 参考登记，**非 derive 对象**（其余文档的挂靠前置）。两份均无 `## 待决问题` 小节 ⇒ 挂靠侧零未决，判据③ 对全库成立 |
| `vision/scope.md` | **blocked** | 非 derive 对象 + **两项仍未定且无处承载、十六天来零推进**：① 美术资源 AI 生成的商用授权 / 参考素材版权立场（`art/_index.md:70` 把落点指向本文件，本文件「美术资源策略」段全文零处涉及）② **初装包体预算**（`art/soundtracks/_index.md:31` / `:37` 同样指向本文件，零承载；它向下卡住整个 `soundtracks/` 分区的时长 / 码率） |
| `terminology.md` · `program-overview.md` · `system-overview.md` | **blocked** | 根级词表 / 运行时调用链 / Godot 工程落地形态，**非 derive 对象**。三份均零占位、零待决且本次复核无失真：`terminology.md` 的「寿元」为合并后单一命线、灵石 / 仙玉双币与以物易物均在表；`program-overview.md` / `system-overview.md` 的 `ComplianceManager` / `MigrationManager` 在职责矩阵与目录树内均在位。低信号：`terminology.md` 未收 09 月新造的屏类名（`CycleEndScreen` / `ChapterEndScreen`）与旋钮名 `FinaleDiff`，但「词表是否收类名」的口径本身无处成文 |
| `art/_index.md` · `art/visuals/_index.md` · `art/soundtracks/_index.md` | **blocked** | 生产流水线说明与资产类目表（`ADR-0040`）；本库不承载生成产物（归 `game-feature-branch/`）⇒ **结构性地无客户端行为面可 derive**。未决：AI 生成资产商用授权（落点 `vision/scope.md`，零承载）· 参考素材二进制是否入库 · guide 粒度 · 生成资产落 `game-feature-branch/` 的目录划分 · UI 元件是否走 AI 生成 · **音频生成工具未定案（自陈「本分区第一前置」）** · BGM 时长码率的包体预算 |
| `art/visuals/art-direction.md` · `art/soundtracks/audio-direction.md` | **blocked** | 两份的 `## 一句话方向` 本身即 `> _（待写）_` ⇒ **判据① 即失败**。已成文的只有基调与三条硬约束；仍占位：色彩 · 光照与材质 · 构图余一行 · 尺寸与格式 · 禁用清单（视觉侧）；配器与音色 · 调式与节奏 · 动态与混音 · 格式与预算 · 禁用清单（音频侧）。`ADR-0100` Accepted 但明写逐项约束落点是本文件 ⇒ ADR 不构成对占位的覆盖 |
| `art/visuals/animations/_index.md` | **blocked** | 明写占位：范围四类 / 技术载体 / 制作方式 / 与战斗节奏与篇章时长预算的关系**全部「待咨询专业人士后确定」**，尚无设计意图。`ADR-0040` 只固定「动画不走 AI 流水线」这一条排除性结论。**`:20`「须可跳过 / 可加速」与 `ux/combat-ux.md:22`「只能加速不能跳过」两侧相抵，无一方标注让步** |
| `art/visuals/references/_index.md` · `art/soundtracks/references/_index.md` | **blocked** | `visuals/` 三条登记已细化到可转 prompt 的粒度，但前两行「素材位置」仍为待定（且与同文件「素材文件的存放（未定）」形成自指闭环）；`soundtracks/` 登记表整表为空——**一条参考都未登记** ⇒ 判据① 的实质内容不存在 |
| `art/visuals/guides/_index.md` · `art/soundtracks/guides/_index.md` | **blocked** | 台账均为空、**零份 guide** ⇒ 无 derive 对象；约定本身已成文，但上游两份 direction 均 blocked 且「guide 粒度」未决 ⇒ 连第一份 guide 都写不了 |
| `decisions/ADR-0002` ~ `ADR-0158`（157 份 Accepted） | **blocked** | 已采纳的决策记录，**非 derive 对象**（作为其余文档的就绪前置，本身不产 FR）。**计数经机械核对一致**：`decisions/` 下 **158 份** `ADR-*.md`，台账 **158 行**，双向比对「有文件无登记」0 条 / 「有登记无文件」0 条，扣除 `ADR-0001`（Proposed 示例）即 **157 份 Accepted**。本次由 131 增至 157：2026-09-05 一次 `/write-adr game`（范围 `all`）固化 `ADR-0133` ~ `ADR-0158` 共 26 份。**⚠ 这 26 份在全部主题文档中仅 3 处反向引用**（见上方台账缺口） |
| `decisions/ADR-0001-example.md` · `decisions/_index.md` · `requirements/_index.md` · `art/*/guides/_TEMPLATE.md` · `content/_TEMPLATE-*.md` · `requirements/_TEMPLATE*.md` | **blocked** | 示例占位（全库唯一非 Accepted 的 ADR）· 台账 · 流水线容器（`requirements/` 下仍**零 FR**）· 模板骨架（6 份），**均非 derive 对象**。`decisions/_index.md` 唯一噪音：抬头写「最新置顶」但 09-05 块内编号非单调（`ADR-0145`…`0133` 排在 `ADR-0158`…`0156` 之前），不构成缺登记或悬空 |

### 建议的 derive 顺序（仅限 ready / partial 项；被依赖者在前）

**落笔前的必须裁决：✅ 无 —— 队列连续第二次零前置。**
**落笔前的零决策修正（不需要任何裁决，但会影响产出正确性）：** ① `systems/_index.md:45` 删「意图（三档揭示）」（第 1 步之前，索引是找文档的第一跳）；② `exchange/` 同批两份的待决区对齐（`common-properties.md:213` vs `_index.md:170`，第 18 步前置）；③ `combat-service.md:589` 的可选奖励候选池追平 `power/_index.md:73` 的 `CombatReward` 口径（第 23 步前置）；④ `life-cycle-service.md:320` 的 `experiencePoint` 待决区措辞收窄（第 16 步前置，否则排除面被高估）；⑤ `account-info.md:51` 撤掉已被跨边界答死的待决问题（第 9 步前置）。

1. `/derive-requirements systems/common-properties.md` —— **唯一整份就绪且被几乎所有内容与服务代码依赖的地基，零前置。** 共有字段类型与两级 RNG 先落，后面每一份 FR 都能直接引用。账号级 RNG 的验收有现成的 8 组测试向量（后端 `contracts/profile-sync.md` §6a），是全库当前最硬的验收标准。文档自己写明排期——`LocalizedText` 与 `DrawPool<T>` 同批、**在写下第一批 `.tres` 之前**。**落笔时按 `Source` 十值写枚举与合法子集表。**
2. `/derive-requirements ux/error-and-blocking-ux.md` —— 翻译键基建（文档自己已点名 `FR-ux-translation-foundation`）。**无上游依赖**，是「每屏从第一行起就用键」这条起手纪律的前置，越早越省返工。**排除面仅剩逐条中文措辞——合规域整片不再排除**（拦截四码与端点三码的取值表在对侧均已成文）。
3. `/derive-requirements systems/architecture.md` + `systems/viewmodel.md` —— 服务骨架：七个 autoload 的注册顺序、EventBus、边界服务的接口 + Offline stub 双实现、`ProfileChangeSpec` 逐列、`OpError` 十值（含 `Purchase`）、`HiddenStatGrant` 三格。**两份同批**：三层切分的定义在前者、展开在后者，分两次必出两份互相打架的 FR。两份均 ready、无排除面。`services/_index.md` 的层级词与七服务 × manager 清单随本步一并兑现，不另产 FR。
4. `/derive-requirements systems/services/profile-service.md` + `systems/player-profile/player-power/common-properties.md` —— 写入通道这一层地基（逐列 spec + `TryApply` 原子性与钳制 + 逐列失败语义 + `Project` 只读投影 + `CapabilityManager` 整面 + modifier 合并算法 + 门面级 `Holds(...)`）。**两份必须同批**。**`CostKey` 写 15 行**（不写死列数）。**排除成就族与 `status` × 拥有 / 失去的存档编码。**
5. `/derive-requirements systems/character-profile/_index.md` + `systems/player-profile/_index.md` + `systems/player-profile/codex/common-properties.md` + `systems/player-profile/game-setting.md` + `systems/character-profile/currency.md` + `systems/character-profile/mana.md` —— 两层 Profile 的存档 schema 地基。**六份同批**：互相引用、共用同一个 `schemaVersion` 与同一条迁移路径。`CharacterData` 字段表（六格 + 十一条加载期校验 + `Affinities` + `RealmArtworks`）与**绑定功法初始层数恒 1** 随本步一并落。**只取存档 / 写入面，不取任何图鉴屏或设置屏呈现，不取任何货币绝对数字**（定价与给予量的权威在 `balance.md`，第 19 步再落）。
6. `/derive-requirements systems/services/content-service.md` —— 内容加载与校验链路（依赖 1、3）。被一切内容读取方依赖。flags 本地缓存与打包工具两项产出整段纳入。**仅排除 disabled 条目被存档引用时的 UX**（该分支须留空、不写 UI）。
7. `/derive-requirements systems/services/sync-service.md` + `systems/services/profile-schema-versions.md` —— 本地缓存原子写 + push 调度与退避 + CAS / 幂等 + 两层 diff + 回声校验完整字段面，**加上 v1 首发形状 33 条清单与 `ProfileShapeCheck` 护栏规格**（依赖 1、3、4、5）。**两份同批**（登记表是迁移器骨架的直接输入，且「别处一律回链」的纪律跨两份成立）。**排除 `achievement` 那一行占位、golden 快照的实际生成与打包管线闸**（挂在 `.csproj` 尚不存在上），**FR 文本里不得出现任何「v1 共 N 格」的计数型表述。**
8. `/derive-requirements ux/screen-flow.md` + `ux/onboarding.md` —— 登录屏 + 角色选择屏 + 主菜单导航骨架 + 储物袋面板 + 玩家档案屏 + **轮回结束屏 / 篇章结束屏 / 图鉴三层浏览 / 剧本段落进结算面板**（依赖 2、3、5）。**两份同批。** 结束屏两族须与第 16 步的摘要组装对齐（摘要字段面的权威在 `life-cycle-service.md` 侧）。**排除成就奖励、`PlayerPower` 触发、`LocationCodex` 单本页、全部待实测手感项。**
9. `/derive-requirements systems/services/account-service.md` —— 会话生命周期 + 四个账号方法 + refresh 两条路径 + `deviceId` 与 refresh token 落点 + 平台密钥库能力矩阵与五条升级触发 + 启动期静默续期 + 合规四域切分与拦截路径 + 失败映射逐 `code` 表（依赖 2、3、7、8）。`systems/player-profile/account-info.md` 随本步同批兑现、不另产 FR。**本次排除面清零**（对侧六端点报文表与端点错误码均已落笔）。
10. `/derive-requirements systems/character-profile/deck/common-properties.md` —— **效果原语层地基**：`EffectData` 子类树 + **八原语全量**（含补齐 `Side` 的 `MoveCardEffect`）+ `StaticModifierData` + `TriggerConditionData` / `TimingIds` + `EffectCondition` + `CardData` 收口 + **21 条加载期校验**。**它是五个内容类型的共同底座，必须先于 11–13。** 纯数据资源型切片、过三条闸。**排除 starter deck、抽 / 弃 / 洗数值与两份注册表的条目；无原语级排除项。**
11. `/derive-requirements systems/character-profile/deck/_index.md` —— `CultivationTechniqueData`（含 `RequiredAffinities` / `MaxCharacterAffinityCount`）+ 灵根准入判定式与四处取池点 + 成员卡反建索引 + `CardSubtypeData` + `CardPool` 三值 + 五个 `DeckChangeOp` + 手牌上限纯上界（依赖 5 的 `CharacterData.Affinities`、10）。**不得先于 5**（准入判定式跨两份文档）。**排除全部规模参数与首批清单；首批全单灵根，多灵根路径不实现。**
12. `/derive-requirements systems/character-profile/power/_index.md` —— `PowerData` 字段面 + **跨载体三行边界判据表（本表是三者边界的唯一权威）** + 强度比例刻度 + 内容编排口径表 + 三格非空校验 + 战斗外触发式校验 + 不得产寿元 + 禁用截断五行表（依赖 4、10）。**排除 `status` 编码、获取 / 失去频次与强度定量三格。**
13. `/derive-requirements systems/character-profile/item/_index.md` —— `ItemData` 两格使用效果面 + `MaxUsesPerCombat` + I-1 ~ I-13 校验 + 战斗外一次事务链路 + 随售通道（依赖 4、5、7、10、12）。**排除回寿护栏口径与道具目录**（回寿护栏是承重卡点，不得以占位数值代替）。
14. `/derive-requirements systems/adventure-event/common-properties.md` —— `EventOption` 数据形态 + 事件收口事务语义 + `lifeSpanCost` 三变异位 + Travel 双禁令 + `AddLooseCard` 取池链（依赖 1、5、10）。**它是后续全部事件类型 FR 的共同类型地基**，必须先于 18。**不取五类配比取值。**
15. `/derive-requirements systems/services/future-event-service.md` + `systems/services/plot-manager.md` —— eventOptions 十步管线 + 三道短缺闸 + `Priority` 抬升判据 + `HiddenStatGrant.Direction` 符号展开点 + `FinaleDiff` 旁路 + `TryGetPlotSegment` 只读转发 + `ChooseBranch` 提交形态与 `BranchLabel` 焊死，以及剧本调制输入。**两份必须同批**。**排除全部权重取值与隐藏属性的逐条推拉映射。**
16. `/derive-requirements systems/services/life-cycle-service.md` + `systems/character-profile/life-span.md` —— 轮回骨架、角色状态分类法、`DefeatReason` 三值与 Finale 旁路、存档点与决策点清单、`SeedManager` 四子流、批次层即时提交、**`DefeatCharacter` / `CompleteChapter` 两处结束摘要组装**，以及寿元的三条不变式与两个扣减来源（依赖 4、14、15）。**两份同批**（`life-span.md` 不单列 FR）。**排除 `lossPerMomentum` ch2/ch3 与回复三档的绝对点数（按 `balance.md` 候选值落笔并标注），排除 `experiencePoint` 验收侧的 N 值——曲线与分布已定，排除面按此收窄。**
17. `/derive-requirements systems/game-progression.md` —— 篇章 / 境界推进与 `eventCountLimit` 配额 + 经验阈值曲线 / `ExperienceGrade` 映射 / `FailureRatio` 折算 / 75% 覆盖率 + 「Finale 前必须升满」硬约束（依赖 15、16）。**排除选择区呈现、blind / ante（无形态）与 `BaseTypeWeights` 取值。**
18. 四个非战斗事件子类型，按依赖薄厚依次：`travel/` → `explore/` → `research/` → `exchange/`（各自两份同批；依赖 14、15、16）。**四份的机制面已闭合**；`travel/` 排除 location 运营替代通道（不得臆造闸门）· `research/` 排除 `MaxTier` 相关取值域与构筑面板竖屏呈现 · `exchange/` 的 barter 全套结构可整体进切片，但**须先对齐两份互相打架的待决区**，且已落表的 25 格定价与两档回收率**可作结构输入引用、不得把绝对数字写进验收条件**。
19. `/derive-requirements systems/balance.md` —— **第一批平衡资源 `.tres`**：已定值 / 已定形的那些表（含 25 格定价外积 · 两条货币产出曲线 · 两档回收率 · `lifeSpanCost` 21 格与预算 ×10 · `FinaleDiff` · 经验阈值全表 · `ExperienceGrade` 映射 · `lossPerMomentum` 篇章表（ch1 = 10，ch2/ch3 按候选值落并标注）· `SelectionWeightGrades`）+ `Content.Single<T>()` 读取面 + 加载期条数校验（依赖 1、6）。**只取已定格，全部未定格显式留空并标注；`lossPerMomentum` 三处口径须先收口成一处。**
20. `/derive-requirements systems/enemies/_index.md` + `enemies/common-properties.md` —— `EnemyData` 数据形态与全部加载期校验 + `EnemyAiProfileData` 与兜底 argmax + 两条护栏（`±2` / `±1 档`）（依赖 10、11、19）。**两份同批。** 纯数据资源型切片、过三条闸。**`Lines` 恒为空数组落笔**（`LineSlot` 待战斗 UX 专场），不因此推迟整份；**排除全部数值缩放。**
21. `/derive-requirements systems/player-profile/codex/enemy-codex.md` + `codex/technique-codex.md` + `codex/_index.md` —— 两本的词条规格与解锁触发 + **族级三层浏览与完成度口径**（依赖 5、8、11、20）。**三份同批**（连锁解锁横跨两本，排布由族总览定死）。**六本可写，`LocationCodex` 的单本页内容区（画图 / 词条载体）整块排除。**
22. `/derive-requirements systems/scoring.md` —— 道念计分规则与两条转换支路（负侧 = 道念差 × `lossPerMomentum`，ch1 = 10）（依赖 19）。**一组纯函数 + 一份平衡资源**，验收靠给定输入验算输出。**排除三档 `BaseReward` 其余 element 与 `RewardPoolId` 取值。**
23. `/derive-requirements systems/services/combat-service.md` —— 战斗机制骨架 + 效果流水线五阶段 + 决策点 D0–D7 + AI 两层结构与 `ViewerSide` 投影 + `StackEntryKind` 五值与栈条目 `itemId`（依赖 10、16、20、22）。**排除战斗内容本体与 Finale 奖励加厚取值；`MoveCardEffect` 方位面不再排除。落笔前先追平 `:589` 的奖励候选池口径。** 验证靠在编辑器里跑一场用占位内容拼出的遭遇观察栈与回合流转。
24. `/derive-requirements systems/player-profile/player-power/_index.md` —— 失去三形态 + 残卷 + 授予池结构（依赖 4、5、12）。**排除战后奖励池三张权重表、四支频次预算与第三条获取渠道。**
25. `/derive-requirements systems/monetization.md` —— **仅兑现段** + 三道闸 + 入口前置条件 + 兑现水位幂等 + `OpError.Purchase` 处置（依赖 1 的账号级 RNG、7 的 push 调度）。**排在最后**：依赖的两个通用内容池当前为空；**唤起内购整段等平台 SDK 选型。**

> **仍然不要**对成就 / 古宝目录 / `LocationCodex` 单本页 / `ux/combat-ux.md` / art 任何文档 derive。玩法侧的欠账收敛为三处：**① `status` × 拥有 / 失去的存档编码**（结构面，一次专场即可关闭，解锁面最宽）；**② 战斗内容本体**（条目目录整体为空，属内容阶段而非设计缺口，连带卡牌道念量纲与 `RarityTier` 分布权重两项）；**③ blind / ante 缩放曲线**（全库唯一连形态都没有的一条）。上次列的第三处 `MoveCardEffect.Side` 已关闭。

### 最短解锁路径（blocked 项，按解锁面从大到小）

1. **一次裁决：`status`（启用 / 禁用）×「拥有 / 失去」两个正交维度如何编码进 schema** —— **本次升为第一位**。能力四类同源，一次答齐即解锁 `item/common-properties.md` · `power/common-properties.md` 两份 blocked，并同时收窄 `profile-service.md` / `power/_index.md` / `player-power/_index.md` / `player-power/common-properties.md` 四份 partial 的排除面。**且它已在权威侧自相矛盾**（`profile-service.md:336` 有语义答案、`:420` 仍记未定），裁决成本因此更低。→ `/analyze-new-ideas`
2. **一次「战斗 / 事件 UX 专场」** —— 解锁面最宽的一次会：① `ux/combat-ux.md` 的两条承重（竖屏分区是否过载 / 栈是否必须常驻 · 战报条目的文案体系）② `enemies/` 的 `LineSlot` 成员（解锁 `Lines` 与三条相关校验）③ `research/` 的构筑面板竖屏呈现与风险档非 hover-only 通道 ④ `codex/_index.md` 的 `LocationCodex` 单本页画图形态。**一场会同时把 `ux/combat-ux.md` 送进 partial 并收窄三份 partial 的排除面。** → `/analyze-new-ideas`
3. **starter deck 设计过程** —— 它同时是四件事的切入点：卡牌产 / 削道念的量纲基准 · 关键字与次类型的首批清单 · `MaxTier` / 每层替换幅度 · `deck/` 两份与 `scoring.md` / `balance.md` 的最后一批取值。**它已取代货币渠道成为玩法侧最宽的取值前置。** → `/analyze-new-ideas`
4. **一次 handoff 定 `Achievement` 条目 schema + 进度模型 + 采集面**（EventBus 被动 vs 主动上报）—— 解锁 `achievement/` 两份，并收窄 `profile-service.md` / `player-profile/_index.md` / `profile-schema-versions.md`（`achievement` 占位行）三份的排除面；奖励目录另需法则 / 古宝条目先存在。→ `/analyze-new-ideas`
5. **一次 handoff 定古宝种类目录 + 次数补充机制 + 可购价格 / 库存权重** —— 解锁 `player-item/` 两份（类型面已由 `ItemData` 覆盖，无需再设计字段），并关闭 `item/_index.md` 的回寿法宝总量护栏这条承重卡点（它是寿元压力线的唯一剩余数量闸，且反向卡住 `life-span.md` 的依赖闭合）。→ `/analyze-new-ideas`
6. **`blind` / `ante` 缩放曲线的形态** —— 全库唯一「连形态都没有」的一条，需一次独立推演而非取值校准。→ `/provide-solution-draft` 后 `/analyze-new-ideas`
7. **`vision/scope.md` 的两条零承载项**（AI 生成资产的商用授权与版权立场 · 初装包体预算）—— 它们被 `art/_index.md` 与 `deferred-content.md` 指名落点为本文件而本文件零承载；答定后再开一次美术方向会与一次音频方向会填实两份 direction 的占位，`art/` 分区才可能开始产 guide。**art 台账类文档即便全部答完仍结构性地不产 FR**，不必期待它们进 derive 队列。→ `/analyze-new-ideas`
8. **平台内购 SDK 的选型与封装层** —— 跨边界，需后端支付渠道选型先落；关闭后 `monetization.md` 的唤起内购段才能进 FR。它是当前唯一未定的跨边界依赖。→ `/analyze-new-ideas backend`
9. `art/visuals/animations/_index.md` 明写「待咨询专业人士」，属**外部输入**，不在本库可自解锁范围；其「须可跳过」与 `combat-ux.md`「只能加速不能跳过」的相抵需在第 2 条那场会上一并裁掉。

## 下一阶段

- **ADR 状态：** `decisions/` 现有 **157 份 Accepted**（`ADR-0002` ~ `ADR-0158`，`ADR-0001` 为 Proposed 示例占位）。
  2026-08-28 的一次 `/write-adr all` 全量扫描（逐份核对 29 份未被任何 ADR 引为来源的 handoff）把散落其中、已落笔主题文档却无档案的定案固化为 **23 份新 ADR**（`ADR-0100` ~ `ADR-0122`），覆盖 sync 族（`revision` CAS · flush 非阻塞 · 闸门口径）· 命名通则 · 战斗 RNG 单流 · 敌人 AI 权重向量 · 效果原语语法 · capability flag / modifier · 道具使用效果面与批次层提交等。
  台账与逐条影响文档见 `decisions/_index.md`。
  2026-08-30 的一次 `/write-adr game` 固化 **10 份新 ADR**（`ADR-0123` ~ `ADR-0132`）：上述 4 条登记候选（灵根修习准入 · `Artwork` 单格与 `RealmArtworks` · 二进制不经 overlay · Exchange 以物易物）全部立档；另逐份核对未被任何 ADR 引为来源的 handoff，补固化 6 条散落定案（寿元合并与显性化 · `StatusChanges` 置值列 · `HiddenStatDirection` · flags 第三层覆盖来源 · `Upgrade` 非闸门点不硬阻塞 · `StackEntryKind.UsedItem`）。
  2026-09-05 的一次 `/write-adr game`（范围 `all`）固化 **26 份新 ADR**（`ADR-0133` ~ `ADR-0158`）：三条登记候选（货币跨篇章结转 · 灵石渠道口径与 Travel 禁令扩三 key · 仙玉不落可售出族）全部立档；另逐份核对 22 份未被任何 ADR 引为来源的 handoff，补固化 23 条散落定案，覆盖 09-02 / 09-03 / 09-05 三批（图鉴三层浏览 · 轮回结束屏与篇章结束屏 · 阵法启动式异能宿主 · 剧本分支落点与 `BranchLabel` 焊死 · 跨载体边界判据 · 神通强度刻度 · 快照按视角过滤 · 合规域切分判据 · `lifeSpanCost` 耗时正比定价 · 剧本层对外面 · schema 登记权威与不写计数 · `FinaleDiff` 旋钮 · 灰态非禁用 · `OpError.Purchase` · 打包工具两项产出 等），并补立两条早期基础定案（两层档案持有骨架 `ADR-0136` · 设计库类模型化组织 `ADR-0137`）。**当前待固化的 ADR 候选：暂无。**
  - ✅ 更早的一条候选「**不设 `GlobalBalanceData` 兜底大表，平衡资源按三问判据（消费者 / 覆写纪律 / 跨字段不变式）逐份切**」（`handoffs/2026-08-22-singleton-balance-resource-registry.md`）已于 2026-08-28 收口：`systems/balance.md` 补上了该判据本身那一小节（此前只有逐处应用），**并直接改写既有的 `ADR-0074`**（它本就是「凡可调数值一律住平衡资源」那条决策的档案，同一件事不另开编号——见本库「改决定直接改 ADR」的治理原则）；`ADR-0074` 后果里自陈的那条欠账随之关闭。**无需再跑 `/write-adr`。**
  （注：ADR 现可自由编辑，改决定直接改 ADR，不再新开取代 ADR。）
- **流水线闭环（07-30）：** design → code 链路补上 `/breakdown-requirements`（一份 FR → 一个文件夹的可执行子需求），完整形态见 `README.md` 与 `requirements/_index.md`。
- **架构闭环缺口：** 8 处**全部闭合**（移出记录见 `answer-logs/log-0725c.md` 与 `log-0726b.md`）；状态表见 `systems/architecture.md` 的「闭环缺口」小节。残留细节已下沉为各焦点分片的普通待决问题。
