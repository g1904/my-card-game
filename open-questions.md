# Open questions — 跨 session 待答清单（索引）

> 本文件是**客户端**（Godot 项目）待答清单的**索引**：问题条目本身按主题拆在 `open-questions/` 下的分片里。
> 后端侧的待答清单在 `backend-design-documents/open-questions.md`（`backend-design` 分支）。
>
> 每次 session 结束时，未答的 Open questions 汇总到对应分片，供下次拾起；一旦答定，就从分片中移除、
> 归档进对应主题文档的 `## 待决问题` / `## 决策`，并在 `answer-logs/log-<draftSuffix>.md` 记一笔。
>
> 本清单**只跟踪仍待答的问题**（不留已解决区），是导航 / 拾取清单，**权威归属在各主题文档**。
>
> 最近更新：2026-09-12b — 全量重整对账：移出 0 · 归拢 1 · 导航订正 3 · 矛盾登记 1（详见 `open-questions/update-log.md`；本次无 answer log）
> （逐次变更摘要见 `open-questions/update-log.md`；已答定问题的逐条移出记录见 `answer-logs/`）

## 分片导航

| 分片 | 内容 |
|------|------|
| `open-questions/update-log.md` | 每次运行的更新摘要（答结 / 推翻 / 新增落点），倒序，**只留最近 10 条**。不含问题条目本身。 |
| `open-questions/update-log-archive.md` | 更早的更新摘要，原样归档、按时间正序。只读，不写新条目。 |
| `open-questions/01-combat.md` | **① 战斗机制**（焦点之首）：能力剥夺与统计计数的残留（片区主体已于 08-10c 答结）、结构与配置（只剩 `EnemyManaLimit`——**`EncounterTighten` 界常量已答定为八格初值**）、内容与数值（多数留待内容扩充后的统计校准；**灵石账 `I(ch1)` 与战斗场数口径、法宝一族份额均已答定移出**）、呈现（形态已整体定案，**赋级带 `±2` / 层数散布 `±1 档` 已答结为「维持不放宽」**，只剩实测校准项与美术取值）。 |
| `open-questions/02-event-options.md` | **② eventOptions 生成流程**：主干（生成 / 加权、物化字段、优先级、寿元打穿、Explore 揭示池、Travel 出场）已全部答结，**只剩两条**——事件类型配比的实测校准复核（五格初值与 `Practice : Standard = 1 : 1` 已定）、`LocationCodex` 的词条深度与单本页画法。 |
| `open-questions/03-adventure-event-types.md` | **③ 逐类型 AdventureEvent 的收口后残留**：五类机制面已全部收口（各自开过专场），片内只剩**只欠取值 / 只欠呈现形态**的格——Travel 定价的实测复核（已有初值 6 / 7 / 18）、Exchange 两组数值格、Explore **两个**待定取值（定价行已并入真身占比）、走火入魔权重。 |
| `open-questions/04-hidden-attributes-plot.md` | **④ 隐藏属性 / 剧本机制**（档位表 · 跨档叙事 · 数据编码 · 寿元增长通道 · **属性清单定稿两项**均已答结；推拉触发亦已于 09-09 答结，逐条目映射与两条剧情线的内容为纯内容编排工作量、不再单列条目）：**只剩三条**——`HiddenStatGrade` 三个映射值，外加**角色专属 SideStory 的激活口与 `CharacterIds` gating 语义**两条工程推演题（同批答，归一次 `/provide-solution-draft`）。 |
| `open-questions/05-service-contracts.md` | **⑤ 服务契约 / 工程侧残留**（API 面 · `revision` · 契约边界 · 翻译键 · 共有属性分层 · 纪律可执行化 · `schemaVersion` 登记表的成就覆盖均已答结）：location 无法被 flags 秒关的运营替代通道、**首次生成 `.csproj` 后的三项实测**（`#if DEBUG` 判据 · `Control` 自动翻译 · 平台凭据存储能力）、v1 golden 形状快照待建。 |
| `open-questions/06-meta-progression.md` | **⑥ 元进程的失败侧 · 图鉴族与商业化**（中长期规划感三角、四支频次预算与失败螺旋容错量均已于 09-06 答结；辨识度三条已于 09-10 收窄为数值 / 实测半；胜侧 `experiencePoint` 单价计入供给账已于 09-10 答结移出）：强度塌缩实测、双灵根批对铺量、分层梯度逐档比例。商业化那半（**载体形态、推出次序与包装原则、叙事轨道无关性均已于 09-12 答定**）：只剩付费系列「在售窗口」落哪个载体、各系列具体名字与主题、双灵根批时点、进阶批复杂度的表达。 |
| `open-questions/cross-boundary.md` | **跨边界承接**：对侧（后端）已定案、本库尚未落笔的条目。**不是待答问题**——答案已有，等的只是落笔；形态、关闭条件与维护者分工见分片抬头。 |
| `open-questions/deferred-content.md` | **已搁置：内容充实**（07-30 起暂不推进）＋ **美术与音频（`art/`，08-04 加入）** ＋ 随内容搁置的 UX 呈现细节 ＋ 尚未设计的占位主题。 |

## 当前焦点：各系统机制细节

> **焦点判据（07-30 定）：** **规则、字段语义、流程与算法 = 机制细节 = 焦点**（分片 ①–⑥）；
> **具体条目目录与数值 = 内容充实 = 搁置**（`open-questions/deferred-content.md`）。
> 与既定开发路线「框架 → 内容 → 平衡与体验 → 社交及其他」的第 ① 阶段一致。
> Source: `handoffs/2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown.md`。
>
> 焦点顺序即分片编号顺序；**① 战斗机制**优先级最高。

## derive 就绪度

> **本小节由 `/assess-derive-readiness` 独占写入**（`/analyze-new-ideas` 与 `/summarize-open-questions` 均不得改动）。就绪度是全库横切判断，须基于一次性全量扫描才有意义；逐次 handoff 顺带的评估会迅速过时且互相矛盾。

**最近全量评估：2026-09-12（由 `/assess-derive-readiness` 产出）。** 扫描范围：`vision/`（3）· `systems/**`（55）· `art/**`（10，不含 2 份 `_TEMPLATE`）· `ux/`（5）· **`narrative/**`（1 —— 本次新增的顶层分区）** + 根级横切 3 份 = **主题文档 77 份**（较上一版 76 份 +1，唯一增量是 `narrative/_index.md`；`content/_index.md` 与 `content/character/_index.md` 属内容层登记表 / 类型档案，另计）。机械对账：`decisions/` **284 份文件 ↔ 台账 284 行**，双向零缺口、零重号、编号连续，**283 份 Accepted + `ADR-0001` Proposed 占位**；`requirements/` **仍零 FR**（3 个文件全是模板与台账）；`content/` **仍零条目**，类型登记表 **20 行**（+1 `character-series/`）、**开张列首次出现 1 个 ✓**（`character/`，2026-09-12 开张）；`inbox/` 顶层在办草稿 **0 份**；`handoffs/` **189 份实际 handoff 中 188 份 `distilled`**，唯一 `raw` 是 `2026-07-12-example.md`（示例占位）⇒ 无未消化输入。本次重估的直接触发是同一会话波次 1 的一次 `/write-adr game`：固化 `ADR-0269` ~ `ADR-0284` 共 **16 份**，并**就地改写 `ADR-0255`**（系列轨道棘轮 单向 → **双向**，不新增编号）。**但改动面更宽的是上一版评估之后落地的两轮提炼**：09-12 的 `/batch-analyze-new-ideas`（5 份已评审草稿，含两处结构性推翻）与 09-12 的系列包装十轴专场（新建 `narrative/` 分区）。五个分片并行只读评估，逐份给证据行号；两处事实矛盾以直读裁决（见文末）。

**全局结论：ready 31 份 · partial 30 份 · blocked 16 份（主题文档 77 份口径，不含 283 份 Accepted ADR 本体与台账 / 模板 / `content/` 两份）—— ready 由 29 增至 31，partial 由 32 减至 30，blocked 由 15 增至 16（增量即新分区 `narrative/_index.md` 本身，不是任何文档降档）。三处升档、一处降级。实质变化有三条：① 上一版记为「全库唯一反向变化」的付费角色系列排除面，**在同一批里被整条兑现** —— 九份 ADR 把载体 / 字段 / 校验 / 购买流程 / 呈现面逐格落定，`character-profile/_index.md` 因此升 ready、`monetization.md` 的结构性卡点消失；② `life-span.md` 与 `item/_index.md` 各自最后一个卡点被 09-11 / 09-12 两轮答结（`lossPerMomentum` = 5 / 10 · 法宝份额 ≈0.35），双双升 ready；③ **唯一降级**：`ADR-0283`（剧情不是付费面）落笔时暴露出一个此前无人承接的机制缺口 —— 角色专属 `SideStory` 的激活口，`plot-manager.md` 由 ready 降 partial。**

- **✅ 付费角色系列由「首批一格不落」变为整段可 derive —— 上一版那条反向变化本次整条兑现。** 九份 ADR 逐格落定并已进正文：`ADR-0269`（`CharacterData` 加 `SeriesId` + `Track`，**必须是带哨兵的枚举而非 `bool IsPaid`**——`[Export]` 未填即取 0，`bool` 的漏填会静默落进免费轨道而轨道发布后不可变；加载期校验由十三条增至 **#14 ~ #18**，其中 #16 / #17 / #18 一律走 `AllIncludingDisabled()`）· `ADR-0270`（`PlayerEntitlement.CharacterSeries`，**写入方只有后端**、客户端无写入通道、不配兑现水位，`schemaVersion` **v2 —— 本库第一次真实 bump**）· `ADR-0271`（回声路径「老档缺字段」分迁移 / 读档两时点，对 `Cosmetic` 同款适用，并明写日后新增受回声约束的字段一律套用本条、不再单独出题）· `ADR-0272`（解锁过滤落 `life-cycle-service.GetSelectableCharacters()`，签名不变、`StartCycle` 零改动、完全不涉 RNG，**绝不写成 `AllEnabled(accountContext)`**）· `ADR-0273`（`Track == Paid` 禁止永久退役、flags 临时关闭允许）· `ADR-0274`（购买段复用、**兑现段整段不存在**）· `ADR-0275`（在售清单内容层自给、`productId` 机械变换、前置条件表两段化 + 三行）· `ADR-0276`（限时在售窗口三条语义）· `ADR-0277`（推销面穷举：Store 三结果态、角色选择屏完全不出现未拥有项）。⇒ **`character-profile/_index.md` 升 ready、`monetization.md` 的结构性卡点消失，第 27 步的排除面由「整段不落」缩到 `K` 阈值与内容取值。**
- **✅ `life-span.md` 由 partial 升 ready —— `## 待决问题` 现明写「无」（`:57`）。** 上一版唯一残留的 `lossPerMomentum` ch2 / ch3 已于 09-11 定案为 **5 / 10**（三章三格全为定值、走恒定占比而非递进；形状锚逐格 9% / 10.0% / 11.3% 通过；N = 2、21 格 `lifeSpanCost` 定价表与 λ 台账零改动）。
- **✅ `item/_index.md` 由 partial 升 ready —— 上一版新生的那个数值格已答定。** 法宝置换在「失去能力」上层 ≈1.0 分子中的份额定为 **≈0.35**，并连带一次预算重新配平：守住 ≈1.0 天花板、其余三支等比压缩 ×0.65（神通 ≈0.32 · 法则置换 ≈0.20 · 法则禁用 ≈0.13），落位**并入既有支、不开第五行** ⇒ `ADR-0265`「四支枚举不扩为五支」逐字成立。残留 `:198` 是内容阶段的道具种类目录，按本表既有口径（`player-power/_index.md:171` 同款）不构成排除面。
- **✅ 两处结构性推翻已在正文收口 —— 不改任何判定，但改 FR 正文内容，落笔时不得沿用旧文本。** ① **`EncounterTighten` 十常量 → 八常量**：手牌上限恒 7 且**任何编排面都动不了**（剧本与事件模板皆不可覆写），`MaxHandLimitTighten` / `MinHandLimit` **取消**（是不存在，而非取 0），`ADR-0077` 已就地改写；`balance.md:1243` 已按八常量口径重写。② **`EncounterSpec` 可空覆写组 四格 → 三格**（`InitialDraw` / `DrawPerTurn` / `EnemyManaLimit`，`HandLimit` 一格删除），`future-event-service.md` 的物化代入面同步由五格改四格。另有两处授权订正：`I(c)` ≈150 / 300 / 480（ch1 占 27%）· 战斗场数 11 / 11.9 / 14。
- **⚠ 本次唯一的反向变化：`plot-manager.md` 由 ready 降 partial。** `ADR-0283` 自陈「暴露出一个机制缺口（已挂待答）」：现有两条 arc 激活通路是 `PlotTriggerId` 与篇章边界，而「角色专属、不由隐藏属性触发」的 `SideStory` **一条都不覆盖** —— 命中判据⑤「无孤儿路径」。连带第二条：`CharacterIds` 的 gating 语义（何时比对、不匹配时是不激活还是惰性）。两条同批归一次 `/provide-solution-draft`（`ADR-0283` 自己指定的下一步）；关掉它 `plot-manager.md` 立即回 ready、第 17 步排除面清零。

**仍在的承重卡点（一条 —— 与上一版同一条，仍零推进）：**

- **🔴 `vision/scope.md` 的两条零承载项：AI 生成资产的商用授权 / 版权立场 · 初装包体预算。** 机械复核（本次重跑）：全文 `包体` / `授权` / `版权` / `商用` **四词仍零命中**，且该文件**仍没有 `## 待决问题` 小节**（现有小节只有 `:3 MVP` / `:18 范围之外` / `:30 平台与约束` / `:42 美术资源策略` / `:48 开发顺序` / `:70 时段形态`）；`art/_index.md:70` 与 `art/soundtracks/_index.md:42` 双双指名它为落点，而它零承载。**它一条串起六处 blocked**：`art/_index.md` · `art/visuals/references/_index.md` · `art/soundtracks/references/_index.md` · 两份 `guides/_index.md`（台账仍零份）· `art/soundtracks/_index.md`（其「第一前置」= 音频生成工具定案，而工具的商用条款正是本条），并同时卡住两份 direction 各余的「格式与预算」格。**一次落笔可同批解开六处，连续第三次是全库投入产出比最高的一次裁决。**

**次承重（非结构、但反向卡住多份的依赖闭合）：一条 —— 连续两次为空后本次重新出现。**

- **🟠 角色专属 `SideStory` 的激活口 + `CharacterIds` 的 gating 语义**（`plot-manager.md:616`–`:617`）。它单独把 `plot-manager.md` 从 ready 打回 partial，并因「须与 `future-event-service.md` 同批」而给第 17 步整批加上一个排除面。归一次 `/provide-solution-draft`，是本次唯一新增的结构性缺口。

**台账 / 投影缺口（不阻塞 derive，但会误导落笔；权威在主题文档，编辑归用户 / `/analyze-new-ideas`）：**

- **🔴 `## 决策(-> ADR)` 回链缺口 —— 第七次报告，本次继续恶化。** 机械核实（对 77 份主题文档全量 grep 后去重）：**`ADR-0183` ~ `ADR-0284` 共 102 份新 ADR，在全部主题文档中只有 `ADR-0183` 与 `ADR-0255` 两个编号被引用到，其余 100 份反向引用为 0**；全库主题文档引用到的**最高 ADR 号即 `ADR-0255`**（唯一一处在 `profile-schema-versions.md:25`）。**本批 16 份新 ADR 无一被任何主题文档回链**，而它们的实质内容**已全部落进正文** —— 缺的只是编号。另有 **`ux/screen-flow.md` 与 `ux/onboarding.md` 的 `## 决策(-> ADR)` 是空壳小节**（只有标题与模板提示行），而 `screen-flow.md:125`–`:129` 正承载着 `ADR-0277` 的全部实质。**`decisions/_index.md` 的「落点」列仍是两侧之间唯一的导航，且是单向的。**
- **🔴 新分区 `narrative/` 的三处登记缺口。** `narrative/_index.md` 全文 38 行**零 ADR 引用**，而 `decisions/_index.md` 有 **6 份 ADR**（`ADR-0278` ~ `ADR-0283`）把它列为受影响文档；它**没有 `## 意图` / `## 决策(-> ADR)` / `## 待决问题` 三个标准小节**；其 `:16` 明写的未定项（本分区的内部结构与文档清单）只登记在 `open-questions/deferred-content.md`，本文档自身无待决节可挂。另两处「一侧独写、对侧无呼应句」的可补回链已由 ADR 自己点名：`ADR-0279` 的 `codex/_index.md` · `ADR-0281` 的 `achievement/_index.md`（两处 ADR 均标注**不改变结论**）。**登记面本身是齐的**：`README.md:54` 文件夹图例与 `.claude/rules/design-library-routing.md:62` / `:68` 的两库结构差异表均已加行。
- **🟠 落笔前的零决策修正（本次登记 12 处，较上一版 13 处净减 1；清掉 4 处、新生 3 处）。** **已清掉的四处**（逐条 grep 复核，零命中）：`travel/_index.md:60` 的旧定价 9 / 10 / 25 与 23 / 25 / 63 · `terminology.md` 的旧篇章时长 30–40 / 35–45 / 45–55（`:92` 现为 45–55 / 50–60 / 60–70）· `ux/combat-ux.md:23` 的旧篇章时长 · `adventure-event/common-properties.md:85` 的旧时长。**本次会写进 FR 正文、必须先清的有四处：**
  ① **`balance.md:1240`**（行号由 `:1208` 后移）仍写「回寿量三档的绝对点数……**折算不等于定案**——三档点数仍欠取值」，而 `ADR-0248` 与 `life-span.md:50` 已把 **50 / 100 / 200、三章通用、不按篇章分条目**写死 —— **第二次登记，一轮未清**（见文末直读裁决）。
  ② **`player-power/_index.md:170`**（行号由 `:159` 后移）仍把「战后奖励池的各档权重与内容侧每档条目数编排口径」列为待定，而 `ADR-0237`（九行权重表 × 篇章乘数 `m(c)`）与 `ADR-0242`（条目数矩阵是编排目标非不变式）早已关闭 —— **第二次登记，一轮未清**。
  ③ **`life-cycle-service.md:398`** 仍写「仍待定：**`Achievement` 的条目 schema 与进度模型**、`PlayerItem` 的解锁 / 获取 / 失去触发」，而前半已被 `ADR-0196` ~ `ADR-0200` 与 `profile-service.md` 的采集面两支整条推翻 —— **本次由一般项升为必清项**（它是「该文档唯一实质残留 = `PlayerItem`」这句判定的直接反证）。
  ④ **`terminology.md:123`** 的「角色系列」词条仍写「一旦免费永不改付费」是**单向棘轮**，与 `ADR-0255` 改写后、`character-profile/_index.md:102` 与 `content/character/_index.md:55` 明写的**双向**口径正面相抵。
  其余八处属陈旧待决项 / 悬空指针 / 相抵旧句 / 版式错位，不改不会写错 FR 但会误导查阅：`monetization.md:250`–`:252` **三处**「ADR 候选待 `/write-adr` 立档」（该批已于 09-12 立档）· `content-service.md:491` 同款「ADR 候选，待 `/write-adr` 立档」应改为回链已 Accepted 的 `ADR-0182` · `life-cycle-service.md:400` 的「隐藏属性仍待定的那一项（增减触发）同样登记在 plot-manager」—— plot-manager 现存三条待决中**没有**这一项 · `profile-service.md:513` 的泛指措辞「各账号级条目的解锁 / 获取 / 失去的具体触发未定」须收窄至 `PlayerItem` · `player-item/_index.md:42` 与 `player-item/common-properties.md:24` 的「可购价格 / 库存权重未设计」（已定案）· `player-item/common-properties.md:13` 的「预期共有字段含价格 / 成本」与 `_index.md:10`「`ItemData` 上不加 `Price` / `Purchasable`」直接相抵 · `program-overview.md` §2 职责矩阵经 grep 复核仍**零命中** `StoreChannelManager` / `CodexManager` / `GrantPoolManager` 三者（权威在 `services/_index.md:50`–`:51`）· `research/_index.md:95` 是一条**已定案句错位挂在 `## 待决问题` 节下**（`MaxTier = 5` 已定），宜移出该节 · `exchange/_index.md:172` 与 `exchange/common-properties.md:232` 是同一条待决的两份副本（宜收成一句回链）。
  **上一版登记、本次未逐条复核**（不构成落笔风险，留待下次）：`profile-schema-versions.md:130` 的「对应」指向 · `art-direction.md:3` 的标题粘连 · `vision/scope.md:46` 的孤立空 bullet。
- **🟡 低信号（口径本身无处成文，不判为失真）：** `codex/common-properties.md` 的「无待决」仍是由缺节推得而非明写；`power/common-properties.md` 全文 26 行、**仍缺三个标准小节**（本次复核确认：现只有 `:5 ## 已定的约束` / `:18 ## 字段清单` / `:25 ## 对应`，这是它未升 ready 的唯一理由）；`game-progression.md` 的 `## 待决问题`（`:214`）**小节体完全空白**——既无条目也无「无」字，全库唯一一处（按缺节推得为零待决，判 ready 不受影响）。

**跨边界闭合（强制检查项）：本次零新欠账；上一版那条已两侧对称关闭，并经本次逐项直读复核。**

- `sync-service.md` → `contracts/envelope.md` · `contracts/profile-sync.md`：**闭合**。
- `account-service.md` → `contracts/auth.md` · `contracts/compliance.md`：**闭合**。
- **付费角色系列 → `contracts/purchase.md` · `contracts/profile-sync.md` · `operations/purchase-ops.md` · `operations/version-matrix.md`：闭合（本次逐项直读复核对侧命中位置）。** 对侧逐项承载：SKU 类别与系列级粒度 · `productId ↔ seriesId` 机械变换 · 验票写入按 `kind` 分流与幂等三格 · **后端写入字段封闭表成文以来的首次扩表** · 「只增不删 / 元素唯一」的服务端保证 · 限时在售窗口的 SKU 表字段与窗口外拒绝语义 · 兼容矩阵 `schemaVersion = 2` 行（**矩阵先加、客户端后发**）。两侧各一份互相回链的 handoff，两侧 `cross-boundary.md` 对本条均已移入「对账基线」。
- **`content-service.md` → `contracts/content-manifest.md`：契约侧闭合，本库仍欠两条承接**（`open-questions/cross-boundary.md:23`–`:24`，均为对侧 2026-09-07 定案，**连续两版一字未动**）：`backend ADR-0051`（manifest 请求路径由客户端自行选路）· **`backend ADR-0052`（CDN 域 4xx 按「入口对象 / 入口之后」两分处置，其中「不对 4xx 走退避重传」是对本库既有下载重试语义的一次收窄）**。机械核实：`content-service.md` 全文 `ADR-0051` / `ADR-0052` / `4xx` **三词零命中**，而 `:222`–`:228` 的既有指数退避语义原样在册 ⇒ 两处并存即互相矛盾。**`content-service.md` derive 前必须先落这两条。**
- 客户端侧待承接 **2 条**（与上一版持平）；**后端库 `cross-boundary.md` 的「待承接」为 0 条**（该库 `open-questions.md` 同步声明）。

### 判定表

| 文档 | 判定 | 卡点 / 就绪切片 |
|------|------|------------------|
| `systems/common-properties.md` | **ready** | 待决明写「（无）」（`:348`）。全库地基、derive 第 1 位、零前置。整面 = 稳定 `Id` 与展示字段三层切分 · 物化模型 · `LocalizedText` · `ContentEnabled` + `AllEnabled()` 双名与编译闸 · `RarityTier` 五档 · `Artwork` 可空 + 占位回落 · `SourceCode` + `Source` 十值与合法子集表 · 两级 seeded RNG（轮回级四子流 + 账号级 SplitMix64，8 组测试向量在对侧 `contracts/profile-sync.md` §6a）· API 契约三形态 / 三失败语义 · `deviceId` 落点 · **分域校验表的三格规则层封死**（`:304`–`:316`，`ADR-0267`：账号级两类都不接 `EventOutcome`、法则另不接 `ExchangePurchase`，`※` 脚注已删） |
| `systems/architecture.md` | **ready** | `:743` 明写「当前无未决项」，架构体检 8 处缺口全闭合（`:724`）。整面 = 三层切分 · 七服务边界与 autoload 注册顺序 · EventBus 总则 · 八条 API 契约总则 · 边界服务双实现骨架 · `OpError` 十值 · `ElementSpec` / `ModifierKey` / 钳制表 · `ProfileChangeSpec.AbilityStatusChanges` 列与「恒不走 modifier pipeline」· `CostKey`/`StatKey` 成员名构成契约只可追加 · 三级纪律判据。**须与 `viewmodel.md` 同批** |
| `systems/viewmodel.md` | **ready** | `:80` 明写「当前无未决项」。整面 = 展示层第三层结构契约（呈现期对象 · 不落存档 / 不进云端负载 · 单向依赖且服务 API 面永不返回 ViewModel · 组装源三件套 · 三条重组装触发面 · 永不渲染清单 · `Artwork` 两级回落）。**须与 `architecture.md` 同批** |
| `systems/services/_index.md` | **ready** | `:80` 明写「各服务的残留待决项见其各自文档；API 契约本身已定案」。整面 = 五级层级词 · 拆分轴 · 七服务 × manager 清单（含 `StoreChannelManager` `:50` 与 `CodexManager` / `GrantPoolManager` `:51`）· `profile-schema-versions.md` 的台账例外说明。**权威在 `architecture.md` 与各服务文档 ⇒ 随第 3 步同批兑现、不另产 FR** |
| `systems/services/account-service.md` | **ready** | `:348` 明写「本文档当前无待决项」。跨边界双向确认闭合（对侧 `contracts/auth.md:573` / `:579`–`:583`）。整面 = 会话生命周期 + 四个账号方法 + refresh 失败拆两条路径 + `AccountInfo` 五字段 + `deviceId` 与 refresh token 落点 + 启动期静默续期 + 平台密钥库四端能力矩阵与五条升级触发 + `ComplianceManager` 四域切分与「不做任何判定」纪律 + 实名表单两条输入侧纪律 + 合规拦截路径与失败映射逐 `code` 表。**`account-info.md` 随本份同批兑现** |
| `systems/services/sync-service.md` | **ready** | `:402`–`:404` 仅剩 `pushId` 后端记忆窗口，明标「属后端侧参数、客户端语义已定」。整面 = 存档点 ↔ push 解耦 + 5 秒防抖 + 立即 flush 清单 + `user://cache/` 原子写 + schema 版本化与两种清晰拒绝 + `CharacterProfile` 级 diff + `revision` CAS / `pushId` 幂等 + 回声校验 + 断线降级与退避阶梯 + JSON camelCase 单点命名策略 + 购买段两条腿（`StoreChannelManager` / `IStoreChannel` / 第四个窄接口 `IPurchaseBackend` / 运行时探测不走 `#if` / 购后强制 pull）。**须与 `profile-schema-versions.md` 同批** |
| `systems/services/future-event-service.md` | **ready** | `:542`–`:546` 待决为空（五类配比与 `combatTier` 配比转指 `balance.md`，管线与公式零改动）。整面 = eventOptions 十步管线 + 候选短缺三道闸 + `EventOutcomeSpec` 字段面 + 置换 / 禁用候选物化时掷定 + `Priority = 1` 抬升判据 + `HiddenStatGrant.Direction` 符号展开点 + Exchange 库存物化与 Explore 取池过滤 + 灵根准入叠入取池链 + `FinaleDiff` 旁路 + 遭遇参数五格代入 + `TryGetPlotSegment` 只读转发 + 「本服务纯本地、全部方法形态 A」。**须与 `plot-manager.md` 同批** |
| `systems/services/plot-manager.md` | **partial**（**本次由 ready 降级 —— 全库唯一的反向变化**） | 上一版整份 ready；`ADR-0283`（剧情不是付费面）落笔时**暴露出一个此前无人承接的机制缺口**，`## 待决问题` 由 1 条增为 3 条（`:615`–`:617`）：`:615` `HiddenStatGrade` 三个映射值（`Minor 2 / Standard 5 / Major 10` 已在 `balance.md:795`，属统计校准、**不构成排除面**）· **`:616` 角色专属 `SideStory` 的激活口** —— 现有两条 arc 激活通路是 `PlotTriggerId` 与篇章边界，「角色专属、不由隐藏属性触发」的 SideStory **一条都不覆盖** ⇒ 命中判据⑤「无孤儿路径」· **`:617` `CharacterIds` 的 gating 语义**（何时比对、不匹配时是不激活还是惰性）。两条同批归一次 `/provide-solution-draft`（`ADR-0283` 自己指定）。就绪切片仍是上一版整面 = `PlotArcData` / `PlotNodeData` + `PlotModulation` 六字段与悬空校验（**新增 `PlotArcData.CharacterIds` 悬空 → `PushError`**）+ 多 `Active` arc 合并算子 + `EncounterTighten` 增量的方向约束与 `Finale` 整档豁免（**现为八常量口径，`HandLimitDelta` 字段已删**）+ 档位表 / 阈值 / 回滞 / 跨档叙事 + 档号方向承重定义 + 隐藏属性不进 `selectCost` + 战斗层零耦合 + 剧本树不分包 + 分支落点与 `BranchLabel` 焊死 + 门面投影 1 → 2 + 两条隐藏属性剧情线 `SideStory` 形态 + **剧本节点正文是碎片 lore 的唯一载体**（`ADR-0279`）。**须与 `future-event-service.md` 同批** |
| `systems/services/profile-schema-versions.md` | **ready** | `## 待决问题` 唯一残留 `:130` = golden 快照待建，文中自证「设计形态已完整、卡的是落地时点（`.csproj` 尚不存在）、**不阻塞任何 derive**」（`ADR-0270` 另记 `profile-shape-v2.json` 同款待建）。整面 = v1 首发形状逐条清单（逐条带权威回链，含 #35 `achievement` / #36 `achievementGroup` / #37 `ProfileChangeSpec` 两列）+ 形态纪律六条（本表不写任何计数）+ `ProfileShapeCheck` 规格整套 + **本次新增 `schemaVersion = 2` 行**（`:23`：新增 `characterSeries`、v1 → v2 迁移器写空列表、迁移之后仍缺即走回声路径的真异常处置；命中形态纪律 ⑤ 第三档 ⇒ 两侧同批落笔并进版本行，**不能**走「不透明段内追加不 bump」那一档；对侧兼容矩阵**先加、客户端后发**）。**条件分支须原样写进 FR**：若付费系列改在首发之前落地，按「首发前的一切改动全部归入 v1」并入 v1 清单、不产生 bump —— 两条路径的结构一字不差，只是登记位置不同。**须与 `sync-service.md` 同批** |
| `systems/character-profile/currency.md` | **ready** | `:43` 明写「无」。整面 = 双币轮回级定位与取值域两行 `ResourceElements` · 跨篇章结转、篇章边界零货币动作 · 灵石获取口径 = 战斗 · 仙玉唯一主动获取 + 四条否定边界 · 花销唯一出口 = 高阶 Exchange · 两币完全不可兑换、售出同币回收因而不产生事实汇率 · 两个修正列恒 `null`。**derive 时不取任何绝对数字**（权威在 `balance.md`） |
| `systems/character-profile/mana.md` | **ready** | `:65` 待决为空，两项数值已转记 `balance.md`。整面 = `manaLimit` / `currentMana` 字段面 · 炼气基线 5/5 不结转 · 每回合恢复三步节拍 · 上限两条来源走 `CostKey.ManaLimit` 增量语义 · `RealmBreakthroughManaBonus = 1`（`:13`）· 费用曲线 `avgManaCost ≈ 5 × manaLimit / 14 ⇒ 2/3/4`（`:39`，`ADR-0184`）· 不设下界护栏 / 死牌转化 · 下降唯一承载点 = 闭关自选风险档 · `currentMana` 落 `activeCombat` |
| `systems/character-profile/item/common-properties.md` | **ready** | `:30` 待决明写「无」。自有面虽窄但闭合 = `SourceCode` 落持有条目 / 本层四值 + 读档兜底 `Unknown` · `Status` 与「拥有 / 失去」正交、拥有 = `magicPack` 成员资格、本轮回禁用是第三维、生效 = 三条与门 · 关掉的法宝仍占储物袋位、`Charges` 不变 · 写入经 `AbilityStatusChanges` / `SetAbilityStatus`。**须与 `profile-service.md` 同批**（自有 FR 面很窄，作该批附属条目而非独立 FR） |
| `systems/enemies/_index.md` · `enemies/common-properties.md` | **ready**（**`common-properties.md` 本次由 partial 升级**） | 两份待决均明写「无」（`_index:262` · `common-properties:90`）。上一版唯一卡点 **`LineSlot` 五成员**清单与逐成员触发时刻已落 `_index:141`–`:159`，`common-properties:39` 明写「`Lines` 可正常填写」并带台词硬纪律（永不承载规则信息）与三条加载期校验；**`IgnoresProtection` 已由 ≈5% 改正为 ≈3%**（`common-properties:26`）。整面 = `EnemyData` 全字段面与加载期校验 + 样本卡组规模 10–25 / 常态 15–18 + 层数篇章基准档 2/3/4 且偏离 ≤ ±1 + `TechniqueRef` 构筑面 + 游离散牌 + `KeyCardIds` + `EncounterScopes` / `ChapterScope` / `PoolScope` 与六条取池校验 + item / power 持有列表两条硬准入 + 赋级带 `±2` + AI 兜底 1-ply 加权效用与确定性 argmax + `AiTerm` 十项 + 三条结构性上界 + **兜底 AI 默认性格 = 镜像玩家基调**（`ADR-0233`）。**两份必须同批**（`EnemyData` 字段面与校验表在两份之间来回引用） |
| `systems/player-profile/account-info.md` | **ready** | `:56` 待决明写「无」。跨边界正面闭合（合规域一格不落 `AccountInfo`，对侧「不下发」清单可逐句对表）。整面 = 五格字段（`AccountId` / `AccountSeed` hex16 / `CreatedAtUtc` / `Identities` / `Nickname`）+ `BoundIdentity` record + 三条方向纪律 + `AccountId` 不受回声约束的反推陷阱 + 首版不做头像 + 不持有账号状态 + 字段增删即 bump 与两路迁移 + `/accountInfo/accountSeed` 透明路径。**随 `account-service` 同批，不另产 FR** |
| `systems/player-profile/game-setting.md` | **ready** | 唯一登记项是三条音量轨「待实测初值」（`:119`），取值 100 / 80 / 100 / false 已存在于 `profile-service.md:190`–`:196` ⇒ 属校准而非缺口。整面 = `ADR-0072` 切分判据与自检反问 + 两侧对照表五行 + 账号级四项 + 设备本地 `device-settings.json` 全套纪律 + `SettingChanges` 通道与 `SettingFields` 配表 + `PushPolicy` + 启动期 locale 归一 + 读档校验三情形 + 加项四步成本。注：外观选用项归属已由 `ADR-0193` 定案为账号级，字段尚未进四字段表（首批不落是明写取向） |
| `systems/player-profile/codex/common-properties.md` | **ready** | 无 `## 待决问题` 小节、无待复核项，存档面整份闭合。整面 = `CodexEntry` 单格 `Id` + `CodexKind` 七值 / `CodexUnlock` + `CodexElements` 零 `Op` + 四行失败语义 + 七本解锁触发（全部搭既有提交、零新增提交点）+ 连锁解锁「1 + N」+ `eventEnd` 入账射程 + `CodexFlavor` + 词条深度分野表。跨边界零配合（对侧已把全族 `*Codex` 顶层键钉进不透明段）。**derive 时排除 `LocationCodex` 的词条正文形态**。低信号：三个标准小节缺，「无待决」由缺节推得 |
| `systems/player-profile/codex/enemy-codex.md` | **ready** | 全文无 `## 待决问题` 小节，`## 决策(-> ADR)` 七条全为结论式。整面 = 五项词条写作规格 + 关键卡 2–3 张由 `KeyCardIds` 显式标注、正文绝不写等级 + 「关键卡牌必在样本卡组内」加载期检查 + 词条②指向真实功法条目的悬空校验 + 慷慨度与三级退让阶梯 + 呈现时刻 = 战斗前确认页 + 图鉴静态性三句 + 资产字段。**derive 时排除 `KeyCardIds` 具体取值与退让阶梯的上调动作。** 注：`combat/_index.md:212` 把「叙事一致性写作口径」委派给本文件，而本文件五项规格中**尚无**「同一敌人须同时说得通切磋 / 厮杀」那一条 —— 落笔时须补，不得当作已有 |
| `systems/player-profile/codex/technique-codex.md` | **ready** | `:70` 待决明写「暂无」。整面 = 词条构成四行全部来自条目既有字段（属性行由呈现层现算、零新增存档字段、且不表达「当前角色能否修」）+ **词条不列该功法的卡牌清单**（承重反查护栏）+ 两条解锁路径皆幂等、升阶不产生新条目、弃置不删条目 + `Pool == Enemy` 照常收录 + 完成度分母取 `AllIncludingDisabled()` + 存档面纯加法空迁移。**derive 时排除功法条目本身与完成度分母绝对值** |
| `systems/player-profile/player-power/common-properties.md` | **ready** | `:53` 待决明写「无」。整面 = `status` 与 `SourceCode` 两个共有字段的合法取值与唯一规则消费点 + 两条正交维度的存档与写入通道（`bool Status` 默认 `true`、老档缺格补 `true`、写入经 `AbilityStatusChanges` / `SetAbilityStatus`、第三维落 `CharacterProfile.disabledAbility`）+ capability flag 通道（扁平枚举不分区 · 正向三词表 `{Reveal, Show, Unlock}` + 禁否定式 · 一 flag ↔ 一处消费点 · `CapabilitiesChanged` 空负载广播 · 叠加 = 幂等集合并）+ modifier pipeline（`ModifierOp {Add, Scale}` 万分比整数 · `ApplyModifier` 单点入口 + 两条钳制 · opt-in 白名单与四类豁免）。**须与 `profile-service.md` 同批**（合并算法权威在服务侧，分批必出两份互不相认的叠加规则） |
| `systems/adventure-event/_index.md` | **ready** | `:44` 待决为空（五类配比与 `combatTier` 配比转指 `balance.md` 的 `BaseTypeWeights`）。`## 意图` 为真实内容（五类分类法表 + 分类粒度判据 + 开局强制构筑归 Research，`:8`–`:29`）。**但切片全部由 `common-properties.md` 与 `future-event-service.md` 承载 ⇒ 不另产 FR，只作分类法参照**（避免撞车） |
| `systems/adventure-event/common-properties.md` | **ready** | `:433` 待决为空。整面 = `EventOption` 物化字段面 + `selectCost` 四条不变式 + `OutcomeRule` / `OutcomeSpec` 五格与九条加载期校验 + 「产出即定稿、不得回查模板重算」+ 事件收口的事务语义 + `PastEventEntry` 全 schema + `AppliedChange` + 寿元回复只走 outcome 侧 + `lifeSpanCost` 形态定值三变异位 + Travel 双禁令（不得回寿且不得产两种货币）+ `AddLooseCard` 取池链 + **隐藏属性编排判据表**（`:301`–`:321`，`ADR-0251`）+ **非战斗产出固定取 `Solid` 表 × `m(c)`**（`ADR-0239`）。**它是后续全部事件类型 FR 的共同类型地基，derive 顺序应最靠前。** 落笔前须清 `:85` 的旧时长副本 |
| `systems/adventure-event/combat/common-properties.md` | **ready**（**本次由 blocked 直升两档**） | 字段面自身闭合：单敌人承载（`:10`）· 遭遇参数落 `EncounterSpec` 且 `EnemyData` 不携带（`:11`）· 战斗资源与等级引用（`:13`–`:14`）。唯一待决是一条**指向 `enemies/common-properties.md` 的跨文档转发**，而该文件现已 ready ⇒ **依赖闭合成立**。它不承载量纲与奖励池取值（那两处在 `_index.md`），故不被 `_index` 的余留卡点污染 |
| `systems/adventure-event/explore/common-properties.md` | **ready**（**本次由 partial 升级**） | `:29` 明写「无。专有字段清单闭合」，专有面收于 `RevealedEventId` 一格（`:12`）。可脱离 `_index.md` 独立 derive —— `_index` 余留的两个待实测初值（真身占比 · 转场时长）不落在本文件的字段面上 |
| `systems/adventure-event/research/common-properties.md` | **ready**（**本次由 partial 升级**） | 字段面 + 三道短缺闸（`:72`–`:86`）+ 两条取池链（`:58`–`:61`）全闭。上一版唯一未闭合的跨文档依赖 **`MaxTier`** 已由 `ADR-0186` / `deck/_index.md:56` / `balance.md:616` 定为上界 **5** ⇒ `ResearchCandidate.Amount`（`:44`）取值域有依据。可先行 derive |
| `systems/game-progression.md` | **ready**（**本次由 partial 升级**） | **`## 待决问题` 现已完全为空**（`:214`）——上一版唯一卡点「选择区排布与滑动手感」已由候选项排布轴 / carousel N 自适应 / 候选条目构件 / 事件选项焦点制一批在 `ux/screen-flow.md` 收口。整面 = 篇章 / 境界推进与检查点重试 · 存档点即结束点、途中死亡从章首重试 · 修行等级体系 · `experiencePoint` 阈值曲线 / 三章合计 / `ExperienceGrade` 四档 / 产出点覆盖率 ≈55% / 失败产出 = `ExperienceGrade` + `FailureRatio` · 「Finale 前必须升满、Finale 自身不供经验」的硬约束与 `FinaleDiff` 咬合 · **难度与数值缩放的分格轴整节**（`:184`–`:204`：两条轴 + 十一条曲线登记 + 「第三条轴须先立 ADR」）· 篇章时长 45–55 / 50–60 / 60–70 · 失败容错 N = 2 · `eventCountLimit` 配额语义与剧本免疫 · Travel 路由 80/20。**它是 `ux/screen-flow.md` 与 `combat-ux.md` 的上游编排层，先行可让后两者的 FR 有稳定的 depends-on 锚** |
| `ux/onboarding.md` | **ready** | `:25` 明写「当前无未决项」。整面 = 强制登录无游客 + 首版只呈现已实现渠道（不遍历 `LoginChannel`）+ 两步握手 UI（验证码框 + 常驻倒计时重发键 + 过期重取，无 hover 提示）+ 绑定 / 解绑不落登录屏 + 首玩篇章门禁 + **角色选择屏「给简介，不给推荐」——一句动词级概括 + 起始功法名列表，走既有 `LocalizedText`、不新增数据结构；首玩局同样不标注推荐项、不新增任何「推荐」标记格**（`:13`，`ADR-0234`，三处权威副本已一并改写）+ **明确不做「首局跳过选择」特判**（原样保留，不受 `ADR-0234` 影响）+ 解锁 = 「有可挑战角色」的动态状态。**与 `screen-flow.md` 同批** |
| `systems/services/content-service.md` | **partial** | 唯一自有待决 = **disabled 条目被存档引用时的 UX**（`:495`，归 `ux/`，`ux/error-and-blocking-ux.md` 侧无对应裁决）⇒ derive 时该分支须留空、不写 UI。**前置未变：跨边界两条待承接必须先落**（对侧 `ADR-0051` manifest 路径自行选路 · **`ADR-0052` CDN 4xx 两分处置，其「不对 4xx 走退避重传」收窄了本文件 `:222`–`:228` 现行的指数退避语义**；本次机械复核 `ADR-0051` / `ADR-0052` / `4xx` **三词零命中**，连续两版一字未动）。就绪切片 = 三层覆盖来源 + 启动期 manifest 比对 + blob 内容寻址 + ES256 detached 签名与 `keyId` 轮换 + 文件级事务 + 增量下载 + overlay 合并与「以 overlay 为准」+ 合并后全量校验 + `ContentEnabled` 不预埋占位 Id + flags 第三层与落盘纪律 + `contentVersion` 记两个 + 断网降级到随包基线 + 语言覆盖率审计 + overlay 剧本例外双闸 + 打包工具两项产出 + 单例内容注册与条数校验 + 基线 `Id` 超集不变式与退役三分 + **本次新增：退役路径表按轨道收窄**（`:101`，`ADR-0273`：`Track == Free` 照常处置、**`Track == Paid` 禁止永久退役**，与 `character-profile/_index.md` 的校验 #18 是同一件事的两处、须同批维护）。`:491` 的「ADR 候选」行应改回链 `ADR-0182` |
| `systems/services/combat-service.md` | **partial** | 就绪切片 = 战斗机制骨架整条（栈与 LIFO + 战报 · `ActionResult` / `CombatFeedEntry` 六类与因果树 · 疲劳为一等栈条目 + `TurnLimit` · 三步回合 · 抽牌堆不重洗、抽空每张扣 1 道念 · 敌人意图整条移除 · AI 两层与 `CombatSnapshot` 按 `ViewerSide` 投影 · **效果流水线五阶段**与 `Evaluate` 纯 / `Apply` 唯一写点 · `StackEntryKind` 五值与栈条目 `itemId` 双向不变式 · `ActiveCombat` 存档块 · 决策点 D0–D7 · `RewardPickState` 与逐项领取 · 可选奖励固定 3 项预抽落存档 · 战斗前确认页 · counters 键空间 · 挂起态恢复与取消 · 单一 `combat` 子流）+ 奖励生成路径整段（`RewardPoolId` 可空 ⇒ 无 D6 · 四类混合 · `RarityTier` × 优势档 × 篇章权重表 · 每池五档全非空 · 寿元道具排除）+ **本轮改写：`EncounterSpec` 的可空覆写组由四格减为三格**（`InitialDraw` / `DrawPerTurn` / `EnemyManaLimit`；**`HandLimit` 一格已从 record 定义与覆写组表删除** —— 手牌上限恒 7，剧本与事件模板皆不可覆写）。卡于两条（`:697` / `:698`）：Finale 奖励加厚幅度（标定项，措辞早于 `ADR-0241` / `0242`，读起来像结构未定）· **战斗内容本体整体为空**（属内容阶段）。**落笔一律取 `EncounterSpec.TurnLimit`，不得把 10 写成常量** |
| `systems/services/life-cycle-service.md` | **partial** | 就绪切片 = 轮回骨架 + 两层持有模型 + 三分类法 + 寿元递减预算与归 0 终态 + `DefeatReason` 三值与 Finale 显式旁路 + 「账号级残卷累加先于角色终结提交」+ `AdvanceEventAsync` 固定结算流程与取消语义 + 非战斗四类的事件内决策点 + `SeedManager` 四子流与 `AttachRngState` 三步 + 残卷掷骰走账号级 SplitMix64 + 存档点永不回退 + 决策点存档 + 批次层即时提交 + 两处结束摘要组装 + 轮回出口三层处置与 `chapterStartSnapshot` + 主动放弃角色 + 痕迹跨篇章只追加 + **本次新增：`GetSelectableCharacters()` 的解锁过滤**（`:89`，`ADR-0272`：`AllEnabled<CharacterData>()` ∩ 绑定条目全启用 ∩（`Track == Free` ∨ 解锁集合含该 `SeriesId`）；签名不变、`StartCycle` 零改动、不涉 RNG、**绝不写成 `AllEnabled(accountContext)`**）。卡于唯一一条实质项：`:398` 的 **`PlayerItem`（古宝）的解锁 / 获取 / 失去触发**（`PlayerPower` 那一半已由 `ADR-0267` / `ADR-0268` 收口，同行已明写）。**同行内「`Achievement` 条目 schema 与进度模型仍待定」（已被 `ADR-0196`~`0200` 推翻，本次升为落笔前必清项）与 `:400` 的「隐藏属性仍待定的那一项（增减触发）」（plot-manager 现存三条待决中并无此项）两句仍在，均属陈旧副本，落笔前须删**；`:399` 的后端 / 账号合规是已裁决的归属排除，不计卡点 |
| `systems/services/profile-service.md` | **partial** | **09-07 记的四条卡点已闭合两条半**：`AchievementManager` 采集面两支（`:301`–`:306`：服务内 spec 旁听 + EventBus 被动订阅）· 两条新列 `AchievementElements` / `AchievementTierElements`（`:37` / `:266`–`:278` + 校验行 `:121`–`:129`）· 两档 60/90 水位与奖励族分配 · 条目 schema 已有权威。整面 = `ProfileManager` 唯一写入面 + `TryApply` 全量校验 / 全有或全无 + `Project` 只读投影 + `ProfileChangeSpec` 逐列与逐列失败语义（**列数不写死**）+ `CostKey` 15 成员 + 产出侧 `LifeSpan < 0` 整批拒绝 + 门面级只读 `Holds(...)` + `SetAbilityStatus` 一个门面覆盖四类持有条目 + 三维生效与门 + `CapabilityManager` 整面 + modifier 合并（顺序无关 ⇒ 不设优先级）+ CodexManager / GrantPoolManager / AchievementManager。卡于唯一一条（`:513`）：账号级条目的解锁 / 获取 / 失去触发。**上一版第二条「`PlayerPower` 平衡边界」已消失** —— `:514` 的豁免清单现把 `PlayerPower` 的三条获取渠道 / 失去三形态 / 频次预算 / 防 pay-grind-to-win 护栏逐项列为「已各有权威」并回链单一权威 ⇒ **实质残留面只剩 `PlayerItem`（+ `Achievement` 触发）**，`:513` 的泛指措辞须同批收窄。**`ItemChargeElement` 只 derive `Delta < 0`**（`:106`：`Delta > 0` → `PushError`，首批只开消耗向） |
| `systems/character-profile/_index.md` | **ready**（**本次由 partial 升级 —— 上一版新增的那条排除面已整条关闭**） | 上一版第②条卡点「`CharacterData` 付费轨道的字段形态 / 解锁校验 / 存档与契约增量」由 `ADR-0269` / `ADR-0270` / `ADR-0273` / `ADR-0282` 逐格落定并已进正文：字段表加 `SeriesId`（`:29`，两段式 `character_series.<snake_case_slug>`）与 `Track`（`:30`）两格、**两格均为模板静态字段 ⇒ 存档增量为 0**（`:34`）· `CharacterTrack` 哨兵枚举 `Unspecified = 0 / Free = 1 / Paid = 2` 与「否决 `bool IsPaid`」的理由（`:110`–`:122`）· **加载期校验由十三条增至十八条**（`:172`–`:176` = #14 ~ #18，其中 #16 / #17 / #18 一律走 `AllIncludingDisabled()`）· `Track == Paid` 退役口径分档表（`:124`–`:126`）· 双向棘轮（`:102`，内容纪律 + `/audit-content` 两个方向各一行，不做运行时机制）· `CharacterSeriesData` 引用形与悬空校验（`:96`–`:101`）。就绪切片其余 = `CharacterData` 六格 + `CharacterProfile` 完整字段表 + 五格新字段 + `Status` 具名子类子表 + `Affinities` 唯一规则后果 = 硬性修习准入 + 首批五角单灵根是编排口径非字段约束 + 角色池全池指定 / 规模 5 + 读档校验 + 绑定功法层数恒 1 + 首玩局缓解 = 选择屏给玩法简介不标推荐（`ADR-0234`）+ 首批五角压平同一复杂度档（`ADR-0259`）+ 双灵根走免费轨道 + 复合功法 / 数量上限双机制（`ADR-0258`）+ 快照与状态机。现存 `## 待决问题` 三条**均不构成排除面**：`:458` 全池指定下强度是否塌缩 —— `ADR-0256` 已收窄为纯实测项 · `:459` 首批付费系列的 `SeriesId` 取值 / 系列名 / 五行构成（内容阶段，欠 `narrative/` 世界观底稿）· `:460` 进阶批「复杂度更高」的表达（内容与数值阶段）。**须与 `deck/_index.md` 同批且不得倒序** |
| `systems/character-profile/life-span.md` | **ready**（**本次由 partial 升级 · `## 待决问题` 现明写「无」**） | `:57` 明写「无。」—— 上一版唯一残留的 `lossPerMomentum` ch2 / ch3 已于 09-11 定案为 **5 / 10**（三章三格全为定值 10 / 5 / 10、走恒定占比而非递进；形状锚逐格 9% / 10.0% / 11.3% 通过；N = 2、21 格 `lifeSpanCost` 定价表与 λ 台账零改动）。整面 = 单值形态与三条不变式 + 与 mana 的非对称对照 + 预算 1000 / +1000 / +3000 / +5000 与跨篇章结转 + 扣减恰两来源与「同一次收口事务落到同一个值」+ 内容侧恒不得写负向 `LifeSpan` + **战斗内不读写寿元**这条资源纪律 + 回复三通道共用一条 `ChangeElement(CostKey.LifeSpan, +n)` + 归 0 → `LifeSpanExhausted` + 明文常驻恒精确、< 10% 转红 + `PastEventEntry.LifeSpanAfter` + `lifeSpanCost = round(t × λ)`（λ = 15 / 17 / 44）+ **回复三档 50 / 100 / 200**（`:50`，三章通用、不按篇章分条目、与 `RarityTier` 及定价一一绑定，`ADR-0248`）+ `R_c` 拆事件侧 / 道具侧两格（`ADR-0246` / `ADR-0266`，Σ 恒等 ⇒ λ 与 21 格定价表零改动）。**derive 纪律不变：不单列 FR，与 combat-service / life-cycle-service 同批**；λ 的两个反推格子不得写进验收断言 |
| `systems/character-profile/deck/_index.md` | **partial**（就绪切片大幅扩张） | 上一版六条卡点关掉四条：**量纲基准 `momentumPerMana = 1`**（`:66`，`ADR-0183`）· 首批只用加法层（`:67`，`ADR-0188`）· 规模口径表与三条回代（`:50`–`:62`：一门 5 张 / `MaxTier` 5 每层 +20% / 起始 15 张，`ADR-0185`/`0186`）· **「内容性格」整节**（`:175`–`:202`：战斗基调 = 产道念为主旋律 / 削拆为反制 · 高光三通道 · **起始卡组不设统一底盘** · 极限精简是被支持的高阶路线 · 削与拆并重且拆解不稀有化 · 五行各持一个主战斗动词 · 业障 ch1 低频但沉重，`ADR-0227`~`0232`/`0235`）。就绪切片其余 = 功法 = 卡组构筑单位 + `CultivationTechniqueData` 两格 + `Pool` 必填 + **灵根修习准入整块与判定式** + 四处玩家侧取池点各叠一层 / 敌人侧不叠 + 成员卡反建索引 + 五个 `DeckChangeOp` 与 `Pool == Enemy` 施加侧闸 + `CardType` 五分与封闭卡集 + 次类型体系 + 卡池三值必填 + 不重洗与疲劳 + 手牌上限 7 纯上界 + 「一切增益减益都是战场条目」。**本次再关两条口径**：通用 / 专属配比 = **按稀有度分层的梯度、无硬限制**（`:171`，`ADR-0257`；低档通用比例高且每种灵根都有低稀有度功法，高档通用限无条件直给型，build-around 高档一律带属性，`/audit-content` 按档统计只报告不阻断）· 多灵根卡池区分 = **复合功法 + 数量上限双机制组合，零新字段零新机制**（`:172`，`ADR-0258`）· 首批五角压平同一复杂度档（`:319`，`ADR-0259`）。卡于（五条，`:325`–`:329`，**全落内容取值**，其中前两条已被上述口径结构性收窄为纯数值格）：双灵根批两类专属内容的对铺量 · 通用 / 专属分层梯度的逐档比例 · 关键字与次类型首批清单为空 · starter deck · 逐张卡牌道念产削取值。**不得先于 `character-profile/_index.md`** |
| `systems/character-profile/deck/common-properties.md` | **partial**（卡点减半） | 上一版两条卡点关掉一条：**抽 / 弃 / 洗数值已定**（`:385`：起手 4 / 每回合 2 / 手牌上限 7 / 起始 15 张）。就绪切片 = `EffectData` 抽象基类 + 一原语一 `[GlobalClass]` 子类树 + **八原语**参数表（`MoveCardEffect` 含 `Side`、`From`/`To` 恒同侧、跨方转移结构上不可表达）+ **加载期校验 21 条** + `StaticModifierData`（`ModifierTarget` 五项冻结 · 万分比整数 · 同层求和 → 只乘一次 → 只取整一次）+ `AbilityData` 两格 XOR 与启动代价面 + `TriggerConditionData` / `TimingIds` 十时点 + `SubjectKind` 相容校验 + `EffectCondition` 三谓词 AND + `SideConstraint` + 关键字哨兵与恰好一层展开 + `CardData` 字段收口 + 目标 / 作用域分建模与 `EntryFilter` + 按视角前提机械化 + 存档面零新增字段。**它是卡牌 / 神通 / 法宝 / 法则 / 古宝五类内容的共同底座**，纯数据资源型切片、过三条闸。卡于唯一一条：starter deck 装哪些牌（`:382`，同时是原语 / 关键字 / 次类型三份首批清单的共同切入点） |
| `systems/character-profile/item/_index.md` | **ready**（**本次由 partial 升级 · 上一版两条卡点关掉一条、另一条属内容阶段**） | 上一版 `:199` 的数值格「法宝置换在上层 ≈1.0 分子中占多少份额」**已于 09-12 答定为 ≈0.35**，并连带一次预算重新配平（守住 ≈1.0 天花板、其余三支等比压缩 ×0.65 ⇒ 神通 ≈0.32 · 法则置换 ≈0.20 · 法则禁用 ≈0.13；落位**并入既有支、不开第五行** ⇒ `ADR-0265`「四支枚举不扩为五支」逐字成立）。现存 `## 待决问题` 仅 `:198` 一条 —— **道具种类目录本身**，明属内容阶段（走 `/scaffold-content-type` + `/author-content`），按本表既有口径（`player-power/_index.md:171` 同款）不构成排除面。整面 = 三层分工表 + `ItemData` 完整字段形态与两条校验 + 使用效果面按世界分两格、必填性由 `UsableScene` 驱动 + 战斗外三列白名单与符号 / pipeline 纪律 + 战斗外使用 = 即时提交非决策点 + 配额面两格分工与哨兵 + **I-1 ~ I-13 加载期校验** + 储物袋跨两层视图与随售通道、两档回收率的加载期硬校验 + 跨载体三行边界判据表的回链 + **回寿法宝总量护栏三口径**（`:141`–`:168`）+ 族护栏必要性判据（`ADR-0261`）+ 战斗外可写 key 白名单 `I-13 = { LifeSpan }`（`ADR-0260`）+ Exchange 逐族库存深度 3/2/1/1/0（`ADR-0262`）+ 事件侧 ≈1.5 件 / 轮回、每条 `Count == 1` 与软校验 `X-1`（`ADR-0263`）。**不得先于 `power/_index.md`**（边界判据表权威在那边）。**落笔前必清 `:36`–`:38` 的 `itemPowerRatio` 陈旧副本，一律引 `balance.md:868`–`:879`** |
| `systems/character-profile/power/_index.md` | **partial** | 就绪切片 = `PowerData` 字段面 + **跨载体三行边界判据表与四条推论**（`:52`–`:64`，**本表是卡牌 / 道具 / 神通三者边界的唯一权威**）+ 获取通道四个 `Source` 已闭合 + 失去恰三形态、不开无同意永久剥夺、买得到卖不掉 + 强度定性三条与 `baseMomentum` 比例刻度 + **战斗内强度闸门 25% 已给初值**（`:67`，`ADR-0190`；`ADR-0244` 维持单闸、只以 `baseMomentum` 计）+ 内容编排口径表（开放 `InitialGrant` / `CombatReward`（收窄 `Standard`·`Finale`）/ `ExchangePurchase`；`EventOutcome` 保留机制零条目；`Travel` / `Explore` 恒不产出；条目数下限 ≥ 5）+ 绑定神通不填 `ExclusiveSource` + 两条加载期校验且明确不做总量前置检查 + 三格至少一格非空 + 「纯战斗外条目不得挂触发式」+ 不得含 `LifeSpan` 产出 + `IsProtected` 与 `IgnoresProtection` 唯一后门 + 禁用截断五行表 + 战斗内运行态走战场条目零结构增量。卡于（两条，`:101`–`:102`，均属内容 / 量纲）：获取侧频次与分布 · 强度定量三格（轮回预期条数 / 效果量系数 / 各 `RarityTier` 条目数），三者共同阻塞于 starter deck 分母 |
| `systems/character-profile/power/common-properties.md` | **partial** | 内容层面已完全闭合：`:8` 形状对标 PlayerPower 且两维正交 · `:9` 生命周期轮回级 = 与 PlayerPower 的唯一本质分界 · `:10` 写入不绕过唯一写入面 · `:11`–`:14` `SourceCode` 四行 · `:23` 完整写出 `bool Status`（默认 true）· 拥有 = `characterPower` 成员资格 · 本轮回禁用第三维 · 写入经 `AbilityStatusChanges` / `SetAbilityStatus`。**未升 ready 的唯一理由 = 文件仍缺 `## 意图` / `## 决策(-> ADR)` / `## 待决问题` 三个标准小节**（全文 26 行，现只有「已定的约束」`:5` / 「字段清单」`:18` / 「对应」`:25`）⇒ 判据①在字面上不成立、判据②无法自证。**补三个小节即可无保留 ready，不需要任何新决策** |
| `systems/player-profile/_index.md` | **partial**（就绪切片本次显著扩张） | 就绪切片 = **17 行字段表整张**（已含成就两键与两条新写入列，`:17`–`:18`）+ 规则层 / 统计层分层判据 + 三样不进 Profile 的排除项 + 三个具名子类 + 受回声校验约束的顶层键 + 残卷 Finale 绑定 + `PlayerStatistics` + `PlayerPowerFragment` 七字段 + `PlayerEntitlement` 两序号与重复面表（含 `Cosmetic` 加法窗口）+ 七 Codex 具名字段 + 四类持有条目 record + **本次新增 `PlayerEntitlement.CharacterSeries` 整套**（`:159`–`:220`，`ADR-0270` / `ADR-0271`）：`IReadOnlyList<CharacterSeriesEntry>` 与 `readonly record struct CharacterSeriesEntry(string SeriesId)`（否决裸 `string[]`，沿用 `<Kind>Id` 命名）· **写入方只有后端**、客户端无写入通道 · **不配兑现水位**（`:198` 明写「照抄一个 `CharacterSeriesRedeemedOrdinal` 是最诱人的错误答案」）· 读档三情形（非数组 / 缺 `SeriesId` → `PushError` + 本次不进 diff + 触发 pull、不钳制；悬空 `SeriesId` → `PushWarning` + 保留；重复 → `PushWarning` + 呈现层去重、不改存档值）· **回声路径「老档缺字段」两时点**（迁移写空列表 / 迁移后读档即真异常，对 `Cosmetic` 同款适用）· 透明路径 `/entitlement/characterSeries` 与「客户端不改写 / 不去重 / 不归一化」（对侧的有序逐元素比较依赖这一条）。卡于唯一一条（`:244`）：**`PlayerItem`（古宝）的解锁 / 获取触发** —— 礼包一条已明写，Exchange 购买与事件产出两侧仍是方向而非定案。**不得先于 `profile-service.md`** |
| `systems/player-profile/codex/_index.md` | **partial** | 就绪切片 = 七本清单 + 三层浏览（索引页 → 单本页 → 词条、七本共用一套排布）+ 完成度只计已解锁顶点 + 共同形状八条 + 四条族级结论（账号级 / 不与成就奖励挂钩因而后端零配合 / 战斗中不可查 / 连锁解锁）+ 边缘顶点三式与呈现三态表、半径固定 1 跳。**剩余卡点仍恰一条**：`LocationCodex` 除连边外的词条深度未定，连带**单本页内容区如何画那张图**（缩放 / 平移 / 顶点布局）与词条载体（`:85`）—— 09-08 的战斗 UI 批未触及此点，`ux/screen-flow.md:425` 反而明写「地域本只有单本页的内容区不套用统一形态」，即该缺口被再次确认为开 ⇒ **六本可写、`LocationCodex` 单本页整块排除**。注：七本表中五本仍 ⟨待播种⟩，不得假定各有专属文档 |
| `systems/player-profile/player-power/_index.md` | **ready** | 就绪切片很厚 = 道统残卷全链 + 战斗内三条与门与稀缺性纪律 + 三形态剥夺表与 `disabledAbility` 三档 + 置换池与四池判据 + 三渠道共用抽取伪码 + `selectCost` 恒空不变式 + 失去三形态 + 同池判据 `(CarrierKind, Scope)` 全同 + 同 `Rarity` + 排除已持有 + 自愿失去阶梯 + 残卷 Finale 绑定 + 授予池权重结构与初值 40/27/18/10/5 + 「一次轮回至多结算一次」的轮回级布尔位 + **失去能力频次预算四支表**（`ADR-0161`；**本轮已按法宝一族 ≈0.35 重新配平**：守住 ≈1.0 天花板、其余三支等比压缩 ×0.65 ⇒ 神通 ≈0.32 / 法则置换 ≈0.20 / 法则禁用 ≈0.13，**并入既有支、不开第五行**；`ADR-0161` 备选段的否决理由已改写 —— 「每 7 轮回一次 ⇒ 几乎不可见」不成立并被撤下，法则禁用 0.13 与神通禁用 0.11 **不再被记为可见性代价**）+ **获取通道恰三条**（`ADR-0267`：`FinaleWin` / `PremiumBundle` / `AchievementReward`，`EventOutcome` / `ExchangePurchase` 规则层封死）+ cycle seed / 计分公平两者与它不相交的论证 + 七道 pay-grind-to-win 护栏 + **账号级获取速率的可算分母 `x ≈ 9–12`**（`ADR-0268`，派生表在 `balance.md:995`–`:1007`）。现存 `## 待决问题` 两条均不构成排除面：**`:170`（行号由 `:159` 后移）的「战后奖励池各档权重」是已被 `ADR-0237` / `ADR-0242` 关闭的陈旧副本 —— 第二次登记、一轮未清，落笔前必清** · `:171` 自陈「属内容阶段，不是设计缺口」（开张动作归 `/scaffold-content-type player-power`） |
| `systems/player-profile/player-item/_index.md` · `player-item/common-properties.md` | **partial** | 就绪切片 = 战斗内 `CardType.Item` 与两级同呈现 + `Scope == Player` 时 `Charges > 0` 硬约束 + 次数即时写 Profile 并堵死重进恢复 + 恒不可售出的代码级常量 + 禁用三档与同池置换 + 定价归属 + `SourceCode` 本层三值与读档兜底。卡于：古宝种类目录（`_index:42`，内容阶段，走 `/scaffold-content-type`）· 正向补充通道本身。**未升 ready 的实质理由是两处文本相抵而非缺口**：`common-properties.md:13` 的「预期共有字段含价格 / 成本、库存、稀有度 / 权重」与 `_index.md:10` 的「`ItemData` 上不加 `Price` / `Purchasable`」**直接相抵**，照现文落笔会写出互斥的字段面；另 `_index:42` 与 `common-properties:24` 仍写「可购价格 / 库存权重未设计」，而两项均已定案（`exchange/_index.md:49`–`:53` 的族 × 稀有度 25 格表、`balance.md:450` 玉 1/2/3/5/8；次数补充首批已收为「不补充」，`profile-service.md:106`：`Delta > 0` → `PushError`）。**三处须在落笔前一并清。**注：`ADR-0247` / `ADR-0248` 只覆盖 `Scope == Character` 的回寿法宝，对古宝零升档。**两份承载面薄，须与 `character-profile/item/_index.md` 同批，`ItemChargeElement` 只 derive `Delta < 0`** |
| `systems/player-profile/achievement/_index.md` · `achievement/common-properties.md` | **partial**（**两份本次由 blocked 升级**） | 三条旧卡点关掉两条（见上方 ✅ 第三条）。就绪切片 = 奖励形态「指定条目 + 成就限定」与 `ExclusiveSource: Source?` + 四条校验含双向唯一性 + 两条推论（`AccountStream` 无需 `AchievementReward` 成员 · 置换两侧不对称）+ 分组两档 60/90 与 80/20 可见比 + 每组启用成就数 ≥ 10 + 两档族分配 60%→古宝 / 90%→法则与三条依据 + 采集面归 `AchievementManager` 零新增存档点 + 两条 record 与两个顶层键 + `Completed` 不可派生的承重论证 + `RewardedTierPercent ∈ {0,60,90}` 水位式幂等 + 六行读档校验 + 三个 `Resource` 类字段面 + `SignalId` 点分封闭常量表与逐行 `FilterKind` + 两条启动期断言 + 组内加权进度公式与只取整一次 + 隐藏成就渲染判据。卡于（两条）：两档专属奖励条目目录（`_index:49`，依赖法则 / 古宝条目先行）· `AchievementSignalIds` 首批清单（`common-properties:107`，形态已定，已指名六条可由既有 EventBus 负载表零改动覆盖）。**两份必须同批**（两个顶层键 + 两条新列 + `player-profile/_index.md:17`–`:18` + v1 首发形状 #37 是同一次落笔面）。**信号面只 derive 已指名的六条，其余留占位、不臆造** |
| `systems/adventure-event/combat/_index.md` | **partial** | 就绪切片 = 三档遭遇参数 · `EncounterSpec`（**可空覆写组现为三格：`InitialDraw` / `DrawPerTurn` / `EnemyManaLimit`，`HandLimit` 一格已删除**）· 胜负与换算 · 六条代价表 · 可读性六通道 · `combatTier` 配比是内容编排口径、管线不掷 tier · `RewardPoolId` 两轴口径（`:38` 频率轴 / 厚薄轴 + 九个具名池 · ≈30% 挂池、`Finale` 恒挂）· 逐条目 `HiddenStatGrade` 三档阶梯 + 顶层判据表。卡于三条（`:215` – `:217`，较上一版 +1）：**是否存在道念相关的状态与倍率**（`momentumPerMana = 1` 已定，此条问的是要不要引入这一层）· 三档奖励厚薄的逐条目绝对取值（`BaseReward` 0.5 / 1.0 / 2.0 × S 已有初值，属内容阶段）· **叙事一致性写作口径**（委派 `enemy-codex.md`，而该文件的词条规格中尚无「同一敌人须同时说得通切磋 / 厮杀」那一条 ⇒ 接收方未落笔）。**上一版登记的 `:210` 与 `ADR-0183` 相抵的旧句已随本轮改写消失** |
| `systems/adventure-event/travel/_index.md` · `travel/common-properties.md` | **partial**（两份同批） | 就绪切片 = `LocationData` + 单份 `LocationMapData` 载体与恒启用 · 80/20 全局常量不可调制 · 目的地取自邻接集合、`DestinationLocationId` 物化时掷定且不得事后算 · 闸门形态（最高 `Priority = 1`、无 `IsMandatory` 字段）· 出度 ≤ 5 · 结算写入判据为 `DestinationLocationId != ""` · 痕迹侧记出发地 · 两条 `StatusAssignment` 由 life-cycle-service 组装 · 不计入 `eventCountLimit` 与剧本免疫 · **货币产出禁令与回寿禁令合为同一行校验** · 定价结构约束（`> 0` 且为同章 `Exchange` 行的 1/3 ~ 1/2，加载期机械校验）。卡于两条（`:94` / `:95`）：Travel 行的具体定价取值（结构约束已定、初值 6 / 7 / 18 在 `balance.md:144`，欠的是实测复核）· **location 无法被 flags 秒关时的运营替代通道**（明写「本次不预设形态」）⇒ **derive 不得为它臆造闸门**。**上一版登记的两处陈旧副本 `:60`（9 / 10 / 25 与 23 / 25 / 63）本次已清**（现写「取值为同章 `Exchange` 行的 1/3 ~ 1/2」，全文对旧值零命中）。`common-properties.md:30` 自有面为空、全文转指 |
| `systems/adventure-event/explore/_index.md` | **partial** | 就绪切片 = 揭示机制整套 —— 真身分布 = 条目池组成 × 既有加权抽取、三处数据类一律不加字段 · 取池期「真身须同样 enabled」+ 真身为 Exchange 时库存池前置须过 · 揭示 = `eventStart` 内一次 `with` 派生且 resolver 按真身选取 · 遮罩卡与其余选项完全同构 · 全屏覆盖层转场、不进屏幕栈、不给部分线索 · `IsRevealed` 只在物化侧 · 派生实例落 `activeEvent` 随后续第一个决策点写盘 · `RevealedEventId` 一格与四条加载期校验 · 「成本取壳产出取真身」不对称。卡于两条（`:121` / `:122`，均归统计校准 / 真机实测，不阻塞结构）：真身占比 `5 : 3 : 2` 的统计回归与转场 ≈1.2s 的真机校准 · **定价表 Explore 行的取值**。**上一版把 `:122` 判为陈旧副本，本次直读推翻**：`balance.md:1242` 的对应待决条明写真身占比校准「**兼定价表 Explore 行 24 / 27 / 70 的标定输入**」⇒ 24 / 27 / 70 是初值而非定案，两处是同一条校准项的上下游、并非相抵。**正文一字不改，本条已从零决策修正清单中移除**（见文末直读裁决） |
| `systems/adventure-event/research/_index.md` | **partial** | 就绪切片 = 构筑面板 = 复数决策槽 · **五类**操作闭合 · 产出面 = 卡组 + `manaLimit` · `manaLimit` 下降挂玩家自选风险档、风险档是唯一内部张力 · 候选物化时掷定走 `RngStream.Reward` · `AllowDecline` 默认 `true` · 不另收资源代价 · 产出禁令扩为「不给任何货币」· 回寿不作为 Research 操作 · 闭关与开局构筑两个取池点各叠 `Pool != Enemy` · 三道短缺闸 · `ResearchSlotSpec` / `ResearchSlot` / `ResearchCandidate` 形态 · 载体 `CostKey.ManaLimit` · `DeckElements` 承载 · 开局构筑 `lifeSpanCost = 0` 覆盖 · 闭关面板竖屏形态与风险档中性标记（`ADR-0221` / `ADR-0222`）。卡于唯一一条：走火入魔风险档权重（只欠取值，`:94`）。**`:95` 不是待决项 —— 它是一条已定案句（`MaxTier = 5` 已定、`ResearchCandidate.Amount` 取值域随之封顶）错位挂在 `## 待决问题` 节下**，属版式瑕疵，宜移出该节 |
| `systems/adventure-event/exchange/_index.md` · `exchange/common-properties.md` | **partial**（两份同批） | 就绪切片 = barter 支付侧二选一整段（`Holds(...)` 前置拒绝 · `CanAfford` 一字不改 · `Source.ExchangeBarter` 只开 `Op == Remove` × `(Item, Character)` · 不持有则灰显且支付要求保持可见 · 不按持有面过滤呈现）· 售出两条通道与同币回收 · 交易逐笔即时提交 · 库存物化经 `RngStream.Shop` 掷定并落存档 · 五个商品族映射到既有仓储 · **族 × 稀有度 25 格定价表与币种分配** · 两条折扣通道 · `ModifierKey.ShopPrice` 单点施加 · 刷新是店级动作恒以灵石计价、**首批关闭刷新（`RerollBaseCost = 0` / `MaxRerollCount = 0`）是一条真决策 ⇒ 刷新机制切片可 derive** · 价签必须标明币种 · 三道短缺闸 · 加载期 / 运行期校验 · 非战斗产出取 `Solid` 表 × `m(c)`（`ADR-0239`）· **逐族库存深度上界 3 / 2 / 1 / 1 / 0**（`ADR-0262`，不为逐族上界新开字段、按 `StockRules` 的 `Kind` 分组求和即得）· **barter 两格护栏**（`ADR-0264`，只报告不阻断）· **软校验 `X-1`**（`ADR-0263`）。卡于唯一一条（两份各持一份副本，`_index:172` / `common-properties:232`）：**`RerollBaseCost` / `RerollCostStep` 的日后取值与单个 Exchange 事件的槽位总数上界**（建议 ≤ 8 仍未定案）。**`ADR-0262` 刻意不替它作答** —— 它明写「Σ 7 ≤ 8 留一格余量是有意的，逐族上界之和顶死总上界会让两条校验永远同时触发」，并把「Σ 顶满 8」列为**已否决**的备选 ⇒ 本条**不得由逐族表反推**。两份副本宜收成一句回链 |
| `systems/balance.md` | **partial**（切片持续扩大） | 就绪切片 = `lifeSpanCost` 21 格与 λ = 15 / 17 / 44 · 七条自校验 · 事件数 114 = 35/36/43 · `D_c` = 79/247/391 · `S(c)` = 10/23/39 · 25 格定价外积与币种分布 · 两档回收率 40% / 15% · `BaseTypeWeights` 五格初值 · `FinaleDiff` 初值与下界 · `experiencePoint` 阈值全表与 `ExperienceGrade` 映射 · **`lossPerMomentum` 三章三格全为定值 10 / 5 / 10**（09-11 定案）· `baseMomentum` 表 · 全局等级序 1–22 · 赋级带 `±2`（维持不放宽）· 起手 4 / 每回合抽 2 / **手牌上限 7 且任何编排面都动不了** · `MaxConcurrentSideArcs` · `TravelFullFanoutChance` · `BatchSizeWeights` · `GrantPoolWeights` 40/27/18/10/5 · 重试上限两行由 `HasPremiumBundle` 选行 · 敌人 AI 兜底权重向量 · 道统残卷三张分档表 · 平衡资源经 `Content.Single<T>()` 取 · 不设兜底大表的三问判据 · 道念量纲三张标定台账 · `CombatRewardPoolWeights` 九行 × `m(c)` · `itemPowerRatio` 四档 0.52/0.62/0.72/0.87 · 回寿护栏与三档绑定 · 隐藏属性 ch1 推拉供给对账表 · 战斗内法则 ≤10% / ≤25% 双闸 · `IgnoresProtection` 3% 分母 · `advantage` 边界三章恒 0.25/0.75 · Exchange 逐族库存深度表 · 事件侧给法宝 ≈1.5 件 / 轮回 · 回寿法宝供给回代台账 · 账号级法则获取速率派生表（`:995`–`:1007`）+ **本轮的三处订正**：`I(c)` ≈**150 / 300 / 480**（ch1 占 27%，原 127/300/480）· 战斗场数 **11 / 11.9 / 14**（原 7/8/8）· **`EncounterTighten` 由十常量改写为八常量**（`:1243`：`MaxTurnLimitTighten` 2 / `MinTurnLimit` 6 · `MaxWinMarginTighten` 1 / `MaxWinMargin` 2 · `MaxInitialDrawTighten` 1 / `MinInitialDraw` 3 · `MaxDrawPerTurnTighten` 1 / `MinDrawPerTurn` 1；**`MaxHandLimitTighten` / `MinHandLimit` 已取消、是不存在而非取 0**，手牌上限降为普通全局平衡常量）。卡于：`## 待决问题` **10 条**（`:1235`–`:1244`，较上一版 12 条净减 2 —— `lossPerMomentum` ch2/ch3 与赋级带两条已于 09-11 答结移出；**其中 `:1240` 已是陈旧副本**，见文末直读裁决 ⇒ **实质 9 条**）——**取值待复核 / 待实测 8 条** · **形态已定零取值 2 条**（三格取池余量 + `K` 明写「可先填 0 而不阻塞落地」· 走火入魔权重）。失真：`## 决策(-> ADR)` 整节仍空。**须与 `scoring.md` 同批** |
| `systems/scoring.md` | **partial**（卡点由两条并为一条） | 就绪切片 = **计分规则与全部转换公式**（一组纯函数 + 一份平衡资源，验收靠给定输入验算输出，不必等一场可运行的战斗）—— 道念 = 胜负判据 · 战斗过程中寿元不被读写 · 产削与下限 0 的每次结算截断（LIFO 因此影响结果）· 疲劳为第二条削减通道 · 胜侧两条支路 + 单价表 · 终止条件 = `EncounterSpec.TurnLimit` · `WinMargin` 在 `Finale` 退场 · `combatTier` 三档遭遇参数 · 战斗层不读写隐藏属性 · 负侧 = 道念差 × `lossPerMomentum`（ch1 = 10）· **三档 `BaseReward` 默认 element = 灵石一格其余恒空**（`ADR-0249`）· **九个具名奖励池与厚薄轴 = 换池**（`ADR-0241`）。卡于唯一一条（`:95`）：逐张卡牌 / 逐条行为的具体产削量 —— **量纲基准已有可复算初值**（`momentumPerMana = 1`），剩余是内容层取值。**FR 须写成「读平衡资源」而非固化数字。须与 `balance.md` 同批** |
| `systems/monetization.md` | **partial**（**排除面本次大幅收窄 —— 上一版那条反向变化已在同一批里整条兑现**） | **付费解锁角色系列由「首批一格不落」变为整段可写**：`ADR-0269` ~ `ADR-0277` 九份把上一版明写归 `/provide-solution-draft` 的载体 / 字段 / 购买流程 / 呈现面逐条落定并已进正文 —— 系列 SKU 归组与 `productId` 由 `SeriesId` **机械变换**（`:188`–`:191`，零新增端点 / 零下发通道 / 零内容字段，客户端不硬编码字面量、不持映射表）· 在售清单走 `AllIncludingDisabled()`（flags 秒关一个角色不让整系列从商店消失，该情形由前置条件置灰承接）· 前置条件表**按作用域两段化 + 新增 6 / 7 / 8 三行**（`ADR-0275`，既有五条一条不删、语义一字不改）· **兑现段整段不存在**（`ADR-0274`：零 `TryApply` / 零 `AccountRng` / 零水位 / 空池三闸一概不适用；跨启动补入口两条；购后 pull 失败不阻塞开新轮回，premium bundle 的阻塞纪律加限定词降为个例）· 限时在售窗口三条语义（`ADR-0276`：不绝版 · 已购永久可用 · 内容层属性不进存档）· 推销面穷举（`ADR-0277`：Store 三结果态、角色选择屏完全不出现未拥有项）· 系列 = 世界观切片、剧情不是付费面（`ADR-0282` / `ADR-0283`）。就绪切片其余 = 外观付费面整段（`ADR-0191`~`0195`）+ `PlayerEntitlement` 两字段与不变式 + `BundleGrantOrdinal` 两条否决论据（承重）+ 空池三道闸 + 兑现事务 + 三渠道选型与封装 + 三个显式步骤 + 失败处置五情形表 + 「无可用渠道 → 入口不渲染」+ 重试上限两档表 + `OpError.Purchase` 一档 + 古宝 `ThisCycle` 禁用对称。卡于（`:260`–`:264` 共 5 条，**结构性的那条已消失**）：`K` 与 `GrantPoolMargin` 取值（闸①断言阈值因此仍不可写死）· 合规交互归后端（已裁决的排除）· 各系列具体名字与主题（内容阶段，欠 `narrative/` 底稿）· 双灵根免费批的具体推出时点（`ADR-0284` 只定序、未定隔几个版本）· 进阶批「复杂度更高」的表达。跨边界：**付费系列两侧已对称落笔、无遗留欠账**（对侧 `contracts/purchase.md` / `contracts/profile-sync.md` / `operations/*` 逐项承载，本次直读复核）。**落笔前必清 `:250`–`:252` 的三处「ADR 候选待 `/write-adr` 立档」** |
| `ux/screen-flow.md` | **ready** | `## 待决问题` 只剩一条纯手感实测项（`:521`：揭示转场 ≈1.2s 与「一次短音效」的真机校准，形态已定 —— 全屏覆盖层 · 任意触点跳过 · 无确认 · 无震动），按本表口径不构成排除面。就绪切片 = 登录屏 + 主菜单五入口导航骨架与 Store 呈现纪律 + **角色选择屏整段** + 主动弃置入口整段 + 非商店平台 Store 入口不渲染 + 揭示转场表 + 战斗前确认页 + `CycleEndScreen` 三变体 + `ChapterEndScreen` 三变体含 ch3 证书 + 剧本段进结算面板与分支全宽按钮 + 图鉴三层浏览 + Exchange 屏 + 同步指示两处 + 寿元显性化整条对位与 10% 阈值 + barter 呈现侧 + 储物袋全屏面板 + 玩家档案屏只读区 + 绑定 / 解绑二次确认 + Settings 屏 + 手势判据 + 候选项列表语言全套 + **本次新增付费系列呈现面**（`:125`–`:129`，`ADR-0277`）：**Store 屏三个新结果态**（系列列表 · 系列详情 · 解锁结果态，不新增屏、不新增主菜单入口）· 详情页付款前必须列出本系列全部角色（复用角色选择屏卡面构件，不展示数值、不标推荐、不写「更强」暗示）· **角色选择屏不变：未拥有的付费角色完全不出现**（不设卡位、不灰显、不加底部入口，被接受的代价是付费面近乎不可发现）· 窗口外 SKU 取置灰 + `STORE_UNAVAILABLE_SERIES_*` 一行说明。**排除面仅剩揭示转场的时长与音效初值。与 `onboarding.md` 同批。** 失真：`## 决策(-> ADR)` 是空壳小节（`:516`–`:518`），而 `:125`–`:129` 正承载 `ADR-0277` 的全部实质 |
| `ux/combat-ux.md` | **partial** | 就绪切片 = **六区竖屏分区与屏高预算表** + **栈判为结算期浮层、不占常驻预算** + 压缩顺序与退让位 + 战报双视图（收起态结构化槽位 · 省略顺序 · 截断 / 落空双符号表 · 翻译键随 `CombatFeedKind` 机械对应）+ 呈现事件流三消费者 + 条目粒度 = 结算事件 + 渲染期才套翻译键 + 不落存档 + 敌方区只读 + 手牌重叠扇与卡面占位 + 道念差徽标 + 不做复合追分指示 + 牌堆可见面 + 只读层图标条与 `K` 初值 + 0 值处置判据 + `AbilityScope` 形状角标 + **卡框色与角标分工** + 详情页与三档禁用表 + 疲劳三格呈现 + **战后奖励面板三段形态** + 结算节拍 0.35–0.5s / 敌人回合总时长 ≤ 4s + 持有物两层面板 + 埋伏只给计数 + 敌人等级用境界名 + **手牌上限 `n/7` 呈现恒取全局常量**（`EncounterSpec.HandLimit` 已随八常量改写删除，呈现侧仍不写死 7）。卡于三条（`:264`–`:266`）：**三步结构中 mana 刷满 / 抽牌节拍与回合内状态消散的动画细节未定**（真缺口）· 屏高预算三档比例的真机实测校准（初值已给）· 五类卡框色的具体色相**与截断符 / 落空符字形**（语汇已由 `ADR-0226` 定为「仙侠意象色、不套五行、意象服从可辨性」，欠的只是在该语汇内取值，归 art guide 编写期）。**上一版登记的 `:23` 旧篇章时长本次已清**（现引 45–55 分钟篇章预算） |
| `ux/error-and-blocking-ux.md` | **partial**（三条待决均不构成排除面） | 就绪切片 = 翻译键基建整条（自命名的 `FR-ux-translation-foundation` 五件事：`res://text/` 分区 CSV · `project.godot` 注册 + 默认 locale `zh` + `fallback = "zh"` · `ErrorText` / `TranslationAudit` · 键命名三条 + 禁令 · 三条审计与定死的调用顺序）+ `ERR_*` 由 `code` 机械变换、无第二张对照表 + `reasonKey` 二级键与前缀式反向审计（三处取值集合已由后端契约填表）+ 内容侧不烘焙可翻译文本 + 两层本地化 + `ErrorText.For` 三参与静默回落 + **灰态判据表六行** + `BlockingNoticeScreen` 一屏三变体与合规七码逐条判过「一条也不进本表」+ 三档版本提示只呈现最高一档 + 起手纪律 + 字面量审计 + 诊断编号出口。卡于（三条，`:384`–`:386`，**均属实测 / 文案定稿，文档自陈不阻塞结构**）：`auto_translate_mode` 在 Godot 4.7 的默认行为待实测（两种结果下键形态 / 分区表 / 三条审计逐字相同）· 四条兜底文案与 `ERR_*` 逐条中文措辞 · `reasonKey` 逐条措辞。**FR 中把逐条中文措辞标为「文案定稿期填」即可** |
| `art/visuals/_index.md` | **partial**（**本次由 blocked 升级**） | 两条卡点关闭：**UI 元件制作路径按材质分两半**（`:44`–`:53`，`ADR-0224`：扁平底子人工做、古物点缀走 AI）· **guide 粒度 = 一类目一份 + 逐条目变量段、例外升格须登记指针**（`:55`–`:61`，`ADR-0225`）。就绪切片 = 八个资产类目表 + `Artwork` 挂点 + 一条内容一张不随境界分版 + 资产变更随发版及其可撤销代价。卡于（两条，`:67`–`:68`，均明写不阻塞字段落地）：生成资产落地 `game-feature-branch/` 的目录划分与完备性校验 · 各类目尺寸格式与图集策略（留待首批 guide 按 Midjourney 输出规格与 Godot 导入实测反推）。**上层 `art/_index.md:70` 的商用授权口径仍向下传导 ⇒ 不判 ready。结构性地无客户端行为面可 derive，不进 derive 队列** |
| `art/visuals/art-direction.md` | **partial**（**本次由 blocked 升级**） | **上一版「判据①直接失败」的理由已消失**：`:5` 的一句话方向现为完整实笔（厚涂写实中式卡牌插画 + 水墨点缀 + 冷光暗调 grimdark + 竖版暗底高对比）。九轴逐节已填：绘画质感与笔触（`:9`）· 基调与残酷表达尺度（`:17`，含不预留上调口子）· **三层色彩策略 + 五类卡框色取仙侠意象色、明确不套五行**（`:31`，`ADR-0226`）· 光照与材质（`:41`）· 人物（`:51`）· UI 元件材质隐喻（`:57`）· 文字与字符及装饰性豁免（`:78`）· 四组禁用清单（`:97`）。**仅两格未填**（均非取向问题）：构图节末的主体占画幅比例 / 视平线 / 安全区（`:74`）· 技术约束表的「尺寸与格式」（`:95`，留待首批 guide 反推）。**本库不承载生成产物 ⇒ 结构性地不产 FR**。失真：`:3` 的 `## 一句话方向` 标题与前段粘连 |
| `art/soundtracks/audio-direction.md` | **partial**（**本次由 blocked 升级**） | **上一版「判据①直接失败」的理由已消失**：`:9` 的一句话方向现为完整实笔（东方史诗骨架 + 四层配器 + 自然小调叠诡暗 + 纯器乐无人声 + 恒定节拍以织体密度推进 + 留白与不安是常态）。九轴逐节已填：基调与三条校准句（`:13`）· 四层配器表 + 诡暗层 + 无人声（`:20`）· 调式与节奏（`:36`）· 动态与混音（`:45` 耳机基准 + 频段分工）· 场景与篇章性格三参数梯度表（`:52`）· 音效材质（`:68`，古物实材质 + 「几十次重复可听」专项校验）· 七条禁用清单（`:84`）。**仅一格未填**：技术约束表的「格式与预算」（`:82`，随包体预算一并定）。**但它比视觉侧多一层阻塞**：音频生成工具未定案（`art/soundtracks/_index.md:41`，本分区第一前置）会让 prompt 组织形态悬空 ⇒ 只判 partial。**结构性地不产 FR** |
| `systems/_index.md` · `ux/_index.md` | **blocked**（非 derive 对象） | 两份均**无 `## 意图` / `## 决策` / `## 待决问题` 三段结构**，是纯导航索引 + 横向纪律宿主：`systems/_index.md` 是文档表 + 两条建档判据；`ux/_index.md` 是导航表 + 三条全库横向纪律（候选项排布轴 · 翻译键 · 内容 / 界面四问判据）+ 两层对照表。**各自的待决项已下沉到对应主题文档**，权威在下游 ⇒ 单独 derive 必重复 |
| `narrative/_index.md` | **blocked**（非 derive 对象 · **本次新增分区**） | 2026-09-12 新建的顶层分区（`ADR-0278`），与 `vision/` · `systems/` · `content/` · `art/` · `ux/` 平级。它是**内部事实源、不是玩家可见面** —— 承载世界观设定 / 时间线 / 人物关系 / 碎片台账，服务的是**内容作者之间的一致性**（写第二个角色的事件时要能查到第一个角色写了什么）。`ADR-0278` 明写「不做设定集、不做 lore 图鉴、不做任何可回看的档案面」，`ADR-0279` 明写碎片 lore 只活在事件描述与 `PlotNodeData.Body` 里、读过即过 ⇒ **结构性地不产任何客户端行为面 FR**，不进 derive 队列。现状：全文 38 行、**只有 `_index.md` 一份**，八条叙事纪律（lore 只撒轮回内 · 与支柱 9 的边界 · 冷的残缺文献体 · 允许文学质地但不许温度 · 体裁纪律压顶 · 交叉纯隐式 · 系列是世界观切片 · 叙事待遇不分付费轨道）均为实笔。**三处登记缺口**（见上方 🔴）：无 `## 意图` / `## 决策(-> ADR)` / `## 待决问题` 三个标准小节 · 零 ADR 回链而有 6 份 ADR 指向它 · `:16` 明写的未定项（内部结构与文档清单）只登记在 `open-questions/deferred-content.md`。**它虽不阻塞 derive，却同时卡住三处内容侧落笔**：付费系列的名字与主题 · `character-series` 内容类型开张 · 首批五角事件与剧本正文的 lore 编排 |
| `content/_index.md` | **blocked**（非 derive 对象 · 另一套闸） | 硬边界「本层不定义字段」+ 三层结构 + **类型登记表 20 行**（较上次 +1 —— 新增 `character-series/`，🟢 字段面已定〔`Id` + `LocalizedText` 名称 + 概述 + 可空 `Artwork`，**无规则字段**〕+ 一条悬空校验，**未开张**，开张需一轮 `/scaffold-content-type character-series`）· **开张列首次出现 1 个 ✓**（`character/`，2026-09-12 开张，`content/character/_index.md` 已成文含字段核对清单十行、交叉引用表、**系列台账**〔`/audit-content` 双向棘轮核对项的基线，当前为空〕与两条 `/audit-content` 新增核对项；条目台账仍 0 条）+ 6 行「内容编排目标对账表」+ 跨类型对账项 + 依赖链 + `Id` 约定 + 状态词汇。它持内容类型的 **scaffold** 就绪度，**与本小节的 derive 就绪度是两套闸，勿混** |
| `vision/pillars.md` · `vision/references.md` | **blocked**（非 derive 对象） | 北极星 / 参考登记，其余文档的挂靠前置。两份均无 `## 待决问题` 小节 ⇒ **挂靠侧零未决，判据③ 对全库成立**。`pillars.md` = 核心幻想 + 九条设计支柱（含第 9 条「信息靠遭遇获得」及其外部情报射程边界）+ 基调手感 + 反目标；`references.md` = 八个参照系的「借什么 / 不借什么」，已被 `art/*/references/_index.md` 细化到 prompt 粒度 |
| `vision/scope.md` | **blocked**（非 derive 对象 · **全库第一承重卡点的宿主**） | **两项仍未定且无处承载、已第二十天零推进**：① AI 生成资产的商用授权 / 版权立场 ② 初装包体预算。机械核实：`包体` / `授权` / `版权` / `商用` **四词零命中**，且**全文没有 `## 待决问题` 小节**；唯一「合规」命中讲的是实名 / 防沉迷 / PIPL / 渠道审核，与本条无关；「美术资源策略」节只有 TBA + 占位挂点。已落笔项：iOS 部署下限 15 · 篇章时长 45–55 / 50–60 / 60–70 · 外观付费五条已回落。失真：`:45` 孤立空 bullet · `:66` 自陈「0 份 FR」，首批 derive 落地即失真 |
| `terminology.md` · `program-overview.md` · `system-overview.md` | **blocked**（非 derive 对象） | 根级词表 / 运行时调用链 / Godot 工程落地形态，供 derive 时查阅。三份均零占位、零 `## 待决问题`；`system-overview.md` 未见新失真。**上一版登记的 `terminology.md:91` 旧目标时长本次已清**（`:92` 现为 45–55 / 50–60 / 60–70 分钟，熟练玩家口径）。**两处失真在册**：`terminology.md:123` 的「角色系列」词条仍写「一旦免费永不改付费」是**单向棘轮**，与 `ADR-0255` 改写后、`character-profile/_index.md:102` 与 `content/character/_index.md:55` 的**双向**口径正面相抵（本次新生，已列为落笔前必清项第 ④ 条）· `program-overview.md` §2 职责矩阵经 grep 复核仍**零命中** `StoreChannelManager` / `CodexManager` / `GrantPoolManager` 三者，停在 09-06 前的形态（权威在 `services/_index.md:50`–`:51`） |
| `art/_index.md` | **blocked**（**全库解锁面最宽的一份**） | 生产流水线三段 · 承重约定 · 继承约束表 · 当前状态（`:64` 已如实更新为「两侧方向文档均已成文」）均实笔。卡于三条（`:70`–`:72`）：**① AI 生成资产的商用授权与参考素材来源的合规口径（硬卡点 —— 明写是「选工具的一个前置」，落点 `vision/scope.md` 零承载）** · ② 参考素材二进制是否入库（仅「暂定只登记来源与描述」，未拍板）· ③ 两份 direction 各余的「格式与预算」格。**本库不承载生成产物（归 `game-feature-branch/`）⇒ 结构性地无客户端行为面可 derive** |
| `art/soundtracks/_index.md` | **blocked** | 六个资产类目表与 guide 编写顺序已定，移动端四条约束实笔。卡于三条（`:41`–`:43`）：**① 音频生成工具的最终定案（承重 · 本分区的第一前置 —— 倾向 Suno 但未拍板，且商用条款是选型的一部分，回指 `art/_index.md:70`）** · ② BGM 时长与码率的包体预算（回指 `vision/scope.md`，零承载）· ③ 三条音量轨 100/80/100 待实测。**第一前置未解，判 blocked 不放宽** |
| `art/visuals/animations/_index.md` | **blocked**（**属外部输入，不在本库可自解锁范围**） | 已承载的实质裁决：`ADR-0170` 的战斗外演出默认可跳过 / 快进 · 战斗内不设跳过控件 · 附论证；待决已收窄为「**在既有三重护栏内**每类动画的时长上界与缓动，不是能否跳过」；结构预案已写。**判定仍 blocked**：抬头明写「本节全部条目待咨询专业人士后确定」，范围四类 · 技术载体 · 制作方式三条全空 ⇒ 判据①的实质设计意图不存在。结构本身待立（预计需一份 `animation-direction.md`，且明写「不要在咨询之前照抄 `guides/` 那一套」） |
| `art/visuals/references/_index.md` · `art/soundtracks/references/_index.md` | **blocked** | **两份的登记内容本次均已到 prompt 粒度**：`visuals/` 三行（三国杀 / 弈仙牌 / 40k 各写清借与不借）· `soundtracks/` **两行**（三国杀主题 BGM 七条借 / 两条不借 · Balatro 定位锚）——上一版「`soundtracks/` 整表为空」已不成立。卡于：**「素材文件的存放（未定）」整节** —— 二进制是否入库未拍板，`素材位置` 列仍是 `_（待定，见下）_`，与上层 `art/_index.md:70` 的版权边界零承载形成自指闭环 ⇒ 判据②③败 |
| `art/visuals/guides/_index.md` · `art/soundtracks/guides/_index.md` | **blocked** | 两份的命名 / 粒度 / 上游 / 类目四条纪律均已定（含类目 guide 与例外升格 guide 的双命名规范、常规战斗与 Finale 各一份、篇章氛围逐篇章一份）。**但台账均为空、零份 guide** ⇒ 无 derive 对象；判据④依赖闭合失败：guide 编写被「尺寸格式 / 格式与预算」（未定）与「商用授权」（零承载）双重阻塞，音频侧另加「工具定案」第一前置 |
| `decisions/ADR-0002` ~ `ADR-0284`（283 份 Accepted） | **blocked** | 已采纳的决策记录，**非 derive 对象**（作为其余文档的就绪前置，本身不产 FR）。**计数经机械核对一致**：`decisions/` 下 **284 份** `ADR-*.md`（编号连续、零重号），台账 **284 行**，双向零缺口，扣除 `ADR-0001`（Proposed 示例）即 **283 份 Accepted**。本次由 267 增至 283：09-12 一次 `/write-adr game` 固化 `ADR-0269`~`ADR-0284`（16 份 —— 付费角色系列的解锁与购买形态 9 条 + 系列包装与叙事承载 7 条），并**就地改写 `ADR-0255`**（系列轨道棘轮 单向 → 双向，承「改决定直接改 ADR」不新增编号）；同批另有 `ADR-0077`（`EncounterTighten` 十常量 → 八常量）与 `ADR-0161`（否决理由改写）的就地改写。**`ADR-0183`~`ADR-0284` 共 102 份中仅 `ADR-0183` 与 `ADR-0255` 两个编号被主题文档引用到，其余 100 份反向引用为 0**（见上方 🔴 台账缺口） |
| `decisions/ADR-0001-example.md` · `decisions/_index.md` · `requirements/_index.md` · `content/character/_index.md` · `art/*/guides/_TEMPLATE.md` · `content/_TEMPLATE-*.md` · `requirements/_TEMPLATE*.md` | **blocked** | 示例占位（全库唯一非 Accepted 的 ADR，284 份中的那一份 Proposed）· 台账 · 流水线容器（`requirements/` 下仍**零 FR**，3 个文件全是模板与台账）· **内容层类型档案**（`content/character/_index.md`，走 scaffold / author 闸，不进 derive 队列）· 模板骨架（6 份），**均非 derive 对象** |

### 建议的 derive 顺序（仅限 ready / partial 项；被依赖者在前）

**落笔前的必须裁决：✅ 无 —— 队列连续第五次零前置。**
**落笔前的零决策修正：⚠ 12 处**（见上方台账缺口的 🟠 第三条；较上一版 13 处净减 1，上一版四处已清）。**本次会写进 FR 正文、必须先清的有四处**：`balance.md:1240` 的「回寿三档点数仍欠取值」（与 `ADR-0248` / `life-span.md:50` 的 50 / 100 / 200 正面相抵，见文末直读裁决）· `player-power/_index.md:170` 的「战后奖励池各档权重待定」（已被 `ADR-0237` / `ADR-0242` 关闭）· `life-cycle-service.md:398` 的「`Achievement` 条目 schema 与进度模型仍待定」（已被 `ADR-0196`~`0200` 推翻）· `terminology.md:123` 的「单向棘轮」（与 `ADR-0255` 改写后的双向口径相抵）。**前两处均为第二次登记、一轮未清。**
**落笔前的跨边界承接：⚠ 2 条**，均落 `content-service.md`（对侧 `ADR-0051` / `ADR-0052`），**必须先于第 7 步**。

1. `/derive-requirements systems/common-properties.md` —— **唯一整份就绪且被几乎所有内容与服务代码依赖的地基，零前置。** 账号级 RNG 的验收有现成的 8 组测试向量（后端 `contracts/profile-sync.md` §6a），是全库当前最硬的验收标准。`LocalizedText` 与 `DrawPool<T>` 同批、**在写下第一批 `.tres` 之前**。**按 `Source` 十值写枚举与合法子集表；分域校验表的三格现是规则层封死（`ADR-0267`），不得写成「暂不开放」。**
2. `/derive-requirements ux/error-and-blocking-ux.md` —— 翻译键基建（文档自己已点名 `FR-ux-translation-foundation`）。**无上游依赖**，是「每屏从第一行起就用键」这条起手纪律的前置，越早越省返工。**排除面仅剩逐条中文措辞**（FR 中标「文案定稿期填」）。**新增落笔面：`STORE_SERIES_*` 与 `STORE_UNAVAILABLE_SERIES_OWNED` / `_PARTIAL` / `_WINDOW` 走 `STORE_` 分区普通键，明确不占 `ERR_` 前缀**（`ADR-0275` / `ADR-0276` / `ADR-0277`）。
3. `/derive-requirements systems/architecture.md` + `systems/viewmodel.md` —— 服务骨架：七个 autoload 的注册顺序、EventBus、边界服务的接口 + Offline stub 双实现、`ProfileChangeSpec` 逐列（含 `AbilityStatusChanges`）、`OpError` 十值、`HiddenStatGrant` 三格。**两份同批**，均 ready、无排除面。`services/_index.md` 的层级词与七服务 × manager 清单随本步一并兑现、不另产 FR。
4. `/derive-requirements systems/services/profile-service.md` + `systems/player-profile/player-power/common-properties.md` + `systems/character-profile/item/common-properties.md` + `systems/character-profile/power/common-properties.md` —— 写入通道这一层地基（逐列 spec + `TryApply` 原子性与钳制 + 逐列失败语义 + `Project` 只读投影 + `CapabilityManager` 整面 + modifier 合并算法 + 门面级 `Holds(...)` + `SetAbilityStatus` 一个门面覆盖四类持有条目 + 两条成就新列）。**四份必须同批**（后三份的自有面很窄，作附属条目而非独立 FR；`power/common-properties.md` 宜先补三个标准小节）。**`CostKey` 写 15 行**（不写死列数）。**`ItemChargeElement` 只 derive `Delta < 0`。只排除 `PlayerItem` 触发一条。** **`PlayerEntitlement.CharacterSeries` 恒不进任何 `ProfileChangeSpec` 列、`ResourceElements` 不加行**（`ADR-0270`：客户端零写入通道，误写的置位当场 `PushError` + 整批拒绝）——写入通道表**不为它加行**是自洽的，不是缺口。
5. `/derive-requirements systems/player-profile/achievement/common-properties.md` + `achievement/_index.md` —— **成就族两份必须同批**（两个顶层键 + 两条新列 + `player-profile/_index.md:17`–`:18` 的字段表行 + v1 首发形状 #37 是同一次落笔面，后端侧已对账、契约零改动）。**信号面只 derive 已指名的六条，其余留占位、不臆造；排除两档专属奖励条目目录。**
6. `/derive-requirements systems/character-profile/_index.md` + `systems/player-profile/_index.md` + `systems/player-profile/codex/common-properties.md` + `systems/player-profile/game-setting.md` + `systems/character-profile/currency.md` + `systems/character-profile/mana.md` —— 两层 Profile 的存档 schema 地基。**六份同批**：互相引用、共用同一个 `schemaVersion` 与同一条迁移路径。**须排在第 5 步之后**（字段表两行直接引用成就两键）。**`character-profile/_index.md` 本次已整份 ready**：`CharacterData` 六格 + `SeriesId` / `Track` 两格（模板静态字段，**存档增量为 0**）+ **十八条加载期校验**（#14 ~ #18 中的 #16 / #17 / #18 一律走 `AllIncludingDisabled()`，按 `AllEnabled()` 统计会让「线上秒关一个问题角色」把启动打崩）+ `CharacterTrack` 哨兵枚举 + `Track == Paid` 退役口径分档表 + 双向棘轮（内容纪律，**不做运行时机制**）。`player-profile/_index.md` 本步落 `PlayerEntitlement.CharacterSeries` 整套（`IReadOnlyList<CharacterSeriesEntry>` · 后端唯一写入方 · 不配兑现水位 · 读档三情形 · 回声路径老档缺字段两时点）。**只取存档 / 写入面，不取任何图鉴屏或设置屏呈现，不取任何货币绝对数字；排除首批付费系列的 `SeriesId` 取值与系列名（内容阶段）。**
7. `/derive-requirements systems/services/content-service.md` —— 内容加载与校验链路（依赖 1、3）。**必须先落两条跨边界承接**（对侧 `ADR-0051` manifest 路径自行选路 · `ADR-0052` CDN 4xx 两分处置与「不对 4xx 走退避重传」的收窄），否则现行重试语义与对侧契约并存即互相矛盾。**退役路径表按轨道收窄要一并落**（`:101`，`ADR-0273`：`Track == Paid` 禁止永久退役 → 加载期 `PushError`；它与 `character-profile/_index.md` 的校验 #18 是同一件事的两处，须同批维护）。**仅排除 disabled 条目被存档引用时的 UX**（该分支须留空、不写 UI）。
8. `/derive-requirements systems/services/sync-service.md` + `systems/services/profile-schema-versions.md` —— 本地缓存原子写 + push 调度与退避 + CAS / 幂等 + 两层 diff + 回声校验，**加上 v1 首发形状清单（含成就三行）与 `ProfileShapeCheck` 护栏规格**（依赖 1、3、4、5、6）。**两份同批。本次新增 `schemaVersion` v2 行**（`:23`：新增 `characterSeries` · v1 → v2 迁移器写空列表 · 迁移之后仍缺即真异常；对侧兼容矩阵**先加、客户端后发**）。**条件分支如实写进 FR**：若付费系列改在首发之前落地，按「首发前的一切改动归入 v1」并入 v1 清单、不产生 bump，两条路径结构一字不差。**排除 golden 快照的实际生成**（`profile-shape-v1.json` 与 `-v2.json` 均挂在 `.csproj` 尚不存在上），**FR 文本里不得出现任何「v1 共 N 格」的计数型表述。** 购买段两条腿留到第 27 步。
9. `/derive-requirements systems/game-progression.md` —— 两份 UX 的上游编排层：篇章 / 境界推进与 `eventCountLimit` 配额 + 经验阈值曲线 / `ExperienceGrade` 映射 / `FailureRatio` 折算 / ≈55% 覆盖率 + 「Finale 前必须升满」硬约束 + **难度与数值缩放的两条分格轴与十一条曲线登记** + Travel 路由 80/20（依赖 1、3）。**排除面为零。**
10. `/derive-requirements ux/screen-flow.md` + `ux/onboarding.md` —— 登录屏 + **角色选择屏（给简介、不标推荐；未拥有的付费角色完全不出现——不设卡位、不灰显、不加底部入口，`ADR-0277`）** + 主菜单导航骨架 + **Store 屏三个新结果态（系列列表 · 系列详情 · 解锁结果态，不新增屏、不新增主菜单入口；详情页付款前必须列出本系列全部角色，复用角色选择屏卡面构件、不展示数值、不标推荐、不写「更强」暗示）** + 储物袋面板 + 玩家档案屏 + 轮回 / 篇章结束屏 + 图鉴三层浏览 + 剧本段落进结算面板 + 主动弃置入口 + 非商店平台 Store 入口不渲染 + 寿元一次性听觉提示 + 候选项列表语言全套（依赖 2、3、6、9）。**两份同批，现均整份 ready。排除 `LocationCodex` 单本页与揭示转场的时长 / 音效初值。**
11. `/derive-requirements systems/services/account-service.md` —— 会话生命周期 + 四个账号方法 + refresh 两条路径 + `deviceId` 与 refresh token 落点 + 平台密钥库能力矩阵与五条升级触发 + 启动期静默续期 + 合规四域切分与拦截路径 + 失败映射逐 `code` 表（依赖 2、3、8、10）。`account-info.md` 随本步同批兑现、不另产 FR。**排除面为零。**
12. `/derive-requirements systems/character-profile/deck/common-properties.md` —— **效果原语层地基**：`EffectData` 子类树 + **八原语全量** + `StaticModifierData` + `TriggerConditionData` / `TimingIds` + `EffectCondition` + `CardData` 收口 + **21 条加载期校验**。**它是五个内容类型的共同底座，必须先于 13–15。** 纯数据资源型切片、过三条闸。**只排除 starter deck；抽 / 弃 / 洗数值已定（起手 4 / 每回合 2 / 手牌上限 7 三章统一 / 起始 15 张），可整块落笔。手牌上限恒 7 且不进任何覆写组。**
13. `/derive-requirements systems/character-profile/deck/_index.md` —— `CultivationTechniqueData` + 灵根准入判定式与四处取池点 + 成员卡反建索引 + `CardSubtypeData` + `CardPool` 三值 + 五个 `DeckChangeOp` + 手牌上限纯上界 + **规模口径表（一门 5 张 / `MaxTier` 5 每层 +20% / 起始 15 张）** + **「内容性格」整节**（依赖 6、12）。**不得先于 6**。**通用 / 专属配比按稀有度分层的梯度、无硬限制（`ADR-0257`）与双灵根的复合功法 / 数量上限双机制（`ADR-0258`）是编排口径，写进 FR 的是口径不是数值。排除逐档比例、双灵根两类专属内容对铺量、关键字与次类型首批清单、starter deck、逐张产削取值；首批全单灵根且五角压平同一复杂度档（`ADR-0259`），多灵根路径不实现。**
14. `/derive-requirements systems/character-profile/power/_index.md` —— `PowerData` 字段面 + **跨载体三行边界判据表（三者边界的唯一权威）** + 强度比例刻度与 **25% 战斗内闸门** + 内容编排口径表 + 三格非空校验 + 战斗外触发式校验 + 不得产寿元 + 禁用截断五行表（依赖 4、12）。**排除获取侧频次分布与强度定量三格。**
15. `/derive-requirements systems/character-profile/item/_index.md` + `systems/player-profile/player-item/_index.md` + `player-item/common-properties.md` —— `ItemData` 两格使用效果面 + `MaxUsesPerCombat` + I-1 ~ I-13 校验 + 战斗外一次事务链路 + 随售通道 + **回寿法宝三格护栏与三档绑定表**（依赖 4、6、8、12、14）。**三份同批**（两层共用 `ItemData`，古宝的类型面无独立 FR 面）。**`item/_index.md` 本次已整份 ready**，排除面只剩内容目录。**`itemPowerRatio` 一律引 `balance.md:868`–`:879`，不得引 `item/_index.md:36`–`:38`。**战斗外可写 key 一律按白名单 `I-13 = { LifeSpan }` 落校验（`ADR-0260`），不得放宽；族护栏按 `ADR-0261` 的判据逐族核对；事件侧产出按 `ADR-0263` 每条 `Count == 1`。排除道具种类目录与古宝种类目录、正向补充通道。**落笔前必清 `player-item/common-properties.md:13` 与 `_index.md:10` 那对直接相抵的句子，以及两处「可购价格 / 库存权重未设计」的陈旧副本。**
16. `/derive-requirements systems/adventure-event/common-properties.md` —— `EventOption` 数据形态 + 事件收口事务语义 + `lifeSpanCost` 三变异位 + Travel 双禁令 + `AddLooseCard` 取池链 + **隐藏属性编排判据表** + **非战斗产出取 `Solid` 表 × `m(c)`**（依赖 1、6、12）。**它是后续全部事件类型 FR 的共同类型地基**，必须先于 19。**本份整份 ready、无排除面**；`adventure-event/_index.md` 随本步作分类法参照、不另产 FR。
17. `/derive-requirements systems/services/future-event-service.md` + `systems/services/plot-manager.md` —— eventOptions 十步管线 + 三道短缺闸 + `Priority` 抬升判据 + `HiddenStatGrant.Direction` 符号展开点 + `FinaleDiff` 旁路 + `TryGetPlotSegment` 只读转发 + `ChooseBranch` 提交形态与 `BranchLabel` 焊死 + 剧本调制输入 + 档位表 / 阈值 / 回滞 / 跨档叙事 / 两项属性定稿 / 两条隐藏属性剧情线形态。**两份必须同批。本次排除面由零变为两条（`plot-manager.md` 降 partial 所致）：角色专属 `SideStory` 的激活口 · `CharacterIds` 的 gating 语义** —— **derive 时不得为它们臆造激活通路**，`PlotArcData.CharacterIds` 只写「填值即角色专属 + 悬空 `PushError`」这一层。物化代入面按 `EncounterSpec` **三格**可空覆写组写（`HandLimit` 已删除），不得沿用四格 / 五格旧文本。
18. `/derive-requirements systems/services/life-cycle-service.md` + `systems/character-profile/life-span.md` —— 轮回骨架、角色状态分类法、`DefeatReason` 三值与 Finale 旁路、存档点与决策点清单、`SeedManager` 四子流、批次层即时提交、两处结束摘要组装、轮回出口三层处置与 `chapterStartSnapshot`、主动放弃角色、痕迹只追加，以及寿元的三条不变式与两个扣减来源 + `R_c` 两格书写（依赖 4、16、17）。**两份同批**（`life-span.md` 不单列 FR）。**`life-span.md` 本次整份 ready、排除面清零** —— `lossPerMomentum` 三章三格全为定值（10 / 5 / 10）、回复三档 50 / 100 / 200 已定案，不得再标待定。**本步另须落 `GetSelectableCharacters()` 的解锁过滤**（`ADR-0272`：`AllEnabled<CharacterData>()` ∩ 绑定条目全启用 ∩（`Track == Free` ∨ 解锁集合含该 `SeriesId`）；**签名不变、`StartCycle` 零改动、不涉 RNG，绝不写成 `AllEnabled(accountContext)`**）。**只排除 `PlayerItem` 的解锁 / 获取 / 失去触发；`:398` 的 `Achievement` 半句与 `:400` 的隐藏属性半句均为陈旧副本，落笔前须删。**
19. 四个非战斗事件子类型，按依赖薄厚依次：`travel/` → `explore/` → `research/` → `exchange/`（各自两份同批；依赖 16、17、18）。**四份的机制面已闭合**；`travel/` 排除 location 运营替代通道（**不得臆造闸门**）与 Travel 行定价取值 · `explore/` 排除真身占比、转场时长与**定价表 Explore 行取值**（本次直读推翻上一版把它判为陈旧副本的结论，见文末）· `research/` 排除走火入魔权重（`:95` 是错位的已定案句，非缺口）· `exchange/` 排除 `RerollBaseCost` / `RerollCostStep` 取值与单事件槽位总数上界（**barter 全套含 `ADR-0264` 的两格护栏、逐族库存深度 `ADR-0262`、软校验 `X-1`、刷新机制含首批关闭均可整体进切片；逐族上界之和 Σ = 7 不得被反推为总上界**）。已落表的定价与回收率**可作结构输入引用、不得把绝对数字写进验收条件**。
20. `/derive-requirements systems/balance.md` —— **第一批平衡资源 `.tres`**：已定值 / 已定形的那些表（含 21 格 `lifeSpanCost` 与 λ 15/17/44 · 25 格定价外积 · 两条货币产出曲线 · 两档回收率 · `BaseTypeWeights` 五格 · `FinaleDiff` · 经验阈值全表 · **`lossPerMomentum` 三章三格 10 / 5 / 10** · `GrantPoolWeights` · `SelectionWeightGrades` · `CombatRewardPoolWeights` 九行 × `m(c)` · `itemPowerRatio` 四档 · **`EncounterTighten` 八个界常量**）+ `Content.Single<T>()` 读取面 + 加载期条数校验（依赖 1、7）。**只取已定格，全部未定格显式留空并标注；三格取池余量与 `K` 可先填 0。`EncounterTighten` 一律按八常量口径写，`MaxHandLimitTighten` / `MinHandLimit` 两常量不存在。**
21. `/derive-requirements systems/enemies/_index.md` + `enemies/common-properties.md` —— `EnemyData` 数据形态与全部加载期校验 + `EnemyAiProfileData` 与兜底 argmax（**默认性格 = 镜像玩家基调**）+ 两条护栏（`±2` / `±1 档`）+ **`LineSlot` 五成员与 `Lines` 稀疏覆写**（依赖 12、13、20）。**两份必须同批**（`EnemyData` 字段面与校验表在两份之间来回引用），两份待决均明写「无」。纯数据资源型切片、过三条闸。**排除全部数值缩放。**
22. `/derive-requirements systems/player-profile/codex/enemy-codex.md` + `codex/technique-codex.md` + `codex/_index.md` —— 两本的词条规格与解锁触发 + 族级三层浏览与完成度口径（依赖 6、10、13、21）。**三份同批**（连锁解锁横跨两本，排布由族总览定死）。**六本可写，`LocationCodex` 的单本页内容区整块排除。** 落笔时须补上 `combat/_index.md:217` 委派而 `enemy-codex.md` 尚无的那条「同一敌人须同时说得通切磋 / 厮杀」写作口径。**碎片 lore 恒不进图鉴**（`ADR-0279`：图鉴族不因 lore 扩张，本数与粒度不变）。
23. `/derive-requirements systems/scoring.md` —— 道念计分规则与两条转换支路 + 三档 `BaseReward` 默认 element（依赖 20）。**一组纯函数 + 一份平衡资源**，验收靠给定输入验算输出。**逐张产削量一律写成「读平衡资源」。须与 20 同批。**
24. `/derive-requirements systems/services/combat-service.md` + `systems/adventure-event/combat/_index.md` + `combat/common-properties.md` —— 战斗机制骨架 + 效果流水线五阶段 + 决策点 D0–D7 + AI 两层结构与 `ViewerSide` 投影 + `StackEntryKind` 五值 + 奖励生成路径整段 + 三档遭遇参数与六条代价表（依赖 12、18、21、23）。**三份同批。`EncounterSpec` 的可空覆写组写三格（`InitialDraw` / `DrawPerTurn` / `EnemyManaLimit`），`HandLimit` 一格不存在。排除战斗内容本体、Finale 奖励加厚取值、道念相关状态与倍率是否存在、叙事一致性写作口径。落笔一律取 `EncounterSpec.TurnLimit`，不得把 10 写成常量。** 验证靠在编辑器里跑一场用占位内容拼出的遭遇观察栈与回合流转。
25. `/derive-requirements ux/combat-ux.md` —— 六区竖屏分区与屏高预算表 + 栈浮层化 + 战报双视图与文案体系 + 卡框色与角标分工 + 详情页三档禁用 + 疲劳三格 + 战后奖励面板三段形态 + 只读层图标条 + 手牌重叠扇（依赖 10、24）。**排除三步结构的 mana 刷满 / 抽牌 / 状态消散动画细节、屏高比例的真机校准、五类卡框色的具体色相与截断符 / 落空符字形。** 手牌上限的 `n/7` 呈现恒取全局常量（`EncounterSpec.HandLimit` 已不存在），呈现侧仍不写死 7。
26. `/derive-requirements systems/player-profile/player-power/_index.md` —— 失去三形态 + 残卷 + 授予池结构 + **四支频次预算表**（可整块落笔并标「待实测校准的初值」；本轮已按法宝一族 ≈0.35 重新配平为 神通 ≈0.32 / 法则置换 ≈0.20 / 法则禁用 ≈0.13，**并入既有支、不开第五行**）+ 三条获取通道与七道 pay-grind-to-win 护栏 + 账号级获取速率的可算分母 `x ≈ 9–12`（依赖 4、6、14）。**排除面为零。落笔前必清 `:170` 的战后奖励池权重陈旧副本。**
27. `/derive-requirements systems/monetization.md` + `systems/services/sync-service.md` 的购买段 —— **兑现段 + 购买段 + 外观预留 + 付费角色系列一并**（三渠道选型与封装、`StoreChannelManager` / `IStoreChannel` / `IPurchaseBackend`、三个显式步骤、失败处置五情形、入口前置条件表**两段化 + 6/7/8 三行**、「无可用渠道不渲染」、`PlayerEntitlement.Cosmetic` 三个加法窗口）+ 三道闸 + 兑现水位幂等 + `OpError.Purchase` 处置（依赖 1 的账号级 RNG、6 的持有集合、8 的 push 调度、10 的 Store 三结果态、18 的取池过滤）。**排在最后**：依赖的两个通用内容池当前为空。**闸①的断言阈值须留空**（`K` 未定），不得以占位数值写死。**付费角色系列本次由「整段不落」转为可写**：在售清单走 `AllIncludingDisabled<CharacterData>()` 按 `SeriesId` 归组、`productId` 由 `SeriesId` **机械变换**（不硬编码字面量、不持映射表）· **兑现段整段不存在**（零 `TryApply` / 零 `AccountRng` / 零水位；**照抄一个 `CharacterSeriesRedeemedOrdinal` 是最诱人的错误答案**）· 跨启动补入口两条（本地持久化 `receiptId` + 收据幂等读 · 启动期查询平台未完成交易并补验票）· **购后 pull 失败不阻塞「开始新轮回」**（premium bundle 的阻塞纪律一字不改，但已加限定词、不再读作通则）· 限时在售窗口的三条语义（窗口外取**置灰 + 一行说明**而非移除控件）。**仍排除**：各系列的具体名字 / 主题 / `SeriesId` slug（内容阶段，欠 `narrative/` 底稿）· 双灵根免费批的具体推出时点 · 进阶批「复杂度更高」的表达 · **在售窗口在内容层的具体承载形态**（落 `CharacterData` 一格还是随 `CharacterSeriesData` 给出，`ADR-0276` 明写留给内容类型开张那一轮）。**落笔前必清 `:250`–`:252` 的三处「ADR 候选待 `/write-adr` 立档」。**

> **仍然不要**对 art 任何文档 / `narrative/` 任何文档 / `LocationCodex` 单本页 / 成就与古宝条目目录 derive。`narrative/` 是**内部事实源、不是玩家可见面**（`ADR-0278`：不做设定集、不做 lore 图鉴、不做任何可回看的档案面）⇒ **结构性地不产任何客户端行为面 FR**，它服务的是内容作者之间的一致性。玩法侧的结构性欠账连续第二次为零 —— 剩余的战斗内容条目属**内容阶段**（走 `/scaffold-content-type` 与 `/author-content`，不走 `/derive-requirements`），本轮另新增一个待开张的内容类型 **`character-series`**（`ADR-0282`，`content/_index.md` 已登记 🟢 未开张，开张需一轮 `/scaffold-content-type character-series`）。全库最宽的剩余卡点仍是 **`vision/scope.md` 的两条零承载项**，它与玩法 derive 队列**完全不相干** —— 即前 27 步可以在它未答定的情况下全部走完。

### 最短解锁路径（blocked / partial 项，按解锁面从大到小）

1. **把「AI 生成资产的商用授权 / 版权立场」与「初装包体预算」两条写进 `vision/scope.md` —— 连续第三次列第一位，且仍是唯一的结构性解锁点。** 这一次落笔可同时解开 **六处 blocked**：`art/_index.md:70` · `art/visuals/references/_index.md` 与 `art/soundtracks/references/_index.md` 的「素材文件的存放」自指闭环 · 两份 `guides/_index.md` 的零台账 · `art/soundtracks/_index.md:41` 的「第一前置」（音频生成工具定案，其商用条款正是本条），并同时补齐两份 direction 各余的「格式与预算」格。**注意：`vision/scope.md` 目前连 `## 待决问题` 小节都没有**，落笔时须一并建节。**art 台账类文档即便全部答完仍结构性地不产 FR**，解锁的是**能否开始产 guide 与生成资产**。→ `/analyze-new-ideas`
2. **角色专属 `SideStory` 的激活口 + `CharacterIds` gating 语义（`ADR-0283` 自己指定的下一步）** —— **本次新进第二位，且是本轮唯一的降级来源。** 现有两条 arc 激活通路（`PlotTriggerId` / 篇章边界）都不覆盖「角色专属、不由隐藏属性触发」的 SideStory；gating 语义另需定「何时比对、不匹配时是不激活还是惰性」。**两条同批**，一次推演即让 `plot-manager.md` 从 partial 回到 ready，并把第 17 步整批的排除面清零。→ `/provide-solution-draft`
3. **一次「战斗 / 事件 UX 收尾专场」** —— 由第二位后移一格（内容未变）。三项：① `ux/combat-ux.md:264` 的三步结构动画细节（mana 刷满 / 抽牌节拍 / 回合内状态消散）② `codex/_index.md:88` 的 `LocationCodex` 单本页画图形态与词条载体 ③ `content-service.md:495` 的 disabled 条目被引用时的 UX。另可顺手带上 `exchange/` 的单事件槽位总数上界与 `RerollBaseCost` / `RerollCostStep`（`ADR-0262` 明写不替槽位上界作答）。**它把 `codex/_index.md` 的最后一条卡点关掉，并收窄三份 partial 的排除面。** → `/analyze-new-ideas`
4. **一次 handoff 定古宝种类目录 + 正向次数补充通道** —— 解锁 `player-item/` 两份与 `player-profile/_index.md:244` / `profile-service.md:513` / `life-cycle-service.md:398` 的**共同残留面**（`PlayerItem` 的解锁 / 获取 / 失去触发）。`PlayerPower` 那一半已闭合，此条现在是三份服务文档排除面的唯一来源。**注：`ADR-0247` / `ADR-0248` 只覆盖 `Scope == Character` 的回寿法宝，对古宝零升档。** → `/analyze-new-ideas`
5. **`narrative/` 的世界观底稿与内部结构** —— **本次新进**。它不解锁任何 derive（本分区结构性不产 FR），但它**同时卡住三处内容侧落笔**：首批付费系列的 `SeriesId` slug / 系列名 / 五行构成（`character-profile/_index.md:459` · `monetization.md:262` · `content/character/_index.md` 的系列台账当前为空）· `character-series` 内容类型的开张 · 首批五角事件与剧本正文的碎片 lore 编排。**正确入口是先建区底稿（`/analyze-new-ideas` 或直接由用户撰写），再 `/scaffold-content-type character-series`。**
6. **starter deck 设计过程** —— 保持既有位置。量纲基准、`MaxTier`、起始 15 张、内容性格七条、通用 / 专属分层口径（`ADR-0257`）与双机制卡池区分（`ADR-0258`）**均已定案**，它不再是设计缺口；剩余是三件内容编排事：关键字与次类型的首批清单 · 逐张卡牌的道念产削取值 · 逐档通用比例与双灵根两类专属内容的对铺量。**正确入口是 `/scaffold-content-type` + `/author-content`，不是 `/analyze-new-ideas`。**
7. **成就与古宝的两档专属奖励条目目录** —— 依赖法则 / 古宝条目先行，属内容阶段（`/scaffold-content-type`）。成就的 schema / 采集面 / 进度模型**已全部关闭**，不再需要 handoff。
8. `art/visuals/animations/_index.md` 的范围四类 / 技术载体 / 制作方式明写「待咨询专业人士」，属**外部输入**，不在本库可自解锁范围。

**以直读裁决的冲突（2 处）：**

1. **`life-cycle-service.md` 的两句陈旧措辞是否已清。** 分片 E 报「已清」（`:387` 为空行、`:389` 为模板引言），分片 F 报「仍在」（`:398` / `:400`）。orchestrator 直读裁定：**仍在**。文档因 09-12 的多轮落笔而变长，`## 决策(-> ADR)` 已后移到 `:388`、`## 待决问题` 后移到 `:395`；分片 E 是按上一版记录的旧行号定位而误判为「已清」。现行原文 `:398` 为「……仍待定：**`Achievement` 的条目 schema 与进度模型**、`PlayerItem` 的解锁 / 获取 / 失去触发」，`:400` 为「**仍待定的那一项（增减触发）同样登记在那份文档的「待决问题」小节**」——而 `plot-manager.md` 现存三条待决（`:615` 映射值 / `:616` SideStory 激活口 / `:617` gating 语义）中**没有**「增减触发」这一项。⇒ **两句均须清理**，其中 `:398` 已升为落笔前必清项第 ③ 条。
2. **`explore/_index.md:122`「定价表 Explore 行的取值」是不是陈旧副本。** 上一版判定它是陈旧待决项（理由：`balance.md:143` 已给 24 / 27 / 70）。orchestrator 直读裁定：**上一版判错，本条是活项，撤销其「落笔前必清」登记。** `balance.md:1242` 的待决条原文把 Explore 真身占比 `5 : 3 : 2` 的统计回归写为「**兼定价表 Explore 行 24 / 27 / 70 的标定输入**」⇒ 24 / 27 / 70 是**初值而非定案**，`explore/_index.md:122` 自陈「Explore 自成一行、不由真身推导已定；**填多少**留待内容扩充后的统计校准」与之逐字自洽。两处并非相抵，而是同一条校准项的上下游。⇒ **正文一字不改**，本条从零决策修正清单中移除。

## 下一阶段

- **ADR 状态：** `decisions/` 现有 **283 份 Accepted**（`ADR-0002` ~ `ADR-0284`，`ADR-0001` 为 Proposed 示例占位）。**待固化的 ADR 候选：暂无。**
  2026-08-28 的一次 `/write-adr all` 全量扫描（逐份核对 29 份未被任何 ADR 引为来源的 handoff）把散落其中、已落笔主题文档却无档案的定案固化为 **23 份新 ADR**（`ADR-0100` ~ `ADR-0122`），覆盖 sync 族（`revision` CAS · flush 非阻塞 · 闸门口径）· 命名通则 · 战斗 RNG 单流 · 敌人 AI 权重向量 · 效果原语语法 · capability flag / modifier · 道具使用效果面与批次层提交等。
  台账与逐条影响文档见 `decisions/_index.md`。
  2026-08-30 的一次 `/write-adr game` 固化 **10 份新 ADR**（`ADR-0123` ~ `ADR-0132`）：上述 4 条登记候选（灵根修习准入 · `Artwork` 单格与 `RealmArtworks` · 二进制不经 overlay · Exchange 以物易物）全部立档；另逐份核对未被任何 ADR 引为来源的 handoff，补固化 6 条散落定案（寿元合并与显性化 · `StatusChanges` 置值列 · `HiddenStatDirection` · flags 第三层覆盖来源 · `Upgrade` 非闸门点不硬阻塞 · `StackEntryKind.UsedItem`）。
  2026-09-05 的一次 `/write-adr game`（范围 `all`）固化 **26 份新 ADR**（`ADR-0133` ~ `ADR-0158`）：三条登记候选（货币跨篇章结转 · 灵石渠道口径与 Travel 禁令扩三 key · 仙玉不落可售出族）全部立档；另逐份核对 22 份未被任何 ADR 引为来源的 handoff，补固化 23 条散落定案，覆盖 09-02 / 09-03 / 09-05 三批（图鉴三层浏览 · 轮回结束屏与篇章结束屏 · 阵法启动式异能宿主 · 剧本分支落点与 `BranchLabel` 焊死 · 跨载体边界判据 · 神通强度刻度 · 快照按视角过滤 · 合规域切分判据 · `lifeSpanCost` 耗时正比定价 · 剧本层对外面 · schema 登记权威与不写计数 · `FinaleDiff` 旋钮 · 灰态非禁用 · `OpError.Purchase` · 打包工具两项产出 等），并补立两条早期基础定案（两层档案持有骨架 `ADR-0136` · 设计库类模型化组织 `ADR-0137`）。
  2026-09-07 的一次 `/write-adr game`（范围 `all`）固化 **24 份新 ADR**（`ADR-0159` ~ `ADR-0182`）：两条登记候选（不设 ante 式章内难度阶梯与两条分格轴 `ADR-0163` · 随包基线 `Id` 超集不变式 `ADR-0182`）全部立档；另逐份核对 19 份未被任何 ADR 引为来源的 handoff，补固化 22 条散落定案，覆盖 09-06 批（失去能力频次预算 · 主动放弃角色 · 篇章时长上移 ≈60 分钟 · 轮回出口三层处置与痕迹只追加 · `AbilityStatusChanges` 写入通道 · 隐藏属性收口两项 · 战后奖励四族与档位闸 · 演出跳过权分档 · 敌人产出同因 · `BaseTypeWeightsData` 与 `combatTier` 编排口径 · 类型修正护栏 · 失败容错 N = 2 与偏紧入账 · 不给距 Finale 读数 · IAP 四条）与 09-02 批（`MoveCardEffect` 补 `Side` · 平台密钥库五条触发条件）。三份历史 handoff 判定不建档：`2026-08-02c`（意图族已被 `ADR-0059` 整条推翻）· `2026-08-12b`（已由 `ADR-0051` 承载）· `2026-07-12-example`（模板示例）。
  2026-09-08 的一次 `/write-adr game`（范围 `all`）固化 **41 份新 ADR**（`ADR-0183` ~ `ADR-0223`）：候选清单当时为空，全部来自逐份核对 6 份未被任何 ADR 引为来源的 handoff 后扫出的散落定案。按批：09-07 共 18 条（战斗量纲基准 `momentumPerMana = 1` · 费用曲线随境界上移与牌流 4/2/7 · 起始卡组 15 张与敌方规模 · `MaxTier` 上界 5 与每层 +20% · ch3 越阶追分承担面 · 道念修正只用加法层 · 永久物 rate · 神通 25% 闸门 · 外观预留三个加法窗口 / 品类 / 持有与选用切分 / 条目形态与发版节奏 / 呈现落点 · 成就存档两键 / 内容 schema 与信号表 / 加权进度与隐藏分母 / 采集面与两条新列 / 两档奖励族分配）· 09-08 共 23 条（栈浮层化 · 竖屏六区屏高预算 · 敌方区只读 · 手牌重叠扇与卡面占位 · 道念差徽标 · 不做复合追分指示 · 牌堆可见面 · 只读层图标条 · 0 值处置判据 · `AbilityScope` 形状角标 · 卡框色与角标分工 · 详情页与禁用档 · 战报文案体系 · 疲劳三格呈现 · 战后奖励面板形态 · `LineSlot` 五成员与台词硬纪律 · 候选项排布轴判据 · 候选条目构件与标注层 · 事件选项焦点制 · carousel 的 N 自适应 · 共用卡宽基准 · 闭关面板竖屏形态 · 风险档中性标注）。
  2026-09-10 的一次 `/write-adr game`（范围 `all`）固化 **31 份新 ADR**（`ADR-0224` ~ `ADR-0254`）：唯一一条登记候选（寿元告警的一次性听觉提示 `ADR-0236`）立档；另逐份核对 6 份未被任何 ADR 引为来源的 handoff（`2026-09-09` / `09-09b` / `09-09c` / `09-09d` / `09-09e` / `09-09f`），补固化 30 条散落定案，覆盖美术方向 3 条（UI 制作路径 · guide 粒度 · 卡框色语汇）· 战斗手感性格 9 条 · 战后奖励稀有度与量纲 9 条（九行权重表与篇章乘数 · 每池五档全非空 · `RewardPoolId` 可空 · 九个具名池 · 条目数矩阵 等）· 回寿护栏 3 条 · 隐藏属性编排 6 条。**两条方向本体（视觉九轴 / 听觉九轴）判定不另立 ADR**——`ADR-0100` 的后果已把美术 / 音频基调的权威分别指向 `art/visuals/art-direction.md` 与 `art/soundtracks/audio-direction.md`；**回寿法宝的 L-0 免费通道排除**已由 `/analyze-new-ideas` 直接写进 `ADR-0066` 的「后果」，承「改决定直接改 ADR」不另开编号。
  2026-09-11 的一次 `/write-adr game`（范围 `all`）固化 **14 份新 ADR**（`ADR-0255` ~ `ADR-0268`）：登记候选 4 条（付费解锁角色系列 · 全角色可通关验收目标 · 通用 / 专属功法稀有度分层梯度 · 多灵根免费轨道与双机制卡池区分）全部立档，其中候选 ④ 因「首批五角同档压平」可被单独推翻而拆为两份（`ADR-0258` / `ADR-0259`）；另逐份核对未被任何 ADR 引为来源的 handoff，补固化 9 条散落定案——道具族供给护栏 7 条（`I-13` 白名单 · 族护栏判据 · 逐族库存深度 · 事件侧产出量与 `X-1` · barter 两格护栏 · 法宝置换并入上层 ≈1.0 · `R_c` 重填与条件概率修正）与 PlayerPower 2 条（三条获取通道 + 三格规则层封死 · 账号级获取速率派生表）。**候选项构件的「第七位 · 行尾操作控件」不另立 ADR**——它改的是 `ADR-0218` 那份档案自身的决定（六个位 → 六位 + 纵向侧第七位），承「改决定直接改 ADR」**已就地改写 `ADR-0218` 并同步台账行**，不新增编号。
  2026-09-12 的一次 `/write-adr game`（范围 `all`）固化 **16 份新 ADR**（`ADR-0269` ~ `ADR-0284`）：候选清单当时为空，全部来自逐份核对未被任何 ADR 引为来源的 handoff 后扫出的散落定案，来源恰为 09-12 的两份 handoff。付费角色系列的解锁与购买形态 9 条（`SeriesId` + `Track` 两格与校验 #14~#18 · `PlayerEntitlement.CharacterSeries` 与 `schemaVersion` v2 · 回声路径老档缺字段两时点 · 取池过滤落 `GetSelectableCharacters()` · 付费轨道退役口径 · 兑现段整段不存在 · 在售清单自给与前置条件表两段化 · 限时在售窗口 · 推销面穷举）· 系列包装与叙事承载 7 条（新建 `narrative/` 分区 · lore 只撒轮回内不留存 · 冷的残缺文献体与素语射程 · 交叉纯隐式 · 系列 = 世界观切片与 `CharacterSeriesData` · 剧情不是付费面 · 推出次序定序）。**轨道棘轮由单向改双向不另立 ADR**——它改的是 `ADR-0255` 那份档案自身的决定，承「改决定直接改 ADR」**已就地改写 `ADR-0255`（标题 / 决策 / 后果）并同步台账行**，不新增编号。一条判定不建档：「首批五角 = 互不相干的五个独行者、共享同一套世界观架构」在主题文档查无实据（详见 `open-questions/update-log.md`）。
  - ✅ 更早的一条候选「**不设 `GlobalBalanceData` 兜底大表，平衡资源按三问判据（消费者 / 覆写纪律 / 跨字段不变式）逐份切**」（`handoffs/2026-08-22-singleton-balance-resource-registry.md`）已于 2026-08-28 收口：`systems/balance.md` 补上了该判据本身那一小节（此前只有逐处应用），**并直接改写既有的 `ADR-0074`**（它本就是「凡可调数值一律住平衡资源」那条决策的档案，同一件事不另开编号——见本库「改决定直接改 ADR」的治理原则）；`ADR-0074` 后果里自陈的那条欠账随之关闭。**无需再跑 `/write-adr`。**
  （注：ADR 现可自由编辑，改决定直接改 ADR，不再新开取代 ADR。）
- **流水线闭环（07-30）：** design → code 链路补上 `/breakdown-requirements`（一份 FR → 一个文件夹的可执行子需求），完整形态见 `README.md` 与 `requirements/_index.md`。
- **架构闭环缺口：** 8 处**全部闭合**（移出记录见 `answer-logs/log-0725c.md` 与 `log-0726b.md`）；状态表见 `systems/architecture.md` 的「闭环缺口」小节。残留细节已下沉为各焦点分片的普通待决问题。
