# Open questions — 跨 session 待答清单

> 每次 session 结束时，未答的 Open questions 汇总到此，供下次拾起；一旦答定，就从此处移除并归档进对应主题文档。最近更新：2026-07-16。

## 已解决（本 session 移出）
- ~~在线存档是否纳入 MVP~~ → **已定**：离线可玩 + 云同步混合模型，纳入 MVP（`00-vision/scope.md`）。
- ~~开发顺序~~ → **已定**：采纳改良版——设计先行 + 垂直切片端到端 + 贯穿式占位数据轻量平衡 + 美术挂点末段替换（`00-vision/scope.md`）。

## 待答（按主题）

### run-manager / 存档
- **篇章继承什么？** 读档续章时从上一篇章带入的具体内容（deck / 法宝 / 属性 / 叙事标记）尚未逐项敲定。**这是解锁 run-manager / map-progression 走向 `/derive-requirements` 的关键一环。**
- **重试上限数值**（是否随境界递减）——平衡待调项 → 未来 `30-content/balance.md`。
- **「每篇章至多一个 ongoing」精确语义：** 解读为同一角色谱系不可并行两个同篇章尝试，但不同谱系可并行——待确认。
- **CharacterProfile 状态机：** `discarded`（主动弃置）vs `defeated`（战败）在元进程后果上有何区别？重试耗尽后角色转入何终态？

### 在线账号 / 云同步（混合模型的实现级待决）
- **后端 / 账号系统选型**（自建 vs 渠道 SDK + BaaS）。
- **同步冲突解决**：本地↔云端存档冲突如何裁决。
- **游客 → 登录迁移**：本地进度并入云端账号（合并 / 覆盖 / 提示选择）。
- **登录渠道平台边界**：微信 / QQ 面向国内移动端；桌面 / 网页 / 海外 iOS 的回退（邮箱 / 手机号 / 游客）是否为跨平台基线。
- **T&S / 隐私合规**：隐私政策、账号注销、数据导出（尤其国内 PIPL 与各渠道审核）。

### UX（screen-flow / onboarding）
- **成就自动发放语义**：90% 按组内数量还是加权进度？发放何种奖励？一次性还是可复触发？
- **PlayerPower「开 / 关」语义**：玩家自选修改器（影响难度 / 计分）还是解锁后可装配的被动？是否影响 seed / 公平性？
- **篇章解锁触发条件**：首玩仅炼气，其余「后续解锁」的触发是什么，与「篇章存档角色是有限资源」如何衔接。
- **登录屏循环视频技术实现**：`VideoStreamPlayer` vs live2d 风格骨骼 / 序列帧（GL Compatibility + 移动 / 网页导出下的资源管线与包体，架构待决）。

### 分类法 / 突破（encounter-combat / map-progression）
- **境界突破（渡劫 / boss）**：复用 战斗 / Combat 类型，还是独立于分类法的存档转场？倾向后者；ADR-0002 已声明此项不在其裁定范围。
- **属性模型**：`faith` 之外，`Status` 还要平衡哪些属性？（沿用 vision 的「类 Reigns 属性平衡」待决项。）

## 下一阶段
- `encounter-combat` 仍有战斗模型、属性模型等核心 Open questions → 暂不能 `/derive-requirements`。
- `run-manager` / `map-progression` 也仍各有待决项，尚未完全详尽。**先答「篇章继承什么」**是解锁二者走向 derive 的关键一环。
- ADR 候选：混合存档模型、开发顺序（均已定方向，可固化到 `50-decisions/`）；「境界存档·重试模型」仍有待确认项，暂不固化。
