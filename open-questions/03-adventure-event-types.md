# ③ 逐类型 AdventureEvent 的收口后残留（焦点）

> 本分片属 `../open-questions.md` 的当前焦点区。逐类型 AdventureEvent 的机制**各开过一场专门 session**（五类各一场，见 `systems/adventure-event/_index.md`），**至此五类全部收口**；本片保留的是收口后**只欠取值 / 只欠呈现形态**的残留。

> **本片区的主干「各类型的结算 / 机制细化」已全部答结**（五类收口，逐条移出记录见 `../answer-logs/`）；下方保留的是各类型收口后**只欠取值 / 只欠呈现形态**的残留。原文如下，作为残留条目的共同前提留存。
>
> 五类分类法已定（`decisions/ADR-0002` 五值枚举 + Combat 的 `combatTier` 三档）。**Combat 已开过多场专场**（07-30b 参战方结构 / mana / 存档，08-15d 意图机制整条移除，08-06d 三档遭遇参数，08-09b Finale 档的残卷与重试，08-17e 全部 Finale 均为天劫战 + 三档与隐藏属性的交互口径）；**五类至此全部收口**（**Exchange 已收口**：仍走 `GenericEventResolver` · 交易逐笔即时提交 · 库存物化时经 `Shop` 子流掷定并落存档 · 定价走「商品族 × 稀有度」统一表 + 两条折扣通道 · 售出面仅 `CharacterItem` 一族且为代码级常量判据 · 刷新机制落地但首批内容默认关闭 · NPC / 势力为风味层不建数据模型 · 支付侧二选一（一条货币 element 或一件点名的轮回级法宝，barter 定值不经池、不参与刷新，白送漏洞以门面 `Holds` 前置拒绝堵死）。**Travel 已收口**：常规出场走 location 类型修正 · 80/20 为全局常量不可调制 · 代价走定价表且 > 0 · 不设途中遭遇 · 换图后无特殊规则。**Research 已收口**：构筑面板 = 复数决策槽 · 五类操作闭合 · 产出面 = 卡组 + `manaLimit` · 候选物化时掷定走 `RngStream.Reward` · 不另收资源代价。**Explore 已收口**：真身分布 = 条目池组成 × 既有加权抽取、三处数据类不加字段 · 取池期附加「真身须同样 enabled」一条过滤 · 揭示 = `eventStart` 内一次 `with` 派生且 resolver 按真身选取 · 遮罩卡与其余选项完全同构、揭示走全屏转场层 · 不给任何部分线索）。→ `systems/adventure-event/<type>/`。
- **闭关构筑面板的走火入魔风险档候选出现权重（08-17b 新增 · 留待内容扩充后的统计校准）。** 形态已定，只欠取值。（同条原列的另两格均已不再待定：开局构筑条目 `lifeSpanCost = 0` 的覆盖登记已随定价表落进 `systems/balance.md`；`Recuperate` 的回复量格随 `decisions/ADR-0127-life-merged-into-lifespan.md` 一并不存在——Research 收敛为纯构筑事件的**五类操作**，回复只留三通道。）→ `systems/balance.md`、`systems/adventure-event/research/_index.md`。
- **Explore 的两个待定取值（08-17c 新增 · 09-11 由三格并为两格）。** 真身占比 `Combat : Exchange : Travel ≈ 5 : 3 : 2`（留待内容扩充后的统计校准回归；它是内容编排口径，不是运行时约束）· 揭示转场时长 ≈ 1.2s 与「一次短音效」（纯手感项，只能真机实测；**与「寿元告警是否伴随音效 / 震动」是两条独立问题**）。形态均已定，只欠取值。**原第三格「定价表 `Explore` 行的取值」已并入第一格**——该行已有取值（`t = 1.6`、**24 / 27 / 70**，见 `systems/balance.md` 的 21 格定价表），它是真身占比的**下游**：占比一旦实测偏移，`t` 与三格取值按同一条式子一并重算，不是一条独立待答项。→ `systems/balance.md`、`ux/screen-flow.md`、`systems/adventure-event/explore/_index.md`。
- **Exchange 剩余的两组待定数值格（留待内容扩充后的统计校准）。** 刷新基价与递增量（首批一律填 0 = 关闭刷新）· 单事件槽位总数上界（建议 ≤ 8）。形态均已定，只欠取值。**定价表 25 格、每格支付币种与两档回收率已有初值**（见 `systems/balance.md`「货币产出与定价」），其绝对数字仍归同一次校准复核。→ `systems/balance.md`、`systems/adventure-event/exchange/_index.md`。
- **Travel 一行定价的实测复核（09-11 改写：从「未填格」改为「已有初值待校准」）。** 结构性约束已定（恒 > 0，且为常规事件基准的 1/3 ~ 1/2，禁令扩三 key），**取值已有初值 6 / 7 / 18**（`t = 0.4`，`Travel ÷ Exchange` = 0.400 / 0.412 / 0.409，落在 1/3 ~ 1/2 —— 见 `systems/balance.md` 21 格定价表的七条自校验）。待定的是**绝对数字随内容扩充后的统计校准**，与上一条 Exchange 的两组数值格同归那一次复核。→ `systems/adventure-event/travel/_index.md`、`systems/balance.md`。
