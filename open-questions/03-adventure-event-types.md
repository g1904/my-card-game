# ③ 逐类型 AdventureEvent 机制（焦点 · 各开一次专门 session）

> 本分片属 `../open-questions.md` 的当前焦点区。逐类型 AdventureEvent 的机制将**各开一次专门 session**（五类各一场），不在一次 handoff 里做完 —— 见 `systems/adventure-event/_index.md`。

- **各类型的结算 / 机制细化。** 五类分类法已定（`decisions/ADR-0002` 五值枚举 + Combat 的 `combatTier` 三档）。**Combat 已开过多场专场**（07-30b 参战方结构 / mana / 存档，08-15d 意图机制整条移除，08-06d 三档遭遇参数，08-09b Finale 档的残卷与重试，08-17e 全部 Finale 均为天劫战 + 三档与隐藏属性的交互口径）；**五类至此全部收口**（**Exchange 已收口**：仍走 `GenericEventResolver` · 交易逐笔即时提交 · 库存物化时经 `Shop` 子流掷定并落存档 · 定价走「商品族 × 稀有度」统一表 + 两条折扣通道 · 售出面仅 `CharacterItem` 一族且为代码级常量判据 · 刷新机制落地但首批内容默认关闭 · NPC / 势力为风味层不建数据模型。**Travel 已收口**：常规出场走 location 类型修正 · 80/20 为全局常量不可调制 · 代价走定价表且 > 0 · 不设途中遭遇 · 换图后无特殊规则。**Research 已收口**：构筑面板 = 复数决策槽 · 六类操作闭合 · 产出面 = 卡组 + `manaLimit` + `lifeTotal` · 候选物化时掷定走 `RngStream.Reward` · 不另收资源代价。**Explore 已收口**：真身分布 = 条目池组成 × 既有加权抽取、三处数据类不加字段 · 取池期附加「真身须同样 enabled」一条过滤 · 揭示 = `eventStart` 内一次 `with` 派生且 resolver 按真身选取 · 遮罩卡与其余选项完全同构、揭示走全屏转场层 · 不给任何部分线索）。→ `systems/adventure-event/<type>/`。
- **闭关构筑面板的三个数值格（08-17b 新增 · 留待内容扩充后的统计校准）。** `Recuperate` 的 `lifeTotal` 回复量 · 走火入魔风险档候选的出现权重 · 开局构筑条目 `lifeSpanCost = 0` 的覆盖登记。形态均已定，只欠取值。→ `systems/balance.md`、`systems/adventure-event/research/_index.md`。
- **Explore 的两个待实测初值（08-17c 新增 · 轻）。** 真身占比 `Combat : Exchange : Travel ≈ 5 : 3 : 2`（留待内容扩充后的统计校准回归；它是内容编排口径，不是运行时约束）· 揭示转场时长 ≈ 1.2s 与「一次短音效」（纯手感项，只能真机实测；**与「寿元告警是否伴随音效 / 震动」是两条独立问题**）。形态均已定，只欠取值。→ `systems/balance.md`、`ux/screen-flow.md`、`systems/adventure-event/explore/_index.md`。
- **构筑面板的竖屏呈现与风险档标注（08-17b 新增）。** 面板与战后奖励面板**在呈现层**同构（候选纵向排列、点按选中）已定方向——交互层不同构（本面板选完全部槽再一次确认提交，奖励面板逐项即时领取 / 跳过）；**风险档需要一个明确的视觉标注 + 无 hover-only 的说明通道**，形态未设计。→ `ux/screen-flow.md`、`systems/adventure-event/research/common-properties.md`。
- **Exchange 的四组待定数值格（08-17d 新增 · 留待内容扩充后的统计校准）。** 「商品族 × 稀有度」定价表每格填多少 · 刷新基价与递增量 · **两档回收率**（商店档 `SellRatePercent` 建议 30–50%；随售档 `PackSellRatePercent` 显著低于该区间——两者的**相对关系已由加载期硬校验固定**，只欠绝对值）· 单事件槽位总数上界（建议 ≤ 8）。形态均已定，只欠取值；**定价表的绝对数字被灵石的获取渠道阻塞**。定价表的每一格现在还要填**支付币种**（灵石 / 仙玉）——哪些格收仙玉同属这批取值，**并直接决定售出 / 随售带来的仙玉净产出敞口的量级**。→ `systems/balance.md`、`systems/character-profile/currency.md`、`systems/adventure-event/exchange/_index.md`。
- **Exchange 是否支持以物易物（08-26 新增）。** 口头意图提到「Exchange event 是以物易物或资源换取道具的场景」，但**以物易物在本库零承载**：`ExchangeOffer` 的支付侧恒为一条货币 `ChangeElement`，`ExchangeStockRule` 上没有「以什么换」的书写位。要支持它需同时动三处——支付侧 element 的形状、库存规则字段、`CanAfford` 语义。待裁定：是确有这一形态需落地，还是「以物易物」只是「以资源换取」的口语说法（后者本库已完整承载）。→ `systems/adventure-event/exchange/_index.md`、`systems/adventure-event/exchange/common-properties.md`。
- **`Practice` / `Finale` 档的奖励厚薄。** 三档的回合数与胜负门槛已定；**`BaseReward` 与 `RewardPoolId` 随档位如何调厚薄**未定，**留待内容扩充后的统计校准**。→ `systems/balance.md`。
- **Combat 三档各推哪一档 `HiddenStatGrade`（08-17e 收窄 · 内容编排）。** 三档默认口径已定（`Practice` 推道心、对位低一档、不推煞气 · `Standard` 逐条目编排 · `Finale` 胜负同推道心）、「胜负同施一份档位、不套 `FailureRatio`」已定；**逐条目的推拉编排与映射值**仍挂在「隐藏属性的增减触发」那条待答项与内容扩充后的统计校准上。→ `systems/services/plot-manager.md`、`systems/balance.md`。
