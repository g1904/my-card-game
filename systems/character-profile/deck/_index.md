# deck

> 角色卡组 deck —— 抽牌堆 / hand / 弃牌堆、seeded 洗牌、deck 变更，以及卡牌 / **CardData** 定义与起始卡组内容设计。

## 意图
> _设计意图，从 handoffs 中提炼。保持更新。_

- **deck 是 CharacterProfile 的一部分。** 卡组、hand、以及打出一张卡的结算，都是单次轮回 / 单角色的状态；deck 随轮回推进被增 / 删 / 升级。Source: `handoffs/2026-07-24-docs-restructure-class-model.md`。
- **卡牌是道念的唯一产出途径（已定案 · 承重）。** 战斗的胜负标尺是道念（见 `systems/scoring.md`），而**道念由打出的卡牌产生**；卡牌**既能给自己加道念，也能削减对方道念**（削减在 0 处截断，无负道念；**截断发生在每一次结算时，溢出量不结转** —— 见 `systems/scoring.md`）。**推论：卡牌设计的第一维度是「产多少 / 削多少」**——它取代了 HP 消耗战里的「打多少伤害 / 挡多少」，也是与 `manaLimit`（每回合刷满）配合出节奏的那一维。战斗**定长 10 个回合**、**起始道念按等级给**，共同框定了「一张牌该值多少道念」的量纲（基准未定，见待决问题）。Source: `handoffs/2026-08-01b-abstraction-levels-combat-numbers-codex-family-and-monetization.md`。
- **战斗中每个参战方各有一个 `DeckModule`（已定案）。** 卡组不是战斗内的全局单件：**每个 character、每个 enemy 各持有一个**，由 combat-service 的 CharacterManager / EnemyManager 各自持有（`DeckModule` = 第三级抽象，见 `systems/architecture.md`）。**敌人也出牌**，且可带定制卡组（如 Finale 的天劫）。本文档持有**角色侧**卡组的设计；敌方卡组的内容形态见 `systems/adventure-event/combat/`。Source: `handoffs/2026-07-30b-combat-level-intent-and-decision-point-saves.md`。
- **卡牌结算 = MTG 的 stack，但**不含交互与优先权**（已定案 · 承重）。** 打出的卡牌**不立即生效**，而是先入栈，按**后进先出（LIFO）**依次结算——**「打出」与「结算」是两个时刻**，结算顺序由此成为一条明确的规则，也为至今空白的**回合内效果 / 状态系统**提供了天然骨架。**但交互（instant / 栈非空时出牌）与优先权传递整体移除**：它们**把对局拉得太长、决策点过多、复杂度高而深度收益小**。**推论 ①：回合回到「我打完换你打」**——只有回合归属方能在自己的行动阶段出牌。**推论 ②：卡牌不多出「能否响应」这一维**，**instant 一类关键字明确不借**——**出牌时机是唯一的、且是全局规则而非卡牌属性**：一张牌只能在**自己回合的行动阶段、且栈为空时**打出（`sorcery speed` 一词亦不借，08-04b 整条弃用；启动式异能与道具的使用窗口与之完全相同）。**推论 ③：stack 的承重点是触发的解决顺序（已定案）**——**在栈上的牌可以触发能力，被触发的能力也进栈**，故**即便只打出一张牌，栈深也可以大于 1**；一次结算连锁产生的多个触发按 **LIFO** 解决，**后触发的先生效**，这使结算顺序成为卡牌设计可利用的资源。**「栈非空时不能出牌」对双方都成立**（不为归属方开口子，「行动阶段连续压栈再统一结算」的路线不采用）。Source: `handoffs/2026-08-02b-stack-without-interaction-and-three-step-turn.md`（收窄 `handoffs/2026-08-02-momentum-conversion-reward-structure-and-mtg-stack.md`）。
- **回合结构 = 开始阶段 / 行动阶段 / 结束阶段三步（已定案），卡牌侧的落点有三处。** ① **抽牌在开始阶段**（归属方 mana 恢复至 `manaLimit` → 触发「回合开始时」→ 抽牌）；② **行动阶段是唯一出牌阶段**；③ **结束阶段触发「回合结束时」并清理回合内状态**，把状态分成「回合内临时」与「跨回合持续」两档。**三步是回合归属方的流程，双方不同时走。** 完整结构见 `systems/services/combat-service.md`。Source: 同上。
- **手牌上限是恒定不变式，不设弃牌机制（已定案）。** **手牌在任何时刻都不得超过上限**——**没有时间限制，也没有「结束阶段弃到上限」这类必须弃牌的机制**。**上限值 = 9**（见 `systems/balance.md`；起手 4 / 每回合抽 2 下，不出牌的玩家第 4 回合撞上限，正常出牌永不撞）。 **推论：约束点落在会让手牌增加的时刻**（抽牌、以及任何「加入手牌」类效果），而不是回合末的一次清算。Source: 同上。
- **满手时抽牌 = 抽不进（已定案 · 08-03 · 抽牌流程的前置条件已就位）。** 满手时抽牌**抽不进——牌留在抽牌堆，这次抽牌无事发生**；**「加入手牌」类效果同理落空**。「抽出即弃」「直接销毁」两条路线**均不采用**。**推论 ①：手牌上限是一条纯上界**——不产生任何弃牌堆流量、不消耗抽牌堆。**推论 ②：弃牌不是被规则强制的动作原语**（回合末不弃、满手也不弃）——**弃牌堆只由「打出后进弃牌堆」与「卡牌效果显式弃牌」填充**，这是弃牌堆流量的完整清单。**推论 ③：抽牌堆顺序不被满手情形扰动**，seeded 洗牌的确定性不因此分叉；「本回合抽 N 张」在满手时等价于抽 0 张。**推论 ④：满手的代价是 tempo 而非资源**——牌没丢，只是这一拍没拿到，手牌上限因此是**逼玩家出牌腾位的节奏约束**，不是惩罚。Source: `handoffs/2026-08-03-battlefield-stack-hand-limit-and-power-item-naming.md`。
- **触发式效果的载体是开放的，不专属卡牌（已定案 · 08-03）。** 牌上的触发器、**场上的持续状态**、**CharacterPower（神通）** 都可能承载触发式效果，**清单开放**、日后可再增。**推论 ①：`CardData` 的触发器字段只是载体之一**——触发的匹配逻辑不能写死在卡牌类型里，需要一个统一的注册 / 匹配面（它坐在 **battlefield** 上，见 `systems/services/combat-service.md`）。**推论 ②：压栈者与载体解耦**——命中后把被触发的能力压入栈的一律是 StackManager。**推论 ③：轮回内 build 的三件套（卡牌 / 法宝 / 神通）在战斗内不再只有卡牌能说话**——神通可承载触发，意味着「这局我变强了多少」在战斗内有第二条表达通道。Source: 同上。
- **战场（battlefield）是牌离开手牌之后的去处（已定案 · 08-03）。** 打出的牌先入**栈**（等待结算），结算后若留下持续效果则落到**战场**（正在生效的东西）；两者各由 combat-service 的 StackManager / BattlefieldManager 持有。**推论：卡牌的生命周期链路补全为「卡组 → 手牌 → 栈 → 战场 / 弃牌堆」**，此前「打出之后到进弃牌堆之间」的那一段有了明确的两个位置。Source: 同上。
- **card / deck / combat 体系将大量借用 MTG 术语（已定案 · 方向）。** 借的是 MTG 的**结算模型与术语体系**（stack、resolve、trigger…），用以简化表达；**规避的仍是它的胜负模型**（血量归零）**与 mana 曲线**——二者不冲突。借词须在 `terminology.md` 登记为已定含义，避免同一个词在本库与 MTG 原义之间漂移。Source: 同上。
- **卡牌类型 = 五类，`CardType` 为 `CardData` 的必填共有字段（已定案 · 承重）。** **法术 `Sorcery`**（一次性，结算后进弃牌堆）· **阵法 `Enchantment`**（永久物落战场，**埋伏是其次类型**）· **法宝 / 古宝 `Item`**（不洗进卡组，存于储物袋）· **神通 / 法则 `Power`**（开局直接入场的受保护永久物）· **业障 `Affliction`**（可打出但无正面效果，唯一作用是把自己送进弃牌堆）。**从卡组打出的永久物只有阵法一类**，故永久物不再区分实体 / 非实体；「场上的持续存在物」全部由阵法及其次类型承载。**卡组只装法术 / 阵法 / 业障——道具与 `Power` 不进卡组**，故抽牌堆的 seeded 确定性完全不受它们影响。**推论 ①：卡牌不再只有一个来源区**，三个来源各自绕开的东西不同：卡组（受抽牌运制约）· 持有的道具（不受抽牌运制约，需玩家动作）· 开局入场（不受抽牌运制约，无需动作）。**推论 ②：类型是「结算生命周期」的分类，与意图类别（敌人行为的展示分类）正交**——两者共用一套枚举会导致「既产道念又削对方道念的牌属于哪类」这种无解归类。**类型间的具体差异化留待日后。** Source: `handoffs/2026-08-04b-mtg-loanwords-card-types-and-intent-snapshot.md`。
- **异能三分：静止式 / 启动式 / 触发式（已定案 · 08-04b）。** **静止式 `static ability`**（不入栈，载体在战场上即持续生效）· **启动式 `activated ability`**（启动后压栈，窗口 = 自己回合的行动阶段、栈为空时，与出牌完全同窗口，**故不重新引入交互**）· **触发式 `triggered ability`**（命中后由 StackManager 压栈）。这补完了 08-03「载体开放」的另一半：**载体说的是「挂在谁身上」，异能类型说的是「怎么生效」，两者正交**。**推论：异能抽为独立的可复用资源 `AbilityData : Resource`**，由 `CardData` / `PowerData` / `ItemData` / 战场条目共同引用——这与 08-03「触发匹配逻辑不能写死在卡牌类型里」是同一条。Source: 同上。
- **不会有凭空生成的牌（已定案 · 08-05 · 全局硬约束 · 承重）。** **本作不存在凭空生成的牌**——「加入手牌」类效果的牌**一律来自既有的区**（抽牌堆 / 弃牌堆 / 卡组），**没有 token、没有运行时新造的卡牌实体**。这以**否定前提**的方式答结了 08-03 的「落空时凭空生成的牌去哪」：问题里那两种情形（生成 token 类新牌 / 从牌库外取牌）**都不会发生**。
  - **推论 ①：满手落空的规则已完备，无需第二条。** 「牌留在原处、这次抽牌无事发生」覆盖全部情形——从抽牌堆来的留在抽牌堆，从弃牌堆来的留在弃牌堆；**不需要「产生后进弃牌堆」这条补充规则**。
  - **推论 ②（承重 · 存档面净减）：一场战斗内的卡牌集合是闭集。** 卡牌只在区之间流转（卡组 ⇄ 手牌 ⇄ 弃牌堆 ⇄ 战场 ⇄ 栈），**总量不增不减**；故决策点存档只需记**各区的 `Id` 序列 + 各 `CardInstance` 的运行态**，**不必为运行时新造的匿名卡分配 id、也不必序列化「这张牌是怎么造出来的」**。与既定的「存档只记 `Id`，`CardType` / `Subtypes` 是静态字段」完全合流。
  - **推论 ③：不借入 MTG 的 token，`CardType` 五分不需要再加一类。** 与已否决的「引入放逐区」同源——现有六处位置与五个类型已能表达全部已定案的流转。
  - **推论 ④：卡组增删只发生在战斗外。** 业障由事件的负向奖励塞进卡组、卡牌由奖励加入卡组，都是**轮回级**变更，走 `ProfileChangeSpec` → `TryApply`；**战斗内没有任何一条增删卡牌总量的通道。**
  Source: `handoffs/2026-08-05-level-band-stack-save-and-token-free-deck.md`。
- **触发条件可跨归属方（已定案 · 08-04b · 答结 08-02b 的待决问题）。** 时点本身有归属方，但**监听方不必是该归属方**——可写「对手的回合开始时」「对手打出牌时」（`TriggerOwnerScope { Self / Opponent / Either }`）。**埋伏牌（阵法的次类型 = 炉石的「奥秘」）靠此成立**：面朝下布置、是永久物、触发后进弃牌堆、同名不可重复布置、对手只知「有一张埋伏」不知是哪张。**推论：埋伏是本作唯一一条「在对手回合发生作用」的通道**——玩家不能在对手回合主动响应，但可以预先布置自动响应的东西；结算入口不变（StackManager 压栈）。Source: 同上。
- **次类型（card subtype）以稳定字符串 id + 注册表表达，不用 C# 枚举（已定案 · 08-04b）。** 每个主类型下都会有次类型；次类型是**内容侧可扩展的**，新增一个 = 新增一个 `.tres`，不改代码（对应 `data-resource-rules.md` 的「让内容保持可加性」）。**推论：次类型必须能被效果引用**（「所有『灵兽』获得 +1」这类牌是次类型存在的主要理由），故次类型 id 要进入效果的目标筛选条件——它不是纯展示标签。**清单与 id 规范已定案，见下方「次类型体系的落地形态」。** Source: 同上。
- **业障（`Affliction`）是负向牌，由事件的负向奖励条目塞进卡组（已定案 · 08-04b）。** 对齐 StS 的 Curse，但**可被打出**——打出没有任何正面效果，唯一作用是把自己送进弃牌堆。**这把死锁风险完全消除**，且与既有定案高度同调：**它不需要任何弃牌机制**（走「打出后进弃牌堆」这条既有通道，弃牌堆流量的完整清单不变），**它的代价同样是 tempo**（占一个手牌位 + 一次出牌动作），与「满手的代价是 tempo 而非资源」自洽为同一条「时间即代价」。**额外代价逐条目而定，通常没有**——多数业障零代价，少数带额外代价（耗 1 mana 才能打出、或打出时削自己 1 点道念）。**推论：校验规则要放宽**——不能硬性要求 mana 费用为 0；改为「允许有 mana 费用与负向效果，但不得有任何正面效果」，后者难以机械校验，故落为内容侧纪律 + `PushWarning` 级软检查。Source: 同上。
- **抽牌堆不重洗，抽空即疲劳（已定案 · 08-11c · 承重）。** **弃牌堆不回流抽牌堆**——一场战斗内只在参战方组装时初洗一次；**抽牌堆为空后每尝试抽一张牌，抽牌方失去 1 点道念**（一次抽 N 张即 −N，下限 0 照常截断）。**推论 ①：卡组是会被真正耗尽的资源**，卡组规模因此成为一条真实的构筑取舍（规模两侧皆不设硬限，见 `systems/balance.md`）——牌少而精的对价就是后期稳定失血。**推论 ②：`DeckModule` 没有重洗代码路径**，抽牌堆的 `Id` 序列在一场战斗内只减不增，与「卡牌集合是闭集」合流（牌流向弃牌堆 / 战场，不再回来）。**推论 ③：满手与疲劳互不触发**——抽牌堆非空但满手 = 牌留在抽牌堆、无事发生、不扣道念；疲劳的条件是「牌堆空」，不是「没拿到牌」。**推论 ④：不设 mulligan**，起始 4 张一次发到位，没有换牌窗口。Source: `handoffs/2026-08-11c-combat-turn-flow-fatigue-and-card-type-reduction.md`。
- **抽牌堆 / hand / 弃牌堆 · seeded 洗牌 · deck 变更。** 抽牌、手牌、弃牌的循环，以及洗牌必须由轮回种子（seed）驱动以保证确定性可复现。deck 变更（增卡 / 删卡 / 升级）来自遭遇（如 Exchange / 奖励）。
- **卡牌 / CardData 定义。** 卡牌是**数据**（`[GlobalClass] partial class CardData : Resource`，以 `.tres` 编写，带稳定唯一 `Id`）；打出一张卡涉及**费用（mana）、目标选择、效果流水线、触发器**。卡牌数值不硬编码，读自数据资源（见 `data-resource-rules.md`）。

> 具体的抽 / 弃 / 洗规则、CardData 字段（费用、目标、效果、触发器）、效果流水线阶段、起始卡组内容等见 `common-properties.md`。

### 次类型体系的落地形态（已定案）

- **`[GlobalClass] partial class CardSubtypeData : Resource`**，字段 = `Id` / `DisplayName` / `Description` / **`AllowedCardTypes : CardType[]`** / `ContentEnabled`。**权威匹配面落在字段上，不落在 id 前缀上**——否则「次类型须与主类型匹配」的校验要靠解析字符串，违反「id 是键、不承载语义解析」。`AllowedCardTypes` **取数组而非单值**：多数次类型只挂一个主类型，但「符箓」这类词在内容扩展时可能同时出现在 `Sorcery` 与 `Item` 上，数组不额外增加成本、单值则日后要改 schema。
- **`ContentEnabled == false` 的次类型仅从图鉴 / 筛选 UI 隐藏**，`Get(id)` 与效果筛选照常解析——次类型本身不被抽取，关掉它不应让引用它的卡牌失效（与「读取侧不过滤」同构）。
- **id 规范 `<maintype>.<name>`**，全小写、`snake_case` 词身、点号分段（`enchantment.ambush` / `creature.spirit_beast` / `sorcery.talisman`）。沿用 `TriggerConditionData.TimingId` 的 `"turn.start"` 惯例——**点号分段的分层 id 在本库已是既有惯例**，不新造风格。**前缀是可读性约定，不是校验依据。**
- **`Ambush` 的最终标识符 = `enchantment.ambush`**（显示名「埋伏」）。**它不是枚举值**（次类型体系明确不用 C# 枚举），故不应有 PascalCase 形态。
- **注册表布局**：目录按主类型分（`res://data/card_subtypes/{sorcery,creature,enchantment,item,power,affliction}/`）**只为人工浏览方便，不承载语义**；DataRegistry **递归扫描整棵树并按 `Id` 建单一全局索引**（不按主类型分表），使 `CardData.Subtypes` 的悬空校验是一次 `TryGetValue`。已知瑕疵：目录与 `AllowedCardTypes` 可能漂移——**这是可读性问题不是正确性问题**（校验只看字段），但应在数据文档里写明目录只是约定。
- **新增加载校验**：`Id` 重复 → `PushError`（报出两个 `.tres` 路径）· `AllowedCardTypes` 为空 → `PushError`（挂不到任何牌上，必是漏填）· **「次类型 X 未被任何筛选条件引用」→ `PushWarning`**（这条可机械化，做）。
- **准入判据（内容侧纪律）**：一个次类型只有在**同时满足**两条时才应存在——① 至少 **3 个内容条目**共享它；② 至少 **1 处目标筛选引用它**（存在 payoff）。**没有 payoff 的次类型就是展示标签**，而次类型明确不是纯展示标签；纯风味标注写进描述文本。
- **清单初值**（内容横向扩展时可整体重排，**重排不动 schema**）：

  | 主类型 | 次类型 id | 中文 | 定位提示 |
  |--------|----------|------|---------|
  | `Sorcery` | `sorcery.talisman` | 符箓 | 低费即时产出的走量档 |
  | | `sorcery.sword_art` | 剑诀 | 高费高削减的爆发档 |
  | | `sorcery.incantation` | 咒术 | 施加持续状态为主，产出弱但改写后续 |
  | `Enchantment` | `enchantment.spirit_beast` | 灵兽 | payoff 的样板（「所有『灵兽』获得 +1」） |
  | | `enchantment.puppet` | 傀儡 | 可被自己的效果消耗 / 献祭的一次性永久物 |
  | | `enchantment.artifact_spirit` | 器灵 | 与法宝联动的一档 |
  | | `enchantment.ambush` | 埋伏 | 面朝下、跨归属方触发、触发后进弃牌堆、同名不可重复布置 |
  | | `enchantment.formation` | 法阵 | 常驻静止式修正，build 骨架 |
  | | `enchantment.ward` | 护阵 | 偏防御 / 减免对方削减的一档 |
  | `Item` | `item.pill` | 丹药 | 即时恢复 / 增益，一次性消耗 |
  | | `item.scroll` | 玉简 | 信息类与便利类（探查若落在道具上，落这一档） |
  | | `item.instrument` | 器物 | 多次数、每场可重复使用 |
  | `Power` | `power.technique` | 功法 | 主流：静止式修正 |
  | | `power.physique` | 体质 | 开局条件式（起始道念 / 起始手牌相关） |
  | | `power.dao_law` | 道则 | 账号级法则专用档，配额受 ≤1/5 纪律约束 |
  | `Affliction` | `affliction.injury` | 伤势 | 零代价档（多数业障） |
  | | `affliction.karma` | 业力 | 带额外代价档（耗 mana / 削己方道念） |

- **`Item` 与 `Power` 也建次类型**：它们不进卡组，但同样带 `Subtypes` 字段，且**储物袋 9 格容量下的分类轴仍有用**（战斗内「随身」抽屉与奖励侧的归类）。

Source: `handoffs/2026-08-06d-combat-open-questions-mass-closure.md`。

### 卡池划分：共用体系，不共用卡池（已定案）

**`CardData.Pool : CardPool { Character, Enemy, Both }`，必填、无默认值、缺失即 `PushError`**（同 `CardType` / `UsableScene` —— **默认值会让漏填的敌方牌悄悄进入玩家奖励池**）。

- **为什么共用 `CardData` 体系**：`DeckModule` 是同一个第三级组件、双方各持一个（两套 `CardData` 意味着它要泛型化或分叉，**为零收益付结构成本**）· 战场是单一记录、栈是单一 StackManager，**双方的牌落进同一个区、经同一条结算路径**（不同数据类型则战场条目的 `SourceId` 无法统一解析）· 异能三分 / 次类型筛选 / 效果元素若写两遍，等于把整套战斗规则实现两份。
- **为什么不共用卡池**：**奖励抽取必须能排除敌方专用牌**（一个字段过滤比另立注册表简单得多，且与 `ContentEnabled` 的过滤位置完全同构——都在产出侧）· 两侧设计目标不同（玩家牌服务于**构筑**，敌人牌服务于**可读的对手行为**：要能被意图汇总成一条结果值、要能写进图鉴的「关键卡牌」）· **`Both` 档保留交集**，避免同一张牌写两份 `.tres`（那会造成 `Id` 二义与图鉴重复）。
- **新增校验**：玩家侧奖励池 / 商店库存的抽取源必须只含 `Pool != Enemy`；`EnemyData` 的样本卡组不得含 `Pool == Character` → `PushError`，报出违规 `Id` 与模板 `Id`。
- **已知代价**：共用体系意味着敌人牌也吃全部加载时校验，**敌方内容的编写门槛与玩家侧相同，内容产能上没有折扣**。
- **「玩家专属、敌人永不使用」的强牌允许存在**（`Pool = Character`），属内容口径，不改规则。
- **敌人卡组规模不设硬限、允许同名条目重复**；抽牌规则与玩家完全同规则（起手 4 / 每回合 2 / 上限 9 / 满手抽不进 / 空堆疲劳 / seeded 洗牌），**但走不同的战斗 RNG 子流**（玩家的一次额外抽牌不打乱敌人牌序）。数值与理由见 `systems/balance.md`、`systems/enemies/`。

Source: `handoffs/2026-08-06d-combat-open-questions-mass-closure.md`。

### 「回合内状态」= 生命周期三件套（已定案）

难点不在「有哪些档」，而在**本作的回合有归属方、双方不同时走三步**——「下回合结束」在双方视角下指向两个不同的时刻，单靠一个枚举表达不完整：

```csharp
public enum EntryLifetime
{
    UntilEndOfTurn, // 回合内：在 CountdownSide 的下一个结束阶段被清理
    ForTurns,       // 持续 N 回合：每逢 CountdownSide 的结束阶段 RemainingTurns--，归 0 时移除
    Indefinite,     // 跨回合：不因回合流转失效，直到被效果移除或战斗结束
}

public enum CountdownSide
{
    Owner,    // 默认：以条目 OwnerSide 的结束阶段为节拍
    Opponent, // 以对手的结束阶段为节拍
    Either,   // 任一方的结束阶段都递减（= 半个回合一跳）
}
```

非永久战场条目的字段 = `OwnerSide` / `IsProtected`（**恒 `false`**）/ `SourceId` / `Lifetime`（必填）/ `CountdownSide`（默认 `Owner`）/ `RemainingTurns`（仅 `ForTurns` 有意义，其余档写 `-1`）。

- **`Indefinite` 与「永久物」不是同一件事（须写死）**：**永久物是「来源是阵法 / `Power`」的类型级判定**；`Indefinite` 是**非永久持续状态的一个生命周期取值**（例：「本场战斗内我方所有符箓 +1 道念」是 `Indefinite` 的持续状态，但它不是永久物、不可被「移除永久物」类效果命中）。**差别落在可被什么效果针对上。**
- **结束阶段的清理判据**：遍历战场 → 永久物 `continue` → `CountdownSide` 未解析到当前归属方则 `continue` → `UntilEndOfTurn` 移除 / `ForTurns` 递减并在归 0 时移除 / `Indefinite` 不动。

  > **「回合内状态」的精确定义 = 战场上 `IsPermanent == false` 且 `Lifetime == UntilEndOfTurn` 的条目。** 结束阶段清理的就是这一集合中 `CountdownSide` 解析到当前归属方的那些，**永远不触碰永久物，也不移除 `ForTurns` / `Indefinite` 条目**（后者只递减）。

- **「持续到下回合结束」不是新档位，是三件套的组合**——这正是三件套优于「多加枚举值」的理由：

  | 自然语言 | 表达 | 实际覆盖 |
  |---------|------|---------|
  | 「本回合内」 | `UntilEndOfTurn` + `Owner` | 施加它的那个回合的结束阶段 |
  | 「持续到你的下回合结束」 | `ForTurns(2)` + `Owner` | 本回合结束 + 己方下一回合结束，共两跳 |
  | 「持续到对手的下回合结束」 | `ForTurns(1)` + `Opponent` | 对手的下一个结束阶段（**跨过己方回合**） |
  | 「持续 3 个回合」（不分归属方） | `ForTurns(3)` + `Either` | 双方任一结束阶段各跳一次 ≈ 1.5 个完整来回 |
  | 「本场战斗内」 | `Indefinite` | 不因回合流转失效 |

  **保留 `UntilEndOfTurn` 作为独立取值**（最常用的一档不该靠 `ForTurns(1)` 的隐式约定表达），使清理判据一眼读出「清的是哪一档」。
- **卡面文案必须显式点明归属方**（「到**你的**下回合结束」而非「到下回合结束」）——三步是归属方的流程，「下回合」在本作是有歧义的。**这是措辞硬要求。**
- **`Either` 保留但内容侧默认不用**（玩家很难心算「双方各跳一次还剩几跳」），使用时 **UI 必须显示剩余跳数**。
- **加载期校验掉无意义组合**：`Lifetime != ForTurns` 时 `RemainingTurns` 必须为 `-1` → 否则 `PushError`；`ForTurns` 时 `RemainingTurns >= 1` → 否则 `PushError`。
- **非永久条目可被针对 / 移除，但效果必须显式声明目标类别。** 目标合法性 = **类别匹配** 且（`IsProtected == false` 或效果带 `IgnoresProtection`）；非永久条目 `IsProtected` 恒 `false`，故常规移除类效果自然命中，**不需要为它开第二条保护规则**。
  - **为什么不「一律不可被针对」**：「驱散对方的临时增益」是卡牌游戏里最经典、最低成本的一条反制面，规则层封死则这类牌**在本作永远写不出来**；而交互移除后节奏张力全部落在效果系统与卡牌设计上，**主动砍掉一整条设计面与这条压力方向相反**。且永久物的「可被移除」已有类型级判据（`IsProtected`），再造一条类型级禁令会让「什么能被针对」变成两条互不相干的规则。
  - **为什么不「可被针对且不需显式声明」**：若目标类别缺省为「任意条目」，一张写着「摧毁目标阵法」的牌会顺手打掉对方的临时增益，**卡面文本与实际行为不符**。
  - **内容侧纪律：针对非永久条目的效果应当稀少**（多数临时增益本就会在 1~2 个结束阶段后自动过期，花一张牌去拆它通常是亏的；价值集中在**拆 `Indefinite` 的持续状态**）。配**清单式 `PushWarning`** 逐条列出可针对持续状态的效果，与 `IgnoresProtection` 的清单式警告同构——作用是让清单始终可人工审阅。
- **本节字段表直接作为战场条目存档 schema 的输入**（见 `systems/services/combat-service.md`）。

Source: `handoffs/2026-08-06d-combat-open-questions-mass-closure.md`。

### 效果 / 状态系统 = 三层（已定案）

```
第一层  EffectData（效果元素）—— 结算时执行的原子操作，数据驱动、可组合
第二层  BattlefieldEntry（战场条目）—— 由「施加持续状态」类元素产出，带上节的生命周期三件套
第三层  求值管线 —— 所有数值在「使用时」按战场上的静止式修正聚合得出
```

**第一层 · `EffectData` 的原子操作清单（初值，开放可加）**：`ModifyMomentum`（产 / 削道念，下限 0 逐次截断）· `Draw`（满手落空）· `Discard` · `ModifyMana`（改当前 mana，**不改 `manaLimit`** —— 那是轮回级的事件推拉）· `ApplyState`（施加一条持续状态 = 产出一个非永久战场条目）· `RemoveEntry`（受目标类别 + `IsProtected` + `IgnoresProtection` 约束）· `MoveCard`（**闭集内的流转，不新造牌**）。**关键是它是数据不是代码分支**：新增一张卡 = 新增一个 `.tres` 并组合已有元素，而不是编辑 switch。

**第二层 · 承重推论：本作不存在「参战方身上的 buff / debuff 列表」这个数据结构。** 「我方本回合所有牌 +1 道念」是**战场条目**（`OwnerSide = Character`、`Lifetime = UntilEndOfTurn`），**不是 CharacterManager 上的一个字段**——因为它要被针对、要被清理、要进存档。**一切增益减益都是战场条目**：这消除了「有的状态在角色身上、有的在场上」的双轨制，也让结束阶段清理成为**唯一**的状态过期通道。

**第三层 · 求值管线 = 加法层 + 乘法层两层**（引入乘法层，「翻倍 / 减半」类效果可以存在）：遍历战场上匹配的静止式修正，**先全部加法、再全部乘法**，最后按下限 0 截断。

- **「加法先于乘法」写成规则而非实现细节**（与「步内顺序是规则的一部分」同一处理）：否则同一组修正在不同顺序下结果不同，而 LIFO 已经承担了一层顺序敏感性，不宜再叠一层。**一旦有卡牌围绕它设计，日后改动成本很高。**
- **静止式修正不入栈**，故不参与 LIFO，**只在求值瞬间被读取**——这正是「静止式异能是 BattlefieldManager 的一条与栈无关的写入路径」的落地形态。
- **乘法层对道念方差的放大**在 ch1 数值标杆专场一并建模。
- 性能面：求值遍历战场条目（量级个位数到十几）且不在 `_Process` 热路径上，风险低；但 **BattlefieldManager 应按 `TimingId` / `CardType` 预建索引**，避免在结算中做 LINQ 分配。

**`CardInstance` 运行态的判据（承重）：**

> **有过期时刻的 → 战场条目；无过期时刻且属于这张牌本体的 → `CardInstance`。**

允许写入实例的只有两类：**无时限的本体改写**（「这张牌本场费用永久 −1」，无过期、无回滚）与**本体计数器**（「这张牌本场已被打出 N 次」，单调不可逆）。三条理由：

1. **回滚是不可承受的复杂度。** 若把「本回合所有牌 +1」写进每张手牌，结束阶段清理时就要**反向回滚**——而 LIFO 连锁下同一回合可能叠了多条修正、其中一些还在结算中途被移除。**求值期聚合根本没有回滚这个概念**：条目消失，下次求值自然不再算它。
2. **动态集合写不进实例。** 「所有『灵兽』获得 +1」作用于一个**随时间变化的集合**——本回合稍后落场的灵兽也该吃到。写进实例只能覆盖施加瞬间已存在的那些，**语义直接错**。
3. **存档面净减。** 一场战斗内卡牌是闭集，存档只需各区 `Id` 序列 + 各 `CardInstance` 运行态；若临时增益进实例，**每个决策点都要序列化一遍全部手牌的临时修正**，而战场条目本来就要落存档。

**边界情形（须写明）**：「本场战斗内所有『符箓』费用 −1」看起来无时限，但它作用于**一个集合**而非某张牌本体 → 仍是**战场条目**（`Indefinite`）。**集合性效果一律走战场，无论有无时限。**

**触发式效果的挂载**：既定部分原样成立（触发注册面坐在战场上、命中后由 StackManager 压栈、载体开放）。**补一条落地要求：非永久条目也可以带 `AbilityData[]`**（例：一条 `ForTurns(2)` 的条目「每当你打出符箓时，产 1 点道念」）；**条目被清理时其上的触发器同时注销**——这是清理逻辑的隐含义务，须写进 BattlefieldManager 的职责。

Source: `handoffs/2026-08-06d-combat-open-questions-mass-closure.md`。

### 探查（probe）的形态（方向已定 · 落地仍归内容横向扩展阶段）

- **升一档，不打穿黑箱（规则层结论，现在就固定）**：完全无信息 → 仅类别；仅类别 → 完整意图；已完整 → 无效果且 UI 禁用。**越阶黑箱不得被任何内容道具打穿**——否则一条结构性规则被一件内容道具整个绕过，境界压迫感失效。
- **载体 = 古宝 + 阵法双载体**（前者「关键回合一定拿得到」，后者 build 向：牺牲一次出牌与场上位置换持续的信息优势）；法则 `Power` 可有但极稀缺；**法术不做**——受抽牌运制约，**信息能力最怕「需要时抽不到」**，且一张只给信息不产道念的牌在 5 回合对局里几乎永远不该打。
- **花费 = mana 为主 + 古宝上叠 `Charges`，明确排除弃牌**：弃牌把一条**信息成本**压在 5 回合对局里极稀缺的**手牌资源**上，代价过重且不可预测；mana 每回合刷满不结转，「花 1 点探查 = 这回合少打一张小牌」代价即时、可感、不跨回合累积。
- **时效**：只揭示当回合已公布的那份快照，不重算、不延续到下回合 ⇒ **探查是纯呈现层开关，`RunCombatAsync` 形状完全不变**。
- 具体道具条目、次数、文案随内容横向扩展阶段落地，属内容排期。

Source: `handoffs/2026-08-06d-combat-open-questions-mass-closure.md`。

## 决策(-> ADR)
> _已定案的决定链接到 decisions/ADR-####。_

- **次类型 schema 与 id 规范（`CardSubtypeData` + `<maintype>.<name>` + 单一全局索引）；`enchantment.ambush` 为埋伏的最终标识符** —— 已定案。Source: `handoffs/2026-08-06d-combat-open-questions-mass-closure.md`。
- **`CardData.Pool` 三值枚举（必填、无默认）：共用体系、不共用卡池** —— 已定案。Source: 同上。
- **回合内状态 = `EntryLifetime` × `CountdownSide` × `RemainingTurns` 三件套；非永久条目可被针对但效果须显式声明目标类别** —— 已定案。Source: 同上。
- **效果系统三层；求值 = 加法层 + 乘法层且「加法先于乘法」是规则；`CardInstance` 运行态判据 = 有无过期时刻；「一切增益减益都是战场条目」** —— 已定案。Source: 同上。

## 待决问题
> _尚未解决，需要一次 handoff/决策。_

- **效果关键字体系与目标规则（承重 · 需一次专门 handoff）。** 效果的原子操作清单与求值管线已定；**可复用的效果关键字词汇表**与**目标规则的完整判据**（谁可以指定谁、合法目标集如何计算）仍是结构占位。→ `common-properties.md`。
- **`CardData` 的完整字段清单与起始卡组内容。** 卡牌类型五分、异能三分、次类型、`Pool`、`Subtypes` 均已定；其余字段（费用、目标声明、效果引用、触发器）与 **starter deck 的具体内容**未设计——**起始卡组正是 ch1 数值标杆专场的切入点**。
- **卡组规模的实际取值。** 两侧均不设硬限（规则层），故规模是内容/构筑层的问题：起始卡组该给多少张、敌人样本卡组的常用区间落在哪里——**疲劳规则使规模直接换算为后期失血速率**，归 ch1 数值标杆专场。
- **道念产 / 削的量纲基准（承重 · 已归属专场）。** 「卡牌产道念、可互削、下限 0」已定；**一张牌该产多少**、**10 个回合内一方的总产出应达到起始 `baseMomentum` 的几倍**——**明确推迟到内容横向扩展阶段**，切入点是**设计起始角色 starter deck 的过程**，届时聚焦并定义 **ch1 的数值标杆**（一场专门的「ch1 数值模型」session）。**越级追分的结论已先给出：可能，但很难，境界差越大越难**——`baseMomentum` 跨度随境界放大正是为此。是否存在道念相关的状态与倍率仍未定。→ `systems/balance.md`、`systems/scoring.md`。Source: `handoffs/2026-08-01b-abstraction-levels-combat-numbers-codex-family-and-monetization.md` + `handoffs/2026-08-02-momentum-conversion-reward-structure-and-mtg-stack.md`。
- **出牌费用 = mana（已定案）。** 每回合出牌资源为 mana，**每回合开始恢复至 `manaLimit`**（炼气基线 5/5）；`manaLimit` 由事件 cost / reward 推拉。→ 见 `../mana.md`。

## 对应
提炼至：`.claude/knowledge/systems/character-profile/deck/_index.md`（待建）。
