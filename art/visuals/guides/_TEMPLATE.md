# <资产名>

- id: `<类目>-<主体slug>`
- 类目: `<卡面插画 | 敌人立绘 | 角色形象 | 图标 | 事件插图 | 屏幕背景 | UI 元件>`
- 对应内容条目: `<systems/... 的条目 Id，或「无」>`
- 状态: `draft`   # draft | iterating | settled

## 继承

继承 `../art-direction.md` 的全部约束。本 guide 只写该资产**特有**的部分。

若本资产需要**偏离**总方向的某一条，在此写明**偏离哪一条、为什么**——不写理由的偏离等于风格漂移。

> _（无偏离则写「无偏离」。）_

## 主体与场景

> _（画什么：主体、动作 / 姿态、环境、氛围、叙事瞬间。）_

## 构图要求

> _（画幅比例、主体占比、视平线、留白与文字区预留、缩略尺寸下必须保留的辨识特征。）_

## 参考素材

> _（本次一并投喂的素材，逐条写「借它的什么」。指向 `../references/_index.md` 的登记条目。）_

## Prompt

```
（投喂给 Midjourney 的 prompt 正文。）
```

**参数：** `<--ar / --s / --v / --sref / 其他>`

## 产出与迭代

| 版本 | prompt 改动 | 结果 | 结论 |
|------|-------------|------|------|
| v1 | 初版 | _（待填）_ | _（待填）_ |

> 记住什么 work、什么不 work。下一份同类目的 guide 从这里起步，而不是从零试错。

## 交付

- 落地路径: `<game-feature-branch/ 中的路径，待命名规则定案后填写>`
- 后处理: _（裁切 / 抠图 / 压缩 / 图集，或「无」。）_
