# Answer log 0910

- **日期：** 2026-09-10
- **来源：** `/summarize-open-questions --lib=game`（全量整理 · `/progress-sync` 波次 3；对账基线含波次 1 固化的 `ADR-0224` ~ `ADR-0254` 共 31 份）
- **移出条数：** 1

---

**`PlayerProfile.achievement` 的条目结构待 `Achievement` schema 答定后补入 `schemaVersion` v1 清单**（原在 `open-questions/05-service-contracts.md` 的「存档 `schemaVersion` 登记表的覆盖对账」条，第 ① 件）
→ **已答定并落笔。** `Achievement` 的条目 schema 与进度模型由 `ADR-0196` ~ `ADR-0198` 定案，采集面由 `ADR-0199` 定案；v1 清单据此补入三行——`#35 PlayerProfile.achievement`（元素 `Achievement`）· `#36 PlayerProfile.achievementGroup`（元素 `AchievementGroupState`）· `#37 ProfileChangeSpec` 的 `AchievementElements` / `AchievementTierElements` 两列。**仍属 `schemaVersion` 1，未产生新 bump**，与登记时的约定一致。（归档去向：`systems/services/profile-schema-versions.md` 的 v1 版本行表 · `systems/player-profile/achievement/common-properties.md` · `decisions/ADR-0196` ~ `ADR-0199`。）
**剩余部分仍留在待答清单**（`open-questions/05-service-contracts.md`）：同条第 ② 件 **v1 的 golden 形状快照 `profile-shape-v1.json` 待建**——属落地排期项，卡在 `game-feature-branch/` 尚无 `.csproj`，与本片三项 `.csproj` 实测同批。

---

**同批清理（非问题条目，不计入移出条数）：** `05-service-contracts.md` 末尾那条「备注（指向索引）」已删除——它指出的三处陈旧前提（`derive 就绪度` 小节内 `sync-service.md` 判定行的「⚠ derive 前置」、「落笔前的零决策修正 ①」、derive 顺序第 7 步的「先补齐 schema bump 清单」）在 2026-09-10 的全量重估中已随该小节整体重写而消失，机械核实三处均零命中。
