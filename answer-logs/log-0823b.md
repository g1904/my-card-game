# Answer log 0823b

- 日期：2026-08-25
- 来源：`inbox/draft-0823b.md` → `handoffs/2026-08-25-enemy-deck-from-techniques-and-ai.md`
- 移出条数：2（另 3 条收窄、6 项 interview 新裁决、2 条新增待答）

## 移出的待答项

**敌人是否也以功法构筑卡组** → **是。** 敌方样本卡组由功法引用列表（`TechniqueRef { TechniqueId, Tier }`）+ 游离散牌列表构成，展开为卡牌集合；敌我共用同一 `CultivationTechniqueData` 类型与同一注册表。图鉴词条②「功法简介」随之指向真实功法条目，不再是纯风味文案。（归档去向：`systems/enemies/_index.md`、`systems/enemies/common-properties.md`、`systems/character-profile/deck/_index.md`、`systems/player-profile/codex/enemy-codex.md`、`terminology.md`）

**敌人各等级的道念产出缩放** → **敌人侧不设专属的隐藏数值乘区**，产出能力的决定因素与玩家完全一致；起始值仍由 `baseMomentum` 给定。残余的「一张牌该产 / 削多少道念」量纲不属敌人侧，已由既有的「卡组规模的实际取值 / 功法的规模参数」两条承载，不重复登记。（归档去向：`systems/balance.md`）

## 本次 interview 的六项裁决（用户逐条拍板，全部采纳推荐）

**`OverridesDeck` 在物化改写取消后如何处理** → **删除字段**。物化改写不再发生，「关键卡牌不得被物化改写」这条检查恒真、豁免格恒无对象；天劫等条目的定制性改由 `Pool == Enemy` 的敌方专用功法承担，不开第二条构筑通道。（`systems/enemies/**`、`systems/services/combat-service.md`、`systems/player-profile/codex/enemy-codex.md`）

**敌人构筑面是否保留游离散牌一格** → **保留**，与玩家侧「卡组 = 功法展开的牌 ∪ 游离散牌」同构。保住了「卡组规模是一条疲劳编排旋钮」这条已成文的编排手段。被接受的代价：散牌不属任何功法，「敌人卡组 = 玩家可习得内容」留有一小块例外。（`systems/enemies/**`）

**条目之间的层数散布是否要护栏** → **立内容编排护栏 + 在基线段明写前提**。敌人功法层数按篇章基准档编排、逐条目偏离 ≤ ±1 档；`lifeTotal` 基线段明写「1.1 余量的成立前提 = 敌我卡组层数相当」。落内容编排口径 + `/audit-content` 汇总（只报告不阻断），不进加载期校验。三条基线数字与两句承重文一字未动。（`systems/balance.md`、`systems/enemies/_index.md`）

**敌人模板级定制 AI 策略是否算强度 / 难度旋钮** → **只表达打法风格，不作旋钮**。难度仍只由 `baseMomentum` 与内容编排承担；`combat-service.md` 推论③中把「规划算法」点名为难度旋钮的措辞已中性化。「策略不得强于兜底多少」是不设校验的软编排口径。（`systems/services/combat-service.md`、`systems/adventure-event/combat/_index.md`、`systems/enemies/_index.md`）

**图鉴词条②的手写正文是否仍写功法名** → **去掉功法名，只写一句路数**；名字由主功法引用字段在呈现层渲染。功法名的唯一权威是功法条目的 `LocalizedText`（多语言 + overlay 可热更），词条里手抄一份没有任何机械检查能发现不一致。硬检查「不含阿拉伯数字」仍只作用于手写那一句。（`systems/player-profile/codex/enemy-codex.md`、`terminology.md`）

**词条②的功法引用字段是单个还是列表** → **单个「主功法」`Id`、必填**，加载期校验「须 ∈ 该敌人的功法列表」→ `PushError`；只带 `Id`、不带 `TechniqueTier`。其余功法在敌人词条里无入口是一屏纪律下被接受的取舍。（`systems/player-profile/codex/enemy-codex.md`）

## 标准默认（自动采纳，未出题）

**敌人引用功法是否加 `Pool != Character` 的加载期闸** → **加**（`PushError`，报出模板 `Id` + 功法 `Id`）。`CardData.Pool` 在卡牌层本就是两侧各一条闸，功法层是它的同构上移；「敌方套牌不过滤」讲的是取池（读取侧 `Get(id)`），不覆盖加载期编排校验。

**`Pool` 字段的定义落点** → 留在 `deck/_index.md`「卡池划分」节，不上移到 `deck/common-properties.md`。该节的三条论证与枚举定义是同一条推理的两半，拆开两边都读不完整；不搬迁仍只有一份权威。

**`LearnTechnique` / `UpgradeTechnique` 的 `Pool == Enemy` 拒绝点** → 补上（`PushError` + 整批拒绝）。既有「敌方专用牌最后一道闸」只挂 `AddLooseCard`，取池过滤挡不住定值奖励 / 剧本推拉 / `TargetId` 这类非取池路径的授予。

其余各分片的标准默认（展开产物不写回条目、派生索引落 ContentRegistry、`ContentEnabled == false` 不连带过滤、同一功法重复列出 `PushError`、`EnemyInstance` 六字段不变、AI 决策取 `combat` 子流的纯函数与对称输入面、图鉴静态性三句不改）详见各主题文档。

## 收窄但未关闭的既有条目

- **敌人 AI 的决策形态** —— 两层结构与策略归属已定、「难度旋钮的落点」已答定并删除；仍未定表达形态、算法、粒度、多回合倾向、兜底与定制的强弱差。
- **功法的规模参数** —— 「层数上限」定名 `MaxTier`，取值仍待定；新增一个面「敌人功法层数的篇章基准档取值」。
- **敌人条目的叙事一致性编写口径** —— 未触及，仍在 `deferred-content.md`。

## 本次新增的待答项

- `Pool == Enemy` 的敌方专用功法在功法图鉴中如何呈现（写入面归功法图鉴一侧）。
- `combat` 子流的三句互相矛盾（先于本次存在，本次未修，落笔一律按「取 `combat` 子流、不派生新流」）。
