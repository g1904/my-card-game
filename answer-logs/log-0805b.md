# Answer log 0805b

- 日期：2026-08-05
- 来源：`inbox/draft-0805b.md` → `handoffs/2026-08-05b-location-fields-event-count-limit-and-skip-refill-closure.md`
- 移出条数：**2**（另 3 条部分答定 / 收窄）

## 移出（已答定）

- **跳过语义的残留细节（能否整批全跳 · 付不起 `skipCost` 时如何表现）** → **两条产出侧保证一次性闭合**：**① 不会生成付不起 `skipCost` 的事件**（判定在每一次物化，含 `TryRefill` 补位那一次，对照当时资源）——「付不起时怎么表现」不会发生；**② 不会生成整批全跳的 eventOptions**——每批至少一个 `IsMandatory == true` 的选项，故「整批全跳」不可能、批次不会被跳空。二者都是**产出侧约束而非消费侧处理**。（归档去向：`systems/adventure-event/common-properties.md` 的「跳过通道的玩法语义」、`systems/services/future-event-service.md` 的「意图」）

- **补位落空的判定规则（含「是否会剩 0 个」与死局兜底）** → **判据 = 当前 location 的 `eventCountLimit` 用尽**（不是事件池耗尽、也不是优先级 / 剧本约束）。配额用尽后本批收窄为**仅剩 Travel**，与「每批至少一个不可跳过的选项」合力保证**任何时刻至少有一个可推进的选项**——**死局兜底问题不再成立**。连带：`TryRefill` 返回 `false` 的语义从「运气不好」变为「本地域已满，该走了」。（归档去向：`systems/services/future-event-service.md`、`systems/adventure-event/common-properties.md`、`systems/game-progression.md`、`systems/adventure-event/travel/_index.md`）

## 部分答定（剩余部分仍留在待答清单）

- **生成 / 加权规则** → **location 层的形态已定**（事件类型出现概率修正 + 敌人模板集合 + `eventCountLimit`），**具体数值明确归内容制作阶段**（移入 `deferred-content.md`）。**仍待答**：每批数量、类型修正的运算形态（乘性 / 加性 / 白名单 + 权重、能否修正到 0）、三层框定的叠加顺序。

- **敌人物化时「充实 / 改写」的规则** → **取池那一半已答定**：`EnemyTemplate` 池由 location 给出，与「赋级 = 角色等级 ±2」（08-05）构成两条正交的轴——**location 决定「派谁来」，等级带决定「有多强」**。**仍待答**：带内五档的分布权重、样本卡组与道具 / power 列表怎么改。

- **location 机制细节** → **字段已定案**（三组）。**仍待答**：数据载体与定名（`LocationData`？）、location 之间的连通关系由谁承载、`eventCountLimit` 的计数口径（跳过是否计入、Travel 自身是否占配额）、location 是否随篇章 / 境界变化。

## 未答定但被本次抬高

- **付不起 `selectCost` 的死锁**：既然每批**必有**一个不可跳过的选项，付不起它就直接卡死轮回；`skipCost` 侧已给出对称先例，但用户本次只对 `skipCost` 表态。仍留在 `02-event-options.md`，权重上调为承重。

## 台账原记（自 `_index.md` 归并）

> 台账瘦身前，`answer-logs/_index.md` 本行记有以下内容，原样保留于此。

location 携带三组字段：事件类型概率修正 · 敌人模板集合 · `eventCountLimit`；配额用尽 → 本批仅剩 Travel；跳过的两条残留细节改由产出侧保证闭合；补位落空判据 = 地域配额用尽
