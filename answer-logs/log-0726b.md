# Answer log 0726b

- 日期：2026-07-26
- 来源：`90-inbox/draft-0726b.md` → `10-handoffs/2026-07-26-event-priority-skip-semantics-and-hotfix-scope.md`
- 移出条数：8（其中 2 条为部分答定，剩余部分仍留在待答清单）

## 移出条目

- **player-profile 子系统范围（除 player-item / player-power 外还有哪些、各成文件夹还是文件）** → **`player-item/`、`player-power/`、`achievements/` 各成文件夹；`account-info.md`、`game-setting.md` 为独立 markdown。**（归档去向：`20-systems/player-profile/_index.md`；新建 `achievements/_index.md`、`achievements/common-properties.md`、`account-info.md`、`game-setting.md`）
  - 注：该问题在清单中重复出现于「⚠ 服务 API 契约」与「元进程 / player-profile」两节，两处均已移出。

- **热更范围边界（overlay 可否新增 `Id`）** → **仅允许修改既有条目的数值 / 文案，不得新增 `Id`。** 新内容只能随版本发布；「旧客户端存档引用未知内容」的风险从根上消失，兼容规则不再必要。（归档去向：`20-systems/services/content-service.md`「热更范围：只改不增」）

- **overlay 与存档的版本耦合（是否冻结轮回的 `contentVersion` 以保 seed 可复现）** → **以 overlay 更新为准，不冻结；明确放弃跨内容版本的 seed 可复现。** 确定性降级为「同一 `contentVersion` 内的性质」。（归档去向：`20-systems/services/content-service.md`、`20-systems/common-properties.md`；并修订 `.claude/rules/state-save-rules.md` 中冲突的措辞）

- **跳过是否也扣 `lifeSpanCost`** → **通常不扣（时间通常不流逝）；少部分事件可带 `skipCost`，按其 element 扣减，寿元可为其中之一。**（归档去向：`20-systems/adventure-event/common-properties.md`、`20-systems/services/life-cycle-service.md`）

- **跳过机制的完整语义（部分答定）** → **单项补位而非整批刷新**（future-event-service 生成一个新事件顶替，**也可能补不出**，此时本批少一个选项）；**计入 `pastEvent`**，作为行为轨迹（类似 action-trace）。（归档去向：`20-systems/adventure-event/common-properties.md`、`20-systems/services/future-event-service.md`）
  - **剩余未答：** 能否整批全跳、付不起 `skipCost` 时如何表现、`pastEvent` 区分两种痕迹的 schema、补位落空的判定规则 —— 仍留在待答清单。

- **`ifMandatory` 由谁置位** → **由 future-event-service 在产出 eventOptions 时动态置位（非内容作者写死）；一批可以全部 mandatory。**（归档去向：`20-systems/services/future-event-service.md`、`20-systems/adventure-event/common-properties.md`、`terminology.md`）

- **元婴 +500 的用途（是否值得保留该字段更新）** → **值得保留：用于确保元婴界面（类似「通关证书」的终局展示面）的数据显示正确。** 它仍非平衡杠杆，但有明确读者。（归档去向：`20-systems/balance.md`、`40-ux/screen-flow.md`）

- **寿元 <10% 显示的 UX 形态** → **标红的数值倒数（red count-down numeral），而非常驻进度条。**（归档去向：`40-ux/screen-flow.md`）

- **架构闭环缺口 5（skip 通道，部分答定 → 已闭合）** → 玩法语义主干补齐（不扣寿元 / 计入 pastEvent / 单项补位），缺口状态由「部分闭合」改为「**已闭合**」，8 处缺口全部闭合；残留细节下沉为普通待决问题。（归档去向：`20-systems/architecture.md`「闭环缺口」）

## 本次新引入的概念（非移出项，供检索）

- **`eventPriority`（事件优先级）** —— AdventureEvent 新增共有字段，与 `ifMandatory` 分属两条约束轴。→ `terminology.md`、`20-systems/adventure-event/common-properties.md`。
