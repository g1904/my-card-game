# Answer log 0730

- 日期：2026-07-30
- 来源：`inbox/draft-0730.md` → `handoffs/2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown.md`
- 移出条数：**3**

---

- **`.claude/rules/*` 与本库的主从关系**（规则是本库的强制执行摘要，还是独立于本库的工程约束？冲突时以谁为准？）
  → **`.claude` 是工程层，对设计只做薄引用。** 它只承载 ① 工程相关的配置与规则、② 可复用的技能；一切设计相关的知识与细节归设计库，在 `.claude` 内只被引用与轻描述。**冲突裁决按性质分流**：设计性内容（机制、数值、字段、契约、流程）→ **以设计库为准**，`.claude` 跟着改；工程性约束（命名、生命周期、热路径、工具 / PATH、目录纪律）→ **以 `.claude/rules/*` 为准**（设计库对此无权威）。判据即「这句话的权威在哪一侧」。ADR-0005 的范围由 `.claude/knowledge/*` **扩展到整个 `.claude`**（含 `rules/` 与 `skills/`）。
  （归档去向：`decisions/ADR-0005-knowledge-thin-reference-layer.md`、`systems/common-properties.md`「与 `.claude` 的主从关系」、`.claude/rules/Context.md`、`.claude/README.md`）
  *剩余部分仍在待答清单：* 现存 `rules/*` 中夹带的设计性表述（`state-save-rules.md` 的确定性边界、`data-resource-rules.md` 的 `AllEnabled()` 语义）是否属「一句话承重纪律 + 回链」的合法形态、还是应进一步瘦身，仍待一次核对。

- **寿元红字倒数的呈现细节**（逐格递减还是持续跳动？常驻哪些屏幕？是否伴随音效 / 震动？）
  → **静态标注（static annotation），位置只在 EventOption 选择界面。** 数值随事件结算而变、**平时静止**，不做持续跳动 / 计时器感的动画；**不做全局 HUD、不进战斗内**（战斗屏幕不承载寿元告警）。
  （归档去向：`ux/screen-flow.md`「意图」、`ux/combat-ux.md`）
  *剩余部分仍在待答清单：* **是否伴随音效 / 震动**未陈述——「静态标注」的措辞倾向于「无强调反馈」，但未明确排除，留在 `ux/screen-flow.md` 的待解问题。

- **combat-service 的 manager 清单中 IntentManager 的归属**（此前作为与 TurnManager / DeckManager 并列的第三个 manager）
  → **IntentManager 降格并入 EnemyManager。** 意图生成不单列 manager，是 **EnemyManager** 的内部职责之一，与敌人实例状态、AI 行为选择同属一个组件（三者共享同一份敌人运行态）。同时定案：**敌人意图通常不向玩家揭示**——对 Slay the Spire「意图常驻预告」的有意背离，意图默认不进呈现层。
  （归档去向：`systems/services/combat-service.md`、`systems/architecture.md`、`systems/services/_index.md`、`systems/_index.md`、`program-overview.md`、`system-overview.md`、`terminology.md`、`ux/combat-ux.md`）
  *新浮现的下一层问题（已进待答清单）：* 「通常不揭示」的例外条件；意图隐藏后玩家凭什么做出牌决策；EnemyManager 内部是否还需再分职能。
