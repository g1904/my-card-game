# Answer log spirit-stone-budget-and-combat-count

- 日期：2026-09-12
- 来源：`inbox/solution-draft-spirit-stone-budget-and-combat-count.md`（经 `handoffs/2026-09-12-spirit-stone-budget-and-combat-count.md` 提炼）
- 移出条数：**1**

**灵石账 `I(ch1)` 与战斗场数口径** → 两半同批收口，全部由既有输入逐步复算得出，不含取向成分。**① `I(ch1) = 150`**：`BaseReward`（5×5 + 5×10 + 1×20 = 95）+ 支路 A 加成（`n₁` 11 × `floor(E₁ 5 / K 1)` = 55）= 150；取全胜口径而非按败率折的 139，依据是 `I_cum` 表头自陈为「未花销**上界**」且经验供给账 `G_c` 同为全胜口径——**两账同口径**正是本条要求的收口，139 降为并置的对照值。**② 战斗场数是两个量、不是三个**：`0.35 × P + 1` 不是独立的第三读数，它就是权重期望那个读数的生成式，此前代入的两个值都不是 `P`；`P` = 管线事件数 = 事件总数 − 闸门 `Travel` − `Finale` = **30 / 31 / 37**（ch1 由参考构成独立验证：35 − 4 − 1 = 30），定义已就地补出。**③ 剩下两个读数各有互斥适用面，不统一成一个数**：参考构成口径 `n_c`（整数 11）用于与**同一份构成**的其他项相加（`I(c)` 与 `G_c` 的加成项），权重期望口径 `n̄_c`（11.5 / 11.9 / 14.0）用于**整章期望**（`F_c` 失败扣减、`S(c)` 反推）——参考构成是一条无失败的清单、读不出败率，强行统一会迫使其中一侧取一个它算不出来的量；ch2 / ch3 无逐类型构成表 ⇒ 两个口径塌成一个。**④ 典型路线对照值 ≈140 / 139 并置登记**（两条独立路径一致到 1 点，互为交叉验证），挂 ⚠ 免责、不进表行。**⑤ 两份 ADR 的陈旧数字连带订正、只改数字**：`ADR-0247` 的 `I(c)` ≈150 / 300 / 480（ch1 占 27%）· `ADR-0134` 的战斗场数 11 / 11.9 / 14（含 Finale）；两处结论与理由链一字不动。（归档去向：`systems/balance.md` · `decisions/ADR-0247-lifespan-item-orchestration-guardrails.md` · `decisions/ADR-0134-spirit-stone-is-combat-loot.md`）

**仍留在待答清单的部分：**

- `E[道念差]`（5 / 12 / 18）与败率 20% 仍是 ⚠ 待实测标定格；150 / 140 / 139 三个数全部依赖它们，故为量级估算而非定稿。已在既有的 `E[道念差]` ch3 偏高与卡牌道念量纲两条待答项下登记，本次不重复立项。
- ch2 / ch3 的逐类型事件构成表补齐后，`I(ch2)` / `I(ch3)` 与 `G_2` / `G_3` 整列须重算，届时加成侧改取那两章的 `n_c`；复核挂钩已在 `systems/balance.md` 两处各留一条，不新增条目。
- λ / `N` / 21 格 / 25 格 / `S(c)` / `G_c` / `D_c` / `F_c` / `C1` 全部未动——本次订正的是**记账的写法与两处过期副本**，不是任何被反推出来的取值。
