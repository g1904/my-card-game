# Answer log 0815a

- 日期：2026-08-15
- 来源：`inbox/draft-0815a.md` → `handoffs/2026-08-15c-event-type-collapse-and-batch-shape.md`
- 移出条数：**6**（另 5 条部分答结、收窄后留在清单）

## 整条移出

**九类分类法是否需要重构 / Explore · Travel 何时并入 ADR-0002 枚举** → **分类法整体收为五类**（Combat / Exchange / Research / Explore / Travel），判据 =「是否共有同一套特征」，类内差异用参数 / 档位表达而非新增枚举值。Explore / Travel 随本次重写正式入枚举，「ADR-0002 补订」这条待办随之消失。（归档去向：`decisions/ADR-0002-adventure-event-taxonomy.md`（整份重写）、`systems/adventure-event/_index.md`、`terminology.md`）

**Mystery 可被遮罩的子类型范围** → Explore 继承元类型语义，**真身取值域 = Combat / Travel / Exchange**；**不含 Research**（卡组编辑是玩家主动规划的动作，藏起来只制造挫败）、**不嵌套自身**。遮罩的仍是一个**固定**事件、进入即揭示，沿用既有 `IsRevealed` / `RevealedEventId` 两个物化字段，不新增机制。（归档去向：`systems/adventure-event/explore/_index.md`）

**每批 eventOptions 的数量** → **常态 3 项，取值区间 1–5**。批次不是固定宽度；1 项的批次合法（与 `Priority = 1` 收窄到单项、Travel 20% 随机档同形，不需要额外规则允许它）。（归档去向：`systems/adventure-event/common-properties.md`、`systems/services/future-event-service.md`）

**Travel 闸门给几个候选、怎么选（全部邻接 vs seeded 抽取）** → **80 / 20 掷定**：80% 列出全部邻接地域并列供选，20% seeded 随机取一个。**常规出场与配额闸门一律适用**；**Explore 揭示出的 Travel 必为随机那一档**。掷定发生在 future-event-service 的物化阶段。连带答定「Travel 不是常驻可选项」——只在配额闸门时必定出现。（归档去向：`systems/adventure-event/travel/_index.md`、`systems/game-progression.md`、`systems/services/future-event-service.md`）

**成本类型的 element 清单 / `CostKey` 的其余成员** → **成本侧只有 `lifeSpanCost` 一项**，其余资源（jade / mana / 道具 / 隐藏属性推拉）**不进成本侧**。但**复合形态 `ProfileChangeSpec` 保留**，不塌缩为单一 `int`——保留的成本是零，塌缩则要连改三处（快照形状 / 物化取负链路 / 物化组装段）。（归档去向：`systems/adventure-event/common-properties.md`）

**开局强制构筑事件归哪一类（标记为「阻断内容编排」）** → **归 Research**，与「Research = 调整 / 升阶卡组」的新定义直接对位；**不需要第六类**，承载机制仍是既有的 `eventPriority = 1`。（归档去向：`systems/adventure-event/research/_index.md`、`systems/adventure-event/_index.md`、`systems/character-profile/deck/_index.md`）

## 部分答结（收窄后留在待答清单）

- **生成 / 加权规则与叠加顺序** —— 每批数量与重算依据已答定；运算形态、叠加顺序、区间两端由什么驱动仍待定。
- **`lifeSpanCost` 的具体分档表** —— **表的形态**已答定（「`eventType` × 篇章」定价表，Combat 行按 `combatTier` 细分，条目只标偏移 / 覆盖值）；**每格的取值**仍归 ch1 数值标杆专场。
- **哪些资源允许被打穿** —— 成本侧只剩寿元 ⇒ 该问收窄为纯寿元的钳制规则（截断到 0 还是允许为负），其余资源不再涉及。
- **「余额不足即拒」还剩哪些消费点** —— 收窄为「只剩 Exchange 的商店购买这一个可能消费点」。
- **`EventOption` 的完整物化字段清单** —— 新增两个待定项（`lifeSpanCost` 是否带区间 / 公式、`combatTier` 是独立字段还是折进 `EncounterSpec`），文本类字段仍不在范围内。

## 因前提消失而作废（非答结）

- **Practice 的风险 / 回报差异**、**Finale 的独立胜负条件**、**Mystery 的揭示权重 / 机制** —— 这三条待答项的措辞建立在「它们是独立 `eventType`」之上。Practice / Finale 的实质内容已在 08-02 / 08-06d / 08-09b 定案且原样保留（改挂 `combatTier`）；Mystery 的揭示机制已由 Explore 承接。剩余未定部分改写为「`Practice` / `Finale` 档的奖励厚薄」与「Explore 揭示池的权重」，留在 `open-questions/03` 与 `02`。
