# Answer log treasure-swap-share

- 日期：2026-09-12
- 来源：`inbox/solution-draft-treasure-swap-share.md`（经 `handoffs/2026-09-12-treasure-swap-share.md` 提炼）
- 移出条数：**1**

**法宝置换在「失去能力」上层 ≈1.0 分子里占多少份额** → **法宝一族合计 ≈ 0.35 次 / 完整轮回**（置换 ≈0.28 / 法宝禁用 ≈0.06 / 古宝禁用 ≈0.01）。裁决口径：**守住 ≈1.0 这个天花板，其余三支等比压缩 ×0.65**——即神通 ≈0.32（置换 .21 / 禁用 .11，内部 2 : 1 保住）· 法则置换 ≈0.20 · 法则禁用 ≈0.13，上层合计 **1.00**、四支合计 **2.11** 逐字不变。**① 给份额是一次对分子的重新配平、不是填空**：既有三支恰好占满 ≈1.0，任何正份额只能从三支里割；等比压缩使全序关系与神通内部比例逐格自动保住，改的是一个数不是形状。份额取 0.35 是窗口上端——低于 ≈0.28 时法宝置换压不过法则置换、「最轻的一端应最常见」只剩算术成立，高于 ≈0.35 时法则禁用被压得过薄；取 0.35 时法宝置换 0.28 对神通置换 0.21 有 1.3× 可辨识梯度。**② 落位并入既有支、不新开第五行**：神通与法宝一族同属轮回级损失（禁用都不影响持有、轮回结束即恢复；置换都写 Profile），故那一行改写为**「轮回级能力损失」**一支、内部分列——行数不变，`ADR-0265`「不扩为五支」逐字成立。**③ 族内三分**：置换 : 禁用 ≈ 4.7 : 1（置换是有同意的正向决策点，法宝在两轴上最轻这一条几乎全部体现在置换那一格）· 法宝禁用 : 古宝禁用 ≈ 6 : 1（兑现「禁用古宝应显著更稀有」）；**一处刻意偏离已写进正文**——法宝禁用取 0.06 而非按两轴严格外推的 ≈0.13，因其单次体感是全表最轻的、撑不起一条 `Rare` 档事件的戏剧分量。首批商店 `PlayerItem` 深度为 0 ⇒ 古宝禁用那 0.01 在首批实际为 0，是预留格。**④ 供给护栏 ③ 同批下调**：≈ 2 次 / 轮回 → **≈ 0.28**，括号依据由「法宝总供给 24 件的 8% 周转率」改写为「上层 ≈1.0 预算内法宝一族份额的置换那一格；折合总供给的 ≈1.2%」——周转率视角的那个数挂错了地方，法宝的日常周转由随售与售出承载，那两条不写「失去能力」这本账。**⑤ 已接受的代价**：法则禁用 0.13 与神通禁用 0.11 落在「每 7~8 个轮回一次」的量级；玩家体感上法宝置换从「一个轮回撞上两次」变成「约每 3~4 个轮回撞上一次」。（归档去向：`systems/player-profile/player-power/_index.md` · `systems/balance.md` · `systems/character-profile/item/_index.md` · `systems/character-profile/power/_index.md` · `decisions/ADR-0265-item-swap-in-ability-loss-budget.md`）

**仍留在待答清单的部分：**

- 换算副本的分母 ≈114 事件仍在漂——**不阻塞**：承重表述取次数不取百分比，分母一改只重算括号里的百分比列。
- 「一次轮回预期获得几条神通」未答定；若神通供给量级大改，「轮回级能力损失」内部的 神通 ≈0.32 : 法宝 ≈0.35 须按同一条式子复核（改的是两格，不是形状）。该复核挂钩已在既有的神通数值条目下，不新增条目。
- 首批是否编排古宝条目未定（现按商店逐族深度 `PlayerItem` = 0 推断为否）；若首批就编排，古宝禁用那 0.01 须从预留格变成实际编排目标，并复核 6 : 1。
- `ADR-0265` 是否重开不在本次范围内——整份裁决以「法宝一族确实计入上层分子」为硬前提。
