# Answer log 0812a

- 日期：2026-08-12
- 来源：`inbox/draft-0812a.md` → `handoffs/2026-08-12f-cultivation-technique-deck-building.md`
- 移出条数：**0 条整条移出 · 1 条部分移出**

## 部分移出

**`CharacterPower`（神通）的机制细节 —— 其中的「获取 / 失去触发」** → **起手那一份已答定：每个角色自带一个绑定神通**，随开局的随机角色分配一并给出，同一角色的每一局都相同（归 `systems/character-profile/power/_index.md`、`systems/character-profile/_index.md`）。

**剩余部分仍留在待答清单**（`open-questions/07-codex-monetization.md`）：与法则的复用边界 · **事件侧**的获取 / 失去触发 · 篇章突破是否随「全部继承」带入 · 与卡牌 / 法宝的边界 · 数量与强度尺度。

## 附：本次由 interview 裁定、但原本不在待答清单上的七项

这些是 `draft-0812a.md` 自身的矛盾与含糊，经两轮 interview 当场裁定，**不构成待答清单的移出**，登记于此仅供回溯（权威归属见 `handoffs/2026-08-12f-*.md` 与各主题文档）：

1. **「炉石竞技场式多轮择一」描述的是哪一段** → **推翻草稿第 1 行**；实际形态 = StS 第一章式的**一个强制 buff 事件**（选一门功法 + 一件法宝，各三选一），构筑的多轮性由 adventureEvent 承载。
2. **「功法」撞名** → 新概念占用「功法」；既有次类型 `power.technique` 功法 → **`power.mystic_art` 秘术**（中文名与 id 一并改）。
3. **卡组是否被功法完全划分** → **否**：功法 + 游离散牌并存，业障 / 单卡奖励两条既有通道不动。
4. **「像宝可梦一样进化」的机制** → **整组替换为更强版本**（每层一整套卡牌定义）。
5. **功法的 level 与既有 level 撞名** → 功法侧另取名 **层数 / `TechniqueTier`**；角色的 `level` 不动。
6. **角色绑定的两门功法能否弃置** → **都能弃，不设限**（用户选择非推荐项）；连带明确「卡组可被弃空」由既有疲劳规则承接，不新增规则。
7. **敌人图鉴词条②「功法简介」的语义** → 保持为**纯风味文案名**，不指向 `CultivationTechnique` 系统概念（敌人是否也以功法构筑另立为待答项）。

## 台账原记（自 `_index.md` 归并）

> 台账瘦身前，`answer-logs/_index.md` 本行记有以下内容，原样保留于此。

**功法（`CultivationTechnique`）= 卡组的构筑单位** —— 一组**必须整组入组**的卡牌，卡组由若干功法构成、数量不限；带**层数 `TechniqueTier`**，**层数提升 = 整组替换为更强的一版**（每层一整套定义，非数值变大、非追加新卡）⇒ **卡组规模不随层数增长**，与疲劳那条压力互不干涉。**功法不是卡组的完全划分**：业障与单卡奖励作为**游离散牌**照常入组 ⇒ **不推翻既有推论④**。**角色升格为有身份的模板 `CharacterData`**：开局随机分配，**自带一个神通 + 两门绑定功法**，每局一致 ⇒ 神通有了第一条确定获取渠道、跨轮回熟悉感有了载体。**开局构筑 = 一个强制 buff 事件**（选一门功法 + 一件法宝，各三选一，取 StS 第一章的味道）——**⚠ 推翻草稿第 1 行「炉石竞技场式多轮择一」**（且该说法全库检索不到既有记载），构筑的多轮性改由 adventureEvent 承载（升阶 / 弃置 / 学新）；承载机制复用既有 `eventPriority = 1` + `ifMandatory`，**不新增机制**。**弃置不设限**（绑定的两门也能弃）⇒ **卡组可被弃空**，由既有疲劳规则承接后果、不加新规则。**定名三裁**：新概念占「功法」· 既有次类型 `power.technique` → **`power.mystic_art` 秘术**（中文名与 id 一并改，否则撞名只是搬到标识符层）· 功法的等级叫**层数 `TechniqueTier`** 而非 level（`level` 是境界内层级、承重字段，不动）。敌人图鉴词条②「功法简介」澄清为**纯风味文案名**，不指向系统概念。**纯本地、纯客户端，无跨库影响，不 bump schema**　｜移出条数原记：0 整条（1 条部分移出、新增待答 7 条）
