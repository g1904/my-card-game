# Answer log 0806b

- 日期：2026-08-06
- 来源：`inbox/draft-0806b.md` → `handoffs/2026-08-06c-skip-channel-removal-priority-two-tier-and-location-codex-edges.md`
- 移出条数：**5**（另 2 条收窄）

## 逐条

- **`LocationCodex` 记不记连边（承重）** → **记。** 词条记录「它通向哪些地域」，玩家可跨轮回**重建整张 `locationMap`**——这是**设计目标（知识 = 力量）**，不是要规避的泄露。连带：`locationMap` 的不可见是「初见不可见」而非「永远不可见」；「中长期规划感 / 方位感」的**地理那一半**由此落地；图的稳定性升格为对玩家的隐性承诺。（`systems/player-profile/codex/_index.md` · `systems/game-progression.md` · `terminology.md`）
  - **仍留清单：显影粒度** —— 去过 A 后列出 A 的**全部**邻接（含未去过的），还是只记**走过的那几条边**？本库现按前者理解，待确认。
- **`skipCost` 的去留** → **概念整体移除**（太复杂、不值得）。（`systems/adventure-event/common-properties.md` · `terminology.md` · `systems/balance.md` · `systems/player-profile/player-power/common-properties.md`）
- **跳过通道的一切残留细节**（能否整批全跳 · 付不起 `skipCost` 如何表现 · `pastEvent` 区分两种痕迹 · 补位落空 · 已定稿批次存续期间资源下降） → **以移除整条通道的方式一次性消解。** 理由：每次选择后 eventOptions 整批重算，**选中一个即等价于跳过其余**。`ifMandatory` 与 `TryRefill` 一并删除；「每批必有不可跳过项」升级为**结构性事实**（本批每一项都是必做项）。（`systems/adventure-event/common-properties.md` · `systems/services/（future-event-service.md, life-cycle-service.md）` · `systems/architecture.md` · `program-overview.md` · `system-overview.md` · `systems/game-progression.md` · `systems/adventure-event/travel/`）
  - **仍留清单：`pastEvent` 快照的字段形态**（痕迹只剩一种，但**未被选中的选项是否随批次快照归档**是新问题）。
- **付不起必做项 `selectCost` 时的终态（承重）** → **支付 `selectCost` 是无条件的可推进行为**：照常支付 → **支付后做终态判定** → 判负则进入既有失败流程。**推翻「付不起则拒绝、不产生任何写入」**；死锁在规则层不成立，产出侧不欠任何可负担性保证。（`systems/adventure-event/common-properties.md` · `systems/services/（life-cycle-service.md, profile-service.md, future-event-service.md）` · `systems/architecture.md` · `program-overview.md`）
- **`eventPriority` 与 `ifMandatory` 的叠加规则** → **问题消解**：`ifMandatory` 已删除，选择约束只剩 `eventPriority` 一条轴。（同上）
- **`eventPriority` 的取值域与置位方** → **两档（0 / 1）**，**由 future-event-service 独占置位，PlotManager 不得改变**。连带：**PlotManager 只调内容不调约束**——剧本的强制性只能靠收窄候选池表达。（`systems/adventure-event/common-properties.md` · `systems/services/future-event-service.md` · `terminology.md`）
  - **仍留清单：`Priority = 1` 依什么条件抬升**（取值域与置位方已定，抬升条件未定）。

## 新增待答（进 `open-questions/02-event-options.md`）

- 「记连边」的显影粒度（承重）
- 哪些资源允许被打穿、各自的截断与终态判据（承重）
- 「余额不足即拒」还剩哪些消费点（`CanAfford` / `CostRejected` / `MissingElement` 是否整体删除）
- 未被选中的选项是否随批次快照归档进 `pastEvent`
- `Priority` 字段是否从 `int` 退化为 `bool`（轻）
