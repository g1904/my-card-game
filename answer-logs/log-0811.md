# Answer log 0811

- 日期：2026-08-11
- 来源：`handoffs/2026-08-11-plot-content-localization.md`（粘贴文本，无 inbox 草稿）
- 移出条数：3

---

**剧本内容归属在客户端还是云端？（原「本地 / 云端内容分界」判据划到云端一侧）** → **本地内容层。** 剧本节点 / 分支 / 文本作为内容条目存于 `res://content/` 基线 + `user://overlay/`，经 ContentRegistry 按 `Id` 读取；**云端剧本服务撤销**，运行时内容零网络请求。裁定依据：原归属从未被论证（07-23 是纯断言，07-25c 的判据是描述性、近乎循环的），且无任何 Accepted ADR 覆盖它。**同时推翻 07-25c 的本地 / 云端分界判据**——新口径为「一切内容属本地内容层，`是否被存档引用` 只决定 overlay 能否为它新增 `Id`」。（归档去向：`systems/services/plot-manager.md`、`systems/services/content-service.md`、`systems/architecture.md`、`program-overview.md`）

**剧本是预写式内容库，还是运行时生成？**（原属后端 `open-questions/05-plot-service.md`） → **预写式内容库。** 剧本文本是人工 / 离线写就的静态内容。这是本地化的前提——运行时生成（LLM）无法本地化，密钥 / 成本 / 内容审核都必须在服务端。**不为「未来可能改成生成式」预留结构**：若将来要改，那是一次新的方向性决策。（归档去向：`systems/services/plot-manager.md`；**后端侧的同名条目须由后端库的一份对应 handoff 移出**）

**剧本服务的请求 / 下发协议、缓存、离线降级、版本化，以及「预取与事务前置的边界」（LRU 容量上限 / 预取失败是否静默）** → **整条消失，不再是待答项。** 它们全部是「逐事件向云端请求文本」的派生物；剧本读取变为纯内存的 ContentRegistry 查找后，**不存在网络失败态**，故事务前置、`user://cache/plot/` LRU 预取、延迟预算、超时兜底、降级文案一并作废，`sync-service` 降级表中的「剧本请求」一行删除。唯一残留的缺失情形是**悬空 key point**，处置为 `PushWarning` + 叙事降级、不阻塞轮回——这是新立的承重规则，不是失败路径。（归档去向：`systems/services/plot-manager.md`、`systems/services/sync-service.md`）

---

**本次同时新增 2 条待答**（不属移出，记此备查）：剧本内容类型的**数据形态**（含「新增剧本条目不得引用本次 overlay 之外的新 `Id`」这条约束的可执行化）· 剧本内容的**体积与分发粒度**。均落 `open-questions/04-hidden-attributes-plot.md`。

**剧透 / datamine 可接受**是本次的一项前提裁定，但它此前不在待答清单内（原方案未把它列为问题），故不计入移出条数；已记入 `systems/services/plot-manager.md` 的撤销理由。
