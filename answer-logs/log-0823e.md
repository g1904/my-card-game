# Answer log 0823e

- 日期：2026-08-26
- 来源：`inbox/draft-0823e.md` → `handoffs/2026-08-26-storage-pack-two-layer-view-and-combat-holdings.md`
- 移出条数：**1 条**（`01-combat.md` 四项形态条中的启动入口半句）+ **5 条** interview 裁决（草稿内部待决，此前未占清单条目）
- 另有 **2 条作废**（前提消失，**非答定**，不记本 log —— 见 `open-questions/update-log.md`）

## interview 裁决（5 条）

**回寿法宝的持有量护栏由谁承接（取消储物袋容量上限后）** → 交给内容编排面（出现频率 / 商店库存深度 / 定价），规则层不设持有上限；另两道加载期条目合法性校验原样保留。剩余部分仍在待答清单：编排面三者的**具体口径**未定，已改写为 `01-combat.md` 的一条承重待答项（`systems/character-profile/item/_index.md`）

**随售产出的币种是什么（草稿 §1 说灵石，§3 的机制说同币回收）** → **同币回收**，币种由「族 × 稀有度」定价表那一格决定；草稿 §1 的「换取基础货币灵石」是错的。连带：仙玉两处「唯一获取通道」改为「唯一**主动**获取通道」，售出 / 随售是同币回流、不新增获取渠道；免费取得的仙玉计价法宝售出所得是**净产出**，代价正面写下（`systems/balance.md`、`systems/character-profile/currency.md`）

**神通 / 法则只读层的详情手势（草稿写点按，既有明写长按）** → **长按打开弹层**（详情 + 启动键合一），点按无语义；既有「可拖拽对象用点按、不可拖拽对象用长按」的手势判据一字不改（`ux/combat-ux.md`）

**账号级古宝在轮回之外的查看落点** → `PlayerProfile` 屏内增一区「持有的古宝」，只读列表 + 剩余 `Charges`、不可售不可用，文案走 `PROFILE_` 分区；**不新增主菜单入口**（`ux/screen-flow.md`、`systems/player-profile/player-item/_index.md`）

**「随售率显著低于商店档」是加载期硬校验还是内容编排口径** → **加载期硬校验**：`SellEnabled == true` 且 `SellRatePercent <= PackSellRatePercent` → `PushError`；两个费率仍是独立旋钮、互不作缺省。同时用户给出新的论证基底：Exchange 大部分不提供回收 ⇒ 随售是**常态的弃置途径**（低回收率使清仓不构成经济来源），提供收购的商店是**罕见的更优机会**——草稿原写的「保住『跑一趟商店』的规划价值」随之作废（`systems/adventure-event/exchange/common-properties.md`、`exchange/_index.md`、`systems/balance.md`）

## 从待答清单移出（1 条 · 半条形态项）

**带启动式异能的 `Power` 那个可点击入口如何与「视觉权重要低」并存** → 启动入口落在长按升起的弹层内（bottom sheet），图标条本体保持纯只读，可发现性由图标上一个静态启动可供性标记补偿。原条目（`01-combat.md` 的「道具区 / 神通法则条 / 埋伏标记 / 卡牌类型标识的四项具体形态」）**其余部分仍在清单内**，四项形态未定，待竖屏分区专场（`ux/combat-ux.md`）

## 标准默认（未出题，直接采纳）

**`Source.PackSell` 的成员表落在哪份文档** → `systems/common-properties.md`（`Source` 枚举唯一权威）；`exchange/` 与 `item/` 只回链不复述。依据：该文件自陈「同一枚举成员表出现在两份 `common-properties.md` 中 ⇒ 第二权威违规」。两处「`ExchangeSell` 是**唯一一个**只出现在 `Op == Remove` 上的成员」改为两个成员

**`Source.PackSell` 是不是随售的痕迹载体** → 不是。`Remove` 侧 `Source` 的唯一存档落点是 `PastEventEntry.AppliedChange`，随售无 `PastEventEntry`、又不落 `SourceCode` ⇒ 它一个字节也不进存档，只进 `TryApply` 可追溯性日志与客服溯源，**事后不可重建**（代价正面写下，对齐「购买次数事后不可重建」的同款取舍）

**商店档「30–50%」建议区间留还是撤** → 保留（限定为商店档），随售档写「显著低于该区间，取值同归统计校准」；删掉「摩擦保住储物袋 9 格的取舍感」半句

**随售的折算基准** → 「族 × 稀有度」定价表基准价，不含 `PriceOffset` / `DiscountPercent` / `ListPrice`（随售无 stock rule、无 offer）；现查表不违反「offer 恢复即读、绝不回查」——那条约束的是已物化的 offer。代价：overlay 轮回中途改表则随售价随之变化，落在同一 `contentVersion` 的确定性边界内

**储物袋详情卡片的售出交互形态** → 「售出」键仅对法宝出现（古宝不出现、不留灰占位——恒真的不可用项不出现）；回收价预览在第一段前可见且同币；就地二段确认，不新开屏不新增弹层；售出后条目移出列表不留占位；文案走 `PROFILE_` 分区普通键

**`PROFILE_MAGICPACK_FULL` 失效后「本地业务拒绝」规范的举例** → 换为「这件道具须在战斗中使用」（`PROFILE_ITEM_COMBAT_ONLY`），论点本身不变

**储物袋在 UX 侧是否需要形态变更** → 不需要。它早已是「全屏面板 · 纵向滚动网格 · 不分页」；真实改动只是删 9 格论证与「99 项 = 约 25 行」这个作废数字，并明写容量不设硬上限。筛选 chip 与排序原样保留，不新增 Scope chip

**`magicPack` 是否随储物袋重定义而改名** → 不改。定名理由改为「命名储物袋的**轮回级那一半**」
