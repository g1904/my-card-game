# Answer log 0823g

- 日期：2026-08-26
- 来源：`inbox/draft-0823g.md` → `handoffs/2026-08-23g-hidden-stat-combat-boundary-event-backdrop-and-itemized-rewards.md`
- 从 `open-questions/` **移出 1 条**；另有 1 条被**收窄**（形态部分仍待定，留在清单）

## 移出（整条答结）

- **隐藏属性与战斗资源的共存面** → **战斗层不读写隐藏属性**：隐藏属性不作为战斗内的资源或结算输入，与战斗的全部交互发生在事件层（生成期调制 · 结算期 outcome 求值 · `eventEnd` 的 `HiddenStatGrade` 推拉）。两处相邻情形不构成反例——`lifeSpanCost` 在择一进入时施加、属事件成本；失败时按道念差扣的是战斗外耐久 `lifeTotal`、不是隐藏属性。这是原始表述「无直接耦合」的收窄版：原措辞按字面会连三条既有通道一并否掉。
  （权威归 `systems/services/plot-manager.md`；`systems/adventure-event/combat/_index.md` 呼应并删同题待决项；`systems/scoring.md`「归属与协作」表加一行回链，不写权威句）

## 收窄（未整条答结，仍在清单）

- **战后奖励面板的形态** → **交互层答定**：候选固定 3 项、预先算定、**逐项列出并逐项领取 / 跳过**（推翻择一语义）、已处置不可反悔、没有「放弃全部」按钮（一项都不领是逐项跳过的自然结果）。**呈现层仍未定**：强制项与可选项如何同屏区分、逐项列表的竖屏排布、已领取 / 已跳过两态的视觉处置、与事件收口的视觉衔接 —— 留在 `open-questions/01-combat.md`，待战斗 UX 专场。
  （`ux/combat-ux.md`、`systems/services/combat-service.md`）

## 本次一并定下、但此前未登记在待答清单里的三条

- **事件背景板按地域区分** → 前期同一地域全部事件共用一张；后期再考虑按事件类型细化。`art/visuals/` 资产类目表新增独立一行，既有「事件插图」行保留并注明前期不产出（两者资产量级差一个数量级，合行会让排期失真）。
- **逐项领取后的存档结构** → `activeCombat` 新增 `reward` 块，`picks[]` 每项带 `RewardPickState { Pending, Claimed, Skipped }`；`picks` 一经写入不可增删重排，恢复时已处置项不允许反悔。
- **战后奖励领取是决策点** → 战斗内决策点清单由 D0–D6 扩为 **D0–D7**：新增 `D6 = 一次领取 / 跳过`，原「战斗收口」顺延为 `D7`。**「是决策点」与「reroll 已封死」并存**——封死 reroll 的是候选预先算定，新增落点的是领取进度这段重算不出来的中途状态。

## 新增待答

- **逐项领取后的奖励厚度重估** → 三项各自可领使单场可选奖励期望价值上移，`Tier` 三档质量落差与 `BaseReward` 相对分量需重估。留待内容扩充后的统计校准，与「三档奖励厚薄的具体取值」同批。→ `open-questions/01-combat.md`、`systems/balance.md`。
