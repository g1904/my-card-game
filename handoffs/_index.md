# Handoffs — Index

设计 handoff 的时间线日志。最新的置顶。每个 handoff 一个文件；可自由编辑 / 修正（非仅追加，历史归 git）。

| id | date | topic | status | distilled-to |
|----|------|-------|--------|--------------|
| [2026-08-16-design-audit-adjudication-and-hand-limit](2026-08-16-design-audit-adjudication-and-hand-limit.md) | 2026-08-16 | **体检 12 项逐条裁决 · 手牌上限 9 → 7**（输入 = `inbox/draft-0815c.md`，五项经 interview 裁定）。08-15d 全库过度设计体检遗留的 12 条待答项由用户逐条裁决，**答结 8 条、部分答结 1 条、确认排期 1 条、新增待答 0 条**。**① 六条判「不是过度设计」，原样保留并补写此前从未落纸的设计初衷**——本次最有价值的产出：这些结构之所以显得可疑，是因为**它们的意图从未被写下来**，而不是因为它们真的过度。**道统残卷 = 分发账户级加强的核心算法**，「不给玩家任何提示」是设计初衷而非疏漏 ⇒ 体检那条质疑的前提不适用（**它本就不打算被玩家学到**），三张分档表 / 五阈值 / 万分比精度全保留，**不减档、不做任何形式的进度可感化**（可感化会把它变成可优化对象，直接抵消设计目的）· **隐藏属性 12 档有真实消费者**——**所有事件都有可能推拉这三个属性，不限事件类型**，且 interview 裁定硬度 = **允许携带而非强制**（不填 = 不推）；**「文案只挂四档」同为正面意图**，它**依赖玩家对修仙题材的基础设定认知**（道心低了会入魔、煞气高了会反噬），**中间档的沉默不是信息缺口，是常识已经填上的部分** · **D2 决策点保留**，预写的「若超预算第一刀砍 D2 的 push」**作废**——D2 兑现的是「退出重进得到同一局面」，在强制在线 · 云端权威下这是玩家对存档的基本信任，不是可牺牲的性能余量 · **挂起态 / 选目标态 UX 整套保留**（暗幕 + 呼吸描边 + 固定指令条 + 连锁进度 + `pending` 存档形态），判据是「≤10% 触发式异能、一场 1~2 次」这个低频**恰恰是它有分量的原因，不是削减理由**——它服务的是本作少数几个真正的战斗内交互时刻 · **`itemPowerRatio` 四档折价**与战斗内法则的强度上沿同属**暂定平衡机制的软标准**，本就不声称是精算结果，作用是在量纲出现之前给内容侧一个相对刻度 · **meta 层**（190 余份文档 · 0 行代码 · 0 份 FR）**是已知且接受的状态，无需处理**——**不中断设计去先落一个最小可跑的战斗回路**，「设计先行」照常执行。**② 两条判「确有问题」并当场改掉**：**战斗内法则 ≤10% / 合计 ≤25% 闸门降格**——两个数保留，但由「难度曲线按老账号全开校准的**可执行形态**」改写为**内容评审参考上沿**，并明写**不得被引用为任何设计的承重依据**（它按纪律阶梯落在第 4 级零保证，却被挂了承重职责，这是本条要修的全部）；承重的只剩定性定位那一条，**不补代理指标**——代理量与真实量之间的偏差同样不可验证，等于用一个新的不可验证换掉旧的 · **`IgnoresProtection` 两侧都动**——编排规则 **R1–R5 五条砍到 2 条**（只留两条硬准入：仅挂 boss 档载体 · 绝不挂玩家可主动获取的内容；篇章配额 / 轮回内至多一次 / 加载期核对表全删，它们是为 1% 那个极小值服务的精算装置）+ 目标频次 **1% → ≈5%**，**承重连带：随 R3 一并删掉 `CycleState` 上那个轮回级 bool ⇒ 该机制在代码侧再无落点，完全退回内容编排层**。**③ 顺带简化**：带内分布权重表**三段合并为一张 5 档表**——意图移除后早 / 中 / 末段的分辨率没有消费面，且分段在 ch2 · ch3 本就天然粗、承诺的「分布右移」从未兑现；**删掉分段同时删掉一处状态读取**（赋级不再需要知道角色在本境界内的等级位置）。**`±2` 带 / 批内去重 / 截断重分配 / PlotManager 调制修正全部保留**，「篇章尾部变险」改由规则单独承载。**④ ⚠ 非体检项的连带定案：手牌上限 9 → 7**——它**推翻了 `balance.md` 原写下的否决论据**「取 7 会让上限成为常态惩罚」，而推翻的方式不是那个论据错了，而是**那个后果本身被接受为新的设计取向**；08-03 推论 ③ 的承重语义随之由「逼玩家出牌腾位的**节奏约束，不是惩罚**」改写为「**会真实咬合的紧约束**」（起手 4 + 每回合抽 2 ⇒ 第 2 回合即 8 > 7，只有稳定出满 2 张才不损失牌流）。**满手抽不进 / 纯上界 / 不产生弃牌堆流量三条规则形态完全不变**，变的只是咬合频率；连带复检 `itemPowerRatio` 中「不占手牌位 ×1.10」那项溢价（占位在紧约束下是更真实的成本）。**否决**「同时把每回合抽牌 2 → 1 以保住原语义」（会推翻「抽 1 则 mana 纯浪费」这条既定论据，为保住一句措辞去动另一条数值线）与「7 只是初值、语义不动」（文档会同时留着否决论据和取值 7，自相矛盾）。**⑤ 两处死结构确认已删**（`DrawCountsAsLoss` / `CombatOutcome.Fled`，08-15d 已执行），本次只清掉三处残留的「已于 08-15d 删除」考古注释——删除这件事本身归 git，活文档不为不存在的字段留讣告。**⑥ 竖屏分区**确认**单开一个专门 session 答疑**，条目留在清单并标注排期。**⑦ 顺带修正两处台账漂移**：`inbox/_index.md` 仍把本草稿记为「空白」· 08-15d 声明法则闸门那条去向 `07-codex-monetization.md` 但**从未真正落进该分片**。**唯一的存档面改动是 `IgnoresProtection` 的轮回级 bool 删除（纯设计面收缩）⇒ 不 bump schema · 对后端库零影响** | distilled | `systems/balance.md`、`systems/adventure-event/combat/_index.md`、`systems/services/combat-service.md`、`systems/services/plot-manager.md`、`systems/player-profile/player-power/_index.md`、`systems/character-profile/deck/_index.md`、`ux/combat-ux.md`、`vision/scope.md`、`open-questions.md`、`open-questions/`（`01-combat.md`、`04-hidden-attributes-plot.md`、`05-service-contracts.md`、`06-meta-progression.md`、`update-log.md`）、`answer-logs/`（`log-0815c.md`、`_index.md`） |
| [2026-08-15d-intent-removal-lifespan-cost-visibility-and-design-audit](2026-08-15d-intent-removal-lifespan-cost-visibility-and-design-audit.md) | 2026-08-15 | **敌人意图整条移除 · 寿元成本按告警档展示 · 全库过度设计体检** —— **① 意图机制整条移除**（三档揭示 · `IntentRevealTier` / `IntentCategory` 两个枚举 · `AbilityData` 上的必填标注 · 20% 归桶 · 回合级综合描述 · 快照语义 · 全套「预估 vs 实际」呈现装置 · 意图区 ≈ 屏高 8% · **探查 probe 通道**，全部作废）。依据 = 库内自述已把它的覆盖面削到接近于无（ch2·ch3 约五分之二同阶遭遇完全无信息、`diff = 0` 众数恒为最低档、「意图揭示不再承担教学职能」、完整档玩家读不出因果、`Special` 是有膨胀风险的兜底桶），而成本需完整支付。可读性改由**六条通道**承担（逐步执行呈现 / 敌人图鉴 / 战场 / 埋伏计数 / 等级→起跑线 / 道念对比），其中**逐步执行呈现由辅助手段升为硬要求**（原理由「偏差必须可解释」换成「敌人回合是唯一的动态情报时刻」）、**敌人图鉴升为事前知识主通道**。**⚠ 已知代价明写接受**：本作已移除交互与优先权，故敌人回合是**信息与交互双零**的一段观看——与 MTG 的差别正在此处。**三处承重论据改挂、定案不变**（精确标注敌人等级 → 「看到等级即看到起跑线」· 境界名展示 → 「让 `baseMomentum` 跨度断层可读」· `EncounterScopes` 共享池 → 论据反而加强）。**连带**：EnemyManager 的「回合级一次性规划」约束解除（可逐张决策，不重新引入交互）· Finale 压迫由四重降三重 ⇒ `WinMargin` 获得上调余量 · 竖屏释放约 8% 且少一处切换动画 · **`MomentumDelta` 四字段不动**（`Declared` = 效果标称量，与已作废的「意图声明值」同名不同物，且随逐步呈现升格而更重要）。**② `selectCost` 只在寿元 Band 2（< 10%）如实展示精确扣减量**，Band 0 / Band 1 完全不显示 —— 即给 08-12d 的隐藏属性档位表**加第五个消费方**，与红字数值倒数同一开关、同时开启，**不新增字段 / 流程 / schema**。**推翻 08-06c 推论 ③ 的「必须如实展示」**，但收窄保留其论据（真正的死路判断只发生在寿元濒尽时，而那正是 Band 2）。**已知代价**：「省着花有跨篇章回报」在常态档不可精细执行 —— 与「eventOption 不标经验数字、不可电子表格化优化」同一条纪律。**③ 全库过度设计体检 14 项**，其中两处可确证的死结构**本次直接删除**（`DrawCountsAsLoss` 三档恒 `false` ⇒ `VictoryRule` 收为单字段；`CombatOutcome.Fled` 无任何逃跑机制 ⇒ 收为三值，**行为完全等价**），其余 12 项作为待答项拆进四个分片，**不裁决其设计取向**。**⚠ interview 四项**（意图去留 / `selectCost` 分档 / 体检产出落点 / 探查处置），全部由用户裁决。**不 bump schema · 对后端库零影响** | distilled | `systems/adventure-event/combat/_index.md`, `systems/services/combat-service.md`, `ux/combat-ux.md`, `systems/balance.md`, `systems/adventure-event/common-properties.md`, `ux/screen-flow.md`, `ux/error-and-blocking-ux.md`, `program-overview.md`, `terminology.md`, `systems/enemies/`, `systems/player-profile/codex/enemy-codex.md`, `systems/character-profile/deck/_index.md`, `systems/player-profile/player-power/_index.md`, `systems/architecture.md`, `systems/game-progression.md`, `open-questions/*` |
| [2026-08-15c-event-type-collapse-and-batch-shape](2026-08-15c-event-type-collapse-and-batch-shape.md) | 2026-08-15 | **事件类型收为五类 · 批次形状 · Travel 80/20 · 寿元定价归属**（输入 = `inbox/draft-0815a.md`，六项经 interview 裁定）。**① 分类法九类 → 五类**（Combat / Exchange / Research / Explore / Travel），判据 =「**是否共有同一套特征**」——共有一套结算形状与呈现形状的归为一类，**类内差异用参数 / 档位表达，而非新增枚举值**。**② Practice / Finale 降格为 `combatTier { Practice, Standard, Finale }`**（对位 Balatro small / big / boss blind），三者共用回合循环、参战方结构与结算代码，差异只在 `EncounterSpec` 的遭遇参数；**既有定案全部原样保留**（8 / 10 / 12 回合、`WinMargin` 0 / 1 / 3-5-8、天劫 Enemy、`±2` 赋级带、一篇章一个 Finale 败后不可重战、失败但存活仍完成篇章、残卷唯一累积源与兑现点），只是挂载点从 `eventType` 移到 `combatTier`。**`combatTier` 是必需的锚点、不是修饰**——Finale 同时是篇章边界闸门、ADR-0004 篇章重试模型的锚点、道统残卷的唯一兑现点，三处都需要一个**可机械判定**的判据，靠内容 `Id` 或 location 配置认出「这是渡劫」是反模式。`EnemyData.EncounterScopes` 改为按档位取值，「先在低风险处解锁图鉴再正式对上」的教学路径保住。**③ Explore 继承元类型语义**：遮罩一个**固定**事件（内容侧已指定，非点击时现掷）、进入即揭示，真身取值域 = **Combat / Travel / Exchange**——**不含 Research**（卡组编辑是玩家主动规划的动作，藏起来只制造挫败）、**不嵌套自身**；沿用既有 `IsRevealed` / `RevealedEventId` 与 `PastEventEntry.RevealedEventId`，**不新增机制**。**④ 社交语境归 Exchange**（「与 NPC 谈条件」与「在商店买东西」共有同一套事件式结算与呈现形状，风味不需要枚举值来承载）。**⑤ Research 收窄为「玩家调整 / 升阶卡组」**，并**答结一条标记为「阻断内容编排」的待答项**：开局强制构筑事件（选一门功法 + 一件法宝，各三选一）**归 Research，不需要第六类**，承载机制仍是既有的 `eventPriority = 1`。**⑥ 批次形状**：常态 **3** 项、区间 **1–5**（**1 项的批次合法**，与 `Priority = 1` 收窄到单项、Travel 20% 随机档天然同形，不需要额外规则允许它）；**整批重算依角色的整体历程、不承接上一批** ⇒ **承重连带：`pastEvent` 从「历史记录」升为「产出侧的一等输入」**（schema 不变——`Unchosen` 轻摘要正好让产出侧读得出「玩家回避了什么」——但「痕迹必须完整可靠」的重要性再抬一级），并坐实 future-event-service 是**无记忆的纯产出侧**（不持有跨批次状态，与「模板不可写回」「产出即定稿」同向）。**⑦ Travel 候选 = 80 / 20 掷定**（80% 列出全部邻接地域并列供选，20% 由 map 子流 seeded 随机取一个；**常规出场与配额闸门一律适用，规则只有一条**；**Explore 揭示出的 Travel 必为随机那一档**）——**⚠ interview 裁定：草稿的「randomly choose one option」与 08-05b 的「闸门给多个并列」两侧都不作废，而是合成一条掷定规则**，`LocationCodex` 的路线规划价值保住、只打了折（八成岔路口有得选，两成被命运推着走）。连带定案 **Travel 不是常驻可选项**（只在 `eventCountLimit` 达成时必定出现）。**⑧ `selectCost` 成本侧只剩 `lifeSpanCost` 一项**（其余资源不进成本侧）**，但复合形态 `ProfileChangeSpec` 保留、不塌缩为单一 `int`**——保留的成本是零，塌缩则要连改三处（`PastEventEntry.SelectCost` 快照形状 / 物化取负链路 / 物化组装段）；**推论：展示纪律简化为「如实展示这一步扣多少寿元」**，玩家不必做多资源心算。**⑨ 寿元定价改为「`eventType` × 篇章」统一定价表**（Combat 一行按 `combatTier` 三档细分，因 8 / 10 / 12 三个回合数意味着战斗时长本就有三档），内容条目**默认不填**、只在需要体现代价差异（含产出向的回寿事件）时标偏移 / 覆盖值——判据本就是全局的目标游玩时长，改一张表即可全局调时长而不必重扫数百个 `.tres`，**它同时缓解了 `balance.md` 既有那条「反推链是脆的」**。**⚠ 另需知悉：草稿「余额扣减与确保可选 is messy」的前提已在 08-06c 整体消解**（`selectCost` 早已改为无条件施加、支付后再判终态，`skipCost` / `ifMandatory` / 「付不起则拒绝」回路均已删除），故该 messiness 不存在，留下的只有「寿元定价怎么归属」那一半。**⚠ 顺带修正一处既有漂移**：`adventure-event/common-properties.md` 的目标时长仍写 15–30 / 15–30 / 20–40，与 `balance.md` 的 08-01b 值不一致，已对齐为 30–40 / 35–45 / 45–55。**不 bump 存档 schema**（`eventType` 枚举收值与 `combatTier` 落 `EncounterSpec` 都发生在无线上存档阶段）；**对后端库零影响**。答结 6 条、部分答结 / 收窄 5 条、因前提消失作废 3 条、新增待答 6 条 | distilled | `decisions/ADR-0002-adventure-event-taxonomy.md`（整份重写）、`systems/adventure-event/`（`_index.md`、`common-properties.md`、`combat/`、`exchange/`、`research/`、`explore/`、`travel/`；`practice/` `mystery/` `social/` `finale/` 四个文件夹合并后删除）、`terminology.md`、`systems/architecture.md`、`systems/_index.md`、`systems/common-properties.md`、`systems/balance.md`、`systems/scoring.md`、`systems/game-progression.md`、`systems/enemies/_index.md`、`systems/character-profile/`（`mana.md`、`deck/_index.md`、`life-total.md`）、`systems/player-profile/`（`player-power/`、`_index.md` 相关）、`systems/services/`（`_index.md`、`combat-service.md`、`future-event-service.md`、`life-cycle-service.md`、`profile-service.md`、`plot-manager.md`）、`program-overview.md`、`ux/combat-ux.md`、`vision/scope.md`、`art/`（两份 `_index.md`）、`decisions/_index.md` |
| [2026-08-15b-monetization-entitlement-purchase-shape-and-scope](2026-08-15b-monetization-entitlement-purchase-shape-and-scope.md) | 2026-08-15 | **商业化整体收口：付费凭证的存档表达 · 购买形态 · 付费面边界 · UX 观感**（`monetization.md` 待决五条里除合规外的四条一并答定；输入为已裁决的 solution-draft，**未触发 interview**）。**① 持有状态 = `PlayerProfile.entitlement: PlayerEntitlement`，类内只有 `BundleGrantOrdinal` 一个字段**（规则字段层 · 严格同步 · 后端可复算 · 客户端永不自行置位；`> 0 ⟺ 已购买`，可重复购买下它**就是**购买次数 ⇒ 不设第二个字段——让重复字段从一开始就不存在，同 `FinaleWinOrdinal` 先例）。**否决 `CapabilityFlag` / modifier pipeline，判据一句话：capability 与 modifier 都是由内容条目聚合出的派生态，付费凭证是账号上的原始事实，派生态不能承载原始事实**；且两条通道都受 08-10c 的轮回级禁用截断 ⇒ 放进去等于在结构上给「花钱买的东西被事件拿走」留后门。订单号 / SKU / 收据 / 金额**不落客户端**。**② 一次授予 = 一次 `TryApply`，序号先算后写**，随机在 spec 组装前掷完；**序号自增与是否抽中无关**（否则下一次购买复用同一 `ordinal` 掷出相同序列、幂等键当场失效）；该 element **显式豁免 modifier pipeline**（经 pipeline = 一条法则能改写付费凭证）。**③ 购买段后端权威 · 兑现段客户端演算**——**⚠ 引入同步模型此前没有的第四种情形「后端主动写入」**，它会让 `cloudRevision` 领先 `baseRevision` 从而踩中 CAS 的 `Conflict` 分支、丢掉玩家刚打完的战斗；**解法不需要任何新机制，只需一条时机纪律「购买只能在主菜单发起且待发队列为空」**（并入既有闸 ② 的前置条件表，不新增拦截点），冲突窗口就此在结构上关闭。**④ 可重复购买；① ② 每次都给、③ ④ 只在首次生效不叠加**（叠加 = 抹平 ADR-0004 唯一的失败压力线；且 08-12e 的闸 ② 只有在可重复形态下才有真实意义 ⇒ 既有设计本就在为重复购买铺路而不自知）；第二次起**必须在付款前**标注删减；起步单一 SKU、金额不落客户端；**连带闸 ① 口径改写为「支撑 K 次重复购买」**。**⑤ 付费面五项明确排除**（付费续命 / 抽卡 / 消耗型货币 / 体力加速 / 广告变现）· **唯一预留 = 纯外观** · **通行证与赛季当前不做**。**⑥ UX = 主菜单一等入口、排末位、安静**（藏进二级面板会因曝光不足诱导出弹窗红点限时角标，那才是「付费才玩得下去」观感的真正来源）+ 三条纪律；**重试耗尽时提示购买明确否决**（情绪最低点 + **结构上本就不可行**）；不可用**置灰不隐藏**并立灰态判据（与 08-06c 的「事件选项不设灰态」不冲突）；新增 `STORE_` 翻译键分区。**⑦ `IPurchaseBackend` 本次不新增**，只在总则 7 预先声明清单 5 → 6。**存档 schema bump 一次、空迁移**；**⚠ 后端侧需一份对应 handoff**（验票 / 订单幂等 / 后端主动 +1 的写入语义 / 复算白名单 / 跨设备重复到账 / 实名与退款）。答结 3 条、收窄 1 条、新增待答 2 条 | distilled | `systems/monetization.md`、`systems/player-profile/_index.md`、`systems/services/profile-service.md`、`systems/services/sync-service.md`、`systems/services/life-cycle-service.md`、`systems/architecture.md`、`system-overview.md`、`ux/screen-flow.md`、`ux/error-and-blocking-ux.md`、`systems/balance.md` |
| [2026-08-15-content-id-technique-shape-and-subtype-reset](2026-08-15-content-id-technique-shape-and-subtype-reset.md) | 2026-08-15 | **条目 id 定案 · 功法承载形态 · 次类型清单归零**（答结 `2026-08-14c` 的全部三项 Open question，两项经 interview 裁定）。**① 条目 `Id` = `<内容类型>.<snake_case_slug>`**，并立一条**前缀词表纪律（承重）**：次类型 id 用主类型前缀（`item.pill` 形态），内容条目 id 用**内容类型全名**前缀 ⇒ **条目 id 绝不用裸 `item.` / `power.`**，四类持有条目一律 `character_item.` / `player_item.` / `character_power.` / `player_power.`（与四词定名一一对应）。该纪律现在是空成本的，但次类型重建时必然重新引入 `item.*`，届时无此纪律即撞车。**② 功法 = 轻量 header 条目 + 每层的卡牌 `Id` 列表；每张卡各自建条目**（用户裁定「每卡单独建立」，成员关系写在**功法侧**）。**否决「功法无独立条目、每张卡带 `(TechniqueId, Tier)` 标记」的三条理由，逐条对上既有定案**：`Rarity` 无处可挂（两处三选一都要抽功法，而「凡会被抽取或置换的内容定义都带 `Rarity`」已定案，纯标记方案里功法只是 DISTINCT 结果、没有实体能进抽取池）· `ContentEnabled` 失去原子性（关一门功法要改 N 张卡，**漏一张即得到半组功法**，直接违反「必须整组入组」）· **存档的功法 `Id` 无从校验**（存档存「功法 Id + 层数」，无注册表条目则悬空校验无对象可查）。附带收益：「这一层是哪几张」一处可读 + overlay 可热更该列表（改既有字段，落在「只改不增」内）。**否决「两侧都写」**（需双向一致性校验且制造第二权威）。**推论：写作顺序先卡牌后功法**（反之中间态必然悬空）；一张卡可被多门功法引用。**不推翻 08-12f 任何一条**——整组入组 / 进化 = 整组替换 / 卡组规模不随层数增长 / 存档存 build 层 / 战斗内不感知功法 / 弃置不设限全部原样成立，本次只把「逐层的卡牌定义挂在它上面」明确为**引用 `Id` 列表**而非内联定义。**③ 次类型：清单归零、机制保留**（**修正**用户输入的「次类型皆为 outdated fillers」——经核对该判断只对**清单**成立、对**机制**不成立）：`CardSubtypeData` / `CardData.Subtypes` / 加载校验 / 准入判据**零改动**，因为**埋伏的整套规则直接以次类型为载体**且**效果的目标筛选按次类型走**（「所有『灵兽』+1」是次类型存在的主要理由）——删机制 = 同时删掉埋伏牌与一整类 payoff 设计。作废的是那张 18 行清单初值（文档本就写明「可整体重排」，本次重排为**空**），**唯一例外 `enchantment.ambush`**（它是埋伏机制的定名、已登记 `terminology.md`、天然满足准入判据）。重建判据不变（≥3 条目共享 + ≥1 处筛选引用；**没有 payoff 的次类型就是展示标签**）。**词表保留**：「功法」一词已被 `CultivationTechnique` 占用，重建 `Power` 次类型时不得再用它、`power.technique` 不得复用（曾用于让位的 `power.mystic_art`「秘术」随清单一并作废）。**④ 事件类与剧本本阶段不开展**（八个子类文档仍是空占位、等各自专场；剧本无可调制对象，顺延）。 | distilled | `content/_index.md`、`systems/character-profile/deck/_index.md` |
| [2026-08-14c-content-authoring-layer](2026-08-14c-content-authoring-layer.md) | 2026-08-14 | **内容创作层开张：新建一级分区 `content/` + 三个内容技能**（输入 = 用户口述工作流，四项裁决经 interview 定下）。此前设计库只承载**类定义**（「一张卡牌有哪些字段」），没有承载**条目实例**（「云梦泽这个地域具体是什么」）的地方——README 明写「没有独立的内容层」。**① 技能粒度 = 单一通用 `/author-content` + 类型档案，不是每类一个技能**：字段清单是设计性内容、权威必须在设计库（ADR-0005），每类一个技能会把十几份清单复制进 `.claude/skills/`，直接制造第二权威（与 08-14 判据卡的硬边界逐字同构）。**推论：新增内容类型 = 写一份类型档案，不新建技能**，技能数不随类型数增长。**② 落点 = `content/<类型>/<id>.md`，与 `systems/` 平级**——`systems/` 持有「这类内容怎么运作」（类），`content/` 持有「有哪些条目」（实例），二者是类 ↔ 实例而非父子，故平级；否决「写进 `systems/` 子树」（条目会增长到成百上千，把实例塞进按概念结构组织的树会让 `_index.md` 的索引职责失效）与「每类一个大文件」（必然膨胀 + 并行编辑冲突）。**③ 交付链路 = 内容条目直接喂 `/blueprint`，不经 `requirements/FR-*`**——FR 那层的价值是把**系统行为**切成可独立构建的增量，而内容最终落地就是一批 `.tres`，其增量边界天然就是条目本身；**如实记下的代价：内容不进 FR 台账 ⇒ 完成度追踪落在各类型档案的条目台账上**。**④ 配套两个技能**：`/audit-content`（Id 唯一性 · 交叉引用不悬空 · 必填缺失 · 稀有度与池分布 · `ExclusiveSource`/`SourceCode` 准入与记账 · 文案覆盖率 · 台账一致性）与 `/scaffold-content-type`（新类型开张 + **就绪度闸门**）；`/plan-content-batch` 本次不做。**⑤ 全量盘点 15 个内容类型**并写入登记表（含就绪度与依赖链）；**两条边界裁定**：**图鉴 Codex 不单开类型**（词条是挂在宿主 `Resource` 上的静态文案，已定案 ⇒ 它是六个宿主类型条目文档里的一个字段块，单开等于给同一份文案造两个落点）· **`ability/`（效果原语）是卡牌/神通/法宝/法则/古宝五类的共同底座**，其语法未定案前那五类只能写风味文案、**写不出可 blueprint 的效果定义**，这条依赖由 `/scaffold-content-type` 的闸门执行。**⑥ 硬边界（承重）：`content/` 不定义字段**——条目与类型档案对每个字段只写「填了什么值 + 权威回链」，出现枚举成员表 / 取值域穷举 / 完整校验语义 / C# 类型定义即违规（同一条「两份表会各自漂移而本库无机制发现不一致」）。**不推翻任何既有 ADR**；改写 README 的「没有独立的内容层」一句。**三项 Open question**：条目 `Id` 形态未定（既有 `power.dao_law` 等是**次类型** id 而非条目 id，默认约定 `<类型>.<snake_case>` 标为待确认）· 九类 AdventureEvent 的条目形状（八个子类文档仍是空占位、等各自专场，故事件类暂不开张）· 一份条目文档 ↔ 一个还是一组 `.tres`（功法「整组卡牌」使后者有真实用例）。 | distilled | `content/_index.md`、`content/_TEMPLATE-type.md`、`content/_TEMPLATE-entry.md`、`README.md`、`.claude/skills/{author-content,audit-content,scaffold-content-type}/SKILL.md` |
| [2026-08-14b-claude-rules-design-content-thinning](2026-08-14b-claude-rules-design-content-thinning.md) | 2026-08-14 | **`.claude/rules/*` 中设计性表述的边界判据**（输入 = `inbox/solution-draft-claude-rules-design-content-thinning.md`，四项取向已裁决 + interview 一项落点裁定）。ADR-0005 已定「rules 里的设计结论只能是**一句话承重纪律 + 回链**」，但那句话**不可判定**（一句话多长？举一个例子算不算展开？）**也无人执行**（`/sync-knowledge` 的对账范围不含 `rules/`）——本次补上它缺的第三样东西：**阈值**与**执行者**。**① 形态合法，不瘦身到纯链接。** 依据不是折中，而是**加载模型差异**：`knowledge/*` 是 Claude **主动去读**的（读它的那一刻本就在找东西，多跳一次即可），`rules/*` 是**前置注入**的（起手 / 触及领域即在上下文里），它要在 Claude **还没有理由去读设计库时**就改变写法——纯链接在注入层失效，因为**写错代码的那一刻，往往正是「没觉得需要查」的那一刻**。ADR-0005 对两者措辞不同（「只留链接」vs「一句话 + 回链」）**不是笔误，是同一条判据在两种加载模型下的两个刻度**；本次是把后者**量化**而非放宽，该对照明写入 ADR 新节节首以免日后被读成「rules 被网开一面」。**② 两步判据。** 第一步归属分类（沿用 ADR-0005，不新增）：讲「游戏是什么」→ 受约束；讲「代码怎么写」→ **rules 自身即权威，写多长都不算副本**；讲引擎 / 语言实践 → 设计库无权威（同 ADR-0005 对 `standards/` 三份的处理）。第二步**投影四件套**：一句祈使 + 该纪律涉及的**标识符名**（名字正是「改变写法」的载体）+ **一句代价说明**（这是让 Claude 不去绕过它的唯一理由，删掉它这条就退化成可商量的口味）+ 一条回链，**缺一不可、多一即越界**。**③ 硬边界七条（可机械检查）：** 代码块 · 表格 · 枚举成员 / 字段清单 · 具体数值 / 阈值 / 默认值 · 完整取值域或分支穷举 · ≥3 步编号流程 · 超过 3 行；理由与 08-14 判据卡逐字同构（两份表会各自漂移而本库无任何机制能发现不一致）。另三条机械规则：**回链一律直指设计库、不经 `knowledge/` 中转**（该层自己也只是薄引用 ⇒ 二跳且中间那跳是额外失效点）· **已被阶梯第 1 / 2 级手段强制的纪律只留一句「由 X 强制」+ 回链**（编译器已在强制时，条款属第 4 级零保证投入且是漂移源）· **没有回链的设计性表述一律违规**（缺回链与「还没想」不可区分）。自检问句：**「把这句话删掉，Claude 会不会写错代码？」**——投影只留**行为后果**，不留**定义**。**④ 执行者 = 扩 `/sync-knowledge` 的对账范围到 `.claude/rules/*`**（技能**不更名**——三处引用它，更名的连带修改多于收益；改为在描述与正文首段写明对账对象 = **整个 `.claude` 的设计投影面**）。判据来自阶梯本身：只在文档层显形 + 违反后会累积 ⇒ 第 3 级，而文档没有编译期，其等价物就是**一个会定期跑的对账技能**。**留在第 4 级（人工评审）已被实证证伪**：`state-save-rules.md` 至今写着「CycleState 拥有 deck、**relic**、灵玉、地图位置、**ante/楼层**……」——三个本作**不使用**的泛体裁词，且类型名与既定的 `CycleStateManager` 不符，正是 ADR-0005 预言的「副本 = 下一次漂移的唯一来源」的现场。**否决新开 `/audit-rules`**（两份高度重复的技能文本各自演化，正是本判据在反对的第二权威）与**否决不设执行者**。**⑤ 唯一显式例外：`design-library-routing.md` 的两库结构表**保留并标注为「**路由用副本**」——它复述的是目录结构（**工程层路由，不是「游戏是什么」**），且是技能执行的前置信息；换成回链等于每次跑设计流水线技能都要先读两份 README，而路由判错的代价是把一侧的意图写进另一侧的库。标注使它成为**已知的、有理由的、被审计覆盖的**副本，与「未标注即违规」不冲突。**⚠ interview 一项：判据定义的落点。** 草稿步骤 1（`common-properties.md` 追加 ~10 行判据卡）与步骤 2（定义与投影**二选一**、两处不得各写一遍硬边界清单）自相矛盾，裁定**定义写 ADR-0005 新增的「### 2. 规则层的具体形态」、`common-properties.md` 只写 ≤5 行投影 + 回链**——ADR 是这条判据的**最小公共祖先**（它已持有总纲那句待量化的话），且该小节现状本就是「摘要 + 完整论证见 ADR-0005」的投影形态，追加判据卡会把它变成第二权威。**落地面（工程层，本 handoff 不执行、用户自行排期）：** 九份规则文件逐份核对 ⇒ 需改 3 份 · 轻改 2 份 · 1 份标注例外 · 3 份不动，**净瘦身 15~20 行且无任何工程约束条目被删**；设计库中 5 处 `Source: .claude/rules/*` 的指认（稳定 `Id` 键 / 字段命名与类型一致性 / Null 校验 / 日志约定 / 原子写入）**全部属工程约束、全部有效，不反转任何一处方向**。**不改机制 / 字段语义 / 存档 schema / API 签名，不 bump schema，无迁移，对 `backend-design-documents/` 零影响**（后端库无 knowledge 引用层、`.claude` 侧无对应物） | distilled | `decisions/ADR-0005-knowledge-thin-reference-layer.md`、`systems/common-properties.md`、`open-questions/05-service-contracts.md`、`answer-logs/log-claude-rules-design-content-thinning.md` |
| [2026-08-14-common-properties-layering](2026-08-14-common-properties-layering.md) | 2026-08-14 | **共有属性的分层判据：定义在最小公共祖先，投影在各落点**（输入 = `inbox/solution-draft-common-properties-layering.md`，两项取向已裁决 + interview 一项范围裁定）。**① 要定的不是一张字段归属表，而是一条判据**——字段清单会一直增长（多份子树的字段清单仍是 `⟨待定⟩`），表会在下一次 handoff 后过时而问题原样复发，判据不会。**② 两种形态、权限不同：定义**（类型 / 枚举清单 / 取值域 / 不变式 / 校验语义 / 与其他字段的关系）**恰好写一层** = 该字段全部挂载面的**最小公共祖先**；**投影**（本层落点 · 本层合法子集 / 默认值 · 本层消费点 · 回链）每个实际落点各一份、≤5 行。**「最小公共祖先」按挂载面算，不按「感觉有多通用」算**（`ExclusiveSource` 只覆盖 `PowerData` / `ItemData` 两个类，但两类落点横跨 `character-profile/` 与 `player-profile/` ⇒ 顶层）。**它是既有实践的归纳而非新规**——`SourceCode`（08-12b）已完全按这个形状写成，本次只是把它从「一次巧合的写法」升为「下一个共有字段照抄的模板」。**③ 承重推论（判据唯一的硬边界）：同一字段的定义级内容出现在两份 `common-properties.md` 中即违规**——投影段里出现枚举成员表 / code 数值 / 完整校验语义都算复制了权威，**两份表会各自漂移而本库没有任何机制能发现它们不一致**；由此得一条可机械检查的审计项。**④ 必须明写「本层无规则消费点」而不是省略**——省略与「还没想」不可区分，写下这句的文档因此是可信的。**⑤ 上移 / 下沉触发器**（pull-up / push-down field 在文档树上同样成立）：上移以**「语义同一」为硬前置**（`RarityTier` vs `Tier` 这类同名不同义不上移，**且不得为了上移而把两个不同概念改成同一个名字**）· 下沉后**顶层不留摘要**（活文档不留考古）· **单一落点的字段不进任何 `common-properties.md`**（写进去会让读者误以为还有别的落点）。**⑥ 某层是否建 `common-properties.md`：按内容建，不按对称建**——两条同时成立才建（该层有子节点共有且全库不适用的内容 · 篇幅已压过 `_index.md` 的索引职责，经验界 ≈40 行）；`adventure-event/`（213 行）与 `enemies/`（45 行）该建已建，`character-profile/`（横切共性只是「随 `defeated` / `completed` 清理」）与 `player-profile/`（横切是两层同步口径，已在 `_index.md` 内成节）**有意不建**。**推论：结构不对称不是缺陷，是判据的正确产物**——不写下这句，日后必有人（或一次 `/update-readme`）以「补齐结构」为由建出空壳，而空壳 `common-properties.md` 是个「看起来该写点什么」的坑，会把本属顶层的字段吸下来复述成第二权威。**⑦ 顶层 `systems/common-properties.md` 重排为 `## 通用约定` / `## 内容共有字段` 两大节**（取向裁定 · **纯重排，不改一个字的语义**）：判据卡落**后者节首而非文件顶部**——判据只对内容字段成立，`## 通用约定` 那批条目不谈「挂载面」也不产生投影段；分节让判据的适用范围一眼可见、新字段有明确的落笔位置。`## 决策` / `## 待决问题` / `## 对应` 三节不变。**⑧ 全量自检：五个顶层共有字段无一需要迁移**（`ContentEnabled` · `LocalizedText` · `Rarity` · `SourceCode`+`Source` · `ExclusiveSource` 的最小公共祖先全是顶层）——**判据是对既成事实的追认，采纳它不产生一次文档搬迁**；这不是巧合，它们能被写进顶层正是因为每一条都是在第二个落点出现时才被抽上去的。`adventure-event/common-properties.md` 对物化模型的完整展开**超出投影格式但不违规**（它展开的是该层自有内容，不是复述）。**⑨ ⚠ interview 一项：草稿写「三份 `SourceCode` 投影段」实为四份**——`player-profile/player-item/`（古宝）也写了一段，草稿的核对表与执行清单都漏了它；裁定**四份一并压回模板**，使硬边界在全库无例外，也贯彻「趁存量小一次做完」的原始理由。压缩的两条硬边界：**「本层无规则消费点」那句必须保留**（本层独有信息，顶层写不出）· **删除的只能是顶层已有的复述**（逐条核对后再删；只存在于叶子的先补进顶层）。顺带清掉 `player-power/` 中 08-10b 遗留的过时表述「没有第二个消费点：不对玩家可见 / 不进图鉴」（08-12b 已改写为「规则消费点唯一 + 非规则用途两处」）。**⑩ 否决四个替代**：完整字段归属表（会过时）· 全部只写顶层不留投影（分域合法子集与「本层无消费点」**本质上是逐层的**，赶到顶层会变成一张四列大表且读者在子树里读不出本层能填什么）· 按「是否落存档 / 是否内容定义」分层（**那是字段的属性、不是位置的属性**，同一条判据要能同时安置 `ContentEnabled` 与 `SourceCode`，只有挂载面做得到）· 顶层拆成两个文件（破坏「一层一份」的结构对称，同一诉求由文件内分节满足）。**不推翻任何 ADR 或既有定案**（07-24「每一层的共有字段抽到 `common-properties.md`」经核为**措辞张力而非决策冲突**，读作「共有字段要显式化」而非「每层必须建档」）。**纯文档结构约定：不改任何机制 / 字段语义 / 存档 schema / API 签名，不 bump schema、无迁移、无跨库影响、对 `.claude/knowledge/*` 零影响。** 下游收益：`/derive-requirements` 与 `/breakdown-requirements` 读一个字段时不必判断「这几处哪一处是权威」；`/sync-knowledge` 的「把副本压回薄引用」有了本库内部的对应判据。答结 1 条、新增待答 0 条 | distilled | `systems/common-properties.md`, `systems/_index.md`, `systems/player-profile/player-power/common-properties.md`, `systems/player-profile/player-item/common-properties.md`, `systems/character-profile/power/common-properties.md`, `systems/character-profile/item/common-properties.md`, `open-questions.md`, `open-questions/`（`05-service-contracts.md`, `update-log.md`）, `answer-logs/`（`log-common-properties-layering.md`, `_index.md`） |
| [2026-08-13-translation-key-rollout-and-content-localization](2026-08-13-translation-key-rollout-and-content-localization.md) | 2026-08-13 | **翻译键的铺开纪律 与 内容条目的多语言形态 `LocalizedText`**（输入 = `inbox/solution-draft-translation-key-rollout-and-content-localization.md`，四项已裁决 + interview 两项落地裁定）。**① 「逐屏改造如何排期」这个提法被否掉**——它默认了一个**不存在的存量迁移场景**（客户端无任何 `.tscn` / `.cs` / `.tres`，UI 文案与内容资产的存量都是零）⇒ 定案改为一条**起手纪律**：每屏从写下第一行起就用翻译键、全库不新增任何 UI 文案字面量。**推论：「随各屏 FR 一并落地」与「集中做一次」不是二选一**（前者是纪律的作用方式，后者要处理的批量迁移根本不会产生）。唯一集中做的是横切基建 **`FR-ux-translation-foundation`**，**不挂任何单屏**——挂在某屏下会让第二个屏的 FR 依赖第一个屏的 FR，凭空造出一条与设计无关的构建顺序。**② 键命名规范只有三条**（`SCREAMING_SNAKE_CASE` · `<PARTITION>_<CONTEXT>_<NAME>` · 一个分区一个 CSV）+ **开放分区表十项**，边界写死为「**分区划的是界面，不是内容域**」（`EVENT_` 装选项框按钮与标题，事件正文一个字也不进）。**③ 新立禁令：`ERR_` 前缀保留给 `code` 的机械像，人不得手写**——手写 `ERR_LOGIN_FAILED` 会在后端某天新增 `code = "login.failed"` 时**撞进同一个键**，使一条后端错误静默显示成为别处写的文案；可执行形态 = **`AuditTranslations()` 改为双向**（反向扫 `errors.csv` 找无对应 `code` 的 `ERR_*` 行）。**④ 内容多语言 = 条目内嵌 `LocalizedText : Resource`**（同一 `Id` / 同一 `.tres`，`Entries: locale → 文本`）：加一门语言 = 在 `.tres` 里加一个键、**零代码改动**；它是「改既有条目的字段值」⇒ **overlay 可热更，线上补一段英文文案不必发版**（这是本形态相对全部替代方案的实质收益）；语言解析只有一个入口。**否决三个替代**——每语言一套 `Id`（撞「只改不增」· 抽取池权重被语言数稀释 · 切语言使存档引用悬空）· 内容文案塞进 `res://text/`（把可热更降级为要发版）· 每语言一个 `[Export]` 字段（把语言数焊进 C# 类）。**⑤ 失败语义必须分方向**：默认语言 `zh` 缺失 / 空串 → 合并后强校验 `PushError` + `throw`；**非默认语言缺失 → 读取侧静默回落 + 合并后一次性覆盖率审计**（英文列既定全占位符 ⇒ 每条内容在 `en` 下都命中回落，逐次警告会刷屏并淹掉真告警；审计报覆盖率使「英文版做到哪一步」成为一个能一眼读到的数）。**⑥ 语言范围封顶中英双语**（`zh` / `en`，无地区码，**不设 `zh_TW`**——简繁差异是机械的，走转换而非第二份手写真值）⇒ 三条推论：`Entries` 键域封闭故 **locale 拼写校验写得起**（`En` / `en_US` 是完全静默的失败面）· 体积 ×2 是**封闭上限**故**按语言分包不再作为候选保留** · 内容层的「占位符」= **缺键**，使「缺 `en` 键 = 未翻译」成为干净可判的条件。**⑦ 两层共用同一个语言开关**（不设第二个内容语言设置，否则玩家能把界面切成英文而卡面留在中文）；配套纪律：**ViewModel 订阅翻译变更通知即重新组装**（`LocalizedText` 不经 `TranslationServer`，不会自己重翻）。**⚠ interview 两项落地裁定**：**locale 在启动期单点归一到二值**（Godot 默认取系统 locale ⇒ 真机 `GetLocale()` 返回 `zh_CN` / `en_US`，与封闭键域对不上 ⇒ **每一条内容文案在所有真机上都命中静默回落，且没有任何症状**）· **CJK 字面量审计以 `OS.HasFeature("editor")` 守卫**（导出包里 `.tscn` 已编译为 `.scn`、`.cs` 不随包，源文件扫描无从执行；不用 `#if DEBUG` 是因其成立性本身仍是一条待实测项）。**排期：`LocalizedText` 与 `DrawPool<T>` 同批**，第二阶段开工前、第一份内容 FR 之前（纯加法窗口在写下第一批 `.tres` 时关闭）。**不推翻任何既有决策**（与 `vision/scope.md`「本地化打磨在 MVP 外」经核为**澄清而非冲突**）；**不 bump 存档 schema · 抽取池零影响 · 纯客户端无跨库影响**。答结 2 条、缩范围 1 条、解除牵连 1 条、新增待答 1 条 | distilled | `ux/error-and-blocking-ux.md`, `ux/_index.md`, `systems/common-properties.md`, `systems/services/content-service.md`, `system-overview.md` |
| [2026-08-12f-cultivation-technique-deck-building](2026-08-12f-cultivation-technique-deck-building.md) | 2026-08-12 | **功法（`CultivationTechnique`）= 卡组的构筑单位；角色升格为有身份的模板**（输入 = `inbox/draft-0812a.md`，两轮 interview 共 7 项裁决）。**① 功法 = 一组必须整组入组的卡牌**，卡组由若干功法构成、数量不限 ⇒ 玩家做构筑决策的颗粒度**不是一张牌而是一套路数**；带**层数 `TechniqueTier`**。**② 进化 = 整组替换**（每层一整套卡牌定义，**不是**数值变大、**也不是**追加新卡）——**推论：卡组规模不随层数增长**，故层数系统与「疲劳把卡组规模换算为后期失血速率」这条既定压力**互不干涉**，只有弃置与学新才动规模。**③ 卡组 = 功法展开的牌 ∪ 游离散牌**：功法是**构筑单位、不是卡组的完全划分**，业障（负向奖励塞入）与单卡奖励照常入组 ⇒ **既有推论④两条通道原样成立、零推翻**。**④ 角色升格为内容条目 `CharacterData`**（区别于 `CharacterProfile`）：开局随机分配，**自带一个神通 + 两门绑定功法、每局一致** ⇒ **神通有了第一条确定的获取渠道**（部分答结 `power/` 的「获取 / 失去触发」），且「这个角色打起来什么手感」成为**可跨轮回积累的知识**。**⑤ 开局构筑 = 一个强制 buff 事件**（选一门功法 + 一件法宝，各三选一，取 **StS 第一章**的味道）——**⚠ 推翻草稿第 1 行「炉石竞技场式多轮择一」**（附带查证：该说法虽写作「As said before」，但**全库检索不到任何既有记载**，从来不是设计库中的定案）；**构筑的多轮性改由 adventureEvent 承载**（升阶 / 弃置 / 学新）。**承载机制不新增**：既有 `eventPriority = 1` + `ifMandatory` 已能表达「本批必须进这个」。**⑥ 弃置不设限**（用户选非推荐项）——**角色绑定的两门功法同样可弃**，角色给的是**起手形状不是永久底盘**；**退化情形明写：卡组可被弃空**，且**不需要新规则**——既有疲劳规则（抽牌堆空后每抽 −1 道念）已完整表达后果，与「打不过也得打、输是正常出口」的既定取向一致。**⑦ 定名三裁**：新概念占「功法」· 既有次类型 `power.technique` 功法 → **`power.mystic_art` 秘术**（**中文名与 id 一并改** —— 只改中文名等于把撞名搬到标识符层，`CultivationTechniqueData` 与 `power.technique` 在代码侧同样混淆）· 功法的等级叫**层数 `TechniqueTier`** 而非 level（`level` = 境界内层级，是敌人意图揭示档的判据、承重字段，**不动**）。**⑧ 自行推演五条**：三选一候选走 `AllEnabled()` + seeded 子流 · 功法与角色都是新内容类型（`CultivationTechniqueData` / `CharacterData`）· **存档存 build 层（功法 `Id` + 层数）而非展开后的牌** · **战斗内完全不感知功法**（组装时已展开，`activeCombat` 与「卡牌是闭集」原样成立、`DeckModule` 不必认识功法）· 三种操作走既有 `ProfileChangeSpec` → `TryApply`，**不新增存档点**。**⑨ 敌人图鉴词条②「功法简介」澄清为纯风味文案名**，不指向系统概念（`EnemyData` 样本卡组仍是直接卡牌列表；敌人是否也以功法构筑另立为待答项）。**不推翻任何既有 ADR 或定案**；**纯本地 · 纯客户端 · 无跨库影响 · 不 bump schema**。整条答结 0 条、部分移出 1 条、新增待答 7 条 | distilled | `terminology.md`, `systems/character-profile/deck/_index.md`, `systems/character-profile/_index.md`, `systems/character-profile/power/_index.md`, `systems/character-profile/item/_index.md`, `systems/adventure-event/_index.md`, `systems/enemies/_index.md`, `systems/player-profile/codex/enemy-codex.md`, `open-questions.md`, `open-questions/`（`01-combat.md`, `02-event-options.md`, `06-meta-progression.md`, `07-codex-monetization.md`, `update-log.md`）, `answer-logs/`（`log-0812a.md`, `_index.md`） |
| [2026-08-12e-ability-grant-draw-pool](2026-08-12e-ability-grant-draw-pool.md) | 2026-08-12 | **账号级能力授予的候选池与排重规则；成就奖励改走「指定条目 + 成就限定」**（输入 = `inbox/solution-draft-ability-grant-draw-pool.md`，§A/§B/§C 三项已裁决 + interview 追加 3 项）。**① 一段抽取，三个调用方**：`AllEnabled()` → 同 `(Kind, Scope)` → 去成就限定（`ExclusiveSource == null`）→ 排除已持有 → [仅置换：锚定 `Rarity`] → 按 `RarityTier` **单张共用权重表**加权 seeded 抽；多条走**无放回**。残卷 · 礼包 · 置换是同一形状的授予，没有理由各写一份（散写必然在某处漏掉 `AllEnabled()` 或漏掉排重）。**② 本次最大的结构性收获：「抽到重复怎么办」不是被回答的，而是被消解的**——排重发生在**取池阶段而非掷骰之后**，池 = 未持有集合 ⇒ **抽不出重复**；而这不是本次的新选择，08-09b 的全局前置「**尚未拥有的法则数 > 0** 才掷骰」**只有在「池 = 未持有集合」时才自洽** ⇒ 既有设计早已隐含答案。由此 **`HasGrantable()` ⟺ 池非空**（与全局前置是同一个判断，不是两个）、`pickedPowerId` 有定义，**08-09b 残卷伪码就此完整可执行**。**③ 三条不过滤的维度各有理由**：不按 `UsableScene`（「战斗内法则 ≤ 1/5」是内容侧**条目比例**纪律，抽取侧再加一道 = 同一条闸门做成两处、且会让实际掉落比例偏离编排意图）· 不按 `status` / `disabledAbility`（生效维度与持有维度正交，被禁用的**照常算已持有**、照常排除出池）· `ContentEnabled` **语义天然吃进来**（关闭者退出池，已持有的照常 `Get(id)` 解析、照常计入 `x`）。**④ ⚠ interview 裁定：账号级 RNG 加具名域 `AccountStream`**——修订 `common-properties.md` / `player-power/_index.md` 既定的两参数 `Hash64(AccountSeed, ordinal)` 为三参数（礼包一旦也走账号级掷骰，两条渠道的序号必然撞出同一序列；**否决「给各渠道分配不相交的序号区间」**——效果相同但更脆、区间约定不可机械校验）；残卷改走 `AccountRng.For(PowerFragment, ordinal)`，**命中判定语义不变**、`(stream, ordinal)` 仍是幂等键、仍不进 `SeedManager` ⇒「增删子流不 bump schema」不受影响。**⑤ ⚠ interview 裁定：闸 ① 收窄为「礼包所需（1/2）+ 可调余量 `GrantPoolMargin`」**——草稿原写「**残卷分档上限** + 礼包之和」，但 `balance.md` 的残卷三表在 `x ≥ 15` 档仍有 `Gain = +1%` / `Cap = 5%`，**残卷没有任何账号级上限**，且「池已取尽 → **静默停摆**」本就是它的**既定正常终局** ⇒ 「单账号可获取上限」不是有定义的量，闸 ① 按原写法永远无法成立；收窄后保住唯一真实目的（保护付费兑现）且可机械校验。**否决**「给残卷设账号级硬上限」（新机制 + 与静默停摆重复承担同一职责）与「删掉闸 ①」（失去启动期大声失败的能力）。**⑥ 礼包空池 = 三道闸 + 不补发**：加载期 `PushError` · **购买入口前置拦截**（把失败点挪到掏钱之前，从「退款争议」降级为「暂不可购买」——**这是「不收钱又不给货」的真正防线**）· 兑现处 `PushError` + 上报 + 计未兑现，**不补发 / 不折价 / 不降级替代**；既定的「付费内容不会被游戏销毁」讲的是**已授予**的不被拿走，本条补的是**未授予**的不被吞掉。否决「灵玉折价补偿」（本作无账号级可支配货币，兜底即新开一套经济）。**⑦ 成就奖励改为「指定条目 + 成就限定」，不进任何抽取池**（推翻草稿初版的「成就也走抽取」）：若成就指定的条目同时在通用池里，玩家可能在达成前就从别处拿到 ⇒ 发放时 `Grant` 指向已持有条目 ⇒ **这一发是空的**；成就是一次性确定回报、无补发机会 ⇒ 不能靠概率侥幸，须由**准入规则**结构性排除。由此得**不变式「成就限定条目在发放那一刻玩家必然尚未持有」**（可断言，违反 → `PushError`）+ 三条 `PushError` 校验，**「不落空」由口头保证变为机械保证**。推论：`AccountStream` 无需 `AchievementReward` 成员（无随机）；**置换两侧不对称**（换入侧永不出现、换出侧不禁止——不落空管的是发放那一刻，不是此后玩家自己的取舍）。**⑧ 新增内容共有字段 `ExclusiveSource: Source?`**（interview 确认保留此名，否决 `GrantChannelLock`）——与 `SourceCode` **名字相邻、方向相反**（准入 vs 记账 · 内容定义 vs 持有条目），`common-properties.md` 并排写出四行对照表；选 `Source?` 而非布尔，因活动限定 / 剧情限定必然重演同一诉求。**⑨ 连带**：`Rarity` 消费点 2 → **3**；`DrawPool<T>` 调用方 3 → **4** 且 **`PickMany` 无放回 + 加权重载**成为契约（加强「第二阶段开工前落地」的排期理由）；profile-service 新增 internal `GrantPoolPicker` 与四个形态 A 门面方法。**⑩ 不 bump 存档 schema**（`ExclusiveSource` 落内容定义；`BundleGrantOrdinal` 待落点确定后再 bump）。答结 2 条、部分答结 2 条、新增待答 1 条。**⚠ 后端侧需一份对应 handoff**（`AccountSeed` 复算多一个参数，可与既有那笔合并） | distilled | `systems/player-profile/player-power/_index.md`, `systems/monetization.md`, `systems/common-properties.md`, `systems/player-profile/achievement/_index.md`, `systems/services/profile-service.md`, `systems/services/content-service.md`, `systems/balance.md`, `open-questions.md`, `open-questions/`（`07-codex-monetization.md`, `update-log.md`）, `answer-logs/`（`log-ability-grant-draw-pool.md`, `_index.md`） |
| [2026-08-12d-hidden-stat-bands-and-crossing-narrative](2026-08-12d-hidden-stat-bands-and-crossing-narrative.md) | 2026-08-12 | **隐藏属性的档位模型与跨档叙事：一张档位表统一四个消费方，文案只挂极值档**（输入 = `inbox/solution-draft-hidden-stat-bands-and-crossing-narrative.md`，两轮共六项已裁决 + interview 追加两项）。**① 三属性共用一套档位表**：道心 `[0,100]` 起始 **50** 双向 **5 档**（阈值 20/40/60/80，**唯一的双臂属性**，带符号档号 `-2..+2` 落 `sbyte`）· 煞气 `[0,100]` 起始 **0** 上行为主 **4 档**（25/50/75）· 寿元既定 **3 档**（30% / 10%）。**有界值域是被「overlay 只改不增」逼出来的**——无界属性的档位只能靠不断加新档追赶，而加档必须发版 ⇒ 有界 + 顶档吸收溢出使档数在整条内容生命周期里是常量。**② 档号方向 = 离常态的距离（承重推演）**：裁决「下行不播叙事」若读成「数值下降不播」，**会把寿元既定的 30% / 10% 提示整条废掉**（它本就是数值下降触发的）；改用距离口径后三属性**共用一条触发规则 `\|newBand\| > \|oldBand\|`**，不需要方向字段、不需要为寿元开特例，也让「远离常态」直接就是绝对值变大。**③ 收敛两套并行说法**：「达**阈值**时触发剧情线」（07-23）与「跨过一个隐藏**档位**」（08-01）**是同一件事**，一张表同时服务**四个消费方且密度互不绑定**（**eventOptions 调制用全部档 —— 这是档位表存在的首要理由** · 剧情线触发 3 档 · 叙事文案 4 档 · 寿元红字 1 档）⇒ **档多 ≠ 文案多**，**为收窄文案而砍档 = 拿主业迁就点缀**。**④ 每档带回滞 δ**（道心 / 煞气 4、寿元 3 个百分点）—— 没有回滞，在阈值上 ±3 震荡的道心会把同一条文案连播五次；**回滞与「下行不播」各管一半**（方向规则挡「上去又下来又上去」里的下来那次，回滞挡同一条线上的反复上行）。**代价明写：档位不再是当前值的纯函数 ⇒ 必须持久化当前所处档。⑤ 文案挂档位不挂事件**（挂事件是 `事件 × 属性 × 档 × 方向` 的组合爆炸，一章即 180 条，且泄露事件↔属性精确映射）· **走内容层**（`content-service.md` 早已逐字点名「状态转换触发的定性文案」属被存档引用 / 只改不增类，本次只是取回 `plot-manager.md`）· **每档 2–3 条候选、等概率随机取一、随机源不带种子**（沿用 08-10b；只影响呈现不产生玩法结果 ⇒ 不占 `SeedManager` 子流）· **只有一组文案，无上行组 / 下行组**（`NarrativeDownIds` 不存在）· **只挂极值档**（全库 4 档：道心 `+2` / 煞气 3 / 寿元 1、2 ⇒ **≈ 6–10 条 / 轮回**、文案总量 8–12 条、约每 9–17 个事件一条），依据 = **本作是 deck building / turn-based card combat game，不是 visual novel** + **因果的主要载体是事件文案本身**（「屠戮山门」的选项不需要旁白告诉他煞气涨了，中间档旁白是重复信息、只会稀释极值那条的分量）。**静默是默认与常态（12 档里 8 档）⇒ 不设 `NarrativeSilent` 布尔**，改用可机械检查的「只有极值档允许配 `NarrativeIds`，其余配了 → 加载期 `PushWarning`」。**⑥ 呈现 = 事件结算面板内一档一行**（独立弹层 = 新结构 + 在最高频操作上加模态，**是 combat-ux 那条纪律的第四个实例**）· **多属性同跨全部逐条陈列、固定优先序 寿元 → 煞气 → 道心，不合并不截断**（合并需要两两组合文案，是组合爆炸的另一形态；规则虽几乎不会触发但**便宜且不写就是个空洞**）· 斜体 / 弱化色、**无图标无数字无属性名** · **寿元 10% 的红字倒数不是叙事而是常驻静态标注**，跨进 Band 2 时两者**叠加**（澄清了一处易被读成矛盾的地方）。**⑦ 寿元百分比的分母 = 新字段 `ChapterLifeSpanBudget`**（承重澄清）——「剩余寿元跨篇章结转」使寿元没有固定分母，拿本章增量作分母会让省着花的玩家在 ch2 开局就超过 100%、阈值含义漂移；**ChapterManager 在篇章边界冻结结转后预算**，是既有职责的一个赋值，**不新增存档点、不新增流程**。**⑧ 落地面**：新增内容类型 `HiddenStatBandData`（**12 个条目**，含七条加载期校验）+ `CharacterProfile.Status` **4 个新字段**（3 × `sbyte` band + `ChapterLifeSpanBudget`，**三个具名字段而非字典**，同 `chapterRetry` 判据）⇒ **bump schema · 空迁移**；`ResolveOutcome` 增 `BandNarrativeIds`（**resolver 侧恒填空**，保住「resolver 只描述结果、不自行写档」）；band 写入**并入 `eventEnd` 那一次 `TryApply`** ⇒「一个事件 = 一次事务 = 一个存档点」原样成立；**不进 `PastEventEntry`**（可由 `AppliedChange` 重放，08-09c 判据；`LifeSpanAfter` 那条例外不得据此扩大）。**⑨ 顺带答定** `TryApply` 钳制规则的隐藏属性那一半（截断到 `[0,100]`、**不构成终态**）。**⚠ interview 追加两项**（草稿未覆盖）：**档位条目恒启用**（`ContentEnabled == false` → `PushError`）——判据是 content-service 的既定不对称「**过滤只在产出侧**，而档位判定是**查表读取**」，关一档会造出档位表空洞 + 假跨档，**立为通则「能被抽取的才配有开关」**，文案条目则照常参与放量；**道心下臂 `-2` 挂 `PlotTriggerId`**，剧情线目录 2 条 → **3 条**（煞气反噬 / **心魔滋生** / 大限将至）——它是「下臂不播文案」那条取舍成立的前提，否则玩家对「掉道心」这条因果链完全无感。**⑩ 不推翻任何既有决策**：疑似冲突（08-12「全库 UI 文案走翻译键」vs 叙事走内容层 overlay）经核**作用于不同文本层**，**四问判据显式化并落 `ux/_index.md`**。答结 2 条（+2 条部分移出）、新增待答 2 条。**纯本地，无跨库影响** | distilled | `systems/services/plot-manager.md`, `systems/character-profile/_index.md`, `systems/services/life-cycle-service.md`, `systems/services/content-service.md`, `systems/balance.md`, `ux/screen-flow.md`, `ux/_index.md`, `open-questions.md`, `open-questions/`（`04-hidden-attributes-plot.md`, `05-service-contracts.md`, `update-log.md`）, `answer-logs/`（`log-hidden-stat-bands-and-crossing-narrative.md`, `_index.md`） |
| [2026-08-12c-identifier-singular-collapse](2026-08-12c-identifier-singular-collapse.md) | 2026-08-12 | **标识符单数收口：`CharacterItem` / `Achievement` / `magicPack`，与 `pastEvent` 类型漂移纠正**（输入 = `inbox/solution-draft-character-item-singular-naming.md`，5 项已裁决 + interview 追加 2 项范围扩展）。**① 立通则「类型名恒为单数，复数只属于集合字段名」，`CharacterItems` 整体作废**——三条依据各自独立成立：四格对称中 `CharacterItems` 是唯一离群项（`PlayerPower` / `PlayerItem` / `CharacterPower` 皆单数，08-03 定名表写的也是 `CharacterItem`）· `XxxData` 内容命名族全单数 · **泛型参数位的复数是双重复数的语义错误**（`List<CharacterItems>` 读作「一个『多件法宝』的列表」）。**② 法宝三层分工一次写死**（这是本次的承重条目——单复数之所以能并存两个月，根因是「`CharacterItem` 指哪一层」从未写清）：`ItemData`（内容定义，两层共用，`Scope == Character` 者即法宝，**不存在 `CharacterItemData` 类型**）↔ `CharacterItem`（持有条目，**一份实例 = 一个集合元素**）↔ **`CharacterProfile.magicPack`**（集合字段）。**③ 集合字段取 `magicPack` 而非 `characterItem` / `characterItems`**：字段直接命名它承载的**已定名概念「储物袋」** ⇒ 单复数之争在此形态下**直接消失**，且 9 格上限 / 按 `ItemId` 堆叠 / `UsableScene` 筛出「随身」等规则全部与字段名对齐，减少一次「概念 → 字段」的翻译；08-10c「单数命名，沿用 `pastEvent` 风格」那句话**原样保住**（`magicPack` 无单复数属性，唯一有张力的复数选项已被否决）。**推论与 08-06d 自洽**：同 `ItemId` 可持有多份、`Charges` 每份各自 ⇒ 元素必须是一份实例而非「一条 Id 一行」，**按 `ItemId` 堆叠是呈现层聚合，不是存储形态**。**④ `Achievements` 一并收口**（同一缺陷类，推演逐条同构）→ 元素类型 `Achievement` + 字段 `achievement`（成就无容器概念可借名，退回既有单数字段风格，零张力）；**⚠ interview 追加：裸写 `Achievements` 一并单数化 + 文件夹 `systems/player-profile/achievements/` 改名为 `achievement/`**（超出原草稿声明的「纯标识符收口、文件夹不动」范围），全库路径引用同步改写，分组结构与两档奖励语义不受影响。**⑤ `pastEvent` 类型漂移一并纠正**：`List<AdventureEvent>` → `IReadOnlyList<PastEventEntry>`，**⚠ interview 追加：三处全部纠正**（原草稿只点名 `life-cycle-service.md`；另两处为 `program-overview.md` 调用链图与 `decisions/ADR-0004`——后者按根约定「ADR 可自由编辑」直接改写，**纯类型标注订正、不改变该 ADR 的任何决策语义**）；顺带中性化 `character-profile/_index.md` 中「先前记为 X」的考古式表述。**⑥ 自行推演补齐草稿漏列的 5 处活文档落点**（`player-item/_index.md` · `architecture.md` 拆分轴段 · `player-profile/_index.md` · `achievement/_index.md` · `item/_index.md` 同行第 2 token），并统一 `Id` / `ItemId` 两种写法为 `ItemId`。**⑦ 纯标识符收口：机制侧零改动、不 bump 存档 schema、无迁移路径**（无线上账号、`game-feature-branch/` 无对应代码 ⇒ 一次改到位的最便宜窗口）；改写**只覆盖活文档**，`handoffs/` / `answer-logs/` / `inbox/archive/` 不回改。**⑧ 新增待答 0 条**——原草稿的前置依赖已由同日 `2026-08-12b-grant-source-per-kind-scope` 反向答结（`CharacterItem` 确定携带 `SourceCode`）。无跨库影响（未触及任何后端报文字段） | distilled | `terminology.md`（法宝 / 储物袋两条改写 + **新增「成就 / Achievement」词条**）, `program-overview.md`, `systems/architecture.md`, `systems/_index.md`, `systems/monetization.md`, `systems/character-profile/`（`_index.md`, `item/_index.md`, `item/common-properties.md`, `power/_index.md`）, `systems/player-profile/`（`_index.md`, `achievement/`（由 `achievements/` 改名）, `player-item/_index.md`, `account-info.md`, `codex/_index.md`, `codex/enemy-codex.md`）, `systems/services/`（`_index.md`, `life-cycle-service.md`, `profile-service.md`）, `ux/screen-flow.md`, `vision/scope.md`, `decisions/ADR-0004-realm-checkpoint-retry-model.md`, `open-questions.md`, `open-questions/`（`07-codex-monetization.md`, `deferred-content.md`, `update-log.md`）, `answer-logs/`（`log-character-item-singular-naming.md`, `_index.md`） |
| [2026-08-12b-grant-source-per-kind-scope](2026-08-12b-grant-source-per-kind-scope.md) | 2026-08-12 | **授予来源 `Source` 从「封闭三值」改为按 `(Kind, Scope)` 分域的开放清单**（输入 = `inbox/solution-draft-grant-source-per-kind-scope.md`，四项取向已裁决）。**⚠ 推翻 08-10b 的「成员清单已穷举、只有三条途径」与「清单是封闭的」**——三条成员全是账号级法则的授予途径，轮回级两类的常规来路（事件 outcome / 战斗奖励 / Exchange 购买 / 开局初始持有）在清单里无合法取值；原待答的两个收口（**收窄字段到账号级两类** / **轮回级恒 `Unknown`**）**全部否决**，该扩的是清单而非字段覆盖面。**① 七值 + 兜底**（`Unknown=0` · `FinaleWin=1` · `PremiumBundle=2` · `AchievementReward=3` · `EventOutcome=4` · `CombatReward=5` · `ExchangePurchase=6` · `InitialGrant=7`），成员来路全部取自既有文档已写死的获取渠道、非新造机制；**前三个 code 已冻结**（后端复算 `x` 依赖），**已删成员的 code 永不复用**。**② 保留单一枚举，不拆四个**——四类共用同一条授予通道（`AbilityChangeElement` 的 `Op == Grant` 与 `ProfileManager.Grant*`），拆分会把 `Source` 形参逼成 `object` / `int`，直接撞「贯穿整条链路的类型一致性」；同型先例 = 08-10c 合并 `PowerScope` / `ItemScope` 为 `AbilityScope`。**③ 分域差异由合法子集校验表承载，不由类型系统承载**——`(Kind, Scope) → 允许的 Source 集合` 静态查表，与置换同池判据共用同一个键；它是**代码常量而非内容资源**（约束代码组装、不约束内容编写 ⇒ 不进 `.tres`、不走 overlay）。**账号级不接 `CombatReward` / `InitialGrant`**（账号级唯一的战斗入口是残卷、已有专用成员）；**轮回级不接 `PremiumBundle` / `AchievementReward`**（发一件随轮回清理的东西作付费 / 成就回报，与「付费内容不会被游戏销毁」正面冲突）；**法则 / 古宝的 `EventOutcome` 与法则的 `ExchangePurchase` 三格暂不开放**，※ 注脚明写这是「暂不开放」而非「语义上不可能」，**日后开放 = 翻一格、无结构改动**。**④ 校验取「入口严、读档宽」**——`Grant` 时非法组合或 `Source == Unknown` → `PushError` + **整批拒绝**（与 `PairKey` 同档）；读档遇不合法的既有条目 → `PushWarning` + **保留原值**。**这条非对称是唯一安全的方向**：读档回落 `Unknown` 会把 `FinaleWin` 法则改判、压低 `x` 并让档位回跳。**⑤ ⚠ 仍不为「置换所得」设成员**——08-10b 的这半句**保留并强化为禁令**（新设 `Replacement` 会立刻打破 `x` 单调不减、重开置换刷分通道）。**⑥ 残卷完全不受影响**：`x` 仍 = `SourceCode == FinaleWin` 的法则数，新增四成员没有一个能出现在法则上；**单调不减 ⇒ 档位只降不回跳**、首胜规则、全局前置、账号级 RNG、幂等键一概不变。**⑦「唯一消费点」表述改写为两层**——**规则消费点仍唯一**（残卷的 `x`，故仍是严格同步 · 后端可复算的纯规则字段）+ **非规则用途两处现成落点**（`TryApply` 可追溯性日志 / 客服溯源）；**代价如实写下：轮回级两类仍无规则消费点**，只是从「字段无意义」降级为「有信息但暂无规则消费者」。**⑧ 新立通则：一个字段不为「部分落点无规则消费点」而拆出第二套同步口径**（`SourceCode` 是首例）。**签名不变 · 不 bump 存档 schema · 无迁移。** 澄清 `SourceCode`（授予渠道 · 持有条目）与 `SourceInstanceId`（禁用来源事件实例 · `disabledAbility` 条目）是两个字段、不得合并。答结 1 条、新增待答 2 条。**⚠ 后端侧承接已就位**（`backend-design-documents/handoffs/2026-08-12-grant-source-code-contract.md`，含未收口的枚举序列化冲突） | distilled | `systems/common-properties.md`, `systems/player-profile/player-power/common-properties.md`, `systems/player-profile/player-item/common-properties.md`, `systems/character-profile/power/common-properties.md`, `systems/character-profile/item/common-properties.md`, `systems/services/profile-service.md`, `systems/player-profile/player-power/_index.md`, `systems/player-profile/_index.md` |
| [2026-08-12-error-copy-and-update-prompts](2026-08-12-error-copy-and-update-prompts.md) | 2026-08-12 | **玩家可见错误文案的归属 · 三条版本提示的呈现与去重 · 阻塞屏三变体**（输入 = `inbox/solution-draft-error-copy-and-update-prompts.md`，四项取向已裁决；+ `draft-0812a.md` 末尾的追加裁决）。**① 文案归 UI 层、键 = 后端 `code`、载体 = Godot 翻译键**——判据「**谁掌握上下文，谁产出**」（与「客户端不持有兼容矩阵副本」方向相反但同源）；**处置表与文案表是两张表、共用同一个键**（`src/Core/` 跑在后端适配层，无界面上下文）；**`code → ERR_*` 是机械变换而非第二张手写表** ⇒ 「处置表加了行、文案表忘了加」这个失效面**不存在**，只可能是翻译条目缺失 ⇒ `PushWarning` + 按 `OpError` 回落四条通用文案（= `class` 默认路径表在文案侧的镜像）+ **启动期审计**（阶梯第 2 级）；**翻译资源随包，不走 overlay**（错误文案无 `Id`、不进抽取池、不被存档引用，塞进去会把一条刻意限窄的热更通道撑宽）。**② 三条「去更新」提示 = 同一根严重度轴上的三档，同一时刻只呈现最高一档**——`combat-ux.md`「不在最高频操作上加提示」的**第三个实例**；② 档复用既有常驻同步指示且**必须换掉「离线」二字**（`Upgrade` 态本会话内永不恢复，「离线」是一个已知为假的承诺）；① 档主菜单可关闭横幅 + **每个 `recommendedVersion` 只提示一次**（落 `user://cache/`）；**推论：全库 semver 比较恰为 2 处且都不阻塞**（可机械检查）。**③ 三种终局态 → 一个 `BlockingNoticeScreen` + 数据驱动变体表**（多于一处必出半配置态）；**⚠ 三变体 ≠ 三处硬阻塞**，阻塞点仍是既定两处；「去更新」地址 = **后端下发 `detail.updateUrl` 为主 + 随包 `ChannelConfig` 兜底 + 皆无则按钮置灰**，落地前**校验 scheme**（内容分发之外的第二个注入面）。**④ 迁移失败先分两种情形**：超客户端支持上界 → 走「需更新」变体（与 `client.version_unsupported` **同因不同径**）；范围内但迁移抛错 → 「存档读取失败」变体 + **必上报一次**（真正的程序缺陷态）；**否决重装**（权威在云端，重装不改变任何东西）与**回退云端旧版**（`revision` 严格单调递增）；**绝不静默放行**——半迁移的 Profile 下一次 push 会把损坏的档写回云端。**⑤ ⚠ 推翻 `OpResult.Detail` 的「面向玩家的原因串」表述**，正式收口为诊断串（理由：兼两个身份则总则 7 那三条承重纪律一条也无法机械检查），连带改写 `account-service.md` 的 `OpError.Compliance` 行。**⑥ 新定：全库 UI 文案统一走 `TranslationServer` 翻译键**，中文为默认与优先制作列、**英文列全部预设占位符**（否决「先用常量表再迁」——晚迁零收益）。**不改任何存档 schema、不改任何 record、无迁移。** 答结 3 条、新增待答 2 条。**⚠ 后端侧需一份对应 handoff**（错误体 `detail` 增更新地址字段，可与 08-11b 那笔合并） | distilled | `ux/error-and-blocking-ux.md`（新建）, `ux/_index.md`, `ux/screen-flow.md`, `systems/architecture.md`, `systems/services/account-service.md`, `systems/services/sync-service.md`, `system-overview.md`, `open-questions.md`, `open-questions/`（`05-service-contracts.md`, `deferred-content.md`, `update-log.md`）, `answer-logs/`（`log-error-copy-and-update-prompts.md`, `_index.md`） |
| [2026-08-11c-combat-turn-flow-fatigue-and-card-type-reduction](2026-08-11c-combat-turn-flow-fatigue-and-card-type-reduction.md) | 2026-08-11 | **战斗流程收口**（输入 = `inbox/draft-combat-system.md`）。**① 先后手由 `EncounterSpec.FirstSide`（`Side?`）决定**——剧情需要时由 future-event-service 物化写入（意图经 plot-manager 调制），`null` 则由 **combat 子流掷**；combat-service 只读不问来源，**不新增服务间运行时依赖**，与「不设先后手抽牌差」并行不悖。**② 抽牌堆不重洗、抽空即疲劳**（每尝试抽一张 −1 道念，下限 0 照常逐次截断）——**承重**：道念的削减通道**从一条变成两条**（卡牌 + 疲劳，后者不入栈、不产生 `PlayResult`），`DeckModule` 没有重洗路径，**满手与疲劳互不触发**（牌堆空才疲劳），卡组耗尽仍不终止战斗。**③ 卡牌侧数值重定**：起手 5 → **4**、手牌上限 10 → **9**、**卡组规模两侧皆不设硬限**（旧值理由「保证永不重洗」随重洗删除而失效，规模改由疲劳承接代价、成为可编排维度）、储物袋 99 → **9**（计数单位 = **按 `Id` 堆叠后的条目数**，故只约束种类数）；每回合抽 2 与敌我对称不变。**④ `CardType` 六值 → 五值**：删 **灵宠 `Creature`**（与阵法规则差别为零），**永久物统一归阵法**，**「实体 / 非实体永久物」二分整条取消**，原灵宠三个次类型迁为阵法次类型。**⑤ 不设 mulligan**（起始 4 张一次发到位）；**起始 mana = `manaLimit`**、三步回合结构与 10 回合 / 各 5 / 道念高者胜均复核确认。**⑥ 资源转换汇率与标杆维持推迟**（ch1 数值标杆专场）。答结待答 2 条、推翻 5 处既有定案、新增待答 4 条 | distilled | `systems/services/combat-service.md`, `systems/scoring.md`, `systems/balance.md`, `systems/character-profile/deck/_index.md`, `deck/common-properties.md`, `systems/character-profile/item/_index.md`, `systems/adventure-event/combat/_index.md`, `systems/enemies/_index.md`, `systems/enemies/common-properties.md`, `terminology.md`, `ux/combat-ux.md`, `ux/screen-flow.md` |
| [2026-08-11b-contract-boundary-and-flags-client-side](2026-08-11b-contract-boundary-and-flags-client-side.md) | 2026-08-11 | **契约边界层的客户端侧承接**（输入 = 后端两份 08-11 契约 handoff 的「客户端侧影响」段；`envelope` 五点 + 它牵出的 `content-delivery` flags 五点一并吃下——只写读取点会让 `content-service.md` 引用一个它没定义的通道）。**① 传输信封走 HTTP 头，客户端 record 一字不改**：`Session.Token` / `AppVersion` / `ContentVersion` 由 `HttpProfileBackend` 搬到 `Authorization` / `X-App-Version` / `X-Content-Version`，`pushId` / `baseRevision` / `schemaVersion` / `reason` 留 push body 的**负载信封**段（CAS 与三分支表原样成立）；新增 `X-Request-Id` —— **它与 `pushId` 是一对反向纪律**（前者每次重试必换、后者跨启动必不变，写反哪个都静默失效）；头处理收敛到 `src/Core/` 一处，与 `BackendSelector` 唯一选择点同构；**枚举值与 C# 枚举名逐字相同 ⇒ 重命名跨边界枚举值即破坏性契约变更**。**② 错误处置 = 以 `code` 为键的数据表**（不是 switch，落 `architecture.md` 总则 7 + `src/Core/`）：不得靠 HTTP 状态码分支 · 不得解析 `message`（`message` + `requestId` 拼进 `OpResult.Detail`，不进玩家弹窗）· 未知 `code` 按 `class` 走**四条保守默认路径**、未知 `class` 当 `Fatal` + 上报 ⇒ **硬阻塞仍只有两处且只由已知 `code` 触发，一个未知 `code` 永不新增第三处**；`Cancelled` / `Migration` 不得进映射表取值域（可机械检查）；**表内不含 `plot` 域**（08-11 剧本已本地化）。**③ `Retry-After` 是退避下界**（`max(本地退避, 服务端值)` + 抖动照叠），限流绝不映 `Conflict`（否则一次限流 = 一次进度丢失）。**④ `Upgrade` 类错误只在登录 / 启动 pull 硬阻塞**；非闸门点保留待发队列 + 非模态提示 + 暂停退避（**唯一解除条件 = 重新登录成功**），**与「缓冲超限 → 软阻塞」的衔接 = 闸门口径完全不变、只换模态文案与选项**（「需更新版本才能同步」/「去更新 · 退出」，**无「重试」**——同步在本会话内永不恢复，软阻塞的本意正是拦住继续累积）；UI 靠新增只读属性 `UpgradeRequired` 单点查询区分变体（同 `PendingCount` 纪律，不新增 `SyncState` 值）。**⑤ flags = `ContentEnabled` 的第三层覆盖来源**（`res://` < overlay < flags），**推翻「overlay 是唯一热更层」**；只作用于产出侧 `AllEnabled()` 取池、不参与合并后强校验、轮回中途可热应用；**启动链插入 `RefreshFlagsAsync`（在 `SignInAsync` 之后、`SyncService.InitializeAsync` 之前——flags 端点需鉴权，而 content-service 是启动链第一步）**；本地缓存 `user://cache/flags.json`（切账号即失效），**并反证后端标注的「离线开局被秒关条目复活」缺口在客户端不存在**——启动 pull 硬阻塞 ⇒ 没有断网开局这条路径。**⑥ 版本协商两条互不兼职**：`minAppVersion`（内容维度、客户端 semver 三段整数比较、跳过 overlay 永不阻塞）vs 服务端判定的强更闸门（客户端不比较 `X-Min-App-Version`、不持兼容矩阵副本）；`manifestSchema` **不受支持 → 跳过更新用基线**（与「不匹配 → 整包重下」分开）。**⑦ manifest 契约对位四条义务**：`path` 穿越校验 · `size` 磁盘预检 · 拒绝 `contentVersion` 回退（不做时间 TTL）· 内置**一组** `keyId → publicKey`。⚠ 后端侧需一份对应 handoff（删 `/v1/plot/…` 与 `plot.unavailable`） | distilled | `systems/architecture.md`, `systems/services/sync-service.md`, `systems/services/content-service.md`, `systems/services/account-service.md`, `open-questions/05-service-contracts.md`, `open-questions/deferred-content.md`, `answer-logs/log-0811_2.md` |
| [2026-08-11-plot-content-localization](2026-08-11-plot-content-localization.md) | 2026-08-11 | **剧本内容本地化：撤销云端剧本服务，改由 content-service 的 overlay 通道承载。** 起因是一个「为什么剧本在后端」的提问，盘点后发现**这条决策从未被论证**——07-23 §3 是纯断言，07-25c 的分界判据「按进度动态请求、一次性呈现、不被存档引用 → 云端」**描述性且近乎循环**（「动态请求」是那个选择的*结果*，被当成了*理由*），且**无任何 Accepted ADR 覆盖剧本归属**（ADR-0003 管存档 / 账号权威）⇒ 本次不推翻任何 ADR，只补上该论证并得出相反结论。**① 剧本节点 / 分支 / 文本改归本地内容层**（`res://content/` 基线 + `user://overlay/`，经 ContentRegistry 按 `Id` 读；**运行时内容零网络请求**，剧本文本在事件发生前已在盘上）；前提两条：**剧本是预写式内容库**（生成式无法本地化——密钥 / 成本 / 审核必须在服务端）· **剧透 / datamine 被接受**（与既定「不承诺防作弊」同调，纯 PvE）。**② 新开「只改不增」的唯一例外：overlay 对剧本内容可新增 `Id`。** 判据 = 那条纪律**唯一**的存在目的是关死「旧客户端存档引用到未知内容」，而剧本文本是内容类别里**唯一不被存档引用**的一类（`CharacterProfile` 只存 key points）⇒ 放开不重新引入该风险；**收益 = 新剧情可热更不发版**（原云端服务提供的正是这项能力）。例外边界写窄两条：只覆盖剧本类型本身（`CardData` / `AdventureEventData` / **状态转换触发的定性文案**照旧只改不增）· 新增剧本条目不得引用本次 overlay 之外的新 `Id`（保住合并后强校验的交叉引用不悬空）。**③ ⚠ 新生唯一风险 + 其承重处置：悬空 key point → `PushWarning` + 叙事降级、不阻塞轮回。** key points 是**指向剧本节点的持久化锚点** ⇒「剧本不被存档引用」只对**文本**成立、对**节点**不成立，overlay / 客户端版本回退即可使其悬空（此前不存在此风险，因云端服务负责解析）；处置与 content-service 的**「读取侧不过滤」不对称原则同构**，代价明写（**静默失去一段剧情与它带来的调制**，被接受——剧本调制是塑造倾向而非硬性结算）；**反向约束 key points 的 schema**：必须能在其剧本节点缺失时被安全跳过。**④ 结构性收敛（本次最大收益）：** 跨进程边界成分 **4 → 3**，`IPlotBackend` / `HttpPlotBackend` / `OfflinePlotBackend` / `BackendSelector.CreatePlot()` / `PlotRequest` **整套作废** ⇒ **总则 7 四接口 → 三接口**、条件编译清单 **6 → 5 处**、`IBootstrappable` 由四个边界服务实现改为三个；PlotManager 全部方法**形态 B → A**（`ResolvePlotAsync` → `TryResolvePlot`、`ChooseBranchAsync` → `ChooseBranch`；依据 `common-properties.md`「形态 B = 跨客户端 ↔ 后端边界」）；`future-event-service` 升为**纯本地服务**；**「manager 不跨边界」由带一个例外变为无例外的结构性事实**。**⑤ 整套为网络失败而生的复杂度消失：** 事务前置 · `user://cache/plot/` LRU 预取 · 延迟预算 · 超时兜底 · 降级文案 ⇒ `sync-service` 降级表**删「剧本请求」一行**，只剩 push / pull 两条。**⑥ 08-09c「`pastEvent` 与 key points 零结构耦合」结论原样保住但换了论据**（旧论据「不让云端剧本服务依赖客户端存档标识空间」随跨进程层消失；新论据 = 可热更内容不得依赖存档运行时标识 + 挂上 `InstanceId` 会直接破坏悬空降级规则）。**⑦ ADR-0003 强制在线 · 云端权威原样成立**——改的是内容载体，不是账号 / 存档模型；跨边界只剩**鉴权 · 进度同步 · 启动期内容分发**。**新增待答 2 条**：剧本内容类型的数据形态（含例外约束的可执行化，属「能上线且线上不可见」⇒ 阶梯第 1 / 2 级）· 剧本体积与分发粒度（本地化后成为真实包体成本）。**ADR 候选**：宜与 content-service 的「内容载体形态」候选合并固化。**⚠ 后端侧需一份对应 handoff**（删 `systems/_index.md` 的 `plot.md`、作废 `open-questions/05-plot-service.md` 整片、`01-contracts.md` 与 `README.md` 的「四个」→「三个」、剧本改由 `contracts/content-manifest.md` 承载） | distilled | `systems/services/`（`plot-manager.md`, `content-service.md`, `future-event-service.md`, `sync-service.md`, `life-cycle-service.md`, `_index.md`）, `systems/architecture.md`, `systems/character-profile/_index.md`, `systems/adventure-event/finale/_index.md`, `program-overview.md`, `system-overview.md`, `terminology.md`, `open-questions.md`, `open-questions/`（`04-hidden-attributes-plot.md`, `update-log.md`）, `answer-logs/`（`log-0811.md`, `_index.md`） |
| [2026-08-10c-ability-disable-replacement-and-player-statistics](2026-08-10c-ability-disable-replacement-and-player-statistics.md) | 2026-08-10 | **「本轮回禁用」与置换型剥夺一次收口；`PlayerStatistics` 与宽松同步口径定形。** 08-06b 只定了语义（法则不被强制剥夺），本次补齐形态。**① 一条上位判据统摄全片区：禁用一律截断在「进入生效面」那一步**，而不是在生效面里做例外判断——与既定的「`status` 关闭 = 不入场」完全同构；分界依据是「神通 / 法则 ≈ 静止式异能、法宝 / 古宝 ≈ 启动式异能」，但**该类比只用于选截断层，不收窄 `Abilities` 取值域**。逐面落地：`Power` 不入场 · `Item` 不进本场可用道具 · 不进 capability 聚合与 modifier 表 · 触发器不注册。**推论：`Power` 入场由两条与门变三条与门**（+ 不在 `disabledAbility` 内），且 `combat-service` 的「不落存档的可重建项」须补 `disabledAbility` 与「本场可用道具的禁用过滤」（**仍确定性，不给 `activeCombat` 加字段**）。**② 承载字段 `CharacterProfile.disabledAbility`**，与 `pastEvent` / `chapterRetry` / `activeCombat` 平级、**不落 `Status` 内**（数值型运行状态 vs 集合型 build 状态）；条目 7 字段，**存「施加时坐标 + 时长」不存「到期坐标」**（判据同 08-09c），三档 `DisableDuration { NextEvent, ThisChapter, ThisCycle }` 在两个既有时点纯函数式剔除；**第一档定名 `NextEvent` 而非 `ThisEvent`**——施加只在 `eventEnd`，「本事件禁用」是空操作，**枚举成员的名字必须说实话**，三档因此无死成员。去重键 `(Kind, Scope, AbilityId)`、重复不叠加取更长者；**禁用不影响持有与 `Charges`、不影响残卷的 `x`**。**③ 置换 = 排除已有 · 同稀有度 · 同 `(Kind, Scope)` · 先看后决 · 拒绝无代价 · 四类通用**（含神通）；抽取走 `AllEnabled()` + `reward` 子流，**空池 → 整个置换成为空操作 + `PushWarning`**（不降级到相邻稀有度）。**新定名 `RarityTier { Tier1..Tier5 }`** 挂 `PowerData` / `ItemData` / `CardData`（缺失 → `PushError`）——**⚠ 与战后奖励的优势档 `Tier { Narrow, Solid, Crushing }` 是两个东西，不得复用、不得换算**，奖励池权重表改按 `RarityTier` 索引。**④ ⚠ 推翻「置换作为选择成本似乎合理」：能力 element 在 `SelectCost` 内恒为空**，三种操作（`Grant` / `Remove` / `Disable`）只出现在 outcome / reward 侧——成本侧只放可计价的量 · 无条件施加与「先看后决」正面冲突 · 能力得失是后果不是入场费 · 换来一条可机械检查的不变式；outcome 侧是一个**形状与战后奖励面板同构的事件内决策点**，候选预先掷定并落决策点存档。**⑤ `ProfileChangeSpec` 由单列表扩为三个平级只读列表**（资源 / 能力 / 统计）——三者施加语义根本不同（走不走 modifier pipeline、要不要钳制、失败是否阻断），压进一个带符号 `int` 是让类型说谎；**置换 = `Remove` + `Grant` 由 `PairKey` 配对，不是一条 `Replace`**（原子性已由 `TryApply` 免费提供）；**「按 `Id` / 随机 / 按 `Scope`」三选一的旧问消解**（element 只承载已定稿 `Id`）。连带 `PowerScope` / `ItemScope` **合并为 `AbilityScope`**。**⑥ `PushWarning` 对称落点定在内容加载侧、形态是「清单列举」而非「比例校验」**（outcome 侧样本量是 1 必然误报；告警落在玩家进程里等于没人看见），告警文案须明写「这个比例不是 1% 的口径」。**⑦ `PlayerStatistics` 具名类挂 `PlayerProfile`**（一个类型就是一道可见的边界），**首批只有 `TotalCyclesCompleted` + `TotalCyclesDefeated`**——**⚠ 推翻 08-06b「首项 = 篇章重试的账号级累计」**，代价明写：**「你在炼气段重开了多少次」目前没有字段回答**。**⑧ 宽松同步口径穷举五处**，第 5 条是防滑坡关键：**统计不得驱动任何发放**，一旦这么用它就变成规则字段必须整体升层。**不新增服务 / manager / EventBus 事件 / 存档点 / RNG 子流**；schema bump 一次、空迁移。 | distilled | `systems/character-profile/_index.md`、`systems/character-profile/power/_index.md`、`systems/character-profile/item/_index.md`、`systems/player-profile/_index.md`、`systems/player-profile/player-power/_index.md`、`systems/player-profile/player-item/_index.md`、`systems/adventure-event/common-properties.md`、`systems/architecture.md`、`systems/services/profile-service.md`、`systems/services/combat-service.md`、`systems/services/life-cycle-service.md`、`systems/services/sync-service.md`、`systems/services/content-service.md`、`systems/common-properties.md`、`systems/balance.md`、`systems/monetization.md`、`terminology.md`、`open-questions/01-combat.md`、`answer-logs/log-ability-deprivation-and-player-statistics.md` |
| [2026-08-10b-grant-source-and-fragment-source-scoping](2026-08-10b-grant-source-and-fragment-source-scoping.md) | 2026-08-10 | **授予来源 `SourceCode` / `Source` 枚举，与道统残卷 `x` 的口径收窄到 `FinaleWin`。** **① 四类持有条目（法则 PlayerPower · 古宝 PlayerItem · 神通 CharacterPower · 法宝 CharacterItem）统一新增 `SourceCode`**，记录「它是被哪条渠道给到玩家的」；**落在持有条目上而非 `PowerData` / `ItemData` 上**——物化模型的直接推论（`XxxData : Resource` 是 ContentRegistry 的共享只读单例，同一条法则可由不同渠道获得，**来源是「这一次获取」的属性**），与 `status` 同层，正是 `character-profile/power/common-properties.md` 早已预留的那一格；写入时刻 = 授予时刻、此后不变，移除后再获得 = 新条目写新来源。**② `Source` 是 C# 枚举，成员带 code 与 value**：code = **显式稳定整数**（存档 / 上行只序列化它，重命名成员不破坏存档、**已删 code 永不复用**），value = 展示文案（可本地化、不落存档）——与「capability flag 用 `enum` 而非字符串 key」「显示字符串与键分离」同构；**成员清单封闭三值**：`FinaleWin` / `PremiumBundle` / `AchievementReward` + **防御性的 `Unknown = 0`**（读档遇缺失 / 未知 code → `PushWarning` + 归入，不阻塞；当前无线上账号故迁移成本为零），**不为事件 outcome / 战斗奖励 / Exchange 购买 / 置换所得 / 开局初始预留成员**。**⚠ 由此新生唯一的剩余待确认项**：三条全属账号级途径，神通 / 法宝的常规来路无合法取值、只能落 `Unknown`（收口两选：把字段收窄到账号级两类 **倾向** ／ 四类照带且轮回级恒 `Unknown`）。**③ 残卷的分档自变量 `x` 收窄为「已拥有且 `SourceCode == FinaleWin` 的法则数」**——原文只点了「odds increase 与 upper limit」，但**两表本就是同一条闸门的两面**（适格 Finale ⟺ 该档 `Gain > 0`），故适格篇章闸门必须同步收窄；**全局前置「尚未拥有的法则数 > 0」仍按全部持有计**（池取尽与来源无关）⇒ 存在合法状态「`x = 0` 但池已被礼包 / 成就取尽 ⇒ 静默停摆」；`x` 仍不落字段（改为一次**带过滤计数**）。**⚠ 推翻 08-09b §6**：礼包**不再使 `x` +1、不再压低上限**，礼包与残卷**完全解耦**；**「获取渠道是打还是买不改变这条曲线」整句作废**——分档自变量的含义**反转**为「靠渡劫拿得越多，后续越难再从渡劫拿到」，理由是分档的用途是给**失败侧产出**一条递减曲线，把付费 / 成就算进去等于让玩家买到的东西掐死自己的残卷线；**付费收益变为纯净收益，这是设计意图（已确认）**——那条有意的负反馈整条消失，**礼包净强度上升是被接受的**，平衡按此校准。**⚠ 连带**：「`x` 单调不减 ⇒ 档位只降不回跳」**原样保住，但保住它的理由换了一条**——旧口径靠「置换是等价交换」，新口径还需要**「置换不改变来源」**（换来的条目继承被换出条目的 `SourceCode`），否则换掉一条 `FinaleWin` 法则即可压低 `x`、刷回高掉率；`balance.md` 三张表数值不变但**口径偏松**（复核提示：偏高则下调阈值 3/5/9/12/15，不动表结构）。**④ 授予通道强制携带来源**：凡授予 power / item 的 `ChangeElement` 必须带 `Source`、无默认值；`GrantPower(string powerId, Source source)`；残卷发放取 `Source.FinaleWin`。**⑤ 答结 08-09b 遗留待答：Finale「失败但存活」的叙事补白归 `plot-manager.md` 的叙事层**（与「隐藏属性跨档定性叙事」同类、同落点 `ResolveOutcome` → `eventEnd`、不新增结构），文案两版**「劫败而身存，破境亦有缺。」/「以败换境，以伤换生。」**，**等概率随机二选一 · 属内容层**（可热更、不随剧本服务下发 ⇒ 在 PlotManager 内划出「剧本正文走云端 / 状态转换触发的定性文案走内容层」的分界；**推论：随机源不必带种子**）；**承重边界：两版均不得暗示道统残卷**（失败侧对残卷彻底隐含仍成立）。**⑥ 成就的两档奖励可以是法则 / 古宝**（新确立的设计意图；不计入 `x` 故不压低掉率，奖励目录本身仍待设计）。**⑦ `SourceCode` 没有第二个消费点**——不对玩家可见、不进图鉴、不参与其他判定 ⇒ **纯规则字段**（严格同步 · 后端可复算）。**剩余待答 1 条**（三值清单与轮回级取值的冲突，见上）。**schema**：四类条目各 +1 字段 ⇒ 版本 bump，老档补 `Unknown`（无实际迁移） | distilled | `terminology.md`, `systems/common-properties.md`, `systems/player-profile/`（`_index.md`, `player-power/_index.md`, `player-power/common-properties.md`, `player-item/common-properties.md`, `achievements/_index.md`）, `systems/character-profile/`（`power/_index.md`, `power/common-properties.md`, `item/_index.md`, `item/common-properties.md`）, `systems/`（`balance.md`, `monetization.md`）, `systems/services/`（`profile-service.md`, `life-cycle-service.md`, `plot-manager.md`）, `systems/adventure-event/finale/_index.md`, `open-questions.md`, `open-questions/`（`06-meta-progression.md`, `07-codex-monetization.md`, `update-log.md`）, `answer-logs/`（`log-0810b.md`, `_index.md`） |
| [2026-08-09e-discipline-enforceability](2026-08-09e-discipline-enforceability.md) | 2026-08-09 | **三条「靠约定执行」的工程纪律一次收口——它们是同一个问题的三个实例**（*正确的写法需要作者主动记得，错误的写法既不报错也不显眼*）。**① 立上位判据「纪律的可执行化」四级阶梯**（1 写不出来 / 2 编译不过 / 3 大声失败 / 4 评审清单）+ 两条选级判据：**能上线且线上不可见 → 必须第 1 或第 2 级**（第 3 级不够——断言只在跑到那一步时生效，而这类违规的症状恰恰是「一切正常」）· **只在开发期显形且会累积 → 第 3 级足够**。落 `systems/architecture.md`，与八条 API 契约总则同层，**ADR 候选**。**② 离线后端（第 1 级）**：先指出一处**技术互斥**——autoload 指向 `.cs` 时不存在场景实例，`[Export]` 没有存储处也没有检视器落点，故 `[Export] bool UseOfflineBackend` **整条作废**；改为**唯一选择点 `BackendSelector`**（承重理由：四个字段 = 四个出错点，最糟失败态是「三个开了一个没开」的**半在线状态**，比全离线更难诊断）+ **四个 `OfflineXxxBackend` 整类包 `#if DEBUG`**（Release 里离线实现根本不存在 ⇒ 问题从「开关会不会配错」降级为「类型存不存在」）；第 3 级兜底 = ProjectSettings `mycardgame/backend/use_offline_backend`（进版本控制、可 diff、原生 feature override）+ BootstrapScreen 横幅与 release×offline 断言。连带**「autoload 直接指向 `.cs`」升为无例外约定**，推论「服务级配置一律走 ProjectSettings，`[Export]` 只留给场景组件」。**③ `AllEnabled()`（第 1 / 2 级）**：**删除中性诱饵名 `All()` 本身**而非改名——漏写过滤的发生机制是「随手写了那个最短最中性的名字」；只留 `AllEnabled()` + `AllIncludingDisabled()`（合并后强校验是后者第一个正当调用方），过渡期 `[Obsolete(error: true)] All()` 恒抛作编译闸。否决「`All()` 语义改为 enabled-only」（**命名诚实性**：叫 `All` 却不返回全部就是把 bug 挪到未来）与 Roslyn 分析器（同一份编译期保证，成本高几个数量级）。**`DrawPool<T>` 采纳但排期到第二阶段开工、第一份内容 FR 之前**（届时三个抽取侧有真实调用方可验形状，且分桶问题大概率已答定；再晚就从「纯加法」退化为「改调用方」）。**④ EventBus 退订（第 3 级）**：`#if DEBUG` 订阅审计，**定位信息取 `d.Method` 不取 `d.Target`**（泄漏时实例已释放，反射元数据不依赖存活）⇒ **订阅无需登记来源、`+=` 惯用形态原样保留**；**时机 = 切屏完成后由 game-progression 调一次**（零热路径成本），否决「每次 `Emit` 顺带检查」（`GetInvocationList()` 分配数组、撞热路径不分配），`_ExitTree` 只打计数摘要不做判定；推论**豁免 autoload 服务的订阅**。**⑤ 穷举纪律：条件编译共 6 处，不得扩张**——服务与 manager 内部一律不得出现 `#if`。**⑥ 三条改动全在工程形态层**：不动存档 schema、不动后端协议、不动玩法语义，且当前无一行存量 C# 需迁移 | distilled | `systems/architecture.md`, `system-overview.md`, `systems/services/content-service.md`, `systems/common-properties.md`, `.claude/rules/data-resource-rules.md`, `open-questions.md`, `open-questions/`（05-service-contracts.md, update-log.md）, `answer-logs/log-discipline-enforceability.md` |
| [2026-08-09d-field-layering-merge-criterion-and-ordinal-naming](2026-08-09d-field-layering-merge-criterion-and-ordinal-naming.md) | 2026-08-09 | **账号级字段的两层通则、合并判据与 `Ordinal` 命名硬约定。** 08-09b 留下的「`FinaleWinOrdinal` 会不会被当成统计侧『总通关数』合并掉」不靠注释解决，靠**三条结构性纪律**关死：**① 分层通则升格**——08-06b / 08-09b 两次就事论事用到的判据（**判据 = 它有没有被「规则」读**）写成 `PlayerProfile` 上账号级字段的通则（两层的同步口径 / 读档校验 / 篡改后果对照 · **依赖方向单向**：规则层可被 UI 读、统计层绝不可被规则读 · **被 UI 读到不改变分层**），并补上真正缺的那条**合并判据**：**可以合并，当且仅当「语义 + 同步口径 + 篡改后果」三者全同；跨层的两个字段按定义永远不满足**（真实风险不是有人故意合并，而是后来者看到两个同名整数理所当然去重，只有正向判据可被主动执行）；**② 统计侧不设「Finale 胜利数」字段**——「渡劫成功了几次」**直读 `FinaleWinOrdinal`**，**让重复字段从一开始就不存在**是最强的防合并手段（依据既有单一真值纪律，而这份真值恰是幂等键）；**③ 统计侧「通关」= 完成整个轮回** `TotalCyclesCompleted`（三篇章全通 · 抵达元婴），一次通关贡献 3 次 Finale 参与、至多 3 次胜利，而 Finale 胜利可完全不伴随通关 ⇒ **两个数在任何账号上都不相等**，比任何注释可靠；**首批不设 `TotalChaptersCompleted`**（与 ordinal 只差 1% 分支、数值几近恒等，恰是最易被误合并的形态）；**④ `Ordinal` 后缀立为规则字段层的命名硬约定**（`Total` / `Count` 归统计层，统计层禁用 `Ordinal`，可机械检查、当前库内零迁移成本）；**⑤ 两层同经 `ProfileManager.TryApply`、同在一次 diff 里，只在校验强度上分开**（规则严格 · 后端可复算 / 统计宽松 · 可容忍丢失），且**明确不做两层交叉一致性校验**。UX 侧：渡劫成功次数与总通关数**并列展示且允许不相等**，措辞不得暗示一致（1% 的「失败但存活」造成的差值是有味道的信息，不是 bug）。**存档 schema 不受影响** | distilled | `systems/player-profile/_index.md`, `systems/services/（life-cycle-service.md, sync-service.md）`, `ux/screen-flow.md`, `terminology.md`, `open-questions.md`, `open-questions/（01-combat.md, 06-meta-progression.md, update-log.md）`, `answer-logs/log-finale-win-ordinal-vs-statistics.md` |
| [2026-08-09c-past-event-trace-schema](2026-08-09c-past-event-trace-schema.md) | 2026-08-09 | **`pastEvent` 痕迹 schema 四问一次收口。** **① 快照字段由判据给出，不逐字段拍板**：「**重算不出来的存，重算得出来的不存**」——文本类字段（显示名 / 描述 / 图标 / **风味文案**）一律留在模板侧、**快照里一个字符串正文都不存**（收益：文案改版永不触发存档迁移），物化产出的数值（`SelectCost` / `Priority` / Mystery 真身 / 敌人赋级）必存；**判据本身写进文档，字段表只是它当下的投影**。**② 痕迹条目 ≠ `EventOption`**，而是「定稿实例快照 + 本次结算的最终账」：`PastEventEntry`（13 字段），核心是 **`AppliedChange`** = `eventEnd` 那一次合并 `TryApply` 的最终 spec（**复用 `ProfileChangeSpec`，不引入新类型**；存最终 spec 一份胜过存若干片段让读取方自己合）；`Seq` 是**时序坐标不是内容键**；**`LifeSpanAfter` 收下并写明为判据的明示例外**（800 B 换掉一次全序列重放，是例外不是先例）；`EventOutcome` **四值**（`Resolved` / `CombatWon` / `CombatLost` / **`Aborted`** ——支付后短路那一步仍然发生过，通常是角色最后一条痕迹），**不为 DnD 选分支预留成员**（枚举增删牵动迁移，可空字段不牵动）。**③ 未被选中的选项 → 归档轻摘要 `UnchosenOptionRef`**：承重推演是「**定稿实例必须落存档」对未选项不成立**——未选项在下一次整批重算即被丢弃、**永不被任何流程消费**，只需**可回溯**而非**可重建**；副产品是 `pastEvent` 由一串孤立事件升为一串**批次**，回避信号以「选择的补集」形态回来。**④ 与 AdventurePlot key points → 零结构耦合、单向只读**：把 `InstanceId` / `Seq` 塞进 key point 等于让**云端剧本服务隐式依赖客户端存档的标识空间**；PlotManager 经既有 `ModulateEventOptions` 只读访问，派生索引**读时计算不落存档**（存了就有两份真相）⇒ **砍掉一条依赖边：`pastEvent` 不再被「key points 粒度」阻塞**。**⑤ 体积 → 不影响 push 粒度**：单事件 ≈ **770 B**（占既有 ~2 KB 预算三分之一），整轮回 ≈ 150 KB；新增**「只追加、不修改既有条目」不变式**（diff 只需表达尾部追加）与**软上限告警**（> 500 条或 > 512 KB → `PushWarning`，因整聚合 pull 是硬阻塞路径）；**明确否决**分页 / 冷热分离 / 独立存档段。**⑥ 连带答结「风味文案不物化，跟随模板数据」** ⇒ 「`EventOption` 完整物化字段清单」的**文本那一半收口**，且 C1 ↔ C2 的那处**被误认的张力整体消解**（两条管的不是同一类字段，`variantKey` 方案作废）。**⑦ 类型修正**：修行历程 `List<AdventureEvent>` → **`IReadOnlyList<PastEventEntry>`**（原标注与物化模型不符）；**bump 存档 schema 版本（空迁移）**；写入点不新增（life-cycle-service 组装 → `ProfileManager`） | distilled | `systems/adventure-event/common-properties.md`, `systems/services/`（`future-event-service.md`, `sync-service.md`, `plot-manager.md`）, `systems/character-profile/_index.md`, `systems/architecture.md`, `terminology.md`, `open-questions.md`, `open-questions/`（`02-event-options.md`, `update-log.md`）, `answer-logs/log-past-event-trace-schema.md` |
| [2026-08-09b-player-power-fragment-finale-bound-drop-chance](2026-08-09b-player-power-fragment-finale-bound-drop-chance.md) | 2026-08-09 | **道统残卷 / `PlayerPowerFragment` 整条焊到 Finale（天劫）上**。**① 三个时刻全部落在 Finale**：**累积 = Finale 失败**（其余一切失败——普通 Combat / Practice 失败、寿元耗尽、`lifeTotal` 耗尽、主动弃置——**一律不累积**）· **掷骰 = Finale 胜利** · **发放 = 该 Finale 的 eventReward 界面**。四条结构性简化：**不需要跨轮回的待发放字段**（`PendingPowerId` 一类中间态不存在）· **整条机制落在既有 Finale 结算链路上**（授予法则成为 `Spoils` 的一个 element，不新增结算阶段 / 存档点）· **累积天然有界**（一篇章一个 Finale + 败后不可重战 ⇒ 每角色每篇章至多累积一次**或**掷骰一次且互斥，**残卷不需要任何额外防刷规则**）· **叙事自洽**（在天劫下失败积攒，在渡劫成功那一刻兑现）。**② 上限 / 基础概率 / 适格篇章按已拥有法则数 `x` 分档**（50/30 → 30/20 → 30/20 → 10/5 → 10/5 → 5/3），**篇章闸门逐档累加地移除**（`x ≥ 5` 移除 ch1、`x ≥ 12` 再移除 ch2）；**承重合一：适格 Finale ⟺ 该档增量 > 0 的篇章** ⇒ 实现侧只需一张按 `(x, chapter)` 索引的表，「在某章输了却只能在别章兑现」的错位不可能出现；**首胜 100% 优先于闸门**（三次首胜是账号生命周期的确定里程碑，被闸门吃掉会造成「第一次渡劫成功却空手」）；全局前置 = 未拥有法则数 > 0，否则**静默停摆**。**③ 生效概率 = `clamp(Accumulated, Base(x), Cap(x))`**，发放后**重置为 `Base(x+1)` 而非归 0**（归 0 会让地板形同虚设），跨档**不清空、只钳制**；`x` 单调不减 ⇒ 档位只降不回跳。**④ 掷骰走账号级 RNG，与 `CycleSeed` 完全解耦** —— `Hash64(AccountSeed, FinaleWinOrdinal) mod 10000`（万分比整数，不用 `float`）；**绝不走 SeedManager 四条子流**（全由 `CycleSeed` 派生，而篇章重试换 `CycleSeed` ⇒ 挂上去即可刷）；**序号即幂等键**；**对轮回可复现性零影响** ⇒ 「与 seed 公平性的关系」的答案 = **两者不相交**；**客户端掷、后端可复算**（`AccountSeed` 落 `AccountInfo` 由后端下发）⇒ **Finale 奖励结算不引入任何新的网络往返**。**⑤ 状态落点 = `PlayerProfile.PlayerPowerFragment`**（`Accumulated` / `FinaleWinOrdinal` / 三个首胜布尔），**不并入账号级统计计数**（08-06b 的「参与判定 vs 纯读数」判据）；**`x` 不落字段**（派生自 `List<PlayerPower>.Count`，落字段即第二份真值）。**⑥ 礼包不重置概率、但压低上限**（`x` +1 ⇒ 可能进更低档）——**有意的负反馈**，付费不吞掉已积累的失败、只让下一条法则来得更慢。**⑦ 玩家侧彻底隐含**：失败结算**无文案 / 无暗示 / 无进度条 / 无百分比**，唯一可见面是命中时的那一次发放。**⚠ 连带推翻**：08-06d 的「**Finale 失败后可再次挑战**」→ **每篇章一个 Finale、败后不可重战**；**新增承重语义**：**Finale 失败但存活（约 1%）⇒ 篇章照常完成、境界照常突破** ⇒ **渡劫胜负不再是篇章推进的闸门**。**⚠ 口径收窄**：08-01 的「失败侧首次有产出」现只对 Finale 失败成立，常规失败只剩 EnemyCodex 遭遇即记 + 失败经验。**新增待答 3 条**：候选池与排重规则（前置依赖）· `FinaleWinOrdinal` 与统计计数的边界 · 1% 存活分支的叙事补白落点（另 `AccountSeed` 协议归后端库） | distilled | terminology.md, systems/player-profile/（_index.md, player-power/_index.md, account-info.md）, systems/adventure-event/finale/_index.md, systems/services/（life-cycle-service.md, profile-service.md, future-event-service.md）, systems/（game-progression.md, monetization.md, common-properties.md, balance.md, scoring.md）, open-questions.md, open-questions/（06-meta-progression.md, 07-codex-monetization.md, deferred-content.md, update-log.md）, answer-logs/（log-legacy-fragment-chance.md, _index.md）, backend-design-documents/open-questions.md |
| [2026-08-09-sync-revision-cas-and-immediate-flush-nonblocking](2026-08-09-sync-revision-cas-and-immediate-flush-nonblocking.md) | 2026-08-09 | **sync 契约两条收口**。**① `revision` = 后端分配的账号级单调递增 `long`**（分配权必须在权威一侧，否则 ADR-0003 被架空；时间戳 / ETag / 客户端分配 / per-`CharacterProfile` 版本号均否决）· 客户端只持基线 `baseRevision`，落 `user://cache/sync-envelope.json` 的**传输层元数据**——**不进 Profile、不 bump 存档 schema、无迁移**，切账号即失效 · 上行 = **CAS 三分支**（相等接受 `+1` / 本地落后 → Conflict 以云端为准 / **本地领先 = 不可能态**，同处置 + `PushError` 上报一次） · **幂等键 `pushId`（承重）**：随待发队列持久化、跨启动重试不变，后端去重回上次结果——**缺它则「请求已达、响应丢失」这一移动网络常态会丢玩家进度**，违「绝不回退存档点」 · 连带修订总则 7 的 `IProfileBackend` 两个返回类型（`ProfileSnapshot` / `PushAck`），服务门面新增只读 `BaseRevision`（`PullProfileAsync` **仍不外泄** `Revision`）。**② 进战斗前 flush 失败不挡玩家**：**flush 是一次「尝试」，闸门是一个「状态」**——`Immediate` 声明的是「不等防抖」，不是「必须成功」；四条既有定案（不阻塞玩家从不按 `PushPolicy` 分叉 · 「不打断进行中的事件」· `SelectCost` 不回滚 · D0 不参与闸门判定）独立指向同一答案 · 连带：战斗后闸门自然对齐 · 滞留计时不因战斗暂停（**是正确行为**）· 该 flush 点的意图 = 长区间前尽力送出已有变更。**③ UX 两项签核**：进战斗前失败**不加额外提示**（由常驻「离线 · 待同步 N」承担，**须在战斗屏内可见**）· 设置屏只读「同步版本 #N」（诊断展示，不进玩法路径） | distilled | systems/services/sync-service.md, systems/architecture.md, systems/services/account-service.md, systems/services/combat-service.md, systems/services/life-cycle-service.md, ux/screen-flow.md, ux/combat-ux.md, backend-design-documents/open-questions.md |
| [2026-08-06d-combat-open-questions-mass-closure](2026-08-06d-combat-open-questions-mass-closure.md) | 2026-08-06 | **战斗待答清单的一次性收口：38 条全部答结**。**⚠ 治理：两条 08-06 裁决相撞，取本次为准** —— 意图三档阈值**整体收紧一级**（ch1 `≤−2` / `−1~+1` / `≥+2`；ch2 · ch3 `≤−1` / `=0` / `≥+1`），**赋级带回退三章统一的对称 `±2`**；**08-06 / 08-06b 的 `[−4, +2]` 非对称带与「降阶 = 碾压」硬门整体作废**（连带作废「ch1 落差只在领先侧拉宽」「ch1 黑箱只集中在末两级」及其补偿之问）；「碾压」重定义为「压到带内下界」；**代价有意接受** —— ch2 · ch3 约五分之二的同阶遭遇全黑箱，「仅类别是常态」只在 ch1 成立。**① 赋级带与意图**：`±2` 是**无例外硬规则**（赋级函数不接受区间覆盖参数，绝境感只能由卡组 / item / power / 遭遇参数表达）· **`lifeTotalLimit` 概念整体删除**（只跟踪单值、无截断，境界基线改由**一次性跃升**承载：`≈ceil(1.1 × 最坏落差)` → 10 / 25 / 40，配套要求回复幅度随境界跳档）· 赋级分布 = 三段权重表 × 调制修正 × 截断重分配 × 批内去重 · `IntentCategory { Offense, Defense, Buff, Special }`（**以道念语义定义、内容侧静态标注、20% 贡献阈值选主类别、完整档一个数字位靠符号区分**）· 等级措辞 = **境界名 + 层级、全局序不上 UI、不做方向标记** · 物化 = **五旋钮管线**（等级先定、**卡组不叠第二条强度曲线**、关键卡牌为可机械检查的改写上界）· **剧情线不可调制模板，改由地点 / 剧情线专属池表达**（PlotManager 权力收敛为选池 / 偏权重 / 拧参数）· **`EnemyData` + `EnemyInstance` 统一定名，嵌 `EventOption` 落存档，本作不存在多敌人场景（字段单数）**。**② 存档与结构**（公理：**决策点 = 战斗状态机唯一可以停下来的地方**）：`ActiveCombat` 挂 `CharacterProfile` 可空块（战场单表 + `kind`、栈数组序即栈序、`pending` 全局至多一个、道具只落本场配额、**`Power` 运行态 = 战场条目的 `counters`**、**闭集不变式作为读档断言**）· 挂起态**恢复回到该选择点、不允许反悔** + 硬要求「选目标态必须自解释」· 需选目标的触发式异能 **≤10%**（稀少改变的是性能预算不是正确性要求）· **D0–D6 决策点清单（保留 D2）**、约 31 点/场 · **`attemptIndex` 整层删除**（篇章重试 = 重开一局不是重打这一局）· 取消只有**两个真触发方**、**`ct` 只在决策点被观察**（取消点与存档点永远重合 ⇒ 对齐问题不存在）、新增 `AdvanceStage.Cancelled`、**退出静默处理** · **processor / handler 的下沉判据**（形态而非职责；三条与门 + 开放 kind + 三条反判据；**第四五级暂不落实例**）。**③ 卡牌与规则**：次类型 `CardSubtypeData` schema + `<maintype>.<name>` id 规范 + 准入判据 + 17 条初值（**`Ambush` → `enchantment.ambush`**）· 起手 5 / 每回合抽 2 / 上限 10 / **不设先后手差**（本作先手 tempo 优势根本不存在）/ **敌人侧完全同值** · **生命周期三件套**（`EntryLifetime` × `CountdownSide` × `RemainingTurns`，「持续到下回合结束」是组合而非新档；**`Indefinite` ≠ 永久物**；卡面须点明归属方）· **非永久条目可被针对但效果须显式声明目标类别** · 效果系统三层 + **加法层 + 乘法层且「加法先于乘法」是规则** · **承重：本作不存在「参战方身上的 buff 列表」** · `CardInstance` 运行态判据 = **有无过期时刻**（集合性效果一律走战场）· **共用 `CardData` 体系、不共用卡池**（`Pool` 必填无默认）+ 敌人卡组固定 15 允许重复 · **探查升一档、不打穿越阶黑箱**（规则层结论，其余细节仍搁置但不再挂待答）· `manaLimit` 幅度**恒为 1** + 分档表（闭关主通道、**战斗类不给**）。**④ 奖励与遭遇参数**：胜侧 = **两条支路**（`1:1 × 可调单价` + 归一化 `advantage` 三档）· 可选奖励**固定 3 项、预先算定并落存档、不重抽、不设放弃通道** · **遭遇参数落 `EncounterSpec`**（改 `sealed record`，`IsFinale` 收编为 `EventType`，胜负判据参数化为两个数）· Practice 8/`(0,false)` · Combat 10/`(1,false)` · Finale 12/`(N,false)`（**难度旋钮 = `WinMargin`，回合数 = 节奏旋钮**；**Finale 失败不直接 defeated**，走既有 `LifeTotalExhausted`，失败后可再挑战；⚠ 四重压迫叠加，先砍 `WinMargin`）· Practice 对手 = **共享同一批 `EnemyData` + `EncounterScopes`**（承重论据 = 图鉴的教学路径）· **enemies 升为 `systems/enemies/`，与 adventure-event 平级** · 四个 API 类型字段全给 + **`MomentumDelta` 四字段承载声明量 vs 实际量** + **新增 `ProvideTarget` 补上目标回传缺口**。**⑤ 数值进程与呈现**：经验阈值由事件总数反推、**境界间重置量纲**（「进阶即归位初期」的直接推论）、`ExperienceGrade` 四档、失败 50%、**承重推论：Finale 之前必须已升满**（⇒ Finale 不供经验，出现条件 = 已达境界巅峰）· 75% 覆盖率 + 档位偏置（**与 location 类型修正自然咬合，不为 location 再加经验字段**）· **ch2/ch3 升级稀疏由常驻经验条补偿**（同时答复「中长期规划感」的进度那一半）· 道具折价**按 `Charges` 分层** + 法则闸门 **单条 ≤10% / 老账号合计 ≤25% / 不得随对局累积**（**ch1 前段只能是纯信息便利类**）· **1% 分母 = 战斗类遭遇数 ⇒ 每 3 轮回一次**（R1–R5 编排规则，**仅一个轮回级布尔位落代码**）· 储物袋不进主菜单 + 滚动网格 + **同 `Id` 可持有多份** + 战斗内视图定名**「随身」** · 选目标态：**唯一合法目标时不进入该态**（规则层，削掉一半进入次数）+ 单点即确认 + `还有 N 项待结算` + 三条禁令 · 偏差呈现：`≈` + 虚线 + 飘字 + ticker + **偏差对照标记**（加速对该步无效）· **意图区固定预留高度 + 敌人回合复用为 ticker**（同一信息槽的两个时态，**明确修订「不换成其他指示」**）· 图鉴五项写作规格（**不含阿拉伯数字**、弱点必须可行动）+ 关键卡牌 3 张显式标注 + 动态页眉 + **战斗开始即可读全部五项**。**新增待答 0 条**（残留按主题重排，多条标注已归 ch1 数值标杆专场） | distilled | terminology.md, systems/enemies/（新建 _index.md + common-properties.md）, systems/（_index.md, architecture.md, balance.md, scoring.md, game-progression.md）, systems/adventure-event/combat/（_index.md, common-properties.md）, practice/_index.md, finale/_index.md, systems/services/（combat-service.md, life-cycle-service.md, sync-service.md, future-event-service.md）, systems/character-profile/（_index.md, life-total.md, mana.md, deck/_index.md, item/_index.md）, systems/player-profile/（player-power/_index.md, codex/enemy-codex.md）, ux/（combat-ux.md, screen-flow.md）, open-questions.md, open-questions/（01-combat.md, update-log.md）, answer-logs/（log-combat-solutions.md, _index.md） |
| [2026-08-06c-skip-channel-removal-priority-two-tier-and-location-codex-edges](2026-08-06c-skip-channel-removal-priority-two-tier-and-location-codex-edges.md) | 2026-08-06 | **eventOptions 专场第二场：跳过通道整体移除（共答结 5 条）**。**① `LocationCodex` 记连边（承重）** —— 词条记「它通向哪些地域」，**跨轮回重建整张 `locationMap` 是设计目标（知识 = 力量）而非泄露**；推论：`locationMap` 的不可见是**「初见不可见」而非「永远不可见」**（地图长在图鉴里不在 HUD 上）· **「中长期规划感」的地理那一半就此落地**（闸门给多个目的地 ＋ 图鉴给每个目的地的去向 ⇒ **能提前两步规划路线**）· **它是六本图鉴里唯一词条间有拓扑关系的**（存档仍是 id 集合，呈现必然是一张逐步显影的图）· **图的稳定性升格为对玩家的隐性承诺**。**② `skipCost` 概念整体移除。** **③ 跳过通道与 `ifMandatory` 整体移除（承重 · 推翻既有建制）** —— 理由是它本就冗余：**每次选择后 eventOptions 整批重算 ⇒ 选中一个即等价于跳过其余**；**设计意图反而更强**——「打不过也得打」**升级为结构性事实：本批每一项都是必做项**，不需要字段表达。**一次删掉五处结构**：`EventOption` 九字段 → **七字段** · `EventOptionBatch` 删 `AnySkippable` 与恒真不变式 · **`AdvanceMode` 整个枚举删除** · future-event-service 五方法 → **四方法**（`TryRefill` 删除）· `CapabilityFlag` 删 `ShowSkipCost`；**连带消解一整族待答**（整批全跳 · 付不起 `skipCost` · `pastEvent` 两种痕迹 · 补位落空 · 批次存续期资源下降）。**④ 付不起必做项 `selectCost` = 照付 → 判定 → 判负进失败流程（承重 · 推翻明文流程）** —— **推翻「付不起则拒绝、不产生任何写入」**；**死锁在规则层不成立且不靠产出侧保证闭合** · **终态由支付后的状态给出，不由「付不起」这个事实给出** · **UI 不设置灰态但须如实展示 `selectCost`**（「明知是死路仍然走」是有意义的决策）· **事务性与可负担性校验被拆开**。**⑤ `eventPriority` = 两档（0 / 1），future-event-service 独占置位、PlotManager 不可改** —— **推论：PlotManager 只调内容不调约束**（剧本的强制性只能靠收窄候选池表达）；**选择约束至此只剩一条轴**，全库「两条约束轴」表述作废。**新增待答 5 条**：记连边的显影粒度（承重）· 资源打穿的截断与终态判据（承重）· 「余额不足即拒」还剩哪些消费点 · 未被选中的选项是否归档进 `pastEvent` · `Priority` 是否退化为 `bool` | distilled | terminology.md, systems/adventure-event/（common-properties.md, travel/_index.md, travel/common-properties.md）, systems/services/（future-event-service.md, life-cycle-service.md, profile-service.md, combat-service.md, _index.md）, systems/architecture.md, systems/game-progression.md, systems/balance.md, systems/player-profile/（codex/_index.md, player-power/common-properties.md）, program-overview.md, system-overview.md, open-questions.md, open-questions/（02-event-options.md, 06-meta-progression.md, update-log.md）, answer-logs/log-0806b.md |
| [2026-08-06b-asymmetric-ch1-band-consented-power-loss-and-chapter-retry-shape](2026-08-06b-asymmetric-ch1-band-consented-power-loss-and-chapter-retry-shape.md) | 2026-08-06 | **08-06 三条 ⚠ 承重裁决项的收口（共答结 5 条）**。**① ch1 赋级带取非对称 `[−4, +2]`，三章带边界全部是内容配置** —— 放宽的动机全在下界，上界的 `+4` 才是落差元凶；**⚠ `lifeTotal` 算术冲突随之消失**（最坏落差回到 **9** < 炼气 10/10，**炼气 `lifeTotalLimit` 不必抬基线**，「一次惨败不打穿耐久」三章全部由规则层封住）；**ch1 的落差只在领先侧拉宽**（领先至多 6 / 落后至多 9）⇒ 第一篇章是「碾压侧更宽、被压侧不变」的一章，新手期得到的是更多「打得动」而非更多「被打穿」；**「越阶只出现在境界末两级」恢复为三章统一的一句话**（上午的「ch1 末四级」「`[−17,+17]`」「ch1 等级差最富变化」三条推论作废）；**ch1 的「完全无信息」档同境界内不可达**（`+2` < `+3` 门槛）⇒ 黑箱全部集中在炼气十二层之后，**黑箱本身成为「你已站在突破前夜」的信号**；带边界是配置 ⇒ **分档退化为取值差异，代码不为分章写分支**。**② 降阶碾压不需要独立的呈现语言** —— 与境界内碾压共用同一套完整意图呈现，不加「不足为惧」式标注；**呈现层只认档位、不认档位的来源**（与「越阶黑箱不给替代线索」同一种克制，**两道硬门都不自我声明**），`CombatSnapshot` 因此不需要「碾压来源」字段。**③ ⚠ 法则不会被强制剥夺（承重）** —— 候选③（真剥夺）否决；采纳的是**按「玩家是否点头」把通道一分为二**：**真正从账号移除只发生在玩家自愿的「置换」中**（有对价），**其余事件一律降级为「本轮回禁用」、不从账号删除**，概率从内容侧限制。**推论：付费内容不会被游戏销毁**（免去一整类客诉与退款争议）· **三级严重度阶梯成形**（本场移除 < 本轮回禁用 < 自愿置换）· **置换是正向设计不是惩罚**（卡组构筑式取舍）· **「本轮回禁用」必须落轮回级状态**（账号级 `status` 开关不能承载）· **1% 的分子现在编排得出** · **`Power` 的「受保护」是三层语义**。**④ `chapterRetry` = 角色级三个具名字段 + 通关后保留计数 + 账号级另有统计计数** —— **⚠ ch1 死字段问题由「两层各司其职」化解**（角色级恒为 0 但无需有意义，「炼气段重开几次」由账号级回答 ⇒ **07-30b 的 ch1 重试语义不必改写**）；**两层口径不同、不是同一个数的两份拷贝**（角色级是闸门输入，账号级是纯读数）；具名字段与「四境三篇章」这条硬事实对齐；**通关后保留 ⇒ 它是历史不只是配额**；**账号级统计计数是 `PlayerProfile` 上新的一类字段族**（与 Achievements 相邻但不同：成就有奖励，统计是纯读数）。**新增待答 5 条**：「本轮回禁用」的承载字段与生效面（承重）· 置换的候选池与对价规则 · 账号级统计计数的形态与范围 · 带边界作为内容配置的落点 · ch1 黑箱只在末两级是否需内容侧补偿（倾向不补） | distilled | systems/balance.md, systems/services/（future-event-service.md, life-cycle-service.md）, systems/adventure-event/combat/_index.md, systems/character-profile/（_index.md, life-total.md）, systems/player-profile/（_index.md, player-power/_index.md）, systems/monetization.md, systems/game-progression.md, ux/combat-ux.md, open-questions/（01-combat.md, update-log.md）, answer-logs/log-0806_2.md |
| [2026-08-06-ch1-band-widening-cross-realm-crush-and-chapter-retry](2026-08-06-ch1-band-widening-cross-realm-crush-and-chapter-retry.md) | 2026-08-06 | **08-05 遗留裁决项的收口（4 条）**。**① ⚠ `±2` 带 × 意图阈值冲突 → 取向 = 调赋级带、不调阈值**：**ch1 赋级带放宽为 `±4`（ch2 · ch3 仍 `±2`）** ＋ **新增「降阶 = 碾压」硬门**（敌人境界更低 ⇒ 一律完整意图，与「越阶 ⇒ 完全无信息」**对称的一对硬门**）；三处 `diff` 门槛原样保留 ⇒「完整意图 = 碾压专属」不必重新定义。**ch2 · ch3 的碾压档由降阶硬门成立**（篇章开头刚突破进新境界时 `−2` 正好探进下一层境界，**规则与「刚突破的修士回头看上一境界如同儿戏」自动吻合**）；**ch1 之所以单独放宽，是因为炼气是最低境界、拿不到这道门**——分档不是随意的。**结构性推论**：碾压档向篇章头部集中、越阶黑箱向尾部集中 ⇒ **一个篇章天然有一条「开局看得清 → 收尾看不清」的信息曲线**；ch1 越阶范围从末两级扩到末四级；ch1 开局落差区间从 `[−9,+9]` 扩到 `[−17,+17]`。**⚠ 代价**：`+4` 侧把 ch1 最坏落差抬回 **17 > 炼气 `lifeTotal` 10**，`±2` 当初正是为封住它而立——**推荐非对称 `[−4, +2]`，但未擅改**。**② 1% 的分母 = 全部 event，且失去法则不限于战斗** —— **带出此前没有的结构：移除 `Power` 有两条通道**，战斗内 `IgnoresProtection` 只作用于**本场**（Profile 不动），**事件 outcome 侧的负向条目是持久的**（写 Profile，需新 element 形态）；连带 **`Power` 的「受保护」语义须分层表述**（`IsProtected` = 战场可针对性，持有权保护是另一回事），1% 的校验面从战斗内容**扩到整个事件池**。**③ sync 缓冲闸门口径 = 事件级存档点**（不含决策点存档）—— 答结方案草稿暴露的张力（决策点 ≈31 点/场，旧口径一场战斗第三个决策点即弹软阻塞）；**闸门计 push 单位、不计本地写入单位**，**「决策点粒度决定 push 防抖压力」这句表述作废**。**④ `attemptIndex` 整层删除，改由 `CharacterProfile.chapterRetry` 承载** —— 篇章重试 = **换一套新的随机流**（非再派生一层），**「重开一局」说的是随机流不是角色**（ADR-0004 的「篇章继承 = 全部继承」不变）；`chapterRetry` 计数 ch1–3 各自重试次数，**是计数器容器、不是上限持有者**。**新增待裁决 3 条**：⚠ ch1 对称 `±4` vs 非对称 `[−4,+2]` · ⚠ 事件侧移除法则（PlayerPower）是否永久剥夺（部分来自 premium bundle，这条不定则 1% 的分子无法编排）· ⚠ `chapterRetry` 的 ch1 计数挂在哪（现状语义下角色级恒为 0，是死字段） | distilled | systems/balance.md, systems/services/（future-event-service.md, sync-service.md, life-cycle-service.md, combat-service.md）, systems/adventure-event/combat/_index.md, systems/player-profile/player-power/_index.md, systems/common-properties.md, systems/character-profile/_index.md, ux/combat-ux.md, open-questions.md, open-questions/（01-combat.md, update-log.md）, answer-logs/log-0806.md |
| [2026-08-05b-location-fields-event-count-limit-and-skip-refill-closure](2026-08-05b-location-fields-event-count-limit-and-skip-refill-closure.md) | 2026-08-05 | **eventOptions 专场第一场：location 升格为带字段的内容条目 + 跳过 / 补位的产出侧闭合**。**① location 携带三组字段** —— **事件类型出现概率修正**（**软**框定：改权重不改可及性 ⇒ **「按地点分池」的旧措辞收窄**，硬分池只发生在敌人那一侧）· **一组特定的 `EnemyTemplate`**（**硬**框定取池）· **`eventCountLimit`**（事件容量上限）；**承重推论：敌人物化的两条轴至此正交** —— **location 决定「派谁来」、角色等级 ±2 决定「有多强」**（答结「物化时充实 / 改写规则」中取池的那一半），且 location 已具备内容条目形态（稳定 `Id` / ContentRegistry / `ContentEnabled` / overlay），**地域的生态与风味不需要另设机制**；**② `eventCountLimit` 达成 → 本批收窄为仅剩 Travel**，**承载机制无需新增**（Travel 以**最高 `eventPriority` + `ifMandatory`** 出场，**这是「一批可以全部 mandatory」的第一个真实用例**）→ **Travel 由可选路由升格为结构性闸门**、**进程形状清晰化（一次篇章 = 若干 location 的串联，之间由 Travel 缝合）**、`eventCountLimit` 成为**与 `lifeSpanCost` 互相约束的第二个时长旋钮**；计数口径推论 = 只计「选择进入并结算」的事件、跳过不计入；**③ 跳过语义的两条残留细节由两条产出侧保证一次性闭合** —— **不生成付不起 `skipCost` 的事件**（判定在每一次物化，含 `TryRefill` 补位那一次）+ **不生成整批全跳的 eventOptions**（每批至少一个 `IsMandatory` ⇒ `EventOptionBatch` 多一条恒真不变式、**批次不可能被跳空**）；**二者都是产出侧约束而非消费侧处理**，问题在源头消解、下游无需分支；**④ 补位落空的判据 = 当前 location 的 `eventCountLimit` 用尽**（不是事件池耗尽 / 剧本约束）→ 与 ③ 合力使**死局兜底问题不再成立**，`TryRefill` 的 `false` 语义由「运气不好」变为「本地域已满，该走了」。**反向抬高：付不起 `selectCost` 的死锁更承重**（每批必有不可跳过项，`skipCost` 已给对称先例但用户只对 `skipCost` 表态）。**流程**：`pastEvent` 痕迹 schema 等走 `/provide-solution-draft`；**event odds 具体数值归内容制作阶段**。**追加拍板（08-06）五条**：**⑤ `locationMap` = 一张全局不变的地域图（新概念）** —— 连边**既不挂 Travel 内容条目、也不运行时算**，**三个篇章共用同一张、future-event-service 高频只读** ⇒ **location 不随篇章变化**（篇章难度差异由**赋级带**承载而非换图，与「等级序是直尺、鸿沟归 `baseMomentum`」同一分工）· **熟悉度成为跨轮回资产** · **工程形态 = 只读静态数据、启动加载常驻、服务只读不写、存档只存当前 location `Id`** · **对玩家不可见**（不给俯瞰图这条不变）；**⑥ `LocationCodex` = 图鉴族第六本（新概念）** —— **不可见 `locationMap` 唯一的显影通道**，「去过即记」，**世界地图靠多次轮回一格一格拼出来** ⇒ **给「中长期规划感 / 方位感来源」这条长期待答提供了第一个具体候选**（方位感来自跨轮回知识而非轮回内俯瞰视图），图鉴族五→六本；**⑦ 闸门给多个并列 Travel 目的地**（⚠ 单数措辞取宽松读法）⇒ **逐批择一的线性进程里唯一一个带地理含义的分岔点**，与 ⑥ 合起来使**跨轮回知识增长直接转化为轮回内决策质量**；**⑧ `eventCountLimit` 只计「选择进入并结算」**（跳过与 Travel 均不计）⇒ 跳过的完整定位 = **只在 `pastEvent` 留痕，任何资源与进度刻度上都不计**；**⑨ 「每批必有不可跳过项」是设计意图不是死锁** —— **打不过也得打，没能承压就输掉这局是合意结果**，**不给**产出侧可负担 / 可战胜保证（「输」是本作的正常出口；反向约束产出侧不要过度保护）。待答：location / `locationMap` 的数据载体 · **`LocationCodex` 记不记连边**（记 = 玩家可跨轮回重建整张图，是目标还是泄露）· 闸门候选数量与抽取规则 · 类型修正的运算形态 · 付不起必做项 `selectCost` 的终态 · 定稿批次存续期间资源下降的态度 | distilled | terminology.md, systems/game-progression.md, systems/services/future-event-service.md, systems/adventure-event/common-properties.md, systems/adventure-event/travel/（_index.md, common-properties.md）, systems/balance.md, systems/player-profile/（codex/_index.md, codex/common-properties.md, _index.md）, systems/_index.md, art/visuals/_index.md, open-questions.md, open-questions/（02-event-options.md, 06-meta-progression.md, deferred-content.md, update-log.md）, answer-logs/（log-0805b.md, log-0805b_2.md） |
| [2026-08-05-level-band-stack-save-and-token-free-deck](2026-08-05-level-band-stack-save-and-token-free-deck.md) | 2026-08-05 | **敌人赋级重定义 + 四条承重残留答结**。**① 对手等级上下限 = 角色当前等级 `±2`**（全局序 1–22 上截断）—— 从 08-03「按境界给的绝对上界」改为**相对 `diff` 的对称带**，且**首次给出下界** → **答结挂了两天的 ⚠ 赋级上界与 lifeTotal 算术冲突**（最坏开局落差 19 → 境界边界处 9，落在 `lifeTotal` 10/10 之内，**规则层自己封住「一次惨败打穿耐久」**，内容侧纪律退为第二道防线）；**推翻**「上界档必然越阶 ⇒ 最难即最不可读」；**新增结构性推论：越阶遭遇只可能出现在每个境界的最后两级**（12·13 / 16·17 / 20·21），压迫感自动向篇章尾部集中；赋级规则**挂 Enemy 不挂事件类型** ⇒ Combat / Practice / Finale 一视同仁。**② 天劫同受此约束**（连带答结「天劫是否天然大幅越级」）—— 篇章末角色在境界巅峰（13/17/21）、下一境界初期为 14/18/22，**`diff` 恰为 +1** ⇒ **不是大幅越级，但天然越阶 ⇒ Finale 全程无意图信息**；「渡劫 = 突破到下一境界」不需要任何规则口子。**③ 栈必须落存档**（**推翻 08-03 推论 ⑤**）—— 依据 = **触发式异能在栈上若需选目标，那次选择本身就是决策点**，故「可退出时刻栈必为空」不成立；连带 **结算不是原子的同步过程**（`RunCombatAsync` 的结算循环须可挂起、可从中途恢复）、**08-02b 的「决策点回落到回合 / 出牌这一级」被推回一层**；**不重新引入交互**（目标选择是结算自身的一部分，不是响应机会）。**④ 埋伏进敌人卡池但不计入意图** —— 玩家侧的「敌方有一张埋伏」计数指示由条件项**升为必做项**；连带确立判据 **「凡不在敌人自己回合发生的东西一律不进意图」**；埋伏是与快照偏差性质不同的第二个不确定源（「根本没说」vs「说了没做到」）。**⑤ `IgnoresProtection` 配额 ≈ 1% 的游戏场景** —— **出现频次**口径而非条目占比口径，故**无法加载时机械化校验**，`PushWarning` 逐条列举不变、1% 落在内容编排侧。**⑥ 不会有凭空生成的牌（全局硬约束）** —— 以**否定前提**答结 08-03 的「落空时凭空生成的牌去哪」；**承重连带：一场战斗内的卡牌集合是闭集**，存档只需各区 `Id` 序列 + `CardInstance` 运行态（存档面净减），不借入 MTG 的 token、`CardType` 六分不需要第七类。**新增待裁决：⚠ `±2` 带与意图揭示阈值的算术冲突** —— ch1 完整意图档**不可达**、无信息档只能靠越阶，ch2·ch3 完整档只剩 `diff = −2` 一个取值，而完整意图的定位正是「碾压专属」。其余待答：`±2` 是硬规则还是默认带 · 境界边界残余量纲 · 挂起态取消语义 · 选目标态的呈现形态 · 「1%」的分母口径 | distilled | systems/balance.md, systems/scoring.md, systems/character-profile/（life-total.md, deck/_index.md）, systems/services/（future-event-service.md, combat-service.md, life-cycle-service.md）, systems/adventure-event/（combat/_index.md, finale/_index.md, practice/_index.md）, systems/player-profile/player-power/_index.md, ux/combat-ux.md, open-questions.md, open-questions/（01-combat.md, update-log.md）, answer-logs/log-0805.md |
| [2026-08-04b-mtg-loanwords-card-types-and-intent-snapshot](2026-08-04b-mtg-loanwords-card-types-and-intent-snapshot.md) | 2026-08-04 | **MTG 借词第一批全部定名，并由定名连带落定四个体系 + 两条结构问题**。**定名**：`sorcery speed` **不借、整条删除**（机制原样保留，改由「**出牌时机（唯一）**：自己回合的行动阶段、栈为空时」这条全局规则表述）· 三步改称 **开始阶段 / 行动阶段 / 结束阶段**（`start step` / `action step` / `end step`，`main phase` 弃用）· **`resolve` = 结算**（专指栈上对象），**战斗收口那处改称「收口」`settle`**（一词两义消除）· `trigger` = 触发。**四个体系**：**① 卡牌类型六分** `Sorcery` 法术 / `Creature` 灵宠 / `Enchantment` 阵法 / `Item` 法宝·古宝 / `Power` 神通·法则 / `Affliction` 业障 —— **卡牌不再只有一个来源区**（卡组 / 储物袋中的道具 / 开局入场的 `Power`，三者绕开的东西不同）；**② 异能三分** 静止式 / 启动式 / 触发式（与「载体」正交；启动式的窗口 = 出牌窗口，**不重新引入交互**；抽为可复用的 `AbilityData`）；**③ 永久物** = 战场条目的**子集**，**永不被结束阶段清理**；**④ 次类型** = 稳定字符串 id + `.tres` 注册表（非 C# 枚举，须能被效果筛选引用），**埋伏 = 阵法的次类型**。**两条结构问题**：**意图 = 快照而非承诺**（公布后不重算，但不保证与执行一致；偏差**不做处理**，EnemyManager 因此不需要一致性校验与回退逻辑；代价 = 呈现层承担解释责任）· **战场与两个参战方 manager 的划线判据 = 「是否在场上生效」而非「属于谁」**（层级不动、单一战场记录 + `OwnerSide`、读侧走 `CombatSnapshot`）。**同轮答结 5 条**：借词定名 · 触发条件可跨归属方（埋伏由此成立，是唯一一条在对手回合发生作用的通道）· 法则能承载战斗内触发（combat-service 首次读 PlayerProfile；**允许但极其稀缺**，`InCombat` 法则 ≤ 1/5，付费战斗价值主要由**有次数限制的古宝**承载）· 意图不一致不做处理 · 战场边界判据。待答：埋伏是否进敌人卡池 / 是否计入意图 · 次类型清单与阵法灵宠的区分轴（留待日后）· `IgnoresProtection` 内容配额 · 道具折价系数 · 储物袋 UI · 战斗内道具与 `Power` 运行态的存档字段 | distilled | terminology.md, systems/architecture.md, systems/services/combat-service.md, systems/services/future-event-service.md, systems/character-profile/deck/（_index.md, common-properties.md）, systems/character-profile/item/_index.md, systems/character-profile/power/_index.md, systems/character-profile/life-total.md, systems/player-profile/player-item/_index.md, systems/player-profile/player-power/_index.md, systems/adventure-event/combat/_index.md, systems/character-profile/mana.md, systems/scoring.md, systems/balance.md, systems/monetization.md, ux/combat-ux.md, open-questions.md, open-questions/（01-combat.md, deferred-content.md, update-log.md）, answer-logs/log-mtg-loanwords-and-card-types.md |
| [2026-08-04-art-audio-library-scaffold](2026-08-04-art-audio-library-scaffold.md) | 2026-08-04 | **美术 / 音频设计库落位：新增顶层 `art/` 与 AI 驱动的资产生产流水线**。**一级分区 = 两个**：**`visuals/`**（已定；内含子分区 **`animations/`** —— **动画归视觉**，同属一条视觉线、继承 `art-direction.md`，**占位**待咨询专业人士，且**不走 AI 流水线故不设 `guides/`**）· **`soundtracks/`**（已定）。**人机协作三段流水线**：人给 vision + 参考素材 → AI 写 art / audio guide（= 结构化 prompt）→ 参考 + guide 一并投喂生成工具（视觉 = **Midjourney**；音频**倾向 Suno 但未定案** —— 故 prompt 正文须保持工具无关、工具专属语法不写死进模板）。由此推出的承重结构：**① 本库只承载 ①②（vision 文本 / 参考登记 / guide 文本），生成出的二进制资产归 `game-feature-branch/`**（design 是纯文档孤儿分支）；**② 总方向文档是每份 guide 的上游而非可选补充**——分次生成必然风格漂移，跨资产一致性只能由共同上游承担；**③ 每份 guide 绑定资产类目**（卡面 / 敌人立绘 / 事件插图 / 屏幕背景 / UI 元件 / BGM / 音效…）才能与 `systems/` 内容条目对齐；**④ guide 可迭代且须记录迭代结果**。继承既有约束：三国杀 / 弈仙牌画风 · grimdark 仙侠基调（明确不温馨）· 手机可读 · GL Compatibility · 竖屏。**本次只立脚手架，内容待填。同轮答结：动画归 visuals（一级分区确定为两个）；音频工具倾向 Suno 但未拍板。** 待答：音频工具最终定案、guide 粒度、资产落地命名规则、参考素材二进制是否入库、animations 范围与载体、AI 生成资产的商用授权口径 | distilled | art/**（新建 12 份）, vision/（references.md, pillars.md）, terminology.md, README.md, open-questions.md, open-questions/（deferred-content.md, update-log.md）, answer-logs/log-0804.md, .claude/rules/Context.md |
| [2026-08-03-battlefield-stack-hand-limit-and-power-item-naming](2026-08-03-battlefield-stack-hand-limit-and-power-item-naming.md) | 2026-08-03 | **战斗「场上」首次显式建模 + 四条承重残留答结 + power/item 中文重定名**。**① 引入 battlefield（战场）并新增 BattlefieldManager 与 StackManager 两个 manager** —— 战场记录场上全部准确数据（生效中的卡牌 / 持续状态 / **触发器注册面**）；**栈与战场是两个区**（栈 = 等待结算的队列、战场 = 已结算并正在生效的东西），结算路径 = 打出 → 入栈 → LIFO 结算 → 效果施加 →（若持续）落到战场 → **「回合内 / 跨回合状态」有了承载结构**、**TurnManager 回落为纯粹的回合状态机**、manager 由三增至五、决策点存档须能恢复战场、EnemyManager 规划意图须读战场、呈现层多一个区；**② 满手时抽牌抽不进**（牌留在抽牌堆，「加入手牌」同理落空；**不抽出即弃、不销毁**）→ 手牌上限是**纯上界**、**弃牌不是被规则强制的动作原语**、抽牌堆顺序不被扰动、满手的代价是 tempo 而非资源；**③ 触发式效果的载体开放**（牌上触发器 / 场上持续状态 / **神通 CharacterPower**，清单可再增）→ 需统一的触发注册面（坐在战场上）、压栈者恒为 StackManager、**轮回级能力必须能被战斗内读到**；**④ 道念下限 0 在每一次结算时截断**（溢出量不结转）→ 更保护落后方、**LIFO 顺序对最终结果有实际影响**、`PlayResult` 须带实际削减量；**⑤ 敌人赋级上界 = 高一个大境界的初期**（炼气 → 最高筑基初期；按**境界**给而非按 `diff` 给）→ 上界档必然越阶 ⇒ **必然完全黑箱**、Finale 天劫等级有了自然候选；**⑥ 中文定名重写 = 法则 / 古宝 / 神通 / 法宝**（PlayerPower / PlayerItem / CharacterPower / CharacterItem，**推翻 07-30b 的「`power` 通译 = 能力」**）→ 中文名不再表达层级对称、层级只在英文标识符上成立、图鉴族五本随之改名。**矛盾待裁决：⚠ 赋级上界与 lifeTotal 的算术冲突**（炼气一层对筑基初期 = 落后 19 > lifeTotal 10，恰是该上界要规避的情形）。其余新增待答：战场与参战方 manager 的边界、战场 / 栈的存档形态、跨归属方触发时点、法则能否承载战斗内触发、token 类「加入手牌」落空的去向、`CharacterItem(s)` 单复数 | distilled | terminology.md, systems/services/（combat-service.md, _index.md, future-event-service.md）, systems/architecture.md, systems/scoring.md, systems/balance.md, systems/character-profile/（_index.md, deck/_index.md, power/_index.md, item/_index.md）, systems/adventure-event/combat/_index.md, systems/player-profile/（_index.md, player-power/_index.md, player-item/_index.md, codex/_index.md）, systems/_index.md, ux/（combat-ux.md, screen-flow.md）, program-overview.md, open-questions.md, open-questions/（01-combat.md, 07-codex-monetization.md, update-log.md）, answer-logs/log-0803.md |
| [2026-08-02c-intent-threshold-inversion-and-aggregate-intent](2026-08-02c-intent-threshold-inversion-and-aggregate-intent.md) | 2026-08-02 | **意图三档的同阶阈值整体下移 + 意图改为回合级综合描述**。**① 新阈值（推翻 08-01b 的取值，篇章分档保留）**：**ch1** `diff ≤ -3` 完整意图 / `-2 ~ 2` 仅类别 / `≥ 3` 完全无信息；**ch2 · ch3** `≤ -2` / `-1 ~ 1` / `≥ 2`（**两端各收紧一级 → 后期境界内每一级差在信息面上更值钱**）；**越阶硬门与三档结构不变**；**② 语义翻转** —— 完整意图从「默认」变成**碾压弱敌专属的即时反馈**，**同级对局（`diff = 0`）只给类别且为有意为之、不做可读性补偿**，「略强 / 略弱」在信息上不作区分 → **第二档成为常态档**（其视觉语言与类别枚举由边缘升为承重）、**探查（probe）价值显著上升**、**意图揭示不再承担教学职能**；**③ 意图 = 回合级综合描述** —— 一个回合对手可打多张牌，故**数值 = 计算后合并的最终结果**、**类别 = 主类别并行陈列**，**不暴露张数与逐张分解** → **EnemyManager 的 AI 必须回合级一次性规划**（排除逐张即时决策）、**意图数值是声明量、与实际结算量可不等**（下限 0 饱和减法 + 触发入栈，与「意图削减量 vs 实际削减量」合流）；**④ 意图仅在玩家回合呈现，内容是敌人的下一个回合**（敌人回合内不呈现）→ AI 规划时点前移到玩家回合开始之前、意图是玩家回合内的常驻决策依据。**⑤ 意图即承诺** —— 公布后**不因玩家行动重算、也不刷新显示**（玩家可据它布局；**EnemyManager 不走响应式 AI 路径**，难度旋钮落在规划算法与卡组；UI 无需表达「意图已变更」）；**⑥ 敌人回合内意图区收起**（不留占位、不换指示）。新增待答：**承诺与执行不一致时如何处理**（计划中的牌无法照原样执行 → 跳过 / 降级 / 临场替换）、意图区收起后的布局稳定性 | distilled | terminology.md, systems/adventure-event/combat/_index.md, systems/services/combat-service.md, ux/combat-ux.md, open-questions.md, answer-logs/log-0802c.md, answer-logs/log-0802c_2.md |
| [2026-08-02b-stack-without-interaction-and-three-step-turn](2026-08-02b-stack-without-interaction-and-three-step-turn.md) | 2026-08-02 | **对同日 08-02 的范围收窄修订：stack 留下、交互与优先权去掉 + 回合三步结构**。**① stack 作为核心结算模型保留**（打出 / 结算两时刻、LIFO、触发与效果系统的骨架仍挂在这条轴上）；**② 交互与优先权传递整体移除** —— **instant（瞬间）类牌**、**栈非空时出牌**、**优先权轮转 / 让渡**全部不借，理由是它们**拉长对局时长、决策点过多、复杂度高而玩法深度收益小**（**推翻 08-02 的「响应窗口一并借入、回合是交互式的」**）；连带 → **「我打完换你打」的简单交替恢复成立**（TurnManager 无优先权内循环）、**「定长 = 每场时长可预测」恢复成立**（无须响应次数上限 / 计时护栏）、**决策点粒度不必覆盖响应窗口**、**EnemyManager 代理面回落**（只在自己回合决策）、**卡牌不多出「能否响应」一维**、**所有牌都是 sorcery speed**；**③ 回合结构 = 三步**（**起始步** 归属方 mana 恢复至 `manaLimit` → 触发「回合开始时」→ 抽牌 ／ **主阶段** 唯一出牌阶段、只有归属方出牌 ／ **结束步** 触发「回合结束时」→ 手牌上限弃牌 → 清理回合内状态），**去掉战斗步骤与双主阶段**；步内顺序是规则的一部分（mana 恢复在触发之前、抽牌在触发之后）。连带定案：**mana 恢复的是回合归属方的**（语义 = 「每次轮到我时刷满」，「响应用谁的 mana」随之消解）、**手牌上限这条规则被确立**（数值未给）、**抽牌时机 = 起始步**、**「回合内状态」成为正式的状态生命周期档位**、**没有 MTG 式攻击阶段**（道念全部经主阶段卡牌产出）。**追加拍板三条**：**④ 栈深由触发式能力入栈撑起** —— 在栈上的牌可以触发能力、**被触发的能力也进栈**，故**只打出一张牌栈深也可 > 1**（「主阶段连续压栈再统一结算」路线**不采用**，「栈非空不能出牌」对双方都成立；LIFO 因此有实际意义，**后触发的先生效**；连带 → 多个削减效果同栈**会真实发生**、**栈必须进入呈现层**）；**⑤ 三步是回合归属方的流程、双方不同时走**（回合开始 / 结束是**有归属方的时点**）；**⑥ 手牌上限是恒定不变式** —— 任何时刻不得超出、**无时间限制与必须弃牌机制**（**推翻首轮三步表的「结束步 → 手牌上限弃牌」**，约束点前移到抽牌等增手牌时刻 → 新增承重待答「满手时抽牌怎么办」） | distilled | terminology.md, systems/services/combat-service.md, systems/character-profile/（deck/_index.md, mana.md）, systems/adventure-event/combat/_index.md, systems/scoring.md, systems/balance.md, ux/combat-ux.md, vision/（references.md, pillars.md）, open-questions.md, answer-logs/（log-0802b.md, log-0802b_2.md） |
| [2026-08-02-momentum-conversion-reward-structure-and-mtg-stack](2026-08-02-momentum-conversion-reward-structure-and-mtg-stack.md) | 2026-08-02 | **把道念从「规则骨架」推到「可结算」**。**① 道念差 → lifeTotal 损失 = 1:1**（不隔系数 / 不分档 → 道念差成为通用刻度，战斗屏上的「落后 8 点」就是「输了掉 8 点」；**连带逼出承重问题**：`lifeTotalLimit` 须与 `baseMomentum` 量纲对齐，否则金丹段一次惨败打穿 10 点耐久）；**② 奖励计算归 combat-service，且「获取奖励」属于战斗流程**（分工 = **计算归战斗、施加归生命周期**；`RunCombatAsync` 尾部含奖励与玩家选择阶段，一次 `TryApply` 不变）；**③ 失败侧仍发 `baseReward`**，少数事件的**额外惩罚以负向条目包在 reward 内**、不另立结构（与 `ProfileChangeSpec` 带符号自洽 → 三档结算产物齐全）；**④ 奖励分两类** = 强制自动计入（例：经验）+ 可选由玩家择一（**参照 StS 战后奖励面板** → 战斗 UX 新增一屏、战后新增一个决策点）；**⑤ Practice / Combat / Finale = Balatro 的 small / big / boss blind**（**回合数与胜负条件都是遭遇参数**，标准 Combat = 10 回合 / 道念高者胜，Practice 更简单、Finale 更难 → TurnManager 长度来自遭遇配置、胜负判据可替换；借难度分档不借出现节律）；**⑥ 越级追分可能但很难**（境界差越大越难 → 确认 `baseMomentum` 跨度放大正为此）；**⑦ `momentum` = `>= 0` 的 Integer**（削减为饱和减法 → 需区分意图 / 实际削减量）。**追加拍板四条**：**⑧ 1:1 不设上限截断**——「一次惨败打穿耐久」**由内容设计侧规避**（约束转移为 future-event-service 的**赋级上界**，`lifeTotalLimit` 无需与 `baseMomentum` 同量级）；**⑨ 奖励选择不是决策点**（奖励**预先算定**，退出重开得同样选项 → 候选生成须落在战斗的确定性边界内）；**⑩ 经验值 `experiencePoint` 是新字段**（每级一个经验阈值、事件**发经验而非直接给等级**、达阈值才升级 → **推翻 08-01 的「事件 reward 直接给等级」**；等级产出因此可细碎连续、「失败给得少」有自然表达）；**⑪ stack 连响应窗口一并借入 —— 回合是交互式的**（结算前对方可插牌 → **推翻「双方各 5 回合、我打完换你打」的简单交替**；TurnManager 多一层「优先权在谁」的内循环、EnemyManager 代理面扩大、卡牌多出「能否响应」一维、「定长 = 时长可预测」被削弱、决策点须覆盖响应窗口）。新增方向：**card / deck / combat 体系大量借用 MTG 术语**（借结算模型与术语，仍规避其胜负模型与 mana 曲线；须在 terminology 登记）。流程：**数值标杆整体推迟到内容横向扩展阶段的「ch1 数值模型」专场**（切入点 = starter deck 设计）+ **优先打磨 ch1 内容** | distilled | terminology.md, systems/scoring.md, systems/balance.md, systems/game-progression.md, systems/services/（combat-service, life-cycle-service, future-event-service）, systems/character-profile/（life-total.md, deck/_index.md）, systems/adventure-event/（combat/_index.md, practice/_index.md, finale/_index.md）, ux/combat-ux.md, vision/（references.md, scope.md）, open-questions.md, answer-logs/log-0802.md |
| [2026-08-01b-abstraction-levels-combat-numbers-codex-family-and-monetization](2026-08-01b-abstraction-levels-combat-numbers-codex-family-and-monetization.md) | 2026-08-01 | **08-01 道念改写的直接续篇：补齐战斗数值与规则骨架 + 三项新增设计**。**① 战斗定长 10 个回合**（双方各 5，打满比道念；**不设提前终止** → TurnManager 变为固定长度循环）；**② 道念的规则骨架**（**由卡牌产出 · 可互相削减 · 下限 0 · 起始道念 = `baseMomentum`(自身等级)** → 等级差直接变成开局起跑线差，越级风险可计算）；**③ 胜利侧也读道念差** → 奖励厚度（道念差成为双向刻度：胜给奖励 / 负扣耐久）；**④ `life` → `lifeTotal`**（**归 0 = defeated**，与寿元归 0 并列的第二条终结路径；**经 event 恢复**；`DefeatReason.CombatLost` **作废** → `LifeTotalExhausted`；`currentHealth/healthLimit` → `lifeTotal/lifeTotalLimit` 待确认）；**⑤ 意图三档分界值给全** = **越阶硬门 + 同阶差值门槛**（越阶一律黑箱；ch1 1–3 仅类别 / >3 无信息，ch2·ch3 =1 仅类别 / ≥2 无信息；**推翻篇章容差表述**；`diff=3` 为原文空档）；**⑥ 敌人等级的来源** = **`EnemyTemplate`（静态数据 + 样本卡组）+ future-event-service 物化时充实赋级**（模板 ↔ 实例通则的第三个实例；等级随物化产物定稿落存档）；**⑦ 全局等级序基数** = 连续 **1–22 不留跳变**（境界鸿沟改由 `baseMomentum` 承载）+ `baseMomentum` 表（炼气 1–12,15 / 筑基 20,24,⟨缺⟩,32 / 金丹 45,55,⟨缺⟩,75 / 元婴 100）；**⑧ 敌人图鉴深度** = **五项文案**（背景 / 功法 / 运作方式 / 特点与弱点 / 关键卡牌）、**一次遭遇全解锁**（逐招式解锁否决；全为文案故不侵蚀越级黑箱、存档近乎 id 集合）；**⑨ 抽象层级放开到五级并定名**（service / manager / **module** / processor / handler；**`DeckManager` → `DeckModule`**，「不设第三级」作废）。新增设计：**图鉴扩为五个成族**（Enemy / CharacterPower / PlayerPower / CharacterItem / PlayerItem，`enemy-codex/` 重构为 `codex/`）；**篇章目标时长上调**（30–40 / 35–45 / 45–55 分钟，熟练玩家口径 → `lifeSpanCost` 绝对水平须大幅下调、事件总数变多）；**首次给出商业化**（premium bundle：随机 1 PlayerPower + 2 PlayerItem + **重试上限 3→9 / 1→3**，使 ADR-0004 重试值由常量降为基线值）。矛盾待确认：ch1 `diff=3` 空档、筑基 / 金丹 `baseMomentum` 各缺一档、`CharacterPower` 为全新概念 | distilled | terminology.md, systems/architecture.md, systems/scoring.md, systems/balance.md, systems/game-progression.md, systems/monetization.md（新增）, systems/character-profile/（life-total.md 由 life.md 改名并重写, _index.md, deck/, item/）, systems/adventure-event/combat/**, systems/services/（_index, combat-service, future-event-service, life-cycle-service, plot-manager）, systems/player-profile/（codex/** 由 enemy-codex/ 重构并扩族, _index.md, player-power/, player-item/）, systems/common-properties.md, systems/_index.md, ux/combat-ux.md, vision/scope.md, decisions/ADR-0004, program-overview.md, open-questions.md, answer-logs/log-0801b.md, .claude/（rules/Context.md, knowledge/architecture.md, knowledge/systems/_index.md, knowledge/autoloads/_index.md） |
| [2026-08-01-momentum-scoring-lifespan-tuning-and-failure-payoff](2026-08-01-momentum-scoring-lifespan-tuning-and-failure-payoff.md) | 2026-08-01 | **玩法循环整体评审后的逐条裁决**。**① 道念 = 计分 = 胜负判据**（momentum = 胜利点数，**战斗胜负 = 道念高者胜**，**推翻**「life 归零」；**life 重定位为战斗外耐久 / 失败惩罚承受量**，只在失败结算时按「角色道念 − 敌人道念」扣减，战斗过程中不参与；「life + mana 双资源」表述**全部推翻** → 战斗内 = **mana（出牌）+ 道念（计分与胜负）**；炼气基线 10/10 · 5/5 数值不变；**空白的 `scoring.md` 由此填满**；`CombatResult` / `CombatSnapshot` / `PlayResult` 需含道念，战斗 UX 主视觉 = 双方道念对比）; **② 寿元定价 = 时长旋钮**（`lifeSpanCost` 由**目标时长** 15–30 / 15–30 / 20–40 分钟反推分档，**预算不变、逐篇章上调**，闭关 Research 更耗；**剩余寿元跨篇章结转**；**内容侧写正数量值、future-event-service 物化时取负** —— 与 `ProfileChangeSpec` 带符号约定**各自成立**）; **③ 隐藏属性跨档给一条定性叙事**（数值仍隐藏、只在跨档触发、复用 `eventEnd` 无新结构；**寿元告警改两段式** 30% 定性 / 10% 红字；Reigns「可见仪表」的替代路径 = 可感知但不可测量）; **④ 等级成长 = 事件产出**（不只 Combat / Practice，**失败也可能给**）+ **敌人等级在 eventOptions 上精确标注**（模糊危险度档位**否决** → 答定「等级差是否可见」= 可见，越级挑战成为可主动选择的风险维度）; **⑤ 失败侧首次有产出**（**EnemyCodex 遭遇即记**、不必击败；**道统残卷** = 失败累积的「下次轮回获得新 PlayerPower」**递增掉落概率**、获得即重置，**不发账号级货币**）; **⑥ 两处「不做」**（跳过配额 / 递增 `skipCost` **不做** —— 跳过只对可选事件开放、前提不成立；`manaLimit` 下界护栏与死牌转化**不做** —— 下降极罕见）; 「气机 / 状态层数造战斗节奏」提案**驳回**（归内容设计阶段）; 未裁决：无俯瞰地图 → 中长期规划感 | distilled | terminology.md, systems/scoring.md（重写）, systems/character-profile/（life.md 重写, mana.md）, systems/adventure-event/（common-properties.md, combat/**, practice/**）, systems/services/（combat-service, future-event-service, life-cycle-service, plot-manager）, systems/balance.md, systems/game-progression.md, systems/architecture.md, systems/_index.md, systems/player-profile/（enemy-codex/**, player-power/_index.md）, ux/combat-ux.md, ux/screen-flow.md, vision/scope.md, vision/references.md, open-questions.md, answer-logs/log-0801.md |
| [2026-07-30b-combat-level-intent-and-decision-point-saves](2026-07-30b-combat-level-intent-and-decision-point-saves.md) | 2026-07-30 | **修行等级体系**（等级 = 境界内层级：炼气 1–13 层 / 筑基 · 金丹 初期·中期·后期·巅峰 / 元婴仅初期；篇章跨度 1→13 · 1→4 · 1→4；**突破后归位初期**；一切等级比较建立在**全局等级序** 1–13 / 14–17 / 18–21 / 22 上）; **敌人意图三档揭示取代「通常不揭示」**（默认揭示 → 超出篇章容差 = 仅类别 → 大幅越级 = 完全无信息**且不给替代线索**；容差 ch1 差 > 3 · ch2/ch3 高一级即降级；「敌人状态可读」补偿方案**否决**）; **探查 probe** = 主动付代价换当回合意图的第二条信息通道（定名已成，形态归内容横向扩展阶段**搁置**；「能力 / 道具授予窥视意图」为其授予形式）; **敌人图鉴 EnemyCodex**（账号级收集，归 PlayerProfile，新建 `player-profile/enemy-codex/`；**只给静态知识不给动态意图**，故不架空越级黑箱）; **combat-service 新增 CharacterManager 与 EnemyManager 平级**（共享参战方接口，差异在驱动方式：代理操作 vs 监听玩家操作；**EnemyManager 内部不再细分**）+ **DeckManager 降为参战方内部组件**（每个 character / enemy 各一卡组，**敌人也出牌**）; **Practice / Finale = 战斗变体**（推翻 Finale「独立结算而非战斗结算」；**渡劫对手 = 天劫，天劫是带定制卡组的 Enemy**；少部分非战斗形态待定制）; **事件过程按决策点落存档**（含战斗内；退出重进恢复同一局面 + RNG 状态，**`selectCost` 不回滚**；草案的事件 <10 / 篇章 <30 重试上限**未采纳**，重试仍按 **ADR-0004** 且计数只有「每角色的篇章重试」、第一篇章为随机起始角色不可指定重试）; **mana 每回合恢复至 `manaLimit`**（推翻「逐步恢复」）+ **`manaLimit` 由事件 cost / reward 推拉**、不随境界自动成长; **power 定译「能力」**; 连带：`attemptIndex` 防 re-roll 动机被决策点存档消解 | distilled | terminology.md, program-overview.md, systems/architecture.md, systems/common-properties.md, systems/game-progression.md, systems/balance.md, systems/services/（_index, combat-service, life-cycle-service）, systems/adventure-event/（combat, practice, finale）, systems/character-profile/（_index, mana, life, deck）, systems/player-profile/（_index, enemy-codex/ 新增）, ux/combat-ux.md, open-questions.md, answer-logs/log-0730b.md |
| [2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown](2026-07-30-claude-engineering-scope-enemy-manager-and-requirement-breakdown.md) | 2026-07-30 | **`.claude` 定位定案 = 工程层**（只承载①工程配置/规则 ②可复用技能；一切设计内容归设计库、在 `.claude` 内只被引用与轻描述；**冲突裁决按性质分流**——设计性以设计库为准 / 工程性以 `.claude/rules` 为准；**ADR-0005 范围由 `knowledge/` 扩到整个 `.claude`**，含 rules 与 skills）; **combat-service：`IntentManager` 降格并入 `EnemyManager`**（意图生成 = EnemyManager 内部职责，与敌人实例状态 / AI 行为选择同组件）+ **敌人意图通常不向玩家揭示**（对 StS「意图常驻预告」的有意背离，默认不进呈现层；例外条件未定）; **九类 AdventureEvent 各开一次专门 session**（流程意图，子文档在等各自专场而非被遗漏）; **寿元红字倒数细节定案 = 静态标注于 EventOption 选择界面**（平时静止、不做全局 HUD、不进战斗内）; **新增技能 `/breakdown-requirements`**（一份 FR → 同名文件夹内的可执行子需求 + 父验收标准覆盖映射表 + 父签核即覆盖子需求；补齐 design→code 闭环，新增 `broken-down` 状态与 `_TEMPLATE-sub.md`）; **open-questions 按焦点重组**（内容充实整体搁置 / 焦点转向系统机制细节：战斗机制 · eventOptions 生成流程 · 逐类型事件 · 隐藏属性与剧本 · 服务契约残留）; 顺手修正 combat-ux 头部「intent 预告」矛盾与 program-overview 的 `RunCombat` 过时签名 | distilled | decisions/ADR-0005-knowledge-thin-reference-layer.md, systems/common-properties.md, systems/services/combat-service.md, systems/services/_index.md, systems/architecture.md, systems/_index.md, systems/adventure-event/_index.md, program-overview.md, system-overview.md, terminology.md, ux/combat-ux.md, ux/screen-flow.md, requirements/_index.md, requirements/_TEMPLATE-sub.md(新增), README.md, open-questions.md, answer-logs/log-0730.md, .claude/rules/Context.md, .claude/README.md, .claude/knowledge/autoloads/_index.md, .claude/skills/breakdown-requirements/(新增) |
| [2026-07-27b-service-api-contracts](2026-07-27b-service-api-contracts.md) | 2026-07-27 | **七服务 API 契约定案（八条总则）**：三种方法形态按边界划分（A 同步直返 / B `Task<OpResult<T>>`+`CancellationToken` / C `Task<T>` 由信号推进；B/C 带 `Async` 后缀）; 三种失败语义与 null-check 一一对应（**业务失败返回 `OpResult` 绝不抛**）+ 统一 `OpResult` / `OpError` / `ApplyResult`; 服务门面固定骨架（manager **`internal sealed`**、不暴露 manager 引用、不返回可变集合）; **Bootstrap 启动契约**（`_Ready` 只装配，I/O 归 `IBootstrappable.InitializeAsync`，Bootstrap 屏幕驱动）; **EventBus 改用 C# 泛型 `event` + `readonly record struct` 负载**（否决 Godot `[Signal]`）+ 14 条负载 schema + 三条负载纪律; **后端接口化**（四个窄接口 × Http/Offline 两份实现）; **结算阶段名取代事件自带钩子**（`eventStart`/`eventEnd` = `AdvanceEventAsync` 流程阶段名 + 两个 `IEventResolver`）; **AdventureEvent 物化模型**（`AdventureEventData` = 模板 / future-event-service = **唯一物化点** / `EventOption` **产出即定稿**、immutable、落存档不重算；`InstanceId` ↔ `EventId` 并存；通则 `CardData`↔`CardInstance`）; `CostSpec`/`RewardSpec` **合并为 `ProfileChangeSpec`**（element 带符号）; `CapabilityFlag` 用 C# `enum`; `CombatResult.Spoils` 为 spec，由 life-cycle 在 `eventEnd` 合并为**一次** `TryApply`; 跨服务纪律措辞收紧（不读写对方字段 / 不伸手进 manager，**方法调用允许**）; 逐服务首版签名骨架; feeds architecture, common-properties, services 全树, adventure-event, system-overview, terminology, open-questions | distilled | systems/architecture.md, systems/common-properties.md, systems/services/（_index + 七份服务文档 + plot-manager）, systems/adventure-event/common-properties.md, system-overview.md, terminology.md, open-questions.md, answer-logs/log-service-api-contracts.md |
| [2026-07-27-content-gating-offline-resilience-and-rng-persistence](2026-07-27-content-gating-offline-resilience-and-rng-persistence.md) | 2026-07-27 | **热更改用内容共有字段 `ContentEnabled`（默认 `true`）翻开关放量**（否决预埋占位 `Id`；**过滤只在产出侧** `AllEnabled()`、读取侧 `Get(id)` 不过滤 → 存档引用零风险；disabled 条目照常参与合并后强校验）; **存档记两个 `contentVersion`**（`StartContentVersion` 写一次不变 / `LastContentVersion` 每存档点更新；不等 = 跨过内容更新；push 信封带 contentVersion/appVersion/revision；不为每日种子预留冻结结构）; **增量下载 = 文件级粒度 + 文件级事务**（overlay / manifest 提交点 / staging 三段，原子 rename 作提交点，永不半套 overlay）+ **manifest 签名**（防误不防作弊；`failReason` 区分 网络/校验/磁盘）; **断线降级：绝不回退存档点**（push 缓冲不阻塞 · pull 硬阻塞 · 剧本事务前置；上限 3 存档点 / 180 s → 软阻塞；恢复先 pull 后 flush、云端 revision 领先则丢弃本地；token 刷新失败视同断线、被挤下线硬阻塞；剧本 LRU 预取缓存）; **RNG 持久化**（`Hash64(CycleSeed, name)` 派生、`State` 恢复权威 + `DrawCount` 迁移保险、子流清单为 SeedManager 常量、增删不 bump schema、战斗内每场再派生防 re-roll）; **存档点与 push 解耦**（本地即时原子写 / 网络 5 秒防抖 + `PushPolicy`；Immediate 含应用失焦挂起）+ **增量 push 按 CharacterProfile 粒度 diff**; **开发路线 = 框架 → 内容 → 平衡与体验 → 社交及其他**（ADR 候选）; 存档 schema bump + 立起空迁移骨架; scope.md 可复现表述修订; feeds content-service, sync-service, account-service, plot-manager, life-cycle-service, common-properties, character-profile, balance, terminology, scope, open-questions, .claude/rules | distilled | terminology.md, systems/services/content-service.md, systems/services/sync-service.md, systems/services/account-service.md, systems/services/plot-manager.md, systems/services/life-cycle-service.md, systems/common-properties.md, systems/character-profile/_index.md, systems/balance.md, vision/scope.md, open-questions.md, answer-logs/log-0727.md, .claude/rules/data-resource-rules.md |
| [2026-07-26-event-priority-skip-semantics-and-hotfix-scope](2026-07-26-event-priority-skip-semantics-and-hotfix-scope.md) | 2026-07-26 | 新增 AdventureEvent 共有字段 **`eventPriority`（事件优先级）**(0 = 可自由择一; 出现更高优先级则有效可选集收窄为最高档; 与 `ifMandatory` 分属两条约束轴); **跳过通道玩法语义定案**(单项补位而非整批刷新、补位可落空、**通常不扣 `lifeSpanCost`**、少数事件带 `skipCost`、**计入 `pastEvent`** 作为行为轨迹); **`ifMandatory` 由 future-event-service 产出时动态置位**, 一批可全部 mandatory; **热更范围 = 只改不增**(overlay 不得新增 `Id`) + **放弃跨内容版本 seed 可复现**(以 overlay 更新为准, 不冻结 `contentVersion`); **player-profile 子系统落位**(item/power/achievements 成文件夹, account-info/game-setting 独立 md); **元婴 +500 的读者 = 元婴界面(通关证书)**; **寿元 <10% UX = 标红数值倒数**; 架构闭环缺口 8 处全部闭合; feeds adventure-event, future-event-service, life-cycle-service, content-service, common-properties, architecture, balance, player-profile, screen-flow, terminology, open-questions, .claude/rules | distilled | terminology.md, systems/adventure-event/common-properties.md, systems/services/future-event-service.md, systems/services/life-cycle-service.md, systems/services/content-service.md, systems/common-properties.md, systems/architecture.md, systems/balance.md, systems/player-profile/（_index + achievements/** 新增 + account-info.md 新增 + game-setting.md 新增）, ux/screen-flow.md, open-questions.md, answer-logs/log-0726b.md, .claude/rules/state-save-rules.md |
| [2026-07-25c-service-manager-hierarchy-and-content-pipeline](2026-07-25c-service-manager-hierarchy-and-content-pipeline.md) | 2026-07-25 | **两级层次 service ⊃ manager**（服务三判据：自有状态机 / 事务性跨字段写 / 外部 I/O 边界）; **拆分轴定案 = 生命周期层 + 行为边界，非数据类型**（否决 power/item/card/resource collection 服务与按事件类型拆服务，附否决理由）; 服务清单收敛为**七个**——新增 account-service / content-service / sync-service / **profile-service**（两层 profile 唯一写入面, 含 ProfileManager / CapabilityManager / AchievementManager） / combat-service, **adventure-plot-service 降级为 PlotManager**; **内容三层管线**(res:// 基线 + user://overlay 热更 + 云端版本校验) + **统一泛型仓储接口**; **本地 / 云端内容分界**判据; 两条唯一入口(ContentRegistry 读 / ProfileManager 写) + **编排顶点 game-progression**; **术语修正: 废弃「微服务」措辞**(service = 进程内模块单例, 唯一真实进程边界是客户端↔后端); 新增两份根级总览 **program-overview.md**(运行时视角) + **system-overview.md**(工程视角: 进程边界 / 文件夹布局 / autoload 注册 / 代码形态 / 离线 stub 策略); 架构 8 缺口闭合 6 + 1 部分; feeds services 全树, architecture, common-properties, terminology, player-profile, open-questions, README | distilled | program-overview.md, system-overview.md, terminology.md, systems/services/**（_index, account, content, sync, profile, combat, plot-manager 新增；life-cycle, future-event 更新；adventure-plot-service 删除）, systems/architecture.md, systems/common-properties.md, systems/_index.md, systems/player-profile/_index.md, open-questions.md, README.md |
| [2026-07-25b-event-cost-fields-capability-flags-and-service-hierarchy](2026-07-25b-event-cost-fields-capability-flags-and-service-hierarchy.md) | 2026-07-25 | AdventureEvent 共有字段补全(eventType/selectCost/skipCost/ifMandatory + eventStart/eventEnd, 隐含引入「跳过事件」通道); PlayerPower 补入 status(启用/禁用); adventure-plot-service **隶属于** future-event-service; 元婴 lifeSpan +500(无玩法影响, 阶梯闭合 1000); selectCost = **定制复合成本类型**(lifeSpanCost 为其一个 element); 已采纳: 展示字段三层切分(Data/运行时/ViewModel) + capability flag & modifier pipeline (均为 ADR 候选); 架构闭环体检(8 处缺口, PlayerProfile 侧待完善为优先待办); 流程治理: **derive 就绪度全量回滚** + 新增 `/assess-derive-readiness` 技能(手动调用, 独占写入); feeds adventure-event, player-power, services 全树, architecture, common-properties, balance, terminology, open-questions, .claude/skills | distilled | terminology.md, systems/adventure-event/common-properties.md, systems/player-profile/player-power/**, systems/services/future-event-service.md, systems/services/adventure-plot-service.md, systems/services/life-cycle-service.md, systems/architecture.md, systems/common-properties.md, systems/balance.md, systems/_index.md, open-questions.md, README.md, .claude/rules/Context.md, .claude/README.md, .claude/skills/analyze-new-ideas, .claude/skills/summarize-open-questions, .claude/skills/assess-derive-readiness(新增) |
| [2026-07-25-lifespan-service-refactor-and-legacy-cleanup](2026-07-25-lifespan-service-refactor-and-legacy-cleanup.md) | 2026-07-25 | 寿元数值化(100/+100/+300, 隐藏→<10%显示→0=defeated); 服务层重命名 run-manager→life-cycle-service / adventure-plot→adventure-plot-service + 新增 future-event-service(产出 eventOptions); character-profile 结构定案(deck/item 文件夹, life/currency/mana 扁平); 治理约定「活文档只保留最新设计」+ 全库遗留清理; feeds terminology, services 全树, character-profile, game-progression, architecture, balance, adventure-event, player-profile, Context.md, README, open-questions | distilled | terminology.md, systems/services/**, systems/character-profile/**, systems/game-progression.md, systems/architecture.md, systems/balance.md, systems/adventure-event/**, systems/player-profile/**, systems/_index.md, .claude/rules/Context.md, README.md, open-questions.md |
| [2026-07-24-docs-restructure-class-model](2026-07-24-docs-restructure-class-model.md) | 2026-07-24 | 全库重构：class-concept 化 systems、30-content 并入、adventure-event/character-profile/player-profile 折叠、run-manager/adventure-plot services 化、architecture.md、explore/travel/地域 新概念、.claude 知识库降为引用层；feeds terminology, systems 全树, open-questions, .claude/knowledge | distilled | terminology.md, systems/**, open-questions.md, .claude/knowledge/* |
| [2026-07-23-adventure-plot-hidden-stats-and-clarifications](2026-07-23-adventure-plot-hidden-stats-and-clarifications.md) | 2026-07-23 | AdventurePlot(隐藏剧本层,四级 Story/Chapter/SideChapter/SideStory + 剧本服务)+隐藏属性(道心/煞气/寿元)；断线缓冲、删游客态、PlayerPower 轻度提升、成就 60/90 双档、Finale 入 ADR-0002 第七类、横向滑动选择、mana 无曲线+基线、后端重账号、encounter 全库覆写、固化 ADR-0003/0004；feeds terminology, pillars, references, scope, screen-flow, onboarding, run-manager, adventure-event-combat, energy-economy, map-progression, adventure-plot, events, balance, adventure-events, ADR-0002/0003/0004 | distilled | terminology.md, pillars.md, references.md, scope.md, screen-flow.md, onboarding.md, run-manager.md, adventure-event-combat.md, energy-economy.md, map-progression.md, adventure-plot.md, events.md, balance.md, adventure-events.md, ADR-0002, ADR-0003, ADR-0004 |
| [2026-07-22-online-cloud-combat-and-meta-clarifications](2026-07-22-online-cloud-combat-and-meta-clarifications.md) | 2026-07-22 | 强制在线云端(取代混合模型) + 战斗模型 life+mana + 篇章继承/状态/解锁/成就 等批量裁定；feeds scope, references, run-manager, map-progression, adventure-event-combat, energy-economy, screen-flow, onboarding, terminology | distilled | scope.md, references.md, run-manager.md, map-progression.md, adventure-event-combat.md, energy-economy.md, screen-flow.md, onboarding.md, terminology.md |
| [2026-07-16-ux-flow-login-and-dev-order](2026-07-16-ux-flow-login-and-dev-order.md) | 2026-07-16 | 登录/主菜单 UX + 在线存档(矛盾) + 开发顺序；feeds screen-flow, onboarding, scope, run-manager | distilled | screen-flow.md, onboarding.md, scope.md, run-manager.md |
| [2026-07-15b-taxonomy-and-checkpoint-clarifications](2026-07-15b-taxonomy-and-checkpoint-clarifications.md) | 2026-07-15 | 分类法定案 + 存档/重试；feeds terminology, adventure-event-combat, run-manager, map-progression, ADR-0002 | distilled | terminology.md, adventure-event-combat.md, run-manager.md, map-progression.md, ADR-0002-adventure-event-taxonomy.md |
| [2026-07-15-adventure-event-profiles](2026-07-15-adventure-event-profiles.md) | 2026-07-15 | terminology + systems/adventure-event-combat, run-manager, map-progression | distilled | terminology.md, adventure-event-combat.md, run-manager.md, map-progression.md |
| [2026-07-13-vision](2026-07-13.md) | 2026-07-13 | vision (+ systems/map-progression, adventure-event-combat) | distilled | pillars.md, references.md, scope.md, map-progression.md, adventure-event-combat.md |
| [2026-07-12-example](2026-07-12-example.md) | 2026-07-12 | systems/deck-hand | raw | — |

<!-- Add new rows at the top. status: raw | triaged | distilled -->
